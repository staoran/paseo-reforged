# <任务名称> Micro Spec

仅在项目要求文件化、任务需要跨会话恢复、影响公共行为或执行中升级风险时复制本模板。明确、低风险、同会话的小修改可以使用等价的执行前复述与最终回写，不为满足格式单独创建文件。

## 任务索引

- task_id：`<编号>`
- 项目规则：`<PROJECT_RULES path and anchor>`
- 任务索引：`<task-index path and anchor or N/A>`
- task plan：`<path and anchor or N/A>`
- progress：`<path and anchor or N/A>`
- 批准状态：`<按项目规则>`

## 背景与目标

- 背景/问题：
- 目标：
- 非目标：

## 范围与非范围

- 范围内：
- 范围外：

## 涉及文件和事实源

| 路径 | 计划变化 | 事实源 |
| --- | --- | --- |
|  |  |  |

## 执行与风险

1. `<动作>`
2. `<动作>`

- 风险与回退：

## 验收

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
|  |  | 待执行 |

## 最终摘要与同步

仅未启用 tracking 的任务在执行后填写一次，不记录逐步时间线；启用 tracking 时删除本节，实际结果与同步只写 progress。

- 实际变更：
- 验收结果：`<引用上表证据或 progress 锚点>`
- 偏差/未完成项：
- 残余风险/下一步：

| 收口项 | 结果/证据 |
| --- | --- |
| 任务索引同步 | `<已同步路径与锚点 / N/A>` |
| Project Sync Scan | `无 / 候选资产、来源与建议落点` |
| 文档同步 | `<已同步文件 / 未同步原因 / N/A>` |
| 提交关联 | `<按 PROJECT_RULES 记录建议或实际关联 / N/A>` |
