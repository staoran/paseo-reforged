# 关键逻辑上下文

本文件记录错误代价高、历史复杂、跨模块或跨端的稳定事实。每个主题都要能定位到源码、契约、测试或事故记录。

## 关键链路

| 链路 | 入口 | 状态/数据流 | 不变量 | 风险 | 验证入口 |
| --- | --- | --- | --- | --- | --- |
| `<flow>` | `<path/function>` | `<summary>` | `<must hold>` | `<risk>` | `<test/doc>` |

## 兼容与迁移

| 主题 | 旧行为 | 新行为/目标 | 兼容边界 | 回退 | 证据 |
| --- | --- | --- | --- | --- | --- |
| `<topic>` | `<old>` | `<new>` | `<boundary>` | `<rollback>` | `<source>` |

## 已知高风险

| 风险 | 触发条件 | 防护 | 失败信号 | 升级路径 |
| --- | --- | --- | --- | --- |
| `<risk>` | `<trigger>` | `<guard>` | `<signal>` | `<owner/doc>` |

## 更新边界

- 只在事实已由代码、测试、契约或复盘证据确认后更新。
- 一次性任务调查写入 findings，不直接进入此文件。
- 记录最小必要摘要，详情链接到 spec、codemap 或验证证据。

