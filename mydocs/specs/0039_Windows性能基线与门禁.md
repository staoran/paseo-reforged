# Windows 性能基线与门禁 Spec

## 0. 状态与索引

| 字段              | 值                                                                        |
| ----------------- | ------------------------------------------------------------------------- |
| task_id           | `0039`                                                                    |
| spec layer        | `Feature Spec`                                                            |
| task status       | `取消`                                                                    |
| mode              | `single_project`                                                          |
| phase             | `Review`                                                                  |
| approval status   | `Plan Approved`                                                           |
| approval source   | `User 于 2026-08-06 21:50 批准 C1-C6 清理；2026-08-09 仅恢复取消审计记录` |
| spec path         | `mydocs/specs/0039_Windows性能基线与门禁.md`                              |
| parent spec       | `mydocs/specs/0038_跨端会话性能与结论优先加载.md`                         |
| supersedes        | `N/A`                                                                     |
| current task unit | `已取消；保留原性能方向、清理结果和恢复来源的摘要审计`                    |
| created / updated | `2026-08-04 08:51 / 2026-08-09`                                           |

本任务不再是 0038 或 0040-0046 的技术依赖，也不能作为执行入口。

## 1. 目标、范围与完成契约

- 原始目标：建立 Windows Electron/Web 的 P50/P95、CPU/RSS、长会话和多 Host 性能基线，并以 profiler/runner/artifact 作为 0038 子项门禁。
- 取消原因：正式墙钟、CPU 和 RSS 基线容易受机器负载污染，且会先建设一套大于当前优化本身的测量框架。用户决定改用请求数、读取数、写次数、mounted row、驻留数量和并发上限等确定性结构合同。
- 当前范围：仅保存取消决策、已完成清理、历史来源和不再执行的边界。
- 范围外：恢复 profiler RPC、runner、schema、artifact、绝对时间预算，或借本记录执行 0040-0046 产品代码。
- Done Contract：0039 的 measurement WIP 已从产品树清理；父子文档不再依赖 0039；历史证据仍可从固定 Git 对象读取。
- 失败或回炉方式：只有用户重新提出独立、可验证的性能测量需求时创建新任务；不复活本编号。

### 1.1 恢复说明

- 2026-08-09 的恢复采用规范化摘要，不复制原 Spec 中大段临时 profiling 输出和调试日志。
- 原始完整 blob 保存在 Git 对象 `20eaefcdfc5eccccd3aefb2961f26ac8c7767e30`，来源提交为 `66136a99243ddd59ebf0c5b24ad644f95a96fb88`。
- 省略内容不是长期事实源，也不影响“取消、已清理、不再依赖”的结论。

## 2. 上下文与决策

### 2.1 已确认事实

- 原 WIP 包含 measurement API、Electron/Chromium runner、结果 schema、性能场景和恢复包。
- 用户于 2026-08-06 批准只执行 C1-C6 清理，不授权任何 0040-0046 产品实现。
- 清理完成后，A/B measurement WIP 为零；结构检查、定向回归和静态检查已在原任务中记录完成。
- 0038 后续改用 deterministic work bounds，不再依赖 P50/P95、CPU/RSS 或 0039 artifact。

### 2.2 stable 影响

- upstream stable 新增 History 搜索，但没有让一套正式性能基线重新变得必要。
- stable 对 0046 增加了完整候选集约束，对 0042/0043 增加了 stored Agent 打开入口；这些影响可由子 Spec 的结构合同验证。
- 结论：0039 继续保持 `取消`，只需恢复总表和父链审计。

## 3. 计划与执行前检查点

- 当前 phase：`Review`。
- approval status / source：`Plan Approved / User；历史清理授权已消耗`。
- 执行清单：`无`。
- 下一步：`无；不得从本 Spec 启动产品代码或测量框架`。
- TDD 判定：`N/A；任务已取消，本轮仅恢复文档`。

## 4. 执行与验证记录

| 项目                | 状态   | 说明                                    |
| ------------------- | ------ | --------------------------------------- |
| C1-C6 清理          | 完成   | 原 measurement WIP 已清理，恢复证据保留 |
| 父级解耦            | 完成   | 0040-0046 使用结构合同，不再等待 0039   |
| 2026-08-09 文档恢复 | 完成   | 恢复规范化取消记录与固定对象锚点        |
| 本轮产品代码        | 未执行 | 用户明确要求暂不执行代码                |

- 未验证项：未重跑历史性能场景、产品测试或静态检查；本轮没有产品代码改动。
- Done Contract 是否由证据满足：`是；取消和清理状态已保存，且不再构成依赖`。

## 5. 评审（Review）

| 评审轴           | 结论   | 证据                                  |
| ---------------- | ------ | ------------------------------------- |
| 取消决策完整性   | `PASS` | 第 1、2 节                            |
| 清理与父级一致性 | `PASS` | 父 Spec 0038 不再登记 0039 技术依赖   |
| 可恢复性         | `PASS` | 固定 Git blob/tree 锚点可追溯完整历史 |

- Overall Verdict：`PASS；Canceled`。
- Blocking Issues：`None`。

## 6. 偏差、变更与反向同步

- `2026-08-06`：用户取消性能基线方向并批准 C1-C6 清理；清理完成后任务收口为 `取消`。
- `2026-08-09`：恢复为精简审计 Spec，不恢复非权威临时日志，不改变任何产品行为。
- Spec 反向同步结果：父 Spec 与 `mydocs/todolist.md` 登记 0039 为 `取消 / 已同步`。

## 7. 恢复、长期知识与提交关联

- 状态说明：`取消 / Review / Plan Approved`。
- 当前卡点：`None`。
- 下一步唯一动作：`无`。
- Resume / Handoff 锚点：本文件第 0、1.1、2.2、4 节；完整历史见固定 Git 对象。
- Project Sync Candidates：`无`。
- 长期文档同步：`N/A`。

### 提交记录

| 提交信息（Commit Message）                            | 提交脚注（Commit Footer） | 关联项目 / 改动或阶段 | 文档同步状态 | 备注                 |
| ----------------------------------------------------- | ------------------------- | --------------------- | ------------ | -------------------- |
| `feat: integrate session performance stack`（本提交） | `N/A`                     | `paseo / 0039`        | `已同步`     | 随 0038 记录取消审计 |
