# 主工作区对齐与发布副本收口 Spec

> 历史迁移说明：本文于 2026-08-02 从 `mydocs/tasks/0006_主工作区对齐与发布副本收口/` 迁移为当前规则集的单文件 Heavy Spec。第 0-9 章是当前权威摘要；第 10 章按源文件保存完整历史原文和 SHA-256，仅用于追溯，不构成新的执行授权。

## 0. 状态与索引

| 字段              | 值                                                |
| ----------------- | ------------------------------------------------- |
| task_id           | `0006`                                            |
| spec layer        | `Feature Spec`                                    |
| task status       | `已收口`                                          |
| mode              | `single_project`                                  |
| phase             | `Review`                                          |
| approval status   | `Plan Approved`                                   |
| approval source   | `User`                                            |
| spec path         | `mydocs/specs/0006_主工作区对齐与发布副本收口.md` |
| parent spec       | `N/A`                                             |
| supersedes        | `N/A`                                             |
| current task unit | `历史任务记录迁移与归档`                          |
| created / updated | `2026-07-24 / 2026-08-02`                         |

## 1. 目标、范围与完成契约

- 背景/问题：该任务使用旧规则集的多文件任务包记录，现需迁移为当前单文件 Spec。
- 最终目标：把本地主工作区安全对齐到已发布 origin/main，同时保留历史和任务资料。
- 当前任务单元：无损迁移历史记录并关闭旧路径。
- 范围内：fetch、保护分支、fast-forward 式 ref 对齐、Node 22 安装与静态验证。
- 范围外：push、force、rebase、reset、clean、删除发布副本或提交 mydocs。
- Done Contract：当前摘要覆盖任务状态、关键事实、方案、执行、验证、风险和提交；全部旧 Markdown 原文及内容哈希保存在第 10 章；旧目录仅在迁移验证通过后删除。
- 失败或回炉方式：任一源文件未被完整嵌入、哈希不符或格式检查失败时，保留旧目录并重新生成，不以不完整的新 Spec 替代源记录。

### 1.1 最小任务单元判断

- 为什么当前任务单元足够小：单个 task_id 只生成一份对应 Spec，不合并不同任务的事实或授权。
- 验证证据：源文件清单、SHA-256、完整内容包含检查和 Markdown 格式检查。
- 模型可自主决定的范围：章节重组、历史摘要和附录顺序；不得改写历史授权或扩大任务结论。
- 拆分决定：`Accepted`

## 2. 上下文与调研

### 2.1 上下文来源

- 需求来源：用户要求将历史文档转为新规则集格式后删除旧文档。
- 项目事实源：`PROJECT.md`、Git 历史、当前源码以及第 10 章保存的旧任务包。
- Codemap：`N/A`
- Codemap Mode：`N/A`
- Context Bundle：`N/A`
- Context Bundle Level：`N/A`
- 关联任务记录：`N/A`

### 2.2 调研结论

已确认事实：

- 本地 main 在保护分支存在的前提下对齐到当时的 origin/main。
- 未使用 reset、rebase、clean 或强制 checkout，未跟踪任务资料得到保留。
- 该任务只有本地 Git 操作和验证，没有产品提交。

- 未知与开放问题：无影响本次历史迁移的开放问题；动态 refs、外部服务和旧基线只按历史快照理解。
- 风险与约束：路径、refs 和发布副本状态仅是 2026-07-24 的历史快照。
- `grilling` 结论（如使用）：未使用；迁移目标与删除范围已由用户明确。

### 2.3 方案与决策

- 备选方案：保留旧任务包、只写摘要后删除、或在新 Spec 中同时保存摘要和完整原文。
- 已选方案：使用当前模板的 0-9 章保存权威摘要，并在第 10 章无损嵌入全部旧文件。
- 选择理由：消除并行真相源，同时避免删除未提交历史记录造成证据丢失。

历史任务关键决策：

- 先建立可恢复分支，再通过 detached HEAD 更新 main ref。
- 保留发布 clone 和 runtime，删除不属于任务范围。

### 2.4 下一步动作

- 下一步唯一动作：N/A；任务已关闭。

## 3. 计划与执行前检查点

### 3.1 文件变化

| 项目/子项    | 文件或子 Spec                                     | 计划变化                     | 原因                     |
| ------------ | ------------------------------------------------- | ---------------------------- | ------------------------ |
| 历史任务记录 | `mydocs/tasks/0006_主工作区对齐与发布副本收口/`   | 合并到当前单文件 Spec 后删除 | 消除旧规则集多文件真相源 |
| 当前 Spec    | `mydocs/specs/0006_主工作区对齐与发布副本收口.md` | 新建                         | 使用当前 Heavy Spec 模板 |

### 3.2 签名与契约

| 项目/子项 | 接口、类型或签名     | 计划变化 | 兼容性         |
| --------- | -------------------- | -------- | -------------- |
| 文档迁移  | 运行时接口与数据契约 | 无变化   | 仅迁移历史记录 |

### 3.3 子 Spec 索引

N/A；该历史 task_id 迁移为单个 Spec。

### 3.4 执行清单

- [x] 1. 读取并归档旧任务包全部 Markdown 文件。
- [x] 2. 建立当前模板要求的状态、目标、上下文、执行、验证和提交摘要。
- [x] 3. 记录源文件 SHA-256 并验证完整内容已嵌入。
- [x] 4. 在新 Spec 验证通过后删除旧目录。

### 3.5 执行前检查点

- 当前目标与任务单元：历史任务单文件迁移。
- 当前 phase：`Review`
- approval status / source：`Plan Approved / User`；用户已明确授权转换并删除旧文档。
- 下一步：验证格式、内容哈希和旧路径引用。
- 风险与回退：验证失败时不删除旧目录；Git 可恢复原 tracked 文件，未跟踪源内容已完整嵌入第 10 章。
- 验证方式：生成器内容包含检查、格式检查、旧路径检索和 Git 范围审查。
- TDD 判定、测试 seam 与验收行为：`N/A`；当前改动只迁移历史文档，不改变运行时行为。
- seam 确认：`N/A`

## 4. 跨项目扩展

N/A；单项目历史文档迁移。

## 5. 执行记录

| 步骤/子项 | 实际变化或子 Spec 锚点                               | 状态   | 偏差与处理                   |
| --------- | ---------------------------------------------------- | ------ | ---------------------------- |
| 1         | 刷新 origin refs 并核对远端 main。                   | 已记录 | 详细时间线见第 10 章原始记录 |
| 2         | 创建保护分支并完成本地 main 对齐。                   | 已记录 | 详细时间线见第 10 章原始记录 |
| 3         | 使用 Node 22 完成依赖、构建声明、typecheck 和 lint。 | 已记录 | 详细时间线见第 10 章原始记录 |
| 文档迁移  | 旧任务包完整原文进入第 10 章                         | 已完成 | 不改写原始记录               |

## 6. 验证

| 项目/验收项 | 命令或步骤                          | 结果   | 证据           | 未验证原因 |
| ----------- | ----------------------------------- | ------ | -------------- | ---------- |
| Git         | HEAD/main/origin 与保护分支核对     | `PASS` | 历史任务记录   | `N/A`      |
| 依赖        | Node 22 npm ci 与 owner stack build | `PASS` | 历史任务记录   | `N/A`      |
| 静态门禁    | typecheck、lint、clean tracked diff | `PASS` | 历史任务记录   | `N/A`      |
| 迁移完整性  | 源文件 SHA-256 与完整内容包含检查   | `PASS` | 第 10.1 节清单 | `N/A`      |

- 集成验证：当前迁移不改变产品代码；历史产品验证结果按上表和第 10 章保留。
- 剩余风险：路径、refs 和发布副本状态仅是 2026-07-24 的历史快照。
- Done Contract 是否由证据满足：是

## 7. 评审（Review）

| 评审轴             | 结论   | 证据或阻塞问题                                        |
| ------------------ | ------ | ----------------------------------------------------- |
| 目标与 Spec 完成度 | `PASS` | 历史结论和完整原文已迁移                              |
| Spec 与执行一致性  | `PASS` | 新摘要不扩大旧授权，原文无损保留                      |
| 实现质量与风险     | `PASS` | 路径、refs 和发布副本状态仅是 2026-07-24 的历史快照。 |

- Overall Verdict：`PASS`
- Blocking Issues：None
- Cross-project consistency：`N/A`

### 7.1 回归风险

| project_id | Regression risk | 依据                              |
| ---------- | --------------- | --------------------------------- |
| `paseo`    | `Low`           | 当前只迁移 Markdown，不改变运行时 |

### 7.2 Touched Projects

N/A。

- Orphan changes：`None`

## 8. 偏差、变更与反向同步

- Plan-Execution Diff：历史任务的计划与执行偏差按第 10 章原记录保存；本迁移不重新裁决。
- Change Log：2026-08-02 将旧多文件任务包迁移为当前单文件 Heavy Spec。
- 用户决策：转换历史文档，验证后删除旧目录；0031 由其他会话处理，不在本迁移范围。
- Spec 反向同步结果：第 0-9 章成为当前权威摘要，第 10 章保存只读历史原文。

## 9. 恢复、长期知识与提交关联

- 状态说明：`已收口`
- 当前卡点：`N/A`
- 下一步唯一动作：N/A；任务已关闭。
- Resume / Handoff 锚点：本文第 0 章；详细历史见第 10 章。
- Project Sync Candidates：无；本迁移不从一次性历史记录推导新的长期规则。
- 长期文档同步：仅更新旧任务路径登记，不改变产品知识。

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联项目 / 改动或阶段 | 文档同步状态 | 备注                       |
| -------------------------- | ------------------------- | --------------------- | ------------ | -------------------------- |
| `N/A`                      | `N/A`                     | `N/A`                 | 已同步       | 该历史任务没有独立产品提交 |

## 10. 历史原始记录

### 10.1 源文件完整性清单

以下哈希按 LF 规范化且去除文件尾空白后计算。

| 源文件         | 规范化字节数 | SHA-256                                                            |
| -------------- | -----------: | ------------------------------------------------------------------ |
| `SPEC.md`      |         3924 | `273c310f02b3faa74d6739f59a43dbdcd94657404f80aaf57ed65250b6ff2d99` |
| `progress.md`  |         2122 | `e25904e16eaccdd486e9fed2360a3bf5fc066c8f721c0ce38eb361d6f61d04fb` |
| `task_plan.md` |          720 | `f9ff01d2154d9108ad5671115f60b90645a5d1e65e120bc5498e679b6968222b` |

### 10.2 原 `SPEC.md`

```text
# Spec：主工作区对齐与发布副本收口

- Task ID: `0006`
- Spec Type: `Task Spec`
- Phase: `Review`
- Status: `COMPLETED`
- Approval Status: 用户于 2026-07-24 明确批准的本地对齐授权已完成并关闭
- Active Project: `E:\Code\paseo`
- Release Clone: `E:\Code\paseo-release-beta-4`
- Tool Runtime: `E:\Code\paseo-release-tools`

## 1. Goal

将主工作区的本地 `main` 安全对齐至已发布的 `origin/main`，保留旧本地历史和全部未跟踪任务资料；在 Node `22.20.0` 下完成依赖刷新、typecheck 与 lint。保留发布 clone，暂不删除任何目录。

## 2. Scope

- 在主工作区更新 `origin` tracking refs，并断言远端 `main` 仍为 `b64f4f35784876021268583b1736ad951495946c`。
- 创建仅本地的 `codex/pre-align-main-045dd0c` 安全分支，保留当前 `045dd0cc6d06f6deafb3be5b9bd7f92abd8e10fb` 历史。
- 通过 detached HEAD 切换和分支 ref 更新令本地 `main` 指向 `origin/main`。
- 使用 `paseo-release-tools` 的 Node 22/npm 运行 `npm ci`、typecheck、lint。

## 3. Out of Scope

- 推送、force push、远端 tag/Release/资产修改或任何公开发布。
- rebase、cherry-pick、`git reset --hard`、`git clean`、删除 `paseo-release-beta-4` 或 `paseo-release-tools`。
- 修改、暂存或提交 `mydocs/`，以及修改 `E:\Code\paseo-reclaude`。

## 4. Adopted Facts

- 已发布远端 `main` 为 `b64f4f357...`，不可变 beta.5 tag object 为 `45ff58f...`。
- `paseo-release-beta-4` clean，且 `main=origin/main=b64f4f357...`；其本地分支没有未在 origin 的提交。
- `paseo-release-tools` 不是 Git 仓库，而是可用的 Node `22.20.0` / npm `10.9.3` runtime。
- 旧主工作区提交 `1c4f22e`、`f310a7f`、`045dd0c` 已在发布链路中以 `02fd21e`、`cff02de`、`a4172e9` 重放；不得再次 cherry-pick。

## 5. Execution Plan

1. `fetch origin --prune --tags`，重新检查 remote SHA、tag 和未跟踪 `mydocs/` 路径冲突。
2. 创建本地安全分支并验证其指向旧 `main`。
3. `git switch --detach origin/main`；在 detached HEAD 下将 `main` ref 更新为 `origin/main`；再切回 `main`。
4. 验证 HEAD、branch/upstream、未跟踪任务资料和 tracked/cached diff。
5. 使用 Node 22 runtime 运行 `npm ci`、`npm run typecheck`、`npm run lint`。

## 6. Done Contract

- 本地 `main=origin/main=b64f4f35784876021268583b1736ad951495946c`。
- `codex/pre-align-main-045dd0c` 可恢复旧本地历史。
- 未跟踪 `mydocs/` 保留，未发生 staged 或 tracked 源码修改。
- Node 22 的依赖安装、typecheck 与 lint 通过。
- `paseo-release-beta-4` 保持原样、clean，留待用户后续决定是否删除。

## 7. Risks and Stop Conditions

- fetch 后远端 main/tag 漂移、未跟踪路径与目标 tree 冲突、任何 tracked/cached diff 出现时停止。
- switch 失败时不使用强制 checkout/reset；保留现场并报告。
- typecheck/lint 失败时记录失败，不把发布 clone 或远端作为修复目标。

## 8. Review Verdict

| 轴              | 结论 | 证据                                                                                             |
| --------------- | ---- | ------------------------------------------------------------------------------------------------ |
| Goal 与范围完成 | PASS | 本地 `main=origin/main=b64f4f357...`；安全分支保留旧历史；未推送或删除目录                       |
| Spec-执行一致性 | PASS | 使用 detached HEAD 更新本地分支，未使用 reset/rebase/cherry-pick/clean，未跟踪任务资料无路径冲突 |
| 内在质量与验证  | PASS | Node `22.20.0` 的 `npm ci`、`build:server`、typecheck 与 lint 通过；tracked/cached diff 为空     |

- Overall Verdict: `PASS`
- Residual Risk: `npm ci` 报告锁定依赖中 77 项 audit findings；本任务未修改依赖，未运行会改变 lockfile 的 `npm audit fix`。
```

### 10.3 原 `progress.md`

```text
# Progress：主工作区对齐与发布副本收口

## 2026-07-24 — 执行授权与初始现场

- 用户批准本地 `main` 对齐，明确要求保留 `E:\Code\paseo-release-beta-4` 到本地验证结束。
- 执行前现场：主工作区 `main=045dd0cc6...`，其 `origin/main` tracking ref 仍为旧 `9952615c...`；GitHub 只读 API 已确认实际远端 `main=b64f4f357...`。
- 本任务不处理 `mydocs/` 的提交，不删除 release clone 或 tools runtime，不修改远端。

## 2026-07-24 — 主工作区对齐完成

- `git fetch origin --prune --tags` 后 `origin/main=b64f4f357...`；本地 beta.4/.5 annotated tag object 分别为 `2c934788...`、`45ff58f...`。
- 远端 tree 中已跟踪的 `mydocs/` 只有五份 2026-07-20 CodeMap 与 Task `0003`；和本地未跟踪 Task `0001/0002/0004/0005/0006` 及 2026-07-22 CodeMap 无同路径冲突。
- 创建仅本地安全分支 `codex/pre-align-main-045dd0c -> 045dd0cc6...`。随后切到 detached `origin/main`，更新本地 `main` ref 并切回；最终 `HEAD=main=origin/main=b64f4f357...`，ahead/behind 为 0/0。
- 全程未使用 reset、rebase、cherry-pick、clean 或强制 checkout；未推送或修改远端。

## 2026-07-24 — Node 22 验证

- 使用 `E:\Code\paseo-release-tools` 的 Node `22.20.0` / npm `10.9.3` 完成 `npm ci`；安装 2587 packages，postinstall patches 与 lefthook prepare 成功。审计报告为 77 项 findings（7 low、38 moderate、25 high、7 critical），未运行会改变依赖的 audit fix。
- 干净安装后的首次 typecheck 因 protocol/client `dist` 声明缺失而失败：消费者看不到 `agent-deep-link` 与新 payload 字段。按项目规则运行既有 `npm run build:server` 重建 highlight/relay/protocol/client/server/CLI owner stack后，第二次 typecheck 通过。
- `npm run lint` 通过，0 warnings / 0 errors。最终 tracked/cached diff 为空，全部原有未跟踪任务资料仍在。
- `E:\Code\paseo-release-beta-4` 保持 clean 且 `main=origin/main=b64f4f357...`；按用户批准暂不删除。`E:\Code\paseo-release-tools` 保留为已验证 runtime。
```

### 10.4 原 `task_plan.md`

```text
# 任务计划：主工作区对齐与发布副本收口

- Task ID: `0006`
- Current Phase: `Review`
- Status: `COMPLETED`
- Approval Status: 已完成并关闭
- Current Blocker: 无

| 步骤                         | 状态      | 证据          |
| ---------------------------- | --------- | ------------- |
| 创建 Task Spec 与执行边界    | completed | `SPEC.md`     |
| fetch 后预检远端与未跟踪路径 | completed | `progress.md` |
| 建立旧 main 安全分支         | completed | `progress.md` |
| 对齐本地 main                | completed | `progress.md` |
| Node 22 依赖与静态验证       | completed | `progress.md` |
| 保留 release clone 并收口    | completed | `progress.md` |
```
