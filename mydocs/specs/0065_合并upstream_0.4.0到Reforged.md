# 合并 upstream v0.4.0 到 Reforged Spec

## 0. 状态与索引

| 字段              | 值                                                                                             |
| ----------------- | ---------------------------------------------------------------------------------------------- |
| task_id           | `0065`                                                                                         |
| spec layer        | `Feature Spec`                                                                                 |
| task status       | `已收口`                                                                                       |
| mode              | `single_project`                                                                               |
| phase             | `Review`                                                                                       |
| approval status   | `Approved`（Execution Approval: Approved）                                                     |
| approval source   | `User`（精确回复 `Plan Approved`，2026-08-14 22:28 +08:00）                                    |
| spec path         | `mydocs/specs/0065_合并upstream_0.4.0到Reforged.md`                                            |
| parent spec       | `N/A`                                                                                          |
| supersedes        | `N/A`                                                                                          |
| current task unit | 本地 `main` 已快进到双父 merge commit；临时分支/worktree 已清理；保持不 push、不打 tag、不发布 |
| created / updated | `2026-08-14 11:28 / 2026-08-15 13:12 +08:00`                                                   |

## 1. 目标、范围与完成契约

- 背景/问题：Paseo Reforged 本地 `main` 与 upstream 正式标签 `v0.4.0` 自共同基线 `v0.3.1` 后均有大量独立改动；直接合并会产生结构、协议、交互和产品意图冲突
- 最终目标：在不丢失 Reforged 已有能力、兼容约束、品牌与发布边界的前提下，把 upstream `v0.4.0` 的正式发布增量合入本地 `main`，并用定向证据证明解决结果
- 当前任务单元：补齐 0066/0067 local-only 功能文件的逐文件执行合同，在隔离 worktree 合并固定 `v0.4.0` commit，解析全部冲突并完成定向验证；Review 后按用户追加授权创建本地 merge commit
- 范围内：`merge/upstream-v0.4.0@f21eba8e91968ac1c216784d1e2d4822f5836321` 与 `v0.4.0^{commit}@b44bb63cf4ce089ab5750b9fc621ed52827b2820` 的三方差异；66 个 Git 冲突文件；77 个双方共同修改但自动合并的文件；0066/0067 的 5 个 local-only 产品文件；跨文件契约和用户交互冲突；实际解析、暂存、验证与本地双父 merge commit
- 范围外：upstream `v0.4.0` 之后的 `upstream/main`（远端当前 `30195d3bd`）；push、PR、tag、发布；合入任何 `origin/main`-only 提交（远端当前 `93ebc4cf4`）；修改或清理原 `main` worktree 的其他脏文件；启动或重启 `6767`；全量本地测试
- Done Contract：固定基线不漂移；逐一解析 66 个 Git 冲突文件并审计 77 个自动合并共同修改文件；0066/0067 的 18 个产品/测试/文档文件全部进入 B/C 或 local-only 清单；附录 D 的双方意图在最终 tree 成立；unmerged 与 conflict marker 均为 0；定向验证完成或明确记录环境阻塞；创建父提交为本地 `f21eba8e9` 与 upstream `b44bb63cf` 的本地 merge commit，worktree clean，且不 push/tag/release
- 失败或回炉方式：若远端标签、HEAD、共同基线、冲突集合、用户边界或关键意图发生变化，停止执行并回到 Research/Plan；若验证暴露未计划的产品分叉，保留 merge 状态和证据并等待用户决策，不静默发明第三种行为

### 1.1 最小任务单元判断

- 为什么当前任务单元足够小：66 个冲突文件、77 个自动合并文件和 5 个新增 local-only 功能文件共享同一基线、依赖顺序和最终集成验证，拆成多个活跃 Spec 会破坏单一合并真相源
- 验证证据：Git 对象 ID、`merge-base`、左右提交计数、`merge-tree --write-tree`、双方文件交集、逐文件提交与 diff 证据
- 模型可自主决定的范围：按已批准矩阵执行保留、采用、组合或删除方案；不得发明第三套产品行为；本地 commit 仅在用户追加明确授权后执行，push、tag、发布边界保持不变
- 拆分决定：`Accepted`

## 2. 上下文与调研

### 2.1 上下文来源

- 需求来源：用户要求调用 `sdd-riper-one` 建立 Heavy Spec 和逐文件解决矩阵并分析交互/意图冲突；随后要求从当前主分支创建隔离分支/worktree重评，现已精确回复 `Plan Approved`，授权补齐逐文件清单后执行，但不提交、推送或发布
- 项目事实源：`PROJECT.md`、根 `AGENTS.md`、`docs/`、`package.json`、双方 Git 对象与历史、相关代码和测试
- Codemap：`N/A`；本任务使用精确 Git 文件交集、冲突矩阵和项目既有架构文档作为范围索引，不另建并行地图
- Codemap Mode：`N/A`
- Context Bundle：`N/A`；需求和事实源已可直接索引，结论全部回写本 Spec
- Context Bundle Level：`N/A`
- 关联任务记录：`mydocs/specs/0060_合并upstream_0.3.1到Reforged.md`、`mydocs/micro_specs/0055_合并upstream_0.3.0-beta.4到Reforged.md`、`mydocs/micro_specs/0056_审计upstream_0.3.0正式版冲突.md`、`mydocs/specs/0041_ReplicaCache写放大治理.md`、`mydocs/micro_specs/0061_恢复最后一条消息编辑入口.md`、`mydocs/micro_specs/0063_修复标题栏边界拖拽死带.md`、`mydocs/micro_specs/0066_控制启动时恢复上次工作区.md`、`mydocs/micro_specs/0067_修复编辑提交的Provider消息定位.md`

### 2.2 调研结论

- 已确认事实：见附录 A-D；当前复现 66 个冲突文件、121 个内容冲突块和 3 个 modify/delete 冲突；与旧矩阵比较仅 `packages/app/src/hooks/use-settings/storage.test.ts` 从自动合并转为 `content × 2`，旧 65 个冲突文件的集合与块数均不变；附录 C 现审计 77 个自动合并文件，附录 D 补入 0066/0067 的新意图
- 未知与开放问题：当前没有阻塞 Execute 的方案分叉；0066 的真实客户端冷启动仍待手工验收，但其已提交行为是本次合并必须保留的基线；若 Git 集合再漂移或出现第三种产品意图，必须停止并回到 Plan
- 风险与约束：实际 merge 只在 `E:\Code\paseo-worktrees\merge-upstream-v0.4.0` 执行，原 `main` worktree 的 `mydocs/todolist.md`、本 Spec 与 `packages/desktop/scripts/dev.ps1` 继续保护且不在其中执行 Git 状态变更；不引入未登记 Skill 路由；协议保持双向兼容；lockfile/Nix 派生值最后重建；不运行全量测试；不触碰 `6767`
- `grilling` 结论（如使用）：`N/A`；基线和范围可从仓库事实确定，当前不需要追加用户访谈

### 2.3 方案与决策

- 备选方案：A. 整体偏向 upstream；B. 整体偏向 local；C. 按契约与意图逐文件三方整合
- 已选方案：C；以 upstream `v0.4.0` 的新架构/正式修复为默认演进方向，以 Reforged 的已验证差异、兼容层、品牌、平台能力和发布边界为必须显式保留项；不兼容时由完成目标和项目规则裁决
- 选择理由：A 会丢失 Reforged 能力和兼容约束，B 会错过上游数据校验、Agent Profiles、文件搜索、任务进度、worktree 和 provider 修复；C 能把冲突收敛为可验证合同
- Innovate：`Skipped`；核心策略已由 Git/任务历史和双方实现约束收敛，剩余工作是逐文件落实，不存在需要用户从多个架构路线中再选一条的分叉

### 2.4 下一步动作

- 先完成 0066/0067 local-only 逐文件清单和批准状态反向同步，再在隔离 worktree 复核固定对象并执行 `--no-commit` merge
- 解析、审计、验证和暂存已获批准；用户于 `2026-08-15 12:24 +08:00` 追加授权只创建本地 merge commit，push、PR、tag 与发布仍未获授权

## 3. 计划与执行前检查点

### 3.1 文件变化

| 项目/子项         | 文件或子 Spec                                                             | 计划变化                                                                                                | 原因                                               |
| ----------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| 合并基线          | `merge/upstream-v0.4.0` / `E:\Code\paseo-worktrees\merge-upstream-v0.4.0` | 仅在获批后从 `f21eba8e9` 合并 `v0.4.0^{commit}`，先复核对象 ID 与隔离 worktree 为 clean                 | 防止标签/HEAD 漂移，并隔离原工作区用户改动         |
| Git 冲突          | 附录 B 的 66 个文件                                                       | 按逐文件矩阵解析，不用整侧覆盖                                                                          | 保留双方可兼容意图                                 |
| 自动合并语义审计  | 附录 C 的 77 个文件                                                       | 检查跨文件契约、重复实现、能力门禁和测试组合                                                            | Git 无冲突不等于行为无冲突                         |
| 项目规则与 Skills | `PROJECT.md`、`skills/paseo*`、`public-docs/skills.md`                    | 接受产品 loop 删除并移除失效路由；Profiles 不绕过 Codex-native 与用户授权边界；不新增 `paseo-help` 路由 | upstream 的文件删除/新增不会自动维护本项目精确登记 |
| 0066 路由入口     | `packages/app/src/app/index.tsx`                                          | 保留设置 hydration gate，并把已安装客户端的默认入口交给 `resolveStartupRoute()`                         | 防止 upstream app shell 组合后绕过启动偏好         |
| 0066 路由决策     | `packages/app/src/navigation/host-runtime-bootstrap.ts`                   | 保留 Native/Electron 默认不恢复、显式开启才恢复；Web、深链、通知和会话内导航保持原行为                  | local-only 文件不会产生 marker，但会受设置契约影响 |
| 0066 路由测试     | `packages/app/src/navigation/host-runtime-bootstrap.test.ts`              | 保留默认 false、显式 true、Web 和外部路由优先级的行为矩阵                                               | 作为启动跨层合同的直接回归门禁                     |
| 0066 路由文档     | `docs/expo-router.md`                                                     | 保留冷启动偏好、平台边界和 hydration 顺序说明，并与最终实现同步                                         | 防止只保留代码而丢失路由事实源                     |
| 0067 Edit E2E     | `packages/app/e2e/browser/edit-last-user-message.ui-contract.spec.ts`     | 保留 distinct replacement 场景，证明 canonical row 最终使用 provider-native ID rewind                   | local-only E2E 是跨 App/server/provider 的验收证据 |
| 派生文件          | `package-lock.json`、`nix/npm-deps.hash`                                  | 所有源码和 manifest 决策完成后重新生成/校验                                                             | 不能从任一侧复制过期依赖图或 hash                  |
| 任务记录          | 本 Spec、`mydocs/todolist.md`                                             | Reverse Sync 阶段、偏差、证据和状态                                                                     | 保持单一真相源                                     |

0066/0067 合计修改 18 个产品、测试或文档文件：其中 13 个双方共同修改文件已逐项进入附录 B/C，剩余 5 个 local-only 文件已在上表逐项登记；两个 micro-spec 作为来源记录列于 2.1，不计入产品树执行矩阵。

### 3.2 签名与契约

| 项目/子项                | 接口、类型或签名                                         | 计划变化                                                                                                              | 兼容性                                                                               |
| ------------------------ | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| Wire schema              | `packages/protocol/src/messages.ts`、`agent-types.ts`    | 合并本地 edit/timeline/provider policy 与上游 fs/profile/task capability；被删除 chat/loop 仅保留有期限 legacy schema | 新字段 optional；旧客户端/daemon 双向解析；兼容 shim 带 `COMPAT(...)` 与移除期限     |
| Agent Profiles           | profile list / apply capability                          | 采用上游 profiles，字段保持 optional；能力同时要求 `agentProfiles` 与 `agentConfigApply`                              | whole-list persistence；apply 有序但非事务，失败可诊断                               |
| Replica Cache            | persisted DTO / cache version / flush scheduler          | 本地写调度架构 + 上游严格 Zod 边界和失效清除；组合格式使用新版本 `6`                                                  | 旧/异常快照 fail closed 并清除，不混读 v3/v5                                         |
| Agent lifecycle          | root agent 与 managed subagent close ownership           | root 保留 runtime-close；managed subagent 采用 layout/open-tab ownership                                              | 不误杀仍打开 subagent，不让已关闭 subagent 显示 active                               |
| Worktree setup           | fresh worktree / `paseo.json` source                     | 始终新建并自动后缀；选定 base ref 的 committed 配置为权威                                                             | 不把 source checkout 未提交设置带入新 worktree                                       |
| Provider terminalization | Claude MCP timeout / Codex retry 与 compaction           | Claude 采用 native timeout；Codex 同时保留本地 retry 过滤和上游 turn-end terminalization                              | 避免超时回归、重试误报和 compaction 悬挂                                             |
| Installed-app startup    | `restoreLastWorkspaceOnLaunch` / `resolveStartupRoute()` | 保留 0066 默认 `false` 的 Native/Electron 冷启动策略；字段显式加入 upstream strict Zod settings schema                | Web、深链、通知与会话内导航不变；旧存储缺字段时默认不恢复                            |
| Edit provider identity   | canonical timeline row / `providerMessageId`             | 保留 0067 的 `messageId -> providerMessageId` 翻译、provider ack fail-closed 和 in-place rewind                       | 不改 App/协议/provider rewind 公开契约；避免合并 AgentManager 生命周期代码时丢失翻译 |
| Keyboard routes          | `Cmd/Ctrl+P`                                             | 采用上游 workspace file search；New Workspace 路由到项目选择                                                          | 快捷键帮助、route 和测试一致                                                         |
| Mermaid                  | markdown fence renderer                                  | 以上游 fence 引擎为唯一实现，移植本地字体、链接与交互等价能力后移除重复引擎                                           | Web/Native 流式渲染、源码、缩放、拖动、链接行为保持                                  |

### 3.3 子 Spec 索引

`N/A`；本任务保持一个 Heavy Feature Spec，逐文件矩阵作为同一合并合同的附录

### 3.4 执行清单

- [x] 0. 从当前 `main@f21eba8e9` 创建 `merge/upstream-v0.4.0` 与隔离 worktree；只读重评集合、块数和新本地意图，发现 1 个新冲突文件后回写 Plan
- [x] 1. 执行前重读本 Spec 的基线、附录 B-D、验证矩阵和批准状态；确认收到精确 `Plan Approved`
- [x] 2. 在隔离 worktree 复核 HEAD/分支、`v0.4.0` tag/commit、merge base、远端 tag、clean status 和 `origin/main` 分叉；任一关键对象漂移即停止并回 Research（固定 tag/HEAD/base/count 稳定；动态远端 ref 前进按范围排除）
- [x] 3. 只在 `E:\Code\paseo-worktrees\merge-upstream-v0.4.0` 开始当前分支与固定 tag commit 的 `--no-commit` merge；不纳入远端 `upstream/main@30195d3bd` 或 `origin/main` 独有提交，不修改原 `main` worktree
- [x] 4. 先解析协议与删除迁移：chat/loop legacy wire、ScheduleSession、capability、Agent Profiles、task progress
- [x] 5. 再解析 server 领域层：Agent lifecycle/provider/session/worktree/archive/file observation，逐项同步相关测试
- [x] 6. 再解析 app 状态层：Replica Cache、HostRuntime、persisted settings、session/layout ownership；确保 strict schema 显式接受 `restoreLastWorkspaceOnLaunch` 且不破坏冷启动路由策略
- [x] 7. 再解析 app 交互层：Mermaid、file links/actions/diff、快捷键、composer、workspace close、标题栏，并同步 i18n
- [x] 8. 解析文档、品牌、Skill 和 package manifests；同步移除 `PROJECT.md` 的失效 `paseo-loop` 路由，Profiles 相关 Skills 保持 Codex-native/用户授权边界，不引入未登记的 `paseo-help` 委派，不整体覆盖 Reforged 规则
- [x] 9. 所有源码与 manifest 稳定后重新生成/校验 `package-lock.json`；用 pinned nixpkgs 的 `prefetch-npm-deps` 在两个独立一次性容器中重算 `nix/npm-deps.hash`，两次结果一致
- [x] 10. 检查 conflict markers、未合并 index、意外文件、冲突矩阵覆盖率和 Plan-Execution Diff；确认 0066 启动恢复与 0067 provider ID 翻译仍在最终 tree；未发现新语义分叉
- [x] 11. 按附录 E 完成受影响协议/存储/server/app 定向测试、typecheck、lint、目标格式与 Git 冻结检查；Windows harness/本机 CLI 环境阻塞已逐项记录，未运行全量本地测试且未启动或重启 `6767`
- [x] 12. 进入 Review，按三轴回写结果、剩余风险和总表状态；合并结果全部暂存并保留 `MERGE_HEAD`，完成用户此前“不 commit、push、tag 或发布”的阶段边界
- [x] 13. 收到用户追加授权后只创建本地 merge commit；核对双父关系、clean worktree 与无 tag，保持不 push、不发布

### 3.5 执行前检查点

- 当前目标与任务单元：在补齐 0066/0067 local-only 逐文件合同后，在隔离 worktree 执行固定 v0.4.0 合并并验证，停止在 merge commit 之前
- 当前 phase：`Execute`
- approval status / source：`Approved / User`；Execution Approval: Approved（`Plan Approved`，2026-08-14 22:28 +08:00）
- 下一步：复核固定基线、远端 tag、工作区隔离和冲突集合；完全一致后开始 `--no-commit` merge
- 风险与回退：最大风险是 strict settings schema 静默拒绝 0066 字段、合并 AgentManager 时丢失 0067 provider ID 翻译、自动合并掩盖其他契约错配、删除旧实现时丢失兼容层、重复 Mermaid/状态引擎和派生依赖失真；执行前任何漂移都回 Research，执行中意图不明回 Plan
- 验证方式：Git 结构检查 + 受影响单文件 Vitest `--bail=1` + workspace typecheck/lint + 目标 format + 必要的 Web/Electron 定向交互；不运行全量套件
- TDD 判定、测试 seam 与验收行为：`N/A`；本轮不改可观察行为、不写测试，Execute 时以双方既有测试和合并回归为主，新增行为测试需先回 Plan 记录 seam
- seam 确认：`N/A`；当前无 TDD 写入

## 4. 跨项目扩展

`N/A`；任务只在 `E:\Code\paseo` 单仓库内进行，不读取或修改其他 checkout

## 5. 执行记录

| 步骤/子项          | 实际变化或子 Spec 锚点                                                                           | 状态   | 偏差与处理                                                                                          |
| ------------------ | ------------------------------------------------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------- |
| Research/Plan      | 仅更新 `mydocs/todolist.md` 与本 Spec；完成 65/78 逐文件矩阵、36 个交互/意图冲突和 38 项验证门禁 | 已完成 | 未执行 merge、未改产品/规则/Skill 文件                                                              |
| 隔离工作区         | 创建 `merge/upstream-v0.4.0@f21eba8e9` 与 `E:\Code\paseo-worktrees\merge-upstream-v0.4.0`        | 已完成 | 用户明确授权此设置；新 worktree 创建后 clean，原 `main` worktree 脏文件未被带入或覆盖               |
| 基线重评           | 确认 local HEAD 从 `54706089c` 变为 `f21eba8e9`；重算 66/77 矩阵、121 块与 0066/0067 意图        | 已完成 | 按旧 Plan 停止条件回 Research；矩阵增量更新后回到 `Plan / Pending`，未执行 merge                    |
| Execute 批准       | 用户精确回复 `Plan Approved`；补齐 0066/0067 的 5 个 local-only 产品文件                         | 已完成 | 授权解析、审计、验证与暂存；当时明确不授权 commit、push、tag 或发布；后续追加 commit 授权已单独记录 |
| 固定 tag 合并      | 在隔离 worktree 保留 `HEAD=f21eba8e9`、`MERGE_HEAD=b44bb63cf`，按附录 B-D 解析源码与语义冲突     | 已完成 | 66 个冲突文件已全部解析；635 个变更文件全部暂存，unmerged/unstaged/untracked 均为 0                 |
| 合并遗漏修复       | 补回 strict settings/local activity/close-tab policy 契约，迁移 AgentManager lifecycle 测试 seam | 已完成 | 都是批准矩阵内的双方意图组合；对应 App/AgentManager 定向测试已通过                                  |
| Mermaid 收口       | 保留单一 upstream fence runtime，加入 Native/Web 安全边界、窄屏工具栏和 viewer 失败源码回退      | 已完成 | 新增浏览器/viewport 定向测试；旧 Mermaid 引擎保持删除                                               |
| Server 编译        | `npm run build:server` 已生成 highlight/relay/client，server typecheck 报缺少 helper import      | 已通过 | 从现有 `agent-projections.js` 恢复 upstream 同名 import；完整 server/CLI build 退出 0               |
| Client SDK fixture | upstream SDK 测试使用 provider options，但默认 server_info 未声明本地兼容 gate capability        | 已修复 | 仅在该测试连接中声明 `agentProviderOptions: true`；保留旧 daemon 缺 gate 时 fail-closed             |
| Lock/Nix 派生      | 最终 manifests 重建 lock；两个独立 Nix 容器对同一 lock 运行 pinned `prefetch-npm-deps`           | 已通过 | lock blob 两次一致；Nix hash 两次均为 `sha256-DtrdFMPeSJo9QNw+5kMSaj/SUeXGtn5wcnZJCIlsPGk=`         |
| 最终冻结           | 根 typecheck/lint、App typecheck、Mermaid browser 单测、cached diff/unmerged/marker/资源检查     | 已通过 | 静态检查退出 0；Mermaid `1/1`；无未合并或残留进程/容器，本轮测试临时目录已清理                      |
| 本地 merge commit  | 用户追加授权后执行 `merge: upstream v0.4.0 into Reforged`                                        | 已完成 | `0aa4e10397e02e1f96e0ff44135d9171f63c144d`；父提交为 `f21eba8e9` 与 `b44bb63cf`；worktree clean     |

## 6. 验证

| 项目/验收项              | 命令或步骤                                                                                                                 | 结果    | 证据                                                                                                                                                                                                 | 未验证原因                                                                                                  |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 当前基线                 | `git rev-parse`、`git merge-base`、`git rev-list --left-right --count`                                                     | PASS    | `f21eba8e9` / `bfec7ac3a` / `111 75`；见附录 A                                                                                                                                                       | N/A                                                                                                         |
| 远端标签                 | 枚举 upstream tags，并精确核对 `refs/tags/v0.4.0` 及其 dereference                                                         | PASS    | 最新正式 tag 仍为 `v0.4.0`；object `020e29703`、commit `b44bb63cf` 均未移动                                                                                                                          | N/A                                                                                                         |
| 冲突集合                 | `git merge-tree --write-tree --name-only --messages HEAD v0.4.0^{commit}`                                                  | PASS    | 66 文件；63 content + 3 modify/delete；121 个 content 块                                                                                                                                             | N/A                                                                                                         |
| 旧/新冲突差分            | 比较旧附录 B 与新 merge-tree 文件/块映射                                                                                   | PASS    | 仅新增 `storage.test.ts` `content × 2`；原 65 文件集合与块数不变                                                                                                                                     | N/A                                                                                                         |
| 自动合并共同修改集合     | 双方 `git diff --name-only` 交集减冲突集合                                                                                 | PASS    | 143 - 66 = 77；只有 `storage.test.ts` 从旧附录 C 移入 B                                                                                                                                              | N/A                                                                                                         |
| Git 冲突逐文件矩阵       | 附录 B 结构计数与块数求和                                                                                                  | PASS    | 66 行；121 个 content 块；3 个 modify/delete                                                                                                                                                         | N/A                                                                                                         |
| 自动合并语义矩阵         | 附录 C 结构计数                                                                                                            | PASS    | 77 行；与固定文件集合一一对应                                                                                                                                                                        | N/A                                                                                                         |
| 交互与验证矩阵           | 附录 D-E 结构计数及方案审阅                                                                                                | PASS    | 38 个交互/意图场景；38 个执行/验证门禁                                                                                                                                                               | N/A                                                                                                         |
| 0065 文档格式            | `npm run format:files -- <0065>` 后 `npm run format:check:files -- <0065>`                                                 | PASS    | 目标格式化完成，复检退出 0                                                                                                                                                                           | N/A                                                                                                         |
| 共享总表格式             | `npm run format:check:files -- mydocs/todolist.md`                                                                         | FAIL    | formatter 报告该文件存在格式差异                                                                                                                                                                     | 内含共享 0066/0067 与用户改动，本轮不整表重排                                                               |
| 产品实现与运行时         | 附录 E                                                                                                                     | PASS    | 协议、SDK、存储、生命周期、provider、watcher、App 状态、Mermaid 与 Edit E2E 均有定向 GREEN 证据                                                                                                      | 未运行完整本地套件，按项目规则留给 CI                                                                       |
| Relay 构建产物           | `npm run build:server` 的 `build:server-deps`                                                                              | PASS    | `packages/relay/dist/index.js` 已生成，highlight/relay/client 均退出 0                                                                                                                               | N/A                                                                                                         |
| Server/CLI 完整构建      | `npm run build:server`                                                                                                     | PASS    | server `build:lib/build:scripts` 与 CLI TypeScript 编译均退出 0                                                                                                                                      | N/A                                                                                                         |
| Server 紧反馈环          | `npm run typecheck --workspace=@getpaseo/server`                                                                           | PASS    | 修复前 6 秒稳定报告 `session.ts:2761-2762 TS2304`；补 import 后退出 0                                                                                                                                | N/A                                                                                                         |
| 0067 Edit E2E            | `npm run test:e2e --workspace=@getpaseo/app -- e2e/browser/edit-last-user-message.ui-contract.spec.ts`                     | PASS    | 3/3；distinct replacement、运行/附件门禁、provider history fallback 均通过                                                                                                                           | N/A                                                                                                         |
| Protocol wire            | 附录 E 第 8 项的 3 个单文件 Vitest                                                                                         | PASS    | `8/8`、`28/28`、`12/12`，均单文件独立退出 0                                                                                                                                                          | N/A                                                                                                         |
| Client daemon API        | `npx vitest run packages/client/src/daemon-client.test.ts --bail=1`                                                        | PASS    | `124/124`                                                                                                                                                                                            | N/A                                                                                                         |
| Client public SDK        | `npx vitest run packages/client/src/index.test.ts --bail=1`                                                                | PASS    | 修复前 `9/10`；补测试 capability 后 `10/10`                                                                                                                                                          | N/A                                                                                                         |
| Client examples          | `npm run typecheck:examples --workspace=@getpaseo/client`                                                                  | PASS    | examples typecheck 退出 0                                                                                                                                                                            | N/A                                                                                                         |
| Agent storage/import     | `agent-storage.test.ts`、`import-sessions.test.ts` 各自单文件运行                                                          | PASS    | `33/33`、`27/27`                                                                                                                                                                                     | N/A                                                                                                         |
| Schedule/worktree        | `schedule-session.test.ts`、`worktree.test.ts`、POSIX 单文件运行                                                           | PASS    | `2/2`、`19/20 (1 skip)`、Windows 下 POSIX `53/53 skip`                                                                                                                                               | N/A                                                                                                         |
| Workspace archive        | 首次并行批次超时后串行重跑 `workspace-archive-service.test.ts`                                                             | PASS    | 串行 `16/16`；并行失败为 Windows 临时目录/句柄竞争，未复现业务失败                                                                                                                                   | 后续验证避免并行 archive/worktree 资源竞争                                                                  |
| Watcher/file observer    | Git observation、file-observer、file-explorer observer 单文件运行                                                          | PASS    | `41/41`、`13/13`、`4/4`                                                                                                                                                                              | N/A                                                                                                         |
| Provider contracts       | registry/config/Codex/OMP/OpenCode/Pi 与 Claude mock 定向测试                                                              | PARTIAL | `49/49`、`5/5`、`135/135`、`36/36`、`96/96`、`60/60`、Claude `68/68 + 2/2`；仅本机 Claude CLI version 探测阻塞                                                                                       | `claude.exe` 为无效 Windows 应用，非代码断言                                                                |
| Hub controllers          | `execution-controller.test.ts`、`relationship-controller.test.ts`                                                          | PARTIAL | execution `7/7`；relationship harness 在默认 5 秒启动超时，串行仍复现                                                                                                                                | Windows harness 启动环境阻塞，未出现产品断言失败                                                            |
| File actions E2E         | `changes-pane.spec.ts --grep=changes.*explicit.*kebab`                                                                     | BLOCKED | selector 仅命中 1 条；默认环境下 isolated daemon 20 秒未监听；补 `PASEO_NODE_INSPECT=0` 与 Git `usr/bin` PATH 后 workspace/diff 已加载，但 Playwright/Metro teardown 超过 184 秒外层时限             | Windows E2E harness 冷启动与 teardown 阻塞；fixture 清理后的 `Workspace unavailable` 截图不作为产品失败证据 |
| 组合 lockfile            | `npm install --package-lock-only --workspaces --include-workspace-root --ignore-scripts`；`fix-lockfile.mjs`；离线二次生成 | PASS    | 根和 10 个 workspace 均为 `0.4.0`；2940 entries；workspace links 完整；缺失离线元数据 0；无 Parcel Watcher；包含 Mermaid/YAML/esbuild；二次 blob 均为 `062809f29098a639ff320c863c2841b6c723decc`     | 未运行 lifecycle scripts，避免修改共享 Git hooks                                                            |
| Nix npm hash             | 两个独立 `nixos/nix:latest` 容器以 pinned nixpkgs `9dcb002c...` 运行 `prefetch-npm-deps`                                   | PASS    | 两次均得到 `sha256-DtrdFMPeSJo9QNw+5kMSaj/SUeXGtn5wcnZJCIlsPGk=`，已写入 sidecar；两个容器均退出 0 并删除                                                                                            | 远端 Docker 不支持 Windows bind mount，未直接运行脚本 wrapper；执行的是脚本解析出的同一 pinned 命令         |
| 根静态检查               | `npm run typecheck`；`npm run lint`                                                                                        | PASS    | typecheck 退出 0；lint 0 warning / 0 error                                                                                                                                                           | N/A                                                                                                         |
| App/Mermaid 静态与浏览器 | App typecheck；`test:browser` 单文件运行 `host.web.browser.test.tsx`                                                       | PASS    | App typecheck 退出 0；Mermaid browser `1/1`                                                                                                                                                          | N/A                                                                                                         |
| 最终 Git 冻结            | `git add -A`；`git diff --cached --check`；unmerged/marker/status/anchor 检查                                              | PASS    | 提交前 635 staged 且无 unstaged/untracked/unmerged/index stages；marker 0；cached diff check 通过                                                                                                    | N/A                                                                                                         |
| 资源清理                 | 检查 worktree 相关进程、临时 Nix 容器、测试目录与 `6767`                                                                   | PASS    | 无相关进程/容器；本轮 `.tmp`、4 个 changes 调试目录和 `packages/app/test-results` 已删除；`6767` 仍监听于 PID `29800`                                                                                | PID 在恢复会话前已由旧记录的 `33056` 变为 `29800`，本轮未启停或重启该服务                                   |
| 本地 merge commit        | `git show -s --format=%H%n%P%n%s HEAD`；`git status --short --branch`；`git tag --points-at HEAD`                          | PASS    | `0aa4e10397e02e1f96e0ff44135d9171f63c144d`；双父 `f21eba8e9` / `b44bb63cf`；worktree clean；HEAD 无 tag                                                                                              | N/A                                                                                                         |
| 提交 hook 门禁           | Lefthook `pre-commit`；手工根 typecheck/lint；staged 格式批次检查                                                          | PARTIAL | hook wrapper 和直接 Lefthook 均因 Windows `cmd.exe` 找不到 `node/npm` 在启动前失败；手工 typecheck/lint 通过；staged 格式首批 50 个文件中 48 个受 CRLF/既有格式基线影响，目标新增/手工文件此前已通过 | 未重排 635 个合并文件；提交使用 `LEFTHOOK=0`，仅在上述等价门禁完成后执行                                    |

- 集成验证：`PASS`；依赖构建、协议/server/app/Mermaid 定向测试、0067 Edit Playwright `3/3`、根/App typecheck、根 lint、两次独立 Nix hash、最终 Git 冻结和本地双父 merge commit 均有证据；File actions 已确认 workspace/diff 可加载，但 Windows harness teardown 阻塞完整场景判定
- 剩余风险：Claude 完整单文件测试受本机无效 `claude.exe` 阻塞，但其余 68 项和 env 2 项通过；Hub relationship harness 默认 5 秒启动超时；File actions E2E 受 Windows isolated daemon 冷启动与 Playwright/Metro teardown 阻塞；完整本地测试、真实 provider、真机和发布验证按范围未运行；本轮 `git ls-remote` 因 TLS 握手失败，远端 tag 以已核对的本地 tag object/commit 为证据
- Done Contract 是否由证据满足：`是`；66 个 Git 冲突和 77 个自动合并共同修改文件已按矩阵处理，0066/0067 意图保留，Nix 派生值重算，本地双父 merge commit 已创建且 worktree clean；push、tag、发布未执行

## 7. 评审（Review）

| 评审轴             | 结论   | 证据或阻塞问题                                                                                             |
| ------------------ | ------ | ---------------------------------------------------------------------------------------------------------- |
| 目标与 Spec 完成度 | `PASS` | 隔离 worktree、固定基线、66/77 文件矩阵、38 个交互/意图场景、派生值和最终冻结均已闭环                      |
| Spec 与执行一致性  | `PASS` | 仅实施批准矩阵内的双方意图组合；追加 commit 授权、hook 环境偏差和最终双父提交均已记录，未 push/tag/publish |
| 实现质量与风险     | `PASS` | typecheck/lint/diff/unmerged/marker 均通过；定向测试覆盖关键契约，环境型缺口保持显式                       |

- Overall Verdict：`PASS`（本地 merge commit 已完成，状态已收口）
- Blocking Issues：`无本地实现阻塞`；push、tag 与发布未获授权，三项环境型验证缺口保留给后续/CI
- Cross-project consistency：`N/A`

### 7.1 回归风险

| project_id | Regression risk | 依据                                                                                               |
| ---------- | --------------- | -------------------------------------------------------------------------------------------------- |
| `paseo`    | `High`          | 607 个 upstream 增量文件、143 个共同修改文件，涉及 wire、持久化、Agent 生命周期、工作区和跨平台 UI |

### 7.2 Touched Projects

`N/A`

- Orphan changes：`None for 0065`；原 `main` worktree 的 `packages/desktop/scripts/dev.ps1`、本 Spec 和总表脏状态不计入合并 worktree；0066/0067 已作为当前 `main` 提交基线的产品意图纳入 0065

## 8. 偏差、变更与反向同步

- Plan-Execution Diff：Execute 新增 0066/0067 的 5 个 local-only 产品文件作为显式逐文件清单；合并过程中补回 strict settings 字段、local activity `phase` 契约、close-tab policy 导出/测试，并将 AgentManager 生命周期测试迁移到 `listAllMetadata()` seam；Mermaid viewer 在二次失败时回退源码；server 编译确认 `session.ts` 手工合并时漏掉 upstream 已有的 `resolveStoredAgentPayloadUpdatedAt` import，已按原签名恢复；Client SDK 测试的 upstream fixture 使用 provider options 却未声明本地 `agentProviderOptions` capability，仅补测试握手能力而不删除兼容 gate；组合 lock 采用 `--package-lock-only --ignore-scripts` 生成以避免共享 Git hook 副作用，语义等价于计划中的 `release:prepare` 依赖解析；Windows 本机无 Nix/WSL，改用项目 0060 已验证的一次性远端容器方法，两次运行脚本解析出的 pinned `prefetch-npm-deps` 命令并得到一致 hash；其余均为批准矩阵内的兼容收口，不改变已批准产品决策
- Plan-Execution Diff：Review PASS 后用户把边界从“保留未提交 merge”精确扩展为“只创建本地 merge commit，不推送、不打 tag、不发布”；正常 Git hook 与直接 Lefthook 均因 Windows `cmd.exe` 丢失 `node/npm` PATH 在检查启动前失败，手工重跑根 typecheck/lint 通过，staged 格式检查复现 CRLF/既有格式基线后未扩大为 635 文件重排；仅在等价证据完成后以 `LEFTHOOK=0` 创建本地 commit
- Change Log：`2026-08-14 11:28 +08:00` 创建 Heavy Spec，固定 Research 范围和初始计划；`12:12 +08:00` 完成 65/78 逐文件矩阵、36 个交互/意图冲突与 38 项验证门禁，进入 Plan checkpoint；`18:49 +08:00` 创建隔离分支/worktree，发现 `main` 增加 0066/0067 两个提交，重评并更新为 66/77、121 块和 38 个意图场景，完成结构/格式校验；`22:28 +08:00` 收到 `Plan Approved`，补齐 5 个 local-only 文件并进入 Execute；执行期间完成依赖构建、App typecheck、布局/存储/启动/生命周期/Mermaid 定向证据，Edit Playwright 首次因隔离 daemon 缺少 relay dist 产物在断言前失败；`2026-08-15 03:56 +08:00` 复核 `6767` 仍由 PID `33056` 监听且无测试端口/相关 worktree 进程；固定 `HEAD/tag/MERGE_HEAD/base/count` 复核通过，观察到 `upstream/main=b03dc804`、`origin/main=ec1d7e13` 已前进但仍排除，`git ls-remote` TLS 失败不改变本地 tag 证据，下一步构建 server/relay 后重跑
- Change Log：`2026-08-15 11:45 +08:00` 从中断点恢复；确认 66 个冲突全部解析、根 typecheck/lint、App typecheck 与 Mermaid browser `1/1` 通过；两个独立 Nix 容器均计算出 `sha256-DtrdFMPeSJo9QNw+5kMSaj/SUeXGtn5wcnZJCIlsPGk=`，替换临时占位；最终 635 个文件全部暂存，cached diff/unmerged/marker/资源检查通过；恢复时 `6767` 已由 PID `29800` 监听，本轮未启停该服务；清理本轮测试产物后进入 Review PASS
- Change Log：`2026-08-15 12:24 +08:00` 用户明确授权只创建本地 merge commit；`12:25 +08:00` 创建 `0aa4e10397e02e1f96e0ff44135d9171f63c144d`，双父、消息、clean worktree、无 tag 与 `6767` 未受影响均核对通过
- Change Log：`2026-08-15 13:12 +08:00` 未发现本会话遗留的 Node/Vitest/Playwright/Metro/Electron 测试进程；用 `--ff-only --autostash` 将本地 `main` 从 `f21eba8e9` 快进到 `0aa4e1039`，既有脏改动已自动恢复且临时 stash 已删除；非强制删除 `merge/upstream-v0.4.0`，注销并删除隔离 worktree 的物理目录；复核 `android-perf-0068` 未受影响、stash 为空、`6767` 仍由 PID `29800` 监听，未 push、tag 或发布
- 用户决策：调用 `sdd-riper-one`；建立 Heavy Spec 和逐文件解决矩阵；继续分析交互/意图冲突；先不执行；随后要求从当前 `main` 开新分支/新 worktree 并重评；批准补齐逐文件清单后执行，最初明确不提交、推送或发布；Review PASS 后追加授权只创建本地 merge commit，仍禁止 push、tag 和发布
- Spec 反向同步结果：固定基线、附录 B-E、local-only 文件清单、Execute/Review/commit/清理证据、hook 偏差、剩余风险和总表 0065 已收口状态已同步

## 9. 恢复、长期知识与提交关联

- 状态说明：`已收口 / Review / PASS / Approved`
- 当前卡点：`无本地任务卡点`；push、tag 与发布仍明确禁止，Claude CLI、Hub relationship harness 和 File actions E2E 的环境型缺口保留为后续风险
- 下一步唯一动作：`无`；push、tag 或发布需要新的明确授权
- Resume / Handoff 锚点：主工作区为 `E:\Code\paseo`，本地 `main=0aa4e10397e02e1f96e0ff44135d9171f63c144d`；父提交为本地 `f21eba8e91968ac1c216784d1e2d4822f5836321` 与 upstream `b44bb63cf4ce089ab5750b9fc621ed52827b2820`；临时分支 `merge/upstream-v0.4.0` 及隔离 worktree 已删除，HEAD 无 tag；Nix hash 为 `sha256-DtrdFMPeSJo9QNw+5kMSaj/SUeXGtn5wcnZJCIlsPGk=`；活跃 Spec 与总表保留在主工作区且未单独提交
- Project Sync Candidates：`无`；当前都是一次性合并事实，留在本 Spec
- 长期文档同步：`已按合并矩阵同步相关 docs/；本次一次性合并事实仅保留在本 Spec`

### 提交记录

| 提交信息（Commit Message）                            | 提交脚注（Commit Footer） | 关联项目 / 改动或阶段  | 文档同步状态      | 备注                                                                             |
| ----------------------------------------------------- | ------------------------- | ---------------------- | ----------------- | -------------------------------------------------------------------------------- |
| `merge: upstream v0.4.0 into Reforged`（`0aa4e1039`） | `N/A`                     | `paseo / v0.4.0 merge` | `已同步至本 Spec` | 本地双父 merge commit；未 push/tag/publish；Spec/总表仍在原 main worktree 未提交 |

## 附录 A：固定基线与冲突统计

| 项目                       | 值                                                                                      | 解释                                                                                                                                               |
| -------------------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 本地分支                   | `merge/upstream-v0.4.0@f21eba8e91968ac1c216784d1e2d4822f5836321`                        | 从当前 `main` 创建的隔离分支；相对当前 `origin/main` ahead 4 / behind 4，该跟踪 ref 不是合并基线                                                   |
| upstream tag object        | `v0.4.0@020e297038326d86b1afff0986afc371dd26020d`                                       | 完整枚举远端 tags 后确认 `v0.4.0` 仍是最新正式 tag；同名 object 已精确核对                                                                         |
| upstream tag commit        | `b44bb63cf4ce089ab5750b9fc621ed52827b2820`                                              | `chore(release): cut 0.4.0`                                                                                                                        |
| merge base                 | `bfec7ac3adc5e8835e873ee75c7b325af6c7a8c3`                                              | upstream `v0.3.1`                                                                                                                                  |
| 分叉计数                   | local-only `111` / upstream-only `75`                                                   | `git rev-list --left-right --count HEAD...v0.4.0^{commit}`                                                                                         |
| upstream 增量              | `607 files, +36707/-14444`                                                              | merge base 到 tag commit                                                                                                                           |
| 本地增量文件               | `734`                                                                                   | merge base 到新 local main                                                                                                                         |
| 共同修改                   | `143`                                                                                   | 双方 name-only 交集                                                                                                                                |
| Git 冲突                   | `66 files`                                                                              | 63 content + 3 modify/delete；121 个 content 块                                                                                                    |
| 自动合并共同修改           | `77 files`                                                                              | 必须做语义审计；仅 `storage.test.ts` 从旧自动合并集合移入冲突集合                                                                                  |
| named-ref 模拟 tree        | `a3d9e428c6d93a5d0d81c49ce6cbcd460a57ca63`                                              | `git merge-tree --write-tree --name-only --messages HEAD v0.4.0^{commit}`；marker 标签为 ref 名，仅诊断对象                                        |
| fixed-OID 模拟 tree        | `a2c47471419d4f1c2d8e93fefa9ac3c372fad7d9`                                              | 两个固定 commit OID 的同一模拟；marker 标签不同会改变 blob/tree hash，但 66 个文件集合、121 个块数完全相同，因此 tree hash 本身不作为 Execute 门禁 |
| upstream/main 远端当前 ref | `30195d3bd18854a7472c5e86dc8f0c9ccda8907a`                                              | 远端 ref 已超出 v0.4.0；本次不拉取、不合入，也不用本地旧 `upstream/main` 计数替代                                                                  |
| origin/main 独有提交       | `cec97b8b9` / `92115248b` / `c45749a28` / `93ebc4cf4`                                   | 不在本次 tag 合并范围；当前 `main...origin/main = 4 / 4`；后续 ref 继续前进也只记录并排除，不作为 0065 Execute 漂移门禁                            |
| 受保护脏文件               | 原 `E:\Code\paseo` 的 `mydocs/todolist.md`、`packages/desktop/scripts/dev.ps1`、本 Spec | 合并 worktree 从 committed `main@f21eba8e9` 创建且 clean；原 worktree 不做 Git 状态变更，不重置或覆盖                                              |

## 附录 B：Git 冲突逐文件解决矩阵

计数口径：`content × N` 中的 N 是固定 merge-tree 中的冲突块数；`modify/delete` 不计内容块。下表恰好 66 行、覆盖 121 个内容冲突块和 3 个 modify/delete 冲突，不使用整侧覆盖。

| 文件                                                                             | 类型 / 块     | Local 意图                                                                                                                               | Upstream v0.4.0 意图                                                                             | 解决方案                                                                                                                                                                                                  | 取舍                                                                  | 定向验收                                                                                                                                      |
| -------------------------------------------------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `CHANGELOG.md`                                                                   | content × 1   | 保留 Reforged 从 beta 到 `0.3.1-beta.1` 的实际发布记录                                                                                   | 增加 `0.4.0-beta.1/.2` 与 `0.4.0` 的上游发布记录                                                 | 保留既有 Reforged 历史，吸收上游 0.4.0 用户可见变更并明确其来源；本次合并不宣称 Reforged 已发布 0.4.0                                                                                                     | 不照搬上游发布时间、仓库链接或已发布状态                              | 标题唯一、时间顺序正确、链接指向 Reforged 或明确标注 upstream，未出现虚假发布声明                                                             |
| `CLAUDE.md`                                                                      | content × 1   | 作为根 `AGENTS.md` 的符号链接目标，承载 Reforged 项目规则与 SDD/授权流程                                                                 | 用文档索引更新自定义文件 watcher 相关说明                                                        | 保留本地规则文件为权威；只把 watcher 的已验证事实同步到对应 `docs/`，不以 upstream 的简化索引覆盖规则                                                                                                     | 放弃 upstream 对该文件的结构重排，避免删除本地门禁                    | `AGENTS.md` 链接仍有效，所有被引用模板、Skill 和文档路径存在，规则无降级                                                                      |
| `CONTRIBUTING.md`                                                                | content × 1   | 保留 Reforged 仓库、开发分支和贡献入口                                                                                                   | 修正文档拼写及措辞                                                                               | 移植纯文字修正，保留 Reforged URL、仓库名、命令和贡献边界                                                                                                                                                 | 只吸收可验证编辑，不恢复 upstream 品牌                                | 链接和命令可解析，无 `getpaseo/paseo` 被误写为本仓库权威来源                                                                                  |
| `docs/agent-lifecycle.md`                                                        | content × 1   | 关闭 root Agent 标签时调用 runtime-close，失败不提交布局删除                                                                             | 父 Agent 归档时保留仍打开的 managed subagent，并用 open-tab label 表示 UI 所有权                 | 按 Agent 类型拆分：root 使用 runtime-close；managed subagent 只更新当前 client 的 open-tab label/布局，父归档仅关闭无人持有的 child                                                                       | 生命周期不再由单一“关标签”等同于归档；需要维护两条明确路径            | 文档、单标签和批量关闭测试覆盖 root 成功/失败、打开 child、最后一个 owner、父归档四类行为                                                     |
| `docs/data-model.md`                                                             | content × 1   | 记录 lazy metadata catalog、持久 Timeline、消息 phase、runtime 状态                                                                      | 删除 chat/loop 数据模型，加入 Agent Profiles、task progress 与 subagent open-tab ownership       | 合并字段与实体；chat/loop 只在有期限的 legacy wire 兼容段出现，不再作为活动存储模型                                                                                                                       | 文档更复杂，但与组合后的协议/存储一致                                 | 文档字段能逐一映射到 schema/record；无已删除 service、表或 CLI 被描述为活动能力                                                               |
| `docs/release.md`                                                                | content × 4   | 保留 Reforged beta/stable 渠道、资产、bundle/package identity 与人工授权边界                                                             | 要求 changelog 基于 PR/Issue 事实并采用可核验措辞                                                | 保留本地发布流程和远端边界，加入逐 PR/Issue 核验与事实化 changelog 门禁；不采用上游仓库的发布身份                                                                                                         | 增加准备成本，减少发布记录误报                                        | 发布命令、workflow 名、资产数和远端来源与本仓库事实一致；仍需独立发布授权                                                                     |
| `nix/npm-deps.hash`                                                              | content × 1   | 对应包含 Reforged Mermaid/Panzoom 等依赖的现有 lockfile                                                                                  | 对应 upstream 0.4.0 lockfile 的固定输出 hash                                                     | 所有 manifest 和 lockfile 收口后运行 `./scripts/update-nix.sh` 重算；任一侧 hash 都不直接采用                                                                                                             | 需要 Nix 环境；Windows 本机不可用时使用项目既有 Linux/Nix 路径        | 两次独立预取值一致，`./scripts/update-nix.sh --check` 通过，hash 只对应最终 lockfile                                                          |
| `package-lock.json`                                                              | content × 6   | 保留 Reforged 依赖、内部包身份和已修复 lock signature                                                                                    | 升级全 workspace 到 0.4.0，加入 Mermaid/YAML/esbuild，移除 Parcel Watcher                        | 先收口全部 manifest，再运行 `npm run release:prepare` 和 `node scripts/fix-lockfile.mjs package-lock.json` 生成组合 lockfile                                                                              | 不手选六个文本块，允许 npm 产生可解释的派生差异                       | 无冲突标记；内部版本全部一致；`npm ci`/workspace link、lock signature 和 Nix hash 校验通过                                                    |
| `packages/app/e2e/browser/changes-pane.spec.ts`                                  | content × 4   | 验证大 diff 懒渲染、连续右键菜单直接切换和既有文件卡片合同                                                                               | 验证树形布局、右键触发、新建/重命名/复制/revert/delete 与不可选中文件名                          | 合并 fixture 选项与场景；以最终树形 UI 为 locator，保留连续右键直切和懒渲染断言，增加新动作与破坏性确认                                                                                                   | 测试更长，需避免依赖排序脆弱的 `diff-file-N`                          | 指定 Playwright spec 通过；两文件连续右键不需先关闭，折叠状态不触发大 diff 物化，新动作均命中正确路径                                         |
| `packages/app/package.json`                                                      | content × 1   | 保留 Reforged App 脚本、平台依赖与包身份                                                                                                 | 版本升到 0.4.0，并加入唯一 Mermaid runtime 构建链依赖                                            | 采用 0.4.0 内部版本和 upstream `build:mermaid-runtime` 依赖；删除本地旧 Mermaid build script 引用，保留 Reforged 身份/平台脚本                                                                            | 本地 Mermaid 引擎不再独立维护                                         | manifest 只有一个 Mermaid 构建入口；目标 build、typecheck 与 lockfile 解析通过                                                                |
| `packages/app/src/agent-stream/layout.test.ts`                                   | content × 1   | 验证 history/live head 展平后的 activity fold、final answer 与 footer                                                                    | 验证 Markdown/Mermaid 的 `streaming`/`complete` phase 传播                                       | 在本地完整 history 边界测试上增加 phase 断言，覆盖 active 与 complete 两态                                                                                                                                | 不采用 upstream 较窄 fixture 替代性能回归场景                         | 单文件 Vitest 通过；fold 项目不丢失，fence phase 与 turn 状态一致                                                                             |
| `packages/app/src/agent-stream/layout.ts`                                        | content × 1   | 用展平后的 `historyItems` 计算真实 history/live boundary                                                                                 | 向 Markdown 布局项目传递 `phase`                                                                 | 保留 `historyItems` 与本地 boundary index，额外传入 `phase: isTurnActive ? "streaming" : "complete"`                                                                                                      | 组合对象字段增加但不改变折叠算法                                      | layout 单测覆盖跨 history/head fold 和两种 phase，无重复/漏项                                                                                 |
| `packages/app/src/assistant-file-links/link.tsx`                                 | content × 1   | 文件链接保留 code-surface、hover、复制忽略的行号标签和跨平台 Text 结构                                                                   | Web 使用统一 `MarkdownLinkText` 改善可读性和链接样式                                             | 让 `MarkdownLinkText` 承载链接行为，在其内部保留本地带 dataset 的 Text/行号标签；Native 继续走现有边界                                                                                                    | Web DOM 多一层但统一样式，需防止嵌套交互元素                          | browser 单测/组件检查确认 hover、复制、line label、源码字体和点击定位均保持                                                                   |
| `packages/app/src/components/file-actions-menu.tsx`                              | content × 4   | 保留显式 trigger、固定宽度、header 和连续 context menu 切换所需结构                                                                      | 增加新建文件/目录、折叠、重命名、复制、revert、删除及 destructive 分组                           | 保留本地 trigger/context 两种入口，共用一个动作构建器；取动作并集，separator 与 destructive 色只由元数据驱动                                                                                              | 动作模型扩大，必须确保 capability/文件类型门禁集中                    | 组件与 changes-pane E2E 覆盖动作可见性、顺序、分隔、危险色、直接切换和禁用态                                                                  |
| `packages/app/src/components/left-sidebar.tsx`                                   | content × 1   | `TitlebarDragRegion ownsWindowTopEdge` 消除 Windows 顶边拖拽死带                                                                         | 开发构建显示 source branch badge                                                                 | 同一 chrome row 同时保留 `ownsWindowTopEdge` 和条件化 `DEV_BUILD_LABEL` badge                                                                                                                             | 标题栏空间减少，需约束 badge 截断                                     | 0063 verifier 继续通过；开发标签可见且不遮挡窗口控制、导航或拖拽区域                                                                          |
| `packages/app/src/components/markdown/renderer.tsx`                              | content × 2   | 使用本地跨平台 Mermaid fence、闭合检测、workspace 字体与链接策略                                                                         | 统一到 `components/markdown/fence`，通过 `info` 与 `phase` 驱动新 Mermaid runtime                | 以上游 fence 为唯一引擎；适配 `info`/`phase`，把本地闭合语义、字体、链接/安全、源码视图和缩放拖动等价能力移入新模块后删除旧入口                                                                           | 放弃两套实现并存；需要一次跨平台迁移                                  | renderer、Mermaid render-model/runtime/source-policy/browser 与指定 E2E 通过；全树无旧入口 import                                             |
| `packages/app/src/components/message.tsx`                                        | content × 4   | 保留 edit eligibility、消息 phase、本地 todo 样式和工具显示偏好                                                                          | 传递 Markdown phase，并用统一 task list status/card 展示实时任务                                 | 采用 upstream task card/phase；保留本地 edit、tool preference 和消息 phase；旧 `todo_list` 输入通过单一映射进入新组件                                                                                     | 删除本地重复 task row 样式，视觉以 upstream 状态模型为准              | message/stream/reducer 测试覆盖 pending/in-progress/completed、edit、fold 与 Markdown streaming                                               |
| `packages/app/src/components/ui/menu/menu-overlay.tsx`                           | content × 1   | `setWebOverlayNode` 维护浮层 passthrough/连续切换节点                                                                                    | 在 overlay scope 抑制浏览器原生 context menu                                                     | 保留节点 ref 语义并组合 `onContextMenu.preventDefault`；若 upstream ref 名承载额外 scope，则用 callback ref 同步两者                                                                                      | 需要一个组合 ref，避免任一行为被覆盖                                  | 连续右键 E2E、浮层层级/外点关闭和浏览器原生菜单抑制均通过                                                                                     |
| `packages/app/src/composer/agent-controls/control.tsx`                           | content × 1   | 保留 risk color 所需 `TextStyle`/style callback 类型                                                                                     | 用 `ComponentProps` 统一 tooltip/trigger props                                                   | 合并类型导入并保持本地样式类型；采用 upstream tooltip props 透传                                                                                                                                          | 仅类型与共享控件组合，无产品取舍                                      | typecheck 通过；risk color、disabled/hover 和 tooltip 行为测试不回归                                                                          |
| `packages/app/src/git/diff-pane.tsx`                                             | content × 2   | 保留懒渲染、展开路径回调、collapsed folders 和直接菜单切换                                                                               | 加入树选择、duplicate/revert 和专用 diff action content                                          | 以 upstream 树/selection 为骨架，保留本地 expanded/collapsed 外部状态与懒物化；动作统一走合并后的 menu builder                                                                                            | selection 与 expansion 分离，状态面增加                               | changes-pane spec 覆盖折叠恢复、未展开大 diff、右键直切、duplicate/revert 目标路径                                                            |
| `packages/app/src/hooks/use-settings/storage.test.ts`                            | content × 2   | 新增 0066 `restoreLastWorkspaceOnLaunch` 默认关闭和显式开启持久化断言                                                                    | 新增各 `THEME_OPTIONS` 持久化测试，严格 schema 异常值回默认                                      | 合并两组测试；配套 `StoredAppSettingsSchema` 明确加 `restoreLastWorkspaceOnLaunch: z.boolean().optional()`，保留默认 `false`，不让严格 parse 把合法 0066 字段清掉                                         | 异常不明细字段仍 fail-closed；合法启动偏好不丢失                      | 单文件 Vitest 覆盖 theme 并集、默认 false、true 持久化、strict 异常回默认                                                                     |
| `packages/app/src/i18n/resources/ar.ts`                                          | content × 2   | 保留 edit、runtime-close、timeline/字体和 Reforged 文案                                                                                  | 增加 diagram、task、subagent、profiles、file actions/worktree 文案                               | 取 key 并集；阿拉伯语保留已有翻译，新 key 采用 upstream 译文，缺失只按既有 fallback 规则处理                                                                                                              | 不为了对齐删掉 local-only key                                         | locale completeness 测试通过，无重复 key，核心新增交互不显示原始 key                                                                          |
| `packages/app/src/i18n/resources/en.ts`                                          | content × 2   | 同上，且 English 是本地新增功能文案事实源                                                                                                | 同上，English 是 upstream 新功能文案事实源                                                       | 精确取 key/语义并集，统一 close/archive/profile/task 术语                                                                                                                                                 | 需人工消除同义 key，不保留两套动作名                                  | resources 测试通过；所有新增 key 在 English 存在且调用点语义一致                                                                              |
| `packages/app/src/i18n/resources/es.ts`                                          | content × 2   | 保留 local-only keys                                                                                                                     | 增加 upstream 0.4.0 keys                                                                         | 取并集并采用对应西语翻译，未翻译项显式 fallback                                                                                                                                                           | 接受少量既有 fallback，不伪造翻译                                     | completeness 与 key parity 通过，新增动作无空字符串                                                                                           |
| `packages/app/src/i18n/resources/fr.ts`                                          | content × 2   | 保留 local-only keys                                                                                                                     | 增加 upstream 0.4.0 keys                                                                         | 取并集并采用对应法语翻译，未翻译项显式 fallback                                                                                                                                                           | 同上                                                                  | completeness 与 key parity 通过，新增动作无空字符串                                                                                           |
| `packages/app/src/i18n/resources/ja.ts`                                          | content × 2   | 保留 local-only keys                                                                                                                     | 增加 upstream 0.4.0 keys                                                                         | 取并集并采用对应日语翻译，未翻译项显式 fallback                                                                                                                                                           | 同上                                                                  | completeness 与 key parity 通过，新增动作无空字符串                                                                                           |
| `packages/app/src/i18n/resources/ko.ts`                                          | content × 2   | 保留 local-only keys                                                                                                                     | 增加 upstream 0.4.0 keys                                                                         | 取并集并采用对应韩语翻译，未翻译项显式 fallback                                                                                                                                                           | 同上                                                                  | completeness 与 key parity 通过，新增动作无空字符串                                                                                           |
| `packages/app/src/i18n/resources/pt-BR.ts`                                       | content × 2   | 保留 local-only keys                                                                                                                     | 增加 upstream 0.4.0 keys                                                                         | 取并集并采用对应巴葡翻译，未翻译项显式 fallback                                                                                                                                                           | 同上                                                                  | completeness 与 key parity 通过，新增动作无空字符串                                                                                           |
| `packages/app/src/i18n/resources/ru.ts`                                          | content × 2   | 保留 local-only keys                                                                                                                     | 增加 upstream 0.4.0 keys                                                                         | 取并集并采用对应俄语翻译，未翻译项显式 fallback                                                                                                                                                           | 同上                                                                  | completeness 与 key parity 通过，新增动作无空字符串                                                                                           |
| `packages/app/src/i18n/resources/zh-CN.ts`                                       | content × 2   | 保留 edit、runtime-close、timeline/字体和 Reforged 中文文案                                                                              | 增加 diagram、task、subagent、profiles、file actions/worktree 中文文案                           | 取 key 并集并统一“关闭运行时/归档/仅关闭标签”等生命周期术语                                                                                                                                               | 需要人工校对，不把不同生命周期动作翻成同一个词                        | resources 测试通过；关键 close/profile/task/file action 文案人工核对                                                                          |
| `packages/app/src/keyboard/keyboard-shortcuts.test.ts`                           | content × 1   | `Cmd/Ctrl+P` 在 New Workspace/输入场景可打开项目选择                                                                                     | `Cmd/Ctrl+P` 在工作区搜索文件                                                                    | 全局采用 upstream `command-center.files`；New Workspace 页面用独立 `workspace.project.pick` action/route 接管项目选择，不共享同一 binding                                                                 | 旧快捷键语义按页面拆分，帮助文案以文件搜索为主                        | 单测覆盖 Mac/非 Mac、message-input、other、New Workspace、command center 已打开等 scope                                                       |
| `packages/app/src/keyboard/keyboard-shortcuts.ts`                                | content × 2   | focus scope 已支持 string/array，服务项目选择例外                                                                                        | 正式化多 scope 并新增 file search binding/help                                                   | 保留 array matcher 的等价实现，采用 upstream file search binding；项目选择不留在全局 binding                                                                                                              | 删除注释/实现重复，保留自定义 binding ID 兼容                         | shortcut 表、help row、override 与 scope 测试通过，无同 chord 同 scope 双命中                                                                 |
| `packages/app/src/runtime/host-runtime.test.ts`                                  | content × 1   | 冷启动时 stored registry 先 ready，session 可稍后建立                                                                                    | 删除 host 前先撤销 push notification                                                             | 保留两个独立测试场景并验证顺序：boot readiness；revoke 完成后 remove/cache cleanup                                                                                                                        | 测试 fixture 需要同时支持 deferred revocation 和 cold session         | 单文件 Vitest 通过，失败 revocation 的处理策略与实现一致，冷启动不误删 host                                                                   |
| `packages/app/src/runtime/replica-cache/index.test.ts`                           | content × 5   | 验证分域 dirty revision、尾随写、并发 flush、失败重试、stop 清理                                                                         | 验证严格持久化 schema、版本失效、越界/损坏清除与降低流式写入                                     | 重构 MemoryStorage 同时记录 attempt/payload/count/gate；保留本地调度测试并加入 upstream schema/失效测试，DTO 版本改为 6                                                                                   | fixture 更复杂但可观测真实写入时序                                    | 单文件 Vitest 覆盖持续流、双 final、并发失败、remove host、v3/v5/损坏 payload、最大尺寸和 stop                                                |
| `packages/app/src/runtime/replica-cache/index.ts`                                | content × 10  | 分 host/域 dirty revision、投影复用、final 合并、flush/drain 与失败不丢脏状态                                                            | Zod 严格校验、incompatible cache 失效清除、版本边界和基础写入降频                                | 以本地 scheduler 为主体，入口/DTO 采用 upstream strict schema；组合格式升到 `6`，解析失败或旧版本 fail closed 并删除存储，写成功后按捕获 revision 清脏                                                    | 不迁移 v3/v5 缓存，首次启动重新同步；换取不混读                       | index.test 全部通过；失败写可重试、并发更新不丢、host 删除立即失效、坏缓存不注入 store                                                        |
| `packages/app/src/screens/settings/appearance/apply-appearance.test.ts`          | content × 1   | 验证独立 workspace 字号和统一 UI font stack                                                                                              | 验证所有 `REGISTERED_THEMES` 切换一致                                                            | 保留字体断言并参数化遍历注册主题；明确 UI 与 workspace font size 常量不同                                                                                                                                 | 增加组合矩阵但不做全视觉测试                                          | 单文件 Vitest 通过；每个主题更新颜色且字体/字号不被重置                                                                                       |
| `packages/app/src/screens/settings/appearance/apply-appearance.ts`               | content × 1   | 使用 `WORKSPACE_FONT_SIZE` 和独立字体设置                                                                                                | 使用 `REGISTERED_THEMES`/`FONT_SIZE` 修复主题切换                                                | 同时导入 registered themes、UI font size 与 workspace font size；分别更新对应 token，不用一个常量覆盖两层                                                                                                 | 维持两套字号语义                                                      | appearance 测试与 typecheck 通过，实时切换主题/字体/字号互不重置                                                                              |
| `packages/app/src/screens/workspace/workspace-bulk-close.test.ts`                | content × 1   | root Agent runtime-close 失败时不移除 tab，并上报 outcome                                                                                | managed subagent 只关闭 layout label，需归档者与 terminal 批量提交                               | 按 close policy 分组测试：layout-only、runtime-close root、terminal；不把同一 Agent 同时发给两条关闭通道                                                                                                  | 批量路径不再是单一 `closeItems`                                       | 单文件 Vitest 覆盖 partial failure、无 client、open child、root/child 混合组和 cleanup 次数                                                   |
| `packages/app/src/screens/workspace/workspace-bulk-close.ts`                     | content × 3   | 对每个 root Agent 调用 `closeAgentRuntimeAndCommit`，成功才清布局                                                                        | `layout-only` child 更新 label；archive Agent 可与 terminal 批量 `closeItems`                    | 先按 policy 分类；child 调 `closeLayoutOnlyAgent` 后清布局；root 走 runtime-close；terminal 单独批量关闭，禁止 root 被二次 archive                                                                        | 多一次分类与串行等待，换取所有权正确                                  | bulk-close 测试确认每个 tab 恰好一个副作用，失败不误删，warning 可诊断                                                                        |
| `packages/app/src/screens/workspace/workspace-screen.tsx`                        | content × 2   | 单标签关闭 root 运行时并在成功后提交布局                                                                                                 | managed subagent 通过 per-client open-tab label 关闭；普通 Agent 归档                            | 复用统一 close policy：managed child 更新 label/重读后只关布局；root 调本地 runtime-close，不再额外 archive；保留 running confirmation                                                                    | root 与 child 的“关闭”结果不同，UI 文案必须区分                       | workspace/bulk-close 测试和定向 E2E 覆盖 running cancel、label failure、runtime failure、last owner                                           |
| `packages/app/src/types/host-connection.ts`                                      | content × 3   | Direct TCP 支持自定义 headers，并在持久化 normalize/equality 中保留                                                                      | 对整个 host registry 使用 strict Zod，坏记录 fail closed                                         | 把 optional normalized `headers` 加入 strict Direct TCP schema，再用 parsed shape normalize；equality 比较 canonical headers                                                                              | strict object 会拒绝未知旧字段；兼容字段必须显式列出                  | host connection/HostRuntime 测试覆盖 headers round-trip、键规范化、坏日期/坏连接/未知字段和默认 appearance                                    |
| `packages/protocol/src/agent-types.ts`                                           | content × 1   | `AgentMessagePhase` 区分 commentary/final answer，并服务 edit/timeline                                                                   | 新增 `AgentTaskItem` 及 task status/activeForm                                                   | 同时导出两个类型，并让 timeline/task payload 引用共享 `AgentTaskItem`                                                                                                                                     | 协议面增大但字段均 optional                                           | protocol 与 stream typecheck/测试通过，旧 payload 不带 task/phase 仍可解析                                                                    |
| `packages/protocol/src/messages.ts`                                              | content × 2   | Hub create error details、timeline/edit/provider policy capability 与安全兼容                                                            | Hub validate RPC、fs/profile/task/worktree capabilities                                          | 保留本地 error schema和旧 capability，加入 upstream validate/RPC/capability；新字段全 optional，legacy chat/loop schema保留带期限兼容但不注册实现                                                         | server_info 变大，旧客户端忽略新字段；暂留 legacy schema              | wire-compat、hub、ws outbound 验证双向解析；`COMPAT(...)` 均有版本与移除日期                                                                  |
| `packages/server/package.json`                                                   | content × 1   | 保留 Reforged server 版本/依赖和目标测试脚本                                                                                             | 升级 0.4.0 内部依赖，移除 Parcel Watcher并加 watcher measurement/repro                           | 采用 upstream watcher/0.4.0 依赖与脚本，保留 Reforged identity及本地仍使用的依赖；最后生成 lockfile                                                                                                       | Parcel 原生依赖移除可改善安装，但 watcher 行为需回归                  | server typecheck、目标 observation tests、package files 与 lockfile 无 dangling Parcel import                                                 |
| `packages/server/src/server/agent/agent-manager.test.ts`                         | content × 1   | 使用 lazy metadata catalog 与 durable timeline，验证本地投影/恢复                                                                        | 验证 parent 存在和 open child label 保护                                                         | fixture 同时提供 `listAllMetadata`/timeline store 与 `getOpenAgentTabLabel`；新增 parent missing/open owner 场景，不删本地性能/恢复断言                                                                   | test harness 依赖面增加                                               | 单文件 Vitest 覆盖 catalog generation、parent missing、opened child、archive child 与 timeline                                                |
| `packages/server/src/server/agent/agent-manager.ts`                              | content × 1   | 遍历使用轻量 `listAllMetadata`避免 materialize 全记录，并保留 0067 canonical edit target 到 `providerMessageId` 的翻译与 ack fail-closed | 归档 children 前确认 parent 存在并尊重 open-tab ownership                                        | 使用 `listAllMetadata` 找 parent/children，parent 缺失报错；仅对候选 child 再 `get` materialize 并执行 owner 检查；合并后保留 `requireLastUserMessageEditTarget()` 返回 `providerMessageId` 并传入 rewind | 保留两阶段读取与 edit ID 翻译，代码多一步但避免性能回退和实际编辑失败 | manager 测试同时证明无全量 materialize、parent missing fail、open child 保留、closed child 归档；0067 Edit Playwright 与 provider ID 断言通过 |
| `packages/server/src/server/agent/agent-sdk-types.ts`                            | content × 1   | 对外导出 `AgentMessagePhase`/notice，支持消息分类                                                                                        | 对外导出 `AgentTaskItem`/notice，支持 live tasks                                                 | 三者全部导出并确保 provider event 类型引用同一协议定义                                                                                                                                                    | 无互斥取舍                                                            | server/app typecheck 通过，provider 编译无本地重复 task 类型                                                                                  |
| `packages/server/src/server/agent/import-sessions.ts`                            | content × 2   | provider session import 有 owner index、prepared record、fingerprint、事务 marker 和崩溃恢复                                             | 用 `listByProviderSession` 精确找 active/archived record并阻止重复导入                           | 保留完整事务状态机；owner 查询改用 `listByProviderSession`，再构造本地 resolved owner/fingerprint；active 拒绝，archived 仅在 cwd/parent 合法时恢复                                                       | 查询 API 更精确，但必须保持旧 marker 可恢复                           | import/transaction 单文件测试覆盖 duplicate、archived revive、cwd mismatch、crash before/after commit 与 title/fingerprint                    |
| `packages/server/src/server/agent/providers/claude/agent.ts`                     | content × 1   | 将 `providerOptions.env` 与 launch env 注入，并强制 MCP 10 分钟 timeout                                                                  | 删除强制 timeout，让 Claude SDK 使用原生 MCP timeout；支持 task progress/可配置 catalog deadline | env overlays 为 `[providerOptions.env, launchEnv]`；删除 `MCP_TIMEOUT`/`MCP_TOOL_TIMEOUT` 注入，其余 upstream provider 行为照收                                                                           | 用户显式 env 可覆盖原生设置；默认不再被 Reforged 强制拉长             | Claude 单测覆盖 env precedence、未注入 timeout、task event 与 catalog deadline                                                                |
| `packages/server/src/server/agent/providers/codex-app-server-agent.ts`           | content × 1   | 忽略 retry 中无 turnId 的 terminal event及 stale turn completion                                                                         | turn 完成时 terminalize pending root compactions，避免 subagent/compaction 悬挂                  | 先执行本地 retry/stale turn gate，仅对当前有效 completion 调 `completePendingRootCompactions()`，再分发 completed/failed/interrupted                                                                      | stale event 不会清当前 compaction，可能需额外明确的 shutdown cleanup  | Codex tests 覆盖 retry、旧 turn、completed/failed/interrupted、root/subagent compaction 与 plan mode                                          |
| `packages/server/src/server/agent/providers/codex/test-utils/fake-app-server.ts` | content × 2   | fake 支持显式 turnId、retry error、warning、plan 和 compaction                                                                           | fake 支持 compaction item、terminal status/error                                                 | 取 API 并集；`completeTurn` 同时接受 threadId/turnId/status/error，保持 current-turn map 清理；保留 retry/warn helpers                                                                                    | fixture 参数更多但避免各测试自造通知                                  | fake 自身消费测试编译，Codex 单文件测试所有通知路径通过                                                                                       |
| `packages/server/src/server/agent/providers/omp/agent.ts`                        | content × 1   | 保留 edit-last-message extension 常量与固定 catalog timeout                                                                              | catalog refresh deadline 改为配置驱动                                                            | 保留 edit extension 常量，删除固定 `OMP_CATALOG_REQUEST_TIMEOUT_MS`，从 registry/runtime settings 获取 deadline                                                                                           | 默认 deadline 服从统一配置                                            | OMP edit、catalog timeout 与 provider registry 定向测试通过                                                                                   |
| `packages/server/src/server/chat/chat-mentions.test.ts`                          | modify/delete | 对 chat mention 查找改用 lazy metadata catalog                                                                                           | 连同 chat rooms/agent loops 删除整个实现与测试                                                   | 接受删除；不移植 catalog 优化到已删除领域，相关能力只保留 legacy wire schema                                                                                                                              | 失去 chat room mention 功能，符合 upstream 产品删除决策               | 全树无 import/CLI/route；旧消息可解析但无活动 handler；server typecheck 通过                                                                  |
| `packages/server/src/server/chat/chat-mentions.ts`                               | modify/delete | 同上，避免全量 Agent materialize                                                                                                         | 删除 chat mentions runtime                                                                       | 接受删除，调用方一并清理                                                                                                                                                                                  | 同上                                                                  | `rg` 无活动引用，server typecheck 与 session tests 通过                                                                                       |
| `packages/server/src/server/hub/execution-controller.test.ts`                    | content × 1   | 验证 create error details 会脱敏 provider options、prompt、Authorization                                                                 | controller 新增 `validateAgentConfiguration`，支持 Hub 多文件 workflow preflight                 | fixture 同时注入 validator 和含 secret 的错误；保留精确脱敏断言并加入 validation success/failure                                                                                                          | validation 失败多一个错误路径                                         | 单文件 Vitest 证明 secret 不出站，validate/create requestId 和错误 code 保持                                                                  |
| `packages/server/src/server/session.ts`                                          | content × 2   | 使用 `ChatScheduleLoopSession`、lazy catalog 与 persistence-handle index                                                                 | 删除 chat/loop，改为独立 `ScheduleSession`；provider session 用精确 query 选最新 record          | 实例改为 `ScheduleSession`，保留 schedule 行为但不带 loop/chat host；lookup 使用 `listByProviderSession` 并按更新时间选取，catalog 路径继续用于其他调用                                                   | 产品 loop 消失；schedule 保留且边界更窄                               | session/schedule/import tests 通过；无 ChatService 构造；重复 handle 选择确定且可恢复                                                         |
| `packages/server/src/server/session/chat/chat-schedule-loop-session.ts`          | modify/delete | 在合并 chat/schedule/loop 类中加入 lazy metadata catalog                                                                                 | 删除该聚合类，由 `session/schedule/schedule-session.ts` 取代                                     | 接受删除；把仍有效的 schedule 通知/Agent 解析语义移入新 `ScheduleSession`，loop/chat 不迁移                                                                                                               | 类路径和测试迁移，loop 功能删除                                       | 新 schedule 单文件测试覆盖 create/update/fire/通知；全树无旧类引用                                                                            |
| `packages/server/src/server/test-utils/session-stubs.ts`                         | content × 1   | stub lazy catalog、generation、materialization 与 persistence handle lookup                                                              | stub `listByProviderSession`/`listByWorkspace` 查询 API                                          | stub 取并集，并给每个方法最小确定默认值                                                                                                                                                                   | 共享 stub 面变大，但减少测试强制 cast                                 | session/server 定向 tests 不因缺失 method 抛错，类型不使用宽泛 `any` 绕过                                                                     |
| `packages/server/src/server/websocket-server.ts`                                 | content × 1   | 广播 provider options/tool policy、edit/timeline/runtime close 能力                                                                      | 广播 fs ops/duplicate/discard、Agent Profiles/config apply/task/worktree 能力                    | features 取并集并保留每个 `COMPAT` 期限；profiles UI 必须同时检查 `agentProfiles` 与 `agentConfigApply`                                                                                                   | capability 数量增加，不实现 fallback                                  | server_info schema/validation 和旧客户端测试通过，缺任一 profile gate 时 UI 隐藏                                                              |
| `packages/server/src/server/workspace-archive-service.test.ts`                   | content × 2   | 归档使用 lazy metadata 列表并保留目录/最后引用所有权测试                                                                                 | 用 `listByWorkspace` 只取目标 workspace 的 stored Agents                                         | fixture 改用 `listByWorkspace`，保留本地所有归档/目录/终端断言并验证不会访问其他 workspace                                                                                                                | 查询更窄，测试需显式断言参数                                          | 单文件 Vitest 覆盖目标隔离、stored-only/live Agent、last-reference 和失败继续策略                                                             |
| `packages/server/src/server/workspace-archive-service.ts`                        | content × 2   | 归档合同依赖 metadata catalog，避免 materialize payload                                                                                  | 精确 `listByWorkspace` 查询提高正确性和性能                                                      | 采用 `listByWorkspace(workspaceId)`；保留本地 worktree 目录所有权、archiving marker、terminal/setup 清理和错误日志合同                                                                                    | 放弃通用全 catalog 路径，减少扫描                                     | archive tests 证明只归档目标 Agent，查询失败仍按既有 fail-soft 处理且不误删目录                                                               |
| `packages/server/src/server/workspace-git-service.observation.test.ts`           | content × 5   | 用 deferred/显式 microtask drain 稳定 watcher 与 fetch race                                                                              | 验证 custom watcher 下 idle refresh 不饿死 daemon，并暴露 in-flight metrics                      | 使用 upstream `runGitFetch`/metrics 断言，同时保留本地 deferred gate 和确定性 drain；不依赖未等待的初始 listener                                                                                          | 测试步骤更显式，减少定时器偶然性                                      | 单文件 Vitest 多次运行稳定；fetch 与 narrow refresh 顺序、metrics 回零、dispose 均满足                                                        |
| `packages/server/src/server/worktree-core.ts`                                    | content × 1   | 同 slug 时复用已有 worktree 并返回 `created:false`                                                                                       | 始终创建 fresh worktree，冲突时自动生成唯一后缀                                                  | 删除 reuse fast path，采用 upstream fresh/unique 语义；返回值始终对应本次创建                                                                                                                             | 增加磁盘占用，但避免旧 worktree 状态/配置污染                         | worktree tests 证明同 slug 连续创建路径/branch 不同，旧 worktree 不被修改                                                                     |
| `packages/server/src/utils/worktree.posix.test.ts`                               | content × 1   | 覆盖既有创建、setup 和 Reforged worktree include 行为                                                                                    | 增加 symlink/readlink 与 fresh worktree、committed `paseo.json` 行为                             | 合并 fs imports和场景；明确配置从选定 base ref 的 committed blob读取，`.worktreeinclude` 仍只处理明确列出的本地文件                                                                                       | 未提交 `paseo.json` 不再隐式进入新 worktree                           | 单文件 Vitest 覆盖 dirty source config、base ref config、copy/symlink、同 slug 后缀及 POSIX cleanup                                           |
| `packages/server/src/utils/worktree.ts`                                          | content × 1   | `checkout-branch` 走本地 helper并保留 metadata base ref                                                                                  | 校验分支、必要时 fetch origin，已 checkout 时创建唯一 local branch                               | 采用 upstream validation/fetch/unique branch流程，保留 `metadataBaseRefName` 与本地配置读取合同                                                                                                           | 不再复用已 checkout branch；网络 fetch 失败明确报 UnknownBranch       | worktree 单测覆盖 local/remote/已 checkout/未知分支和 metadata base；不复制 source 未提交配置                                                 |
| `skills/paseo/SKILL.md`                                                          | content × 1   | 保留 Reforged 品牌、当前 MCP/CLI 用法、授权与 `6767` 保护                                                                                | 加入 Profiles 驱动的 Agent 创建和 provider diagnosis，并委派给新 `paseo-help`                    | 保留本地身份/权限边界；内联经过核验的 profile materialization/诊断最小流程，不委派未在 `PROJECT.md` 登记的 `paseo-help`                                                                                   | 根 Skill 稍长；避免隐式引入新 Skill 路由                              | Skill frontmatter/命令与 0.4.0 CLI/tool catalog 一致；无未登记委派、无 upstream-only 路径/远端被当作本地权威                                  |

## 附录 C：自动合并共同修改文件语义审计矩阵

`Auto` 只表示 Git 文本层没有产生 marker。下表恰好 77 行，逐项记录双方意图、自动合并后的语义风险、收口方案和验收；其中“保留自动结果”也必须完成所列审计后才能接受。

| 文件                                                                        | Local 意图                                                                                                   | Upstream v0.4.0 意图                                                             | 自动合并语义风险                                                                         | 解决方案                                                                                                                                                                  | 定向验收                                                                                                     |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `docs/architecture.md`                                                      | 记录 session 性能栈、lazy catalog 与 root runtime-close                                                      | 删除 chat/loop并加入 workspace rename                                            | 文档可能同时描述已删除聚合 session 与新生命周期                                          | 保留本地性能/所有权模型，删除活动 chat/loop 叙述并加入 rename、ScheduleSession 与 child ownership                                                                         | 名词和组件路径可在最终树找到；与 lifecycle/data-model 文档无矛盾                                             |
| `docs/development.md`                                                       | 记录本地 Mermaid 构建和 Reforged 开发流程                                                                    | 记录 clean worktree config、desktop clean shutdown、rename 与 custom watcher     | 自动结果可能仍让开发者运行已删除的旧 Mermaid build script或 Parcel watcher               | 改为唯一 upstream Mermaid runtime 构建命令，保留 Reforged 环境/端口规则并吸收新 watcher、shutdown、worktree 事实                                                          | 所有命令在最终 manifest 存在；无旧 Mermaid/Parcel 路径；`6767/6768` 规则不变                                 |
| `docs/glossary.md`                                                          | 保留现有 workspace/Agent/生命周期术语                                                                        | 加入 Agent Profiles并删除 chat/loop 术语                                         | 被删除词条可能仍被其他段落引用                                                           | 加入 profile/config apply，移除活动 chat/loop 定义；legacy wire 仅链接兼容章节                                                                                            | glossary 引用闭合，术语与协议类型同名                                                                        |
| `docs/hub.md`                                                               | 保留 Reforged Hub policy/兼容处理                                                                            | Hub 不可达时 daemon 本地断连                                                     | 离线状态可能与本地持久 session/重连语义冲突                                              | 合并断连策略，明确本地 session 数据保留和再次关联条件，不弱化本地安全 policy                                                                                              | Hub relationship tests 与文档状态转换一致                                                                    |
| `docs/providers.md`                                                         | 记录 providerOptions/toolPolicy 与 Reforged provider 行为                                                    | 可配置 catalog deadline、Claude native timeout、Codex compaction terminalization | 文档可能把 provider deadline 与 run/MCP timeout 混为一谈                                 | 分别记录 catalog refresh deadline、provider native timeout、turn terminalization和用户 env precedence                                                                     | 每个配置项有真实字段/默认源，未声称强制 10 分钟 MCP timeout                                                  |
| `package.json`                                                              | 保留 Reforged root metadata、workspace 脚本和 0.3.1 线                                                       | 根版本改为 0.4.0                                                                 | 版本能自动合并，但 repository/homepage/description 可能仍指 upstream 或版本不同步        | 采用 `0.4.0` 共享版本，保留 Reforged repository/homepage/description 与现有脚本；用同步脚本核对所有 workspace                                                             | root/internal package versions一致；identity 字段与本地发布边界一致                                          |
| `packages/app/app.config.js`                                                | 保留 Reforged app name、slug、bundle/package ID 与平台配置                                                   | 限制 Android provider snapshot storage                                           | 新配置可能在覆盖对象中重置本地 ID 或 release channel                                     | 保留所有 identity 字段，仅并入经过 schema 校验的 Android storage 设置                                                                                                     | Expo config 输出含 Reforged IDs且 provider snapshot 上限生效                                                 |
| `packages/app/e2e/browser/agent-timeline-pagination-old-daemon.spec.ts`     | 验证旧 daemon timeline分页、hydration 与 local timeline兼容                                                  | 验证损坏 persisted client state 被拒绝                                           | strict cache可能让 old-daemon fixture 在进入分页前被清空                                 | fixture 提供合法旧版本 state，并另建损坏 state 场景；保留 old-daemon 双向兼容断言                                                                                         | 指定 Playwright spec 通过，合法旧状态可用、损坏状态 fail closed                                              |
| `packages/app/e2e/browser/appearance-theme-picker.spec.ts`                  | 稳定 Reforged 测试 seam/locator                                                                              | 主题选择与 runtime theme保持一致                                                 | 本地字体设置可能被 theme 切换重置但测试未覆盖                                            | 保留稳定 locator，增加 theme 后 UI/workspace font不变的断言                                                                                                               | 指定 spec 通过且 reload 后 theme/font组合保持                                                                |
| `packages/app/e2e/browser/new-workspace-codex-mode-preferences.spec.ts`     | 保留 New Workspace 的 Codex mode/preferences 行为                                                            | 严格校验 persisted settings                                                      | 合法 local-only preferences可能被 strict schema 丢弃                                     | 扩展 schema后保留合法字段，并对坏字段单独回退；项目选择 action按新路由触发                                                                                                | 指定 spec 覆盖 valid round-trip、invalid fallback 与 New Workspace `Cmd/Ctrl+P`                              |
| `packages/app/e2e/browser/workspace-model-restart.spec.ts`                  | 保留 workspace model restart和合并后稳定合同                                                                 | persisted client state严格校验                                                   | restart可能恢复默认值而不是用户选择                                                      | 使用最终 schema生成 fixture并验证 reload/restart 后 model、mode、font和profile兼容                                                                                        | 指定 spec 通过，无静默丢失合法设置                                                                           |
| `packages/app/src/agent-stream/strategy-web.test.tsx`                       | 验证 activity folding、retained layout和 final-first 渲染                                                    | 验证 Mermaid streaming phase                                                     | 自动组合可能只在 view传 phase，strategy未保持边界                                        | 在既有性能 fixture中加入 fence phase/identity断言                                                                                                                         | 单文件 browser Vitest覆盖 fold、streaming/complete切换且 DOM不重复挂载                                       |
| `packages/app/src/agent-stream/strategy-web.tsx`                            | 保留 history/live 展平和折叠策略                                                                             | 向 message renderer传 Markdown phase                                             | phase可能由当前 turn状态错误地套到历史块                                                 | 只对 live/current turn传 streaming，历史和结束 turn传 complete；保留本地 item identity                                                                                    | strategy test与layout test通过，历史 Mermaid不反复重绘                                                       |
| `packages/app/src/agent-stream/view.tsx`                                    | 保留 tool display preference、edit入口、hydration和性能选择器                                                | 展示 live task progress与 Mermaid                                                | 新 task items可能绕过本地 fold/selector造成重渲染                                        | 将 task作为现有 stream item投影接入，复用 memo/折叠边界并传 phase                                                                                                         | view/strategy/reducer tests通过，tool preference和edit仍生效                                                 |
| `packages/app/src/app/_layout.tsx`                                          | 管理 ReplicaCache启动/flush、session性能和 Reforged providers                                                | 挂载 Agent Profiles和修复 theme初始化                                            | provider顺序可能在 settings/host ready前读写坏数据或重复启动 cache                       | 明确 boot顺序：严格读取 settings/hosts，启动 cache，再挂 profile UI；theme只初始化一次                                                                                    | HostRuntime/ReplicaCache tests与app typecheck通过，无重复 subscription/write                                 |
| `packages/app/src/components/ui/combobox.tsx`                               | 保留 Agent mode risk colors与跨平台选择行为                                                                  | 为 Agent Profile picker扩展 combobox                                             | profile项可能覆盖 risk color/disabled/hover style                                        | 复用共享 item模型，risk颜色作为显式variant，profile glyph/summary作为内容slot                                                                                             | mode/profile组件测试和typecheck通过，各状态颜色可区分                                                        |
| `packages/app/src/components/ui/context-menu.tsx`                           | 连续右键时直接切换目标菜单且左键不穿透                                                                       | 支持完整 file manager context actions                                            | 新 action group可能重新引入先关闭再打开或事件穿透                                        | 保留本地受控open/target切换与pointer处理，动作内容用合并后的builder                                                                                                       | 0024相关测试和changes-pane spec通过，原生菜单不出现                                                          |
| `packages/app/src/composer/agent-controls/mode.test.ts`                     | 验证 mode risk color/选择                                                                                    | provider切换后 profile settings保持有效                                          | profile覆盖值可能指向新 provider不存在的 mode                                            | materialize profile后按provider catalog校验/回退，同时保留risk metadata                                                                                                   | 单文件Vitest覆盖跨provider、missing mode、颜色与用户显式选择                                                 |
| `packages/app/src/composer/agent-controls/mode-control.tsx`                 | 显示 mode risk颜色                                                                                           | 所有 composer controls显示tooltip                                                | tooltip wrapper可能吞掉样式/disabled事件                                                 | 在现有risk-colored trigger外组合统一tooltip，不改变按键/焦点处理                                                                                                          | mode/control tests与键盘可达性检查通过                                                                       |
| `packages/app/src/contexts/session-context.tsx`                             | 保留lazy Agent加载、session启动性能和catalog ready边界                                                       | 删除未使用message processing，host移除时停止notification                         | 自动结果可能残留重复subscription或在catalog未ready时清session                            | 删除确实无消费者的处理器；保留lazy loading顺序，并在host remove生命周期中先revoke/stop再clear                                                                             | HostRuntime/session-store tests通过，mount/unmount无残留listener                                             |
| `packages/app/src/file-pane/pane.tsx`                                       | 保留file link定位、selection和稳定合并合同                                                                   | 改善Markdown文件preview可读性                                                    | 新preview可能绕过本地workspace字体或链接解析                                             | 采用upstream preview布局，复用本地Markdown renderer、link handler和workspace typography                                                                                   | file pane/browser测试覆盖Markdown、源码切换、行号链接和长文本                                                |
| `packages/app/src/hooks/use-settings/storage.ts`                            | 持久化 workspace typography、tool display、0066 `restoreLastWorkspaceOnLaunch` 等 Reforged 字段              | Zod 校验持久 state 并统一 theme fallback                                         | whole-object strict parse 会把未列入 schema 的合法本地字段整体拒绝                       | 使用版本化 strict envelope 和显式字段默认；把 `restoreLastWorkspaceOnLaunch: z.boolean().optional()` 与其他 local 字段全部列入；只在结构/版本不兼容时清空，字段迁移可检索 | storage tests 覆盖旧版本、坏枚举、额外字段、启动偏好和全部 local 字段 round-trip                             |
| `packages/app/src/i18n/resources.test.ts`                                   | locale completeness、runtime-close/edit和RelativeTime fallback                                               | profiles/task/file-search新key完整性                                             | 自动合并资源文件后测试期望仍可能只覆盖一侧                                               | key基准取最终English集合，保留例外白名单但不新增静默缺失                                                                                                                  | 单文件Vitest覆盖9 locale，无原始key泄漏                                                                      |
| `packages/app/src/keyboard/actions.ts`                                      | 定义独立`workspace.project.pick`动作                                                                         | 定义`command-center.files` workspace搜索动作                                     | 两个action可能被同一handler或route误接                                                   | 同时保留action ID；根registry处理files，New Workspace registration处理project.pick                                                                                        | shortcut/action registry tests证明每个action只有一个owner                                                    |
| `packages/app/src/panels/agent-panel.tsx`                                   | 保留new-agent hydration、timeline快速路径和render隔离                                                        | 显示live task progress                                                           | task更新可能把整个panel从retained状态唤醒或覆盖final projection                          | 将task更新限制在现有stream selector，保持hydration gate和final-first逻辑                                                                                                  | panel/stream tests与render profiler证据无明显订阅扩大                                                        |
| `packages/app/src/runtime/host-runtime.ts`                                  | 管理cold registry、ReplicaCache dirty flush与host session生命周期                                            | 严格host state校验并在remove前revoke notification                                | revoke、cache remove、session clear顺序不当会丢恢复数据或留下通知                        | 固定顺序：parse/boot；remove时await revoke，flush/remove cache，stop session，再删除registry；失败策略可诊断                                                              | host-runtime tests覆盖cold boot、revoke deferred/failure、cache和session cleanup                             |
| `packages/app/src/screens/settings/appearance/appearance-section.tsx`       | 提供独立workspace字体/字号及tool display设置                                                                 | 修复theme selection一致性                                                        | theme组件重渲染可能把local表单字段重置                                                   | 合并为同一settings draft，各字段独立update并由applyAppearance统一提交                                                                                                     | appearance tests和指定E2E证明切换任一项不重置其他项                                                          |
| `packages/app/src/screens/settings-screen.tsx`                              | 保留 Windows 标题栏顶边所有权修复，并在 Native/Electron General 区提供 0066 冷启动恢复开关                   | 增加 metadata generation model 设置                                              | 自动合并可保留两个 section，但平台 gate、顺序或 header 可能制造拖拽死带/遮挡             | 保留 `ownsWindowTopEdge` 传递；General 区保留 `restoreLastWorkspaceOnLaunch` switch 及 Native/Electron gate，Host 设置区加入 metadata；不在 chrome row 放表单             | storage/resources 测试、settings UI 开关检查和 0063 Electron verifier 通过                                   |
| `packages/app/src/stores/session-store.test.ts`                             | 覆盖conclusion-first timeline、lastMessageAt、resident count和edit状态                                       | 删除unused processing并加入task progress                                         | task/reducer变更可能破坏local summary index或时间字段                                    | 在原fixture中接入task events，保留local projection断言并验证remove cleanup                                                                                                | 单文件Vitest覆盖task更新不改变lastMessage/edit/conclusion语义                                                |
| `packages/app/src/stores/session-store.ts`                                  | 维护timeline索引、summary lane、lastMessageAt与resident metadata                                             | 处理task progress并简化unused message路径                                        | 同一event可能被两套reducer消费或task覆盖agent status                                     | 由session-stream reducer单点产出task item，store只更新对应agent/timeline字段；删除真正unused路径                                                                          | store/reducer tests通过，无重复item、status和timestamp回退                                                   |
| `packages/app/src/styles/theme.ts`                                          | 独立UI/workspace font token和Agent mode risk colors                                                          | 注册可切换themes并统一`FONT_SIZE`                                                | 常量同名/默认theme覆盖可能丢workspace字号                                                | 保留`WORKSPACE_FONT_SIZE`及risk palette，采用`REGISTERED_THEMES`和明确UI `FONT_SIZE`，命名域分离                                                                          | typecheck与appearance tests通过，所有theme含local必需token                                                   |
| `packages/app/src/styles/unistyles.ts`                                      | 运行时应用live typography                                                                                    | 注册/切换全部upstream themes                                                     | register时捕获旧字体值会使后续live update无效                                            | 注册结构只提供默认token，runtime updater每次合并当前typography                                                                                                            | appearance tests与手工theme/font切换矩阵通过                                                                 |
| `packages/app/src/timeline/session-stream-reducers.test.ts`                 | 验证conclusion-first projection、phase和edit replay                                                          | 验证task progress映射                                                            | 两类item排序/终态规则可能相互覆盖                                                        | 追加task cases到现有序列测试，明确task不成为final conclusion且phase保留                                                                                                   | 单文件Vitest覆盖pending→active→complete与final answer并存                                                    |
| `packages/app/src/timeline/session-stream-reducers.ts`                      | 生成持久timeline/summary lane并保留message phase                                                             | 将provider task event转成统一task list                                           | 旧`todo_list`和新task event可能重复                                                      | 建立单一canonical task reducer，以稳定ID合并状态；旧todo输入走compat adapter                                                                                              | reducer/stream tests无重复，reload后任务状态稳定                                                             |
| `packages/app/src/types/stream.test.ts`                                     | 覆盖edit、retry、message phase和现有stream decode                                                            | 覆盖task items与Codex compaction terminalization                                 | schema并集可能让旧item过度收窄或错误接受坏task                                           | 新字段optional并逐分支校验；保留local retry/phase fixtures                                                                                                                | 单文件Vitest验证旧/new payload、坏status和compaction结束                                                     |
| `packages/app/src/types/stream.ts`                                          | 定义Reforged edit/retry/phase stream shape                                                                   | 加入task stream shape                                                            | duplicate local task类型或必填字段会破坏旧daemon                                         | 引用protocol `AgentTaskItem`，字段optional；保留local variants                                                                                                            | app/protocol typecheck与stream tests通过                                                                     |
| `packages/client/src/daemon-client.test.ts`                                 | 覆盖timeline/import/provider policy/runtime-close与自定义headers                                             | 删除chat/loop client方法，加入fs/profile/worktree RPC与host removal              | 自动结果可能保留调用已删server handler的方法或遗漏headers                                | 删除活动chat/loop API，保留legacy decode；取新RPC测试并验证headers仍贯穿WebSocket factory                                                                                 | 单文件Vitest覆盖request/response配对、capability和headers                                                    |
| `packages/client/src/daemon-client.ts`                                      | 支持timeline快速路径、edit/runtime-close、provider policy与connection headers                                | 新增profiles/fs/worktree/SDK所需RPC并移除chat/loop                               | method表、pending request map与message union可能不一致                                   | 采用upstream RPC表，移植local extensions和header normalization；删除chat/loop发送方法但保留兼容接收                                                                       | client/protocol tests与typecheck通过，无unhandled response type                                              |
| `packages/client/src/index.ts`                                              | `PaseoClientConfig.headers`支持反向代理自定义header                                                          | 形成0.4.0可发布SDK：新handle API、agent run/wait、profiles/provider等待          | auto merge只加类型字段，可能没有把headers传给DaemonClient；旧未发布handle API与新API混杂 | 采用upstream 0.4.0 SDK surface及examples，显式把normalized headers传到底层；不保留无仓内消费者的旧`latest/refetch/provider config`别名                                    | `index.test.ts`、daemon-client test、examples typecheck和package dry-run通过                                 |
| `packages/desktop/package.json`                                             | 保留Reforged desktop identity/build配置和现有版本                                                            | 升级到0.4.0内部包                                                                | name/version能合并但bundle/repository/build metadata可能漂移                             | 版本跟随0.4.0，identity/发布渠道取本地，依赖取最终workspace图                                                                                                             | desktop typecheck/package dry-run，bundle/package IDs保持Reforged                                            |
| `packages/desktop/scripts/dev.ps1`                                          | 保留 Windows Reforged 开发 env/路径；原 `main` worktree 另有 mixed-EOL 脏状态，但合并分支基于 committed blob | 传递 source branch 供 `DEV_BUILD_LABEL` 显示                                     | 自动合并可能产生 EOL 整文件 churn；不应反向影响原 worktree                               | 在 clean 隔离 worktree 保留 Reforged env/路径并合入 branch label；只审计该 worktree 的目标 diff，不在原 worktree 执行 refresh/stash/reset/checkout                        | 隔离 worktree 的 normalized/local 意图一致，`DEV_BUILD_LABEL` 传入，无 EOL 整文件 diff；原文件 raw hash 不变 |
| `packages/desktop/scripts/dev.sh`                                           | 保留Reforged开发环境与脚本路径                                                                               | desktop退出时清理workspace terminals                                             | trap/cleanup可能覆盖本地env或错误kill主daemon                                            | 合并trap只清理由脚本启动的进程/terminal，保留本地端口和PASEO_HOME边界                                                                                                     | shell静态检查/目标脚本测试；不触碰6767或非本进程                                                             |
| `packages/desktop/src/main.ts`                                              | 保留Reforged app identity、窗口/daemon启动行为                                                               | 窗口退出时等待workspace terminals clean shutdown                                 | shutdown次序可能先杀daemon导致清理RPC失效                                                | 先请求renderer/daemon清理目标terminals，再关闭受管daemon/窗口；保留identity和超时兜底                                                                                     | desktop unit/Electron定向测试，退出无残留child且不挂住                                                       |
| `packages/protocol/src/messages.hub.test.ts`                                | 保留Hub create/error安全合同                                                                                 | 增加multi-file workflow validation消息                                           | validate/create union可能tag碰撞或漏error details                                        | 取消息并集，validate与create使用独立literal/requestId并共享安全error schema                                                                                               | 单文件Vitest覆盖每种tag和坏payload                                                                           |
| `packages/protocol/src/messages.wire-compat.test.ts`                        | 覆盖edit/timeline/provider policy与旧daemon                                                                  | 覆盖task progress                                                                | compat fixture可能未包含新增capability或把task设必填                                     | 扩充new-daemon fixture，保留old-client/old-daemon双向测试；task/profile字段optional                                                                                       | 单文件Vitest两个方向通过，legacy chat/loop带期限仍可parse                                                    |
| `packages/protocol/tests/validation/ws-outbound.test.ts`                    | 验证Agent runtime-close出站消息                                                                              | 验证fresh worktree/config response                                               | outbound allowlist可能漏一侧消息                                                         | allowlist/schema取并集，不使用passthrough掩盖未知tag                                                                                                                      | 单文件Vitest覆盖close、fs/profile/worktree与未知tag拒绝                                                      |
| `packages/server/src/server/agent/agent-storage.test.ts`                    | 覆盖lazy metadata catalog、timeline/edit字段与generation                                                     | 删除chat/loop存储并加入精确query                                                 | migration可能删掉local字段或query读取不一致                                              | 保留record字段和catalog generation，删除活动chat/loop索引，加入`listByProviderSession/listByWorkspace`一致性测试                                                          | 单文件Vitest覆盖reload、query、stored-only、edit/timeline字段                                                |
| `packages/server/src/server/agent/agent-storage.ts`                         | durable timeline、lazy catalog、last-message edit和metadata snapshot                                         | 移除chat/loop前置存储并提供精确query                                             | 新query若绕过catalog generation或返回未排序记录会破坏import/session                      | 在同一storage真相源实现query，复用索引/排序，不删除local record字段                                                                                                       | storage/import/archive tests通过，query与catalog对同一数据集一致                                             |
| `packages/server/src/server/agent/mcp-server.test.ts`                       | MCP Agent工具使用lazy catalog并保留本地权限合同                                                              | 增加Agent Profiles tools                                                         | list/apply profiles可能绕过capability或materialize全catalog                              | fixture采用catalog API，profile list/apply按whole-list和双capability语义验证                                                                                              | 单文件Vitest覆盖list/apply、权限、空profile、catalog不全量读取                                               |
| `packages/server/src/server/agent/provider-registry.ts`                     | 保留Reforged provider发现与已合入0.3.1行为                                                                   | catalog refresh deadline可配置                                                   | 各provider仍可能读取自己的固定常量                                                       | 由registry解析单一deadline并注入provider；provider-specific operation timeout不混用                                                                                       | provider registry/config tests覆盖默认、有效、非法值和每个adapter                                            |
| `packages/server/src/server/agent/providers/claude/agent.test.ts`           | 覆盖providerOptions/toolPolicy和本地Claude行为                                                               | 覆盖task progress/native timeout                                                 | 测试自动合并后可能没断言env overlay和timeout移除                                         | 加入`providerOptions.env` precedence、无强制MCP env、task event和catalog deadline场景                                                                                     | 单文件Vitest通过且不依赖真实Claude凭据                                                                       |
| `packages/server/src/server/agent/providers/codex-app-server-agent.test.ts` | 覆盖edit、retry/stale turn与本地Codex状态                                                                    | 覆盖completed subagent不再active、compaction terminalization和task               | terminal event顺序可能让一侧断言假绿                                                     | 组合事件序列测试retry→current turn→compaction→subagent completion，并检查每次emit                                                                                         | 单文件Vitest覆盖所有状态且无pending compaction/activity                                                      |
| `packages/server/src/server/agent/providers/mock-load-test-agent.test.ts`   | 生成大规模activity/final-first用于性能测试                                                                   | 生成Mermaid stream phase                                                         | mock序列可能与真实phase/task顺序不同                                                     | 保留本地规模/折叠序列，加入部分与完整fence事件                                                                                                                            | 单文件Vitest验证预期item数、phase和final位置                                                                 |
| `packages/server/src/server/agent/providers/mock-load-test-agent.ts`        | 模拟高负载stream和本地message phase                                                                          | 模拟Mermaid streaming                                                            | 同一mock若输出两套fence shape会重复渲染                                                  | 只输出最终canonical stream shape，以phase推进一条fence                                                                                                                    | mock test与app strategy tests对齐                                                                            |
| `packages/server/src/server/agent/providers/omp/runtime.ts`                 | 支持edit-last-message extension runtime                                                                      | catalog deadline配置化                                                           | timeout可能误用于edit extension 30s路径                                                  | catalog和edit timeout使用不同配置/常量，互不覆盖                                                                                                                          | OMP runtime/agent tests分别验证超时                                                                          |
| `packages/server/src/server/agent/providers/omp/test-utils/fake-omp.ts`     | fake支持edit extension请求/结果                                                                              | fake支持可延迟catalog response                                                   | 延迟控制可能阻塞edit测试                                                                 | 为catalog和edit建立独立gate/响应helper                                                                                                                                    | OMP定向tests无共享timer泄漏                                                                                  |
| `packages/server/src/server/agent/providers/opencode-agent.ts`              | 保留0.3.1 provider/tool policy行为                                                                           | 恢复model-default variant、task progress和catalog deadline                       | profile/config materialization可能把undefined variant固化成错误值                        | 保留undefined代表provider default，只在用户/profile显式值时发送；接入task/deadline                                                                                        | OpenCode tests覆盖default/explicit variant、task和timeout                                                    |
| `packages/server/src/server/agent/providers/pi/agent.ts`                    | 支持edit-last-message                                                                                        | catalog deadline配置化                                                           | 同OMP，固定edit流程可能受catalog abort影响                                               | 两类deadline隔离并保留edit capability                                                                                                                                     | Pi/provider registry tests通过                                                                               |
| `packages/server/src/server/agent/tools/paseo-tools.ts`                     | Agent tools解析lazy metadata catalog                                                                         | 加入profile list/apply tools                                                     | profile apply可能读取/写入部分列表或绕过父Agent权限                                      | 使用whole-list storage API；先校验caller/capability，再有序apply，失败返回部分进度但不伪装事务                                                                            | paseo-tools/MCP tests覆盖权限、顺序、失败和catalog调用量                                                     |
| `packages/server/src/server/bootstrap.ts`                                   | 组装timeline store、catalog与session性能依赖                                                                 | 组装profiles、provider deadlines、Hub断连和hostname slug                         | constructor顺序/同一依赖可能被创建两次                                                   | 以单例storage/catalog/registry注入Session、MCP和Hub；profiles/deadline从同一config解析                                                                                    | bootstrap/config/session tests与server typecheck通过，无重复watcher/store                                    |
| `packages/server/src/server/config.test.ts`                                 | 保留Reforged defaults、home/listen/identity                                                                  | 验证provider catalog deadline配置                                                | 新字段默认可能改变6767保护或路径                                                         | 新deadline字段按upstream默认/校验加入，其他本地默认逐项不变                                                                                                               | 单文件Vitest覆盖缺省、有效、非法值和Reforged路径                                                             |
| `packages/server/src/server/hub/execution-controller.ts`                    | 保留0.3.1 provider policy兼容、create error details与脱敏                                                    | 增加multi-file workflow的validate前置                                            | validate可能绕过本地两阶段安全preflight或返回未脱敏error                                 | validate复用同一configuration resolver且无副作用；create仍执行local安全偏序和脱敏                                                                                         | controller/hub protocol tests覆盖validate/create一致、无Agent副作用和secret redaction                        |
| `packages/server/src/server/hub/relationship-controller.test.ts`            | 保留CI稳定fixture与本地Hub合同                                                                               | Hub不可达时本地断连、初始slug用daemon hostname                                   | 时间/hostname fixture可能不确定                                                          | 使用固定hostname和deferred连接，保留local断言并新增disconnect状态                                                                                                         | 单文件Vitest稳定通过，无真实网络/时钟依赖                                                                    |
| `packages/server/src/server/hub/test-utils/relationship-harness.ts`         | 提供Reforged Hub测试基础                                                                                     | 支持offline disconnect场景                                                       | harness默认自动重连会掩盖断连状态                                                        | 暴露显式connect/disconnect gate，默认仍与既有tests兼容                                                                                                                    | relationship tests可观察每个状态且teardown无listener                                                         |
| `packages/server/src/server/session.test.ts`                                | stored-only Agent报告closed、lazy catalog和本地session合同                                                   | 删除chat/loop，加入host removal与Hub workflow                                    | 旧fixture可能仍创建ChatService或把stored-only child错误归档                              | 迁移fixture到ScheduleSession/query API，保留stored-only/runtime-close断言并加入host removal                                                                               | 单文件Vitest覆盖旧本地场景和新session lifecycle                                                              |
| `packages/server/src/server/session.workspaces.test.ts`                     | workspace/archive/provisioning性能与catalog集成                                                              | 删除chat/loop并更新host removal                                                  | workspace关闭可能同时走旧chat cleanup和新archive path                                    | 删除旧chat cleanup，保留本地archive/worktree合同并采用新query                                                                                                             | 单文件Vitest无旧service引用，workspace状态/目录正确                                                          |
| `packages/server/src/server/websocket-server.relay-reconnect.test.ts`       | 保留imported session title与relay reconnect                                                                  | 删除chat/loop并支持新SDK连接行为                                                 | reconnect fixture可能发送已删除消息或漏headers/features                                  | 使用最终server_info和client handshake；保留title/reconnect断言，不发送活动chat/loop                                                                                       | 单文件Vitest覆盖断连重连、title、features与header认证                                                        |
| `packages/server/src/services/quota-fetcher/service.test.ts`                | 使用窄logger seam并保持CI稳定                                                                                | 从modern `state.vscdb` key读取Cursor token                                       | 新DB fixture可能重新扩大logger mock或遗漏legacy fallback                                 | 保留窄seam，增加modern key优先和既有fallback/无token场景                                                                                                                  | 单文件Vitest通过，无真实用户DB/secret输出                                                                    |
| `packages/website/src/components/landing-page.tsx`                          | 保留Paseo Reforged品牌、链接和产品定位                                                                       | 删除chat rooms/agent loops营销文案                                               | 自动结果可能留下不存在的功能或恢复upstream品牌                                           | 保留Reforged identity，移除loop/chat claim并用0.4.0真实profiles/task/worktree能力替代                                                                                     | 文案与最终功能/链接一致，构建不引用删除资源                                                                  |
| `packages/website/src/content/alternatives/openchamber.md`                  | 使用Reforged品牌/比较边界                                                                                    | 删除chat/loop对比项                                                              | 比较表可能陈述已删除能力                                                                 | 保留品牌，只写可由当前代码/文档验证的差异                                                                                                                                 | 无loop/chat陈述，内部链接有效                                                                                |
| `public-docs/skills.md`                                                     | 保留Reforged Skill安装/使用路径                                                                              | 删除loop相关产品Skill说明                                                        | 当前`PROJECT.md`仍登记paseo-loop，文档与本地路由会分裂                                   | 文档接受产品loop删除；Execute时同步移除失效项目路由，Profiles相关Skills按最终授权边界说明                                                                                 | 文档、PROJECT登记与实际files三者一致                                                                         |
| `public-docs/troubleshooting.md`                                            | 保留Reforged路径、端口与不重启主daemon边界                                                                   | 增加provider diagnostic排查                                                      | upstream命令/默认路径可能指向错误安装或鼓励重启                                          | 吸收只读diagnostic流程，优先使用status报告路径；保留Reforged身份和6767授权边界                                                                                            | 所有CLI参数由0.4.0 help/source核实，无自动restart建议                                                        |
| `public-docs/worktrees.md`                                                  | 记录`.worktreeinclude` copy/symlink安全语义                                                                  | 推荐`origin/main`、加入workspace rename                                          | 新fresh-worktree实现或committed config规则可能与旧include段冲突                          | 保留include安全段，加入fresh/unique、committed `paseo.json`、remote base和rename；说明source未提交配置仅由include显式带入                                                 | 文档示例命令存在，worktree tests逐条映射关键规则                                                             |
| `README.ja.md`                                                              | 保留Reforged品牌、安装链接和本地功能                                                                         | 删除chat/loop                                                                    | 翻译可能仍宣传已删功能或链接upstream安装                                                 | 以English最终结构为基准，保留Reforged identity并移除已删功能                                                                                                              | 无chat/loop、链接有效、版本/包名与最终manifest一致                                                           |
| `README.ko.md`                                                              | 保留现有韩文入口与Reforged差异                                                                               | 删除chat/loop                                                                    | 同上                                                                                     | 同步English最终功能集，保留local identity                                                                                                                                 | 同上                                                                                                         |
| `README.md`                                                                 | Reforged主入口、fork身份、安装/发布来源                                                                      | 删除chat/loop并增加TypeScript SDK/integration入口                                | upstream SDK链接/安装命令可能指向官方仓库，且旧功能文案残留                              | 保留Reforged身份与下载来源；采用0.4.0 SDK内容但链接指向本仓库实际docs/examples；移除chat/loop                                                                             | README链接检查、示例typecheck，功能列表与最终树一致                                                          |
| `README.zh-CN.md`                                                           | 保留Reforged中文入口、链接和功能说明                                                                         | 删除chat/loop                                                                    | 中文与English可能漂移                                                                    | 按最终English语义同步，保留本地身份并校对关键生命周期/Profile术语                                                                                                         | 无已删功能或错误链接，版本/命令一致                                                                          |

## 附录 D：交互冲突与意图冲突矩阵

这些冲突由文件删除、双方分别新增文件、自动合并或跨层契约形成，不一定产生 Git marker。下表恰好 38 行；解决方案与附录 B/C 同属 Execute 合同；执行中出现不同事实必须停止并回到 Plan。

| 场景                                              | Local 意图                                                                                                                        | Upstream v0.4.0 意图                                                              | 冲突类型                          | 解决方案                                                                                                                                                            | 取舍                                                                                         | 验收                                                                                                                                                        |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 产品 loop 删除与项目 Skill 路由                   | `PROJECT.md` 精确登记 `skills/paseo-loop/SKILL.md`，该 Skill 调用 `paseo loop` CLI                                                | 删除 loop CLI/runtime和该 Skill                                                   | 失效路由 / 规则冲突               | 接受产品 loop 删除；在 Execute 中显式删除 `PROJECT.md` 的 `paseo-loop` 登记并同步 `public-docs/skills.md`，不伪造替代 CLI                                           | 项目失去该私有 loop Skill；持续监控只能使用当前宿主已提供且另行授权的机制                    | `rg "paseo-loop\|paseo loop" PROJECT.md skills public-docs packages` 无活动路由/命令，legacy schema例外有期限                                               |
| Agent Profiles 与已登记 advisor/committee/handoff | 当前项目上级只允许 Codex 原生子智能体，且委派需用户明确授权                                                                       | upstream Skills 改用产品 `list_profiles/create_agent` 自动选 profile              | 授权 / 工具所有权冲突             | 产品 App/server/API 接受 Agent Profiles；三个项目 Skill 不照搬产品级创建指令，保持用户授权与 Codex-native 限制，并把任何 profile 选择视为产品功能而非本项目编排后门 | 无法用 upstream profile 自动替当前 Codex 子智能体选 provider                                 | Skill 内容与 `PROJECT.md` 一致；未出现未授权 Paseo Agent/第三方 agent CLI 调用                                                                              |
| `paseo-help` 新 Skill                             | 当前 `PROJECT.md` 未登记，根 `paseo` Skill自含Reforged诊断/安全边界                                                               | upstream 新增 `skills/paseo-help/SKILL.md`，根 Skill 准备委派                     | 私有路由 / 品牌冲突               | 保留新增文件作为未登记、不可自动发现的上游产品资产；根 `paseo` 不委派它，只内联经核验的最小诊断流程；不新增 `PROJECT.md` 路由                                       | 上游 help 内容不会自动参与本项目会话                                                         | `PROJECT.md` 无登记、根 Skill 无委派；文件内命令/路径若保留均通过0.4.0事实核验且不覆盖Reforged边界                                                          |
| Mermaid 两套引擎                                  | local 在 `components/mermaid`、`mermaid/` 与旧 build script 实现跨平台渲染、源码、安全链接、Panzoom                               | upstream 在 `components/markdown/fence/mermaid` 引入统一runtime、phase与新测试    | 重复实现 / 资源与安全冲突         | 以上游 fence 引擎为唯一实现；先移植local workspace字体、闭合/源码、链接策略、Native WebView安全、缩放拖动等价能力，再删除local旧组件、runtime和build脚本            | 文件删除面较大，但避免两份Mermaid包/runtime和行为漂移                                        | 全树只有一个render入口/build脚本；old imports为0；unit/browser/E2E覆盖Web/Native、streaming、source、zoom、links                                            |
| Markdown stream phase 与本地activity fold         | local将history/live head展平并按phase/final answer折叠                                                                            | upstream用`streaming/complete`决定Mermaid解析和渲染                               | 时序 / 重渲染冲突                 | phase只绑定当前live turn；历史项固定complete；fold前后保持item identity，partial fence不作为完成图缓存                                                              | 流式图可能在完成时重绘一次                                                                   | layout/strategy/Mermaid tests证明历史不反复渲染、partial source不泄漏为完成图                                                                               |
| root Agent关闭与managed subagent关闭              | root tab关闭需要真实runtime-close，失败不删布局                                                                                   | managed child tab由per-client open label拥有，父归档保留仍打开child               | 生命周期 / 所有权冲突             | 单关、批量关、父归档共用`resolveCloseAgentTabPolicy`；root走runtime-close，managed child只更新label/布局，最后owner关闭后才允许生命周期归档                         | “关闭标签”不再对应单一后端动作                                                               | root/child/混合批量/父归档测试；每个Agent恰好一个副作用且失败不误删                                                                                         |
| stored-only Agent状态                             | local持久记录存在但无runtime时必须显示closed                                                                                      | upstream task/subagent owner更新可能重新投影active                                | 状态投影冲突                      | runtime状态优先于存储/label推断；task或open label不把无runtime root标为running，managed child active状态按明确provider lifecycle                                    | 需要区分UI可见与运行态                                                                       | session/manager/store tests覆盖stored-only、opened child、completed child                                                                                   |
| Schedule保留而chat/loop删除                       | local的schedule与chat/loop共用聚合session并依赖catalog                                                                            | upstream删除聚合实现，新增独立`ScheduleSession`                                   | 领域拆分 / 功能保留冲突           | 迁移仍有效的schedule create/update/fire/notification到独立session；chat room与loop不迁移，legacy wire仅解析                                                         | 删除loop交互但保留schedule/heartbeat                                                         | schedule tests通过；全树无旧聚合类/ChatService活动引用                                                                                                      |
| `Cmd/Ctrl+P`                                      | local在New Workspace和composer可切换项目                                                                                          | upstream在工作区打开文件搜索                                                      | 快捷键 / route冲突                | 已进入workspace时执行`command-center.files`；New Workspace页面注册独立`workspace.project.pick`；同scope不允许双binding，帮助表显示文件搜索并由页面说明项目选择动作  | 同一chord按页面有不同合法语义                                                                | keyboard tests与两条指定E2E覆盖Mac/Windows、输入焦点、command center状态和New Workspace                                                                     |
| File action扩展与连续右键直切                     | local菜单支持同一浮层直接换目标且防左键下穿                                                                                       | upstream增加树选择和大量context actions                                           | 事件路由 / destructive action冲突 | 保留受控target切换、overlay ref和contextmenu suppression；所有新动作复用单一builder，revert/delete必须确认且由capability gate控制                                   | 菜单状态机复杂度增加                                                                         | changes-pane E2E覆盖直切、禁用/危险动作、错误目标和原生菜单抑制                                                                                             |
| Worktree slug复用                                 | local同slug复用既有worktree                                                                                                       | upstream始终创建fresh并自动后缀                                                   | 数据隔离 / 用户预期冲突           | 采用fresh语义；同slug/已checkout branch产生唯一目录和local branch，绝不修改旧worktree                                                                               | 更多目录与分支，需要用户后续归档                                                             | worktree tests连续创建两次且旧目录/HEAD/config不变                                                                                                          |
| 新worktree的`paseo.json`来源                      | local reuse可能继承source checkout未提交配置                                                                                      | upstream强调clean worktree与saved settings                                        | 配置权威 / 脏状态传播冲突         | 选定base ref中committed `paseo.json`为权威；source未提交配置不复制，除非`.worktreeinclude`显式列出其他本地文件且安全策略允许                                        | 用户未提交配置不会自动生效                                                                   | dirty-source与different-base测试证明新worktree读取base blob，git status保持预期clean                                                                        |
| `.worktreeinclude`与fresh worktree                | local支持copy/symlink本地资源                                                                                                     | upstream fresh/clean目标可能被include变脏                                         | 安全 / clean状态冲突              | 继续支持include，但先创建fresh worktree再按local安全策略物化；文档要求目标branch忽略这些路径，未忽略时显式呈现status而非静默清理                                    | clean是默认而非绝对；显式include可产生可见文件                                               | POSIX/Windows相关tests覆盖escape、nested symlink、copy、junction和status                                                                                    |
| Replica Cache scheduler与strict schema            | local按host/domain revision合并写入并保证失败不丢dirty                                                                            | upstream严格Zod、版本失效与简化降频                                               | 持久化 / 并发冲突                 | DTO升到6；读入口strict/fail-closed，写侧保留revision scheduler；只有对应capture成功才清dirty，remove host立即清缓存                                                 | v3/v5缓存不迁移，首次启动重新同步                                                            | index/HostRuntime tests覆盖坏缓存、旧版本、并发、失败、移除和stop                                                                                           |
| Direct TCP headers与strict registry               | local允许自定义headers并规范化比较                                                                                                | upstream strict connection schema未声明headers                                    | 兼容字段被拒绝                    | 将optional headers显式纳入strict schema，解析后canonicalize；不放宽其他unknown字段                                                                                  | 旧未知字段仍被拒绝                                                                           | headers round-trip/equality/invalid registry测试，实际client handshake带header                                                                              |
| Agent Profiles schema与capability                 | local已有providerOptions/toolPolicy/edit等可选配置且旧daemon兼容                                                                  | upstream profiles与config apply依赖新daemon                                       | 协议版本 / 假成功冲突             | profile字段全部optional；UI同时要求`agentProfiles`和`agentConfigApply`；缺gate只显示升级提示，不做写入fallback                                                      | 旧daemon不能使用profiles                                                                     | old/new server_info测试、capability unit和Profile E2E通过                                                                                                   |
| Profile持久化与apply失败                          | local settings偏好逐字段持久                                                                                                      | upstream profiles whole-list保存，apply有序非事务                                 | 原子性 / 部分生效冲突             | profiles以whole-list替换持久；apply顺序固定并返回具体失败/已应用步骤，UI不得声称回滚或全成功                                                                        | 失败可能留下部分provider配置，用户需重试/修正                                                | materialize/form/migration/MCP tests覆盖顺序、部分失败和reload                                                                                              |
| provider catalog deadline与操作timeout            | local各adapter有固定catalog/edit timeout                                                                                          | upstream统一catalog refresh deadline可配置                                        | timeout语义混用                   | registry单点解析catalog deadline并注入；edit、run、MCP等操作timeout保持各自来源，不复用catalog值                                                                    | 配置项更多但语义清楚                                                                         | provider registry/config及各adapter测试区分每类timeout                                                                                                      |
| Claude env与MCP timeout                           | local保留`providerOptions.env`且强制10分钟MCP timeout                                                                             | upstream只用launch env并恢复Claude native timeout                                 | 用户配置 / 可靠性意图冲突         | overlays按`providerOptions.env`后`launchEnv`组合；不自动写`MCP_TIMEOUT`/`MCP_TOOL_TIMEOUT`，用户显式env仍可设置                                                     | 默认长调用不再由Reforged强制延长                                                             | Claude unit证明env precedence与默认无强制timeout                                                                                                            |
| Codex retry/stale event与compaction终态           | local过滤无turnId retry completion及旧turn completion                                                                             | upstream任何turn终态都关闭pending root compaction                                 | 异步事件 / 错误终态冲突           | 先验证当前turn/retry状态，再terminalize对应pending compaction；shutdown另做全量cleanup，subagent activity独立                                                       | stale通知不再“顺便”清理当前状态                                                              | Codex序列tests覆盖retry、stale、三种terminal status、root/child compaction                                                                                  |
| task progress与local message phase/todo           | local有commentary/final_answer和todo/fold投影                                                                                     | upstream新增`AgentTaskItem`与live task styling                                    | 类型重复 / timeline排序冲突       | 协议保留phase并加入task；旧todo通过compat adapter进入单一task reducer，task不替代final conclusion                                                                   | 兼容层暂时增加映射                                                                           | protocol/stream/reducer/message tests无重复task，final-first仍成立                                                                                          |
| chat/loop删除与wire双向兼容                       | local旧客户端/daemon兼容规则禁止直接删schema                                                                                      | upstream删除runtime/storage/client APIs                                           | 协议 / 产品删除冲突               | 删除活动实现、RPC发送面和存储模型；保留旧wire literal/schema为期限compat，只做解析/明确unsupported响应，不复活功能                                                  | schema在一段时间内大于实际能力                                                               | wire tests覆盖旧消息解析且server_info不宣传能力；每个shim有`COMPAT`期限                                                                                     |
| provider session query与import事务                | local用persistence handle index、fingerprint/marker恢复                                                                           | upstream用`listByProviderSession`阻止重复并选择record                             | 数据一致性 / 恢复冲突             | 精确query作为owner数据源，本地事务marker与fingerprint仍决定prepared/committed恢复；active拒绝、archived按cwd/parent校验恢复                                         | 多记录时需确定排序与错误信息                                                                 | import/storage/session tests覆盖重复、多记录、崩溃点和title                                                                                                 |
| metadata catalog与精确storage query               | local避免全量materialize并用generation保证一致                                                                                    | upstream新增`listByWorkspace/listByProviderSession`                               | 性能 / 一致性冲突                 | query与catalog共享同一索引和record源；列表用metadata，只有目标操作再materialize；query不静默推进错误generation                                                      | storage API面增加                                                                            | manager/archive/import测试断言调用量与返回一致                                                                                                              |
| Hub validate与Reforged安全preflight               | local create有provider policy兼容、安全偏序和secret脱敏                                                                           | upstream新增无副作用validate供multi-file workflow                                 | 授权绕过 / 错误泄露               | validate复用相同canonical resolver但禁止创建workspace/Agent/provider session；create继续两阶段policy不扩权；两者错误都经脱敏                                        | validate可能比provider启动少部分运行时检查，必须明确                                         | Hub tests证明validate零副作用、结果与create配置一致、secret不出站                                                                                           |
| TypeScript SDK改版与custom headers                | local给client config增加proxy headers，旧handle API仍在树中                                                                       | upstream把0.4.0 SDK改为`current/refresh/run/waitForFinish`并发布examples          | 公共API / 透传遗漏                | 采用upstream 0.4.0公开surface和examples；把local normalized headers端到端传给DaemonClient；不混入无消费者的旧alias                                                  | 0.x版本接受上游breaking surface，local唯一能力headers保留                                    | client tests、examples typecheck、dry-run package和实际factory header断言                                                                                   |
| Theme registry与workspace typography              | local独立UI/workspace字体字号和risk colors                                                                                        | upstream注册themes并修复切换一致性                                                | token覆盖 / 设置丢失              | token命名域分离，theme update只换颜色/默认；runtime typography独立合并，profile appearance同样不覆写字体                                                            | theme对象字段更多                                                                            | appearance/storage/theme tests及指定E2E覆盖组合切换/reload                                                                                                  |
| Installed-app 冷启动恢复与 strict settings        | 0066 为 Native/Electron 增加默认 `false` 的 `restoreLastWorkspaceOnLaunch`，等待偏好 hydration 后决定进入项目选择或恢复 workspace | upstream 用 strict Zod 读取 persisted settings，并新增 theme/metadata settings UI | 静默数据拒绝 / 启动路由冲突       | 在 `StoredAppSettingsSchema` 显式加 optional boolean，保留默认 false、hydration gate、已安装客户端开关与九语言；Web、深链、通知和会话内路由不应用该 gate            | strict schema 继续为其他 unknown/bad 字段 fail-closed；0066 真实客户端手工验收仍需合并后复核 | storage/host-runtime-bootstrap/resources 单文件测试；Electron/Native 开关两态冷启动；Web 仍恢复原 workspace                                                 |
| Edit canonical ID 与 provider rewind ID           | 0067 在验证最新 canonical user row 后解析 `providerMessageId ?? messageId`，provider 未 ack 时 fail-closed                        | upstream 重构 AgentManager 生命周期串行化、parent/child 归档与 open-tab ownership | 跨层标识 / 同文件意图遗失         | 合并生命周期代码时保留 `requireLastUserMessageEditTarget()` 的 row 解析和 provider-native ID 传递；不改 App/协议/provider rewind 契约，不放宽 ack 前编辑            | 同一 AgentManager 同时承载 lifecycle 与 edit 边界，评审时需分别验证                          | `agent-manager.test.ts` 覆盖 provider ID/ack 和 lifecycle；`edit-last-user-message.ui-contract.spec.ts` 的 distinct replacement 通过且 unknown toast 不出现 |
| Windows标题栏顶边与DEV build badge                | local`ownsWindowTopEdge`修复`HTCLIENT`死带                                                                                        | upstream在sidebar chrome显示source branch                                         | 原生hit-test / 布局冲突           | 同一row保留top-edge ownership并放置受限badge；badge pointerEvents none且ellipsis，不覆盖controls                                                                    | 可用宽度减少                                                                                 | 0063 Electron CDP/WM_NCHITTEST复核与dev label可见性通过                                                                                                     |
| Desktop clean shutdown与主daemon保护              | local开发/桌面脚本有Reforged端口和`6767`保护                                                                                      | upstream退出时清理workspace terminals                                             | 进程所有权 / 破坏性副作用         | 只清理由当前desktop session拥有的terminal/child；先RPC清理再停止受管进程；绝不重启/杀非本轮6767                                                                     | timeout时可能留下需报告的进程而非强杀                                                        | desktop tests/CDP后检查启动进程和监听端口均回收，原6767 PID不变                                                                                             |
| Custom watcher与本地race修复                      | local用deferred/microtask drain稳定Git observation tests                                                                          | upstream移除Parcel并防idle refresh饿死daemon                                      | 时序 / 资源生命周期冲突           | 采用custom watcher实现及metrics，保留确定性test gate；dispose必须取消watcher、fetch和timer                                                                          | 平台行为需Windows/Posix分别验证                                                              | observation/file-observer目标tests稳定多跑，结束无watcher/子进程                                                                                            |
| Reforged identity与0.4.0版本                      | local维护fork repository、bundle/package IDs、下载/发布远端                                                                       | upstream所有版本/文档/网站指向官方0.4.0                                           | 品牌 / 发布权限冲突               | 源码版本和内部依赖采用0.4.0；所有身份、origin、bundle/package ID和发布入口保留Reforged；合并不等于发布                                                              | 版本号与发布状态分离，changelog需说明                                                        | manifest/config/docs全局扫描无误恢复官方identity；无push/tag/release动作                                                                                    |
| Changelog事实与当前发布状态                       | local已有beta发布记录且0062仍发布中                                                                                               | upstream `0.4.0` changelog已正式发布                                              | 时间线 / 事实冲突                 | 引入上游变更记录但标注upstream来源，不把Reforged 0.4.0写为已发布；0062状态不被覆盖                                                                                  | changelog结构需区别代码基线与发行物                                                          | 总表0062保持原状态，CHANGELOG无虚假Reforged 0.4.0日期/资产                                                                                                  |
| Lockfile与Nix hash                                | local依赖图含独有Mermaid/Panzoom等                                                                                                | upstream新增新Mermaid runtime依赖并移除Parcel                                     | 派生文件 / 供应链冲突             | manifest全部决策后再`release:prepare`、fix-lockfile和Nix预取；不手选hash/lock块                                                                                     | 需要网络和Nix环境，结果可能大                                                                | lock内部版本/签名一致；两次Nix值相同；`--check`通过                                                                                                         |
| `dev.ps1` mixed-EOL 与隔离 worktree               | 原 `main` worktree 显示 M 但 normalized blob 与 index 相同，用户字节不得覆盖                                                      | upstream 修改该文件传 source branch                                               | 工作树 / 行尾冲突                 | 实际 merge 改到从 committed `main` 创建的 clean 隔离 worktree；不在原 worktree 执行 merge/refresh/stash/reset；最终只审计 upstream branch-label 意图差异            | 原 mixed-EOL 不再阻塞 merge，但活跃 Spec/总表暂留原 worktree，提交前需明确同步               | 隔离 worktree 创建后/merge 前均 clean；最终 `dev.ps1` diff 无 EOL 整文件 churn；原文件 raw hash 不变                                                        |
| 固定tag与漂移remote refs                          | local 分支固定为 `f21eba8e9`；`origin/main` 是动态排除项，当前为 `93ebc4cf4`                                                      | upstream tag 固定为 `b44bb63cf`，远端 `upstream/main` 已移到 `30195d3bd`          | 范围 / 可复现性冲突               | Execute 只合固定 tag commit；不先 pull origin/main、不合 upstream/main；任一 local/tag OID 或 merge-base 漂移回 Research，仅排除 ref 前进不重算矩阵                 | 暂不包含两边后续修复                                                                         | preflight local/tag OID、merge-base、66/77 计数与 121 块完全匹配附录 A                                                                                      |
| 文档/locale与实现同步                             | local-only edit/runtime/brand文案跨9 locale和docs                                                                                 | upstream profiles/task/fs/worktree文案跨同文件与新增docs                          | 信息架构 / 术语冲突               | English key和最终代码为基准取并集；中文重点校对生命周期，其他locale用已有译文/fallback；docs删除活动loop/chat并保留compat说明                                       | 部分非核心翻译可能fallback                                                                   | resources test、`rg`删除能力、文档路径/命令检查均通过                                                                                                       |

## 附录 E：定向验证矩阵

当前均为“计划、未运行”。测试严格遵守项目规则：每次只运行一个受影响的 Vitest 文件并带 `--bail=1`；Playwright 每次只运行一个指定 spec；不运行任何 workspace 的完整 test suite，不启动、重启或接管 `6767`。

| 域 / 顺序                        | 命令或步骤                                                                                                                                                                                                                                                           | 通过条件                                                                                                                                                                | 失败回炉点                                                                                 |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 0. 批准门禁                      | 重读 0、3、附录 A-D；确认用户精确回复 `Plan Approved`                                                                                                                                                                                                                | `approval status=Approved` 且授权范围仍不含 commit/push/tag/release                                                                                                     | 未批准即停止，不能以本 Spec 存在代替批准                                                   |
| 1. 固定基线                      | 在隔离 worktree 运行 `git rev-parse HEAD`、`git rev-parse v0.4.0^{commit}`、`git merge-base HEAD v0.4.0^{commit}`、`git rev-list --left-right --count HEAD...v0.4.0^{commit}`                                                                                        | 依次匹配 `f21eba8e9196...`、`b44bb63cf4ce...`、`bfec7ac3adc5...`、`111 75`                                                                                              | 任一漂移回 Research，重新生成 66/77 矩阵，不继续 merge                                     |
| 2. 远端范围                      | `git ls-remote --tags upstream refs/tags/v0.4.0 refs/tags/v0.4.0^{}`；只读取 `upstream/main` 与 `origin/main` 但不合入                                                                                                                                               | tag object/commit 与附录 A 相同；`upstream/main@30195d3bd`、`origin/main@93ebc4cf4` 及它们的后续提交仍排除                                                              | 远端 tag 移动或范围要求变化时回 Plan；仅被排除分支前进只更新记录                           |
| 3. 工作区隔离                    | 在新 worktree 运行 `git status --short --branch`、`git worktree list --porcelain`；原 worktree 仅读核对已知脏文件与 `dev.ps1` raw hash                                                                                                                               | 新 worktree 为目标分支且 merge 前 clean；原 worktree 保持原状，不做 refresh/stash/reset/checkout                                                                        | 新 worktree 出现任何未解释改动即停止；原 worktree 字节变化则停止调查                       |
| 4. merge结构                     | 固定 commit 开始 `--no-commit` merge 后重算 merge-tree 集合；解析完成后 `git diff --name-only --diff-filter=U` 与 `rg -n "^(<<<<<<<\|=======\|>>>>>>>)"`                                                                                                             | 开始时仍为 66 冲突/121 块；结束时 unmerged=0、marker=0；附录 B/C 每行均有 resolution 证据                                                                               | 新文件/新意图冲突、矩阵计数漂移或整侧覆盖即回 Plan                                         |
| 5. Skill/删除一致性              | `rg -n "paseo-loop\|paseo loop\|ChatScheduleLoopSession\|ChatService\|chat-mentions" PROJECT.md skills packages public-docs docs`；检查`skills/paseo/SKILL.md`委派                                                                                                   | 仅允许带期限legacy wire/历史Spec引用；无活动route/import/CLI；无未登记`paseo-help`委派                                                                                  | 有活动引用则删除迁移未完成，回执行步骤4/8                                                  |
| 6. Manifest与lock                | `npm run release:prepare`；`node scripts/fix-lockfile.mjs package-lock.json`；检查所有workspace version/internal dependency                                                                                                                                          | 所有内部版本为0.4.0、lock无旧workspace边/Parcel残留且signature完整                                                                                                      | manifest尚未收口或npm产生无法解释差异时停止，不手修lock文本块                              |
| 7. Nix派生值                     | 在Nix可用的受控环境运行`./scripts/update-nix.sh`，独立重算一次，再运行`./scripts/update-nix.sh --check`                                                                                                                                                              | 两次值一致且check通过                                                                                                                                                   | 不可用则明确记录未验证和CI风险；值不一致回manifest/lock步骤                                |
| 8. Protocol wire                 | 对以下每个文件分别运行`npx vitest run <one-file> --bail=1`：`packages/protocol/src/messages.wire-compat.test.ts`、`packages/protocol/src/messages.hub.test.ts`、`packages/protocol/tests/validation/ws-outbound.test.ts`                                             | 每个文件独立通过；旧/new方向、Hub validate/create、capability与unknown tag合同成立                                                                                      | 任一schema必填化、tag冲突或兼容失败回协议解决，不用测试放宽掩盖                            |
| 9. Client/SDK                    | 分别运行`packages/client/src/daemon-client.test.ts`与`packages/client/src/index.test.ts`；随后`npm run typecheck:examples --workspace=@getpaseo/client`                                                                                                              | 新SDK surface、headers、RPC pending map和examples全部一致                                                                                                               | headers未透传或旧/new API混杂回client resolution                                           |
| 10. Agent storage/import         | 分别运行`packages/server/src/server/agent/agent-storage.test.ts`、`packages/server/src/server/agent/import-sessions.test.ts`                                                                                                                                         | catalog/query、timeline/edit字段、重复import与事务恢复全部通过                                                                                                          | 数据选择/marker/fingerprint歧义回storage/import方案，禁止跳过恢复测试                      |
| 11. Agent lifecycle/edit         | 分别运行 `packages/server/src/server/agent/agent-manager.test.ts`、`packages/server/src/server/session.test.ts`、`packages/app/src/screens/workspace/workspace-bulk-close.test.ts`；再单独运行 `packages/app/e2e/browser/edit-last-user-message.ui-contract.spec.ts` | root runtime-close、stored-only、opened child、父归档与批量 partial failure 合同成立；canonical ID 翻译为 provider ID，distinct replacement 成功且 unknown toast 不出现 | 出现双 archive、误删 tab、active 误报、ack 前编辑或 provider ID 回归即回 AgentManager 方案 |
| 12. Schedule                     | `npx vitest run packages/server/src/server/session/schedule/schedule-session.test.ts --bail=1`                                                                                                                                                                       | schedule create/update/fire/notification保留且无chat/loop依赖                                                                                                           | schedule行为丢失回删除迁移步骤                                                             |
| 13. Workspace archive/worktree   | 分别运行`packages/server/src/server/workspace-archive-service.test.ts`、`packages/server/src/utils/worktree.test.ts`、`packages/server/src/utils/worktree.posix.test.ts`                                                                                             | 精确workspace query、目录所有权、fresh/unique、committed config与include安全通过                                                                                        | 复用旧worktree、复制dirty config或误删目录即回worktree方案                                 |
| 14. Git/file watcher             | 分别运行`packages/server/src/server/workspace-git-service.observation.test.ts`、`packages/server/src/server/file-observer/index.test.ts`、`packages/server/src/server/file-explorer/observer.test.ts`                                                                | fetch不饿死daemon、metrics归零、watcher teardown稳定                                                                                                                    | race重现或句柄残留回custom watcher/测试gate                                                |
| 15. Claude/provider deadline     | 分别运行`packages/server/src/server/agent/providers/claude/agent.test.ts`、`packages/server/src/server/agent/provider-registry.test.ts`、`packages/server/src/server/config.test.ts`                                                                                 | env precedence、native MCP timeout、catalog deadline默认/非法值合同成立                                                                                                 | timeout语义混用或强制env残留回provider方案                                                 |
| 16. Codex                        | `npx vitest run packages/server/src/server/agent/providers/codex-app-server-agent.test.ts --bail=1`                                                                                                                                                                  | retry/stale turn、三种终态、root compaction和completed subagent均正确                                                                                                   | pending activity/compaction或stale误清理回事件顺序方案                                     |
| 17. OMP/其他providers            | 分别运行`packages/server/src/server/agent/providers/omp/agent.test.ts`以及受实际diff影响的OpenCode/Pi provider单文件测试                                                                                                                                             | edit extension、provider default和catalog deadline互不干扰                                                                                                              | 某provider无现成稳定test seam时记录并回Plan决定补测试，不静默宣称验证                      |
| 18. Hub                          | 分别运行`packages/server/src/server/hub/execution-controller.test.ts`、`packages/server/src/server/hub/relationship-controller.test.ts`                                                                                                                              | validate零副作用、create不扩权、secret脱敏、offline断连/hostname slug成立                                                                                               | 任何安全偏序或脱敏失败为blocking，回Hub解决                                                |
| 19. Replica/HostRuntime          | 分别运行`packages/app/src/runtime/replica-cache/index.test.ts`、`packages/app/src/runtime/host-runtime.test.ts`                                                                                                                                                      | DTO v6 strict parse、dirty revision、失败重试、remove/revoke/cold boot通过                                                                                              | 持久化丢更新或坏缓存注入store为blocking，回cache方案                                       |
| 20. Settings/theme/startup       | 分别运行 `packages/app/src/hooks/use-settings/storage.test.ts`、`packages/app/src/navigation/host-runtime-bootstrap.test.ts`、`packages/app/src/i18n/resources.test.ts`、`packages/app/src/screens/settings/appearance/apply-appearance.test.ts`                     | strict settings 保留全部 local 字段，`restoreLastWorkspaceOnLaunch` 默认 false/true 持久化与 hydration 路由成立，theme 与 UI/workspace 字体互不重置                     | 合法启动偏好被清空、已安装客户端路由回归或 theme 覆盖字体则回 schema/router/token 方案     |
| 21. Agent Profiles unit          | 分别运行`packages/app/src/agent-profiles/internal/capabilities.test.ts`、`materialize-profile.test.ts`、`profile-form-model.test.ts`、`packages/app/src/agent-profiles/migration/index.test.ts`                                                                      | 双capability、optional字段、provider materialize、whole-list migration与partial error合同成立                                                                           | gate或migration假成功回profile方案                                                         |
| 22. Stream/task/layout           | 分别运行`packages/app/src/agent-stream/layout.test.ts`、`packages/app/src/timeline/session-stream-reducers.test.ts`、`packages/app/src/types/stream.test.ts`、`packages/app/src/stores/session-store.test.ts`                                                        | phase/fold、task状态、final conclusion、edit/timeline字段组合成立                                                                                                       | 重复task、final丢失或历史phase漂移回stream reducer                                         |
| 23. Mermaid Node tests           | 对最终保留的`packages/app/src/components/markdown/fence/mermaid/{render-model.test.ts,source-policy.test.ts,runtime/request-driver.test.ts,runtime/runtime-html.test.ts}`逐文件运行Vitest                                                                            | source policy、runtime消息、stream phase和HTML生成全部通过                                                                                                              | 旧/新引擎测试同时存在视为未收口；安全策略失败为blocking                                    |
| 24. Mermaid browser tests        | 对`runtime/runtime.browser.test.ts`及迁入新引擎后的Panzoom/diagram browser测试逐文件运行`npx vitest run --project browser <one-file> --bail=1`                                                                                                                       | Chromium中SVG非空、source/zoom/pan/link与cleanup通过                                                                                                                    | canvas/SVG空白、资源加载或交互失败回Mermaid迁移                                            |
| 25. Keyboard/i18n                | 分别运行`packages/app/src/keyboard/keyboard-shortcuts.test.ts`、`packages/app/src/i18n/resources.test.ts`                                                                                                                                                            | chord/scope无双命中；9 locale key集合完整                                                                                                                               | action owner或key不一致回keyboard/i18n方案                                                 |
| 26. File actions E2E             | `npm run test:e2e --workspace=@getpaseo/app -- e2e/browser/changes-pane.spec.ts`                                                                                                                                                                                     | 树布局、懒diff、连续右键、新动作/destructive确认通过                                                                                                                    | locator仅因最终结构变化可更新；行为失败回实现，不降断言                                    |
| 27. File search E2E              | `npm run test:e2e --workspace=@getpaseo/app -- e2e/browser/command-center-file-search.spec.ts`                                                                                                                                                                       | workspace内`Cmd/Ctrl+P`搜索文件且focus restore正确                                                                                                                      | 与New Workspace route冲突回action registration                                             |
| 28. Profiles E2E                 | 分别运行`agent-profiles-picker.spec.ts`与`agent-profiles-settings.spec.ts`，每次只传一个spec给App Playwright脚本                                                                                                                                                     | create/edit/apply/reload、缺capability提示与错误状态通过                                                                                                                | 任何silent save/apply视为blocking                                                          |
| 29. Mermaid E2E                  | `npm run test:e2e --workspace=@getpaseo/app -- e2e/browser/mermaid-streaming.spec.ts`                                                                                                                                                                                | partial→complete、源码/图切换、缩放拖动和链接策略通过                                                                                                                   | 两引擎DOM、空白图或unsafe link回迁移                                                       |
| 30. New Workspace/appearance E2E | 分别运行`new-workspace-codex-mode-preferences.spec.ts`、`workspace-model-restart.spec.ts`、`appearance-theme-picker.spec.ts`                                                                                                                                         | project picker、strict settings、model/theme/font reload组合通过                                                                                                        | 合法设置丢失回settings/router方案                                                          |
| 31. Desktop静态与脚本            | `npm run typecheck --workspace=@getpaseo/desktop`；运行受影响的 `dev-runner-config.test.mjs` 既有入口；在隔离 worktree 核对 `dev.ps1` 目标 diff，只读确认原 worktree raw hash 未变                                                                                   | Reforged identity、source branch label 和 clean shutdown 编译/测试通过，隔离 diff 无 EOL 整文件 churn，原文件未被触碰                                                   | 脚本拥有错误进程、出现 EOL 整文件 diff 或原 raw hash 变化时停止                            |
| 32. Windows标题栏实机            | 在隔离`6768`开发实例运行`npm run verify:electron-cdp --workspace=@getpaseo/desktop`，必要时复用0063的`WM_NCHITTEST y=46..52`检查；结束后回收本轮进程                                                                                                                 | drag continuity gap=0，`ownsWindowTopEdge`仍唯一，badge不遮挡；原生点为`HTCAPTION`且真实顶边仍`HTTOP`                                                                   | 任一命中回归回left-sidebar/chrome布局；不触碰6767                                          |
| 33. 跨workspace声明              | `npm run build:client`；必要时`npm run build:server`；然后`npm run typecheck`                                                                                                                                                                                        | 全workspace typecheck退出0，声明与最终协议/SDK一致                                                                                                                      | 先按项目规则重建声明再判断源码错误；仍失败则blocking                                       |
| 34. Lint/format                  | `npm run lint`；对`git diff --name-only`中的本任务文件分批显式运行`npm run format:files -- <paths>`，再`npm run format:check:files -- <paths>`                                                                                                                       | lint退出0；仅目标文件产生可解释格式差异；format check退出0                                                                                                              | 不运行无关全树格式化；若format触及用户文件，停止并恢复本任务产生的格式差异                 |
| 35. 品牌/文档                    | 搜索`getpaseo/paseo`、官方homepage、bundle/package IDs、loop/chat活动描述、旧Mermaid路径；逐一按允许清单审计                                                                                                                                                         | upstream仅作为明确来源/历史链接；运行身份、发布入口和bundle IDs均为Reforged；文档命令存在                                                                               | 未解释命中回文档/identity步骤                                                              |
| 36. 最终Git与资源                | `git status --short`；`git diff --check`；`git diff --name-only --diff-filter=U`；检查本轮启动进程、监听端口和临时文件                                                                                                                                               | 无unmerged/marker/whitespace error；仅计划文件；无本轮遗留进程；`6767`原进程未变                                                                                        | 任何残留先清理或记录blocking，不进入Review/commit讨论                                      |
| 37. 明确不运行                   | 不运行`npm test`、任何workspace完整`test`、完整Playwright套件、真实provider/EAS/release命令；不commit/push/tag/publish                                                                                                                                               | 本轮只得到定向实现证据，完整矩阵交CI/后续明确授权                                                                                                                       | 需要扩展到真实provider、发布或远端写入时另行请求授权                                       |
