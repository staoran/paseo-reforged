# 发布新版本 Spec

> 历史迁移说明：本文于 2026-08-02 从 `mydocs/tasks/0014_发布新版本/` 迁移为当前规则集的单文件 Heavy Spec。第 0-9 章是当前权威摘要；第 10 章按源文件保存完整历史原文和 SHA-256，仅用于追溯，不构成新的执行授权。

## 0. 状态与索引

| 字段              | 值                                |
| ----------------- | --------------------------------- |
| task_id           | `0014`                            |
| spec layer        | `Feature Spec`                    |
| task status       | `已收口`                          |
| mode              | `single_project`                  |
| phase             | `Review`                          |
| approval status   | `Plan Approved`                   |
| approval source   | `User`                            |
| spec path         | `mydocs/specs/0014_发布新版本.md` |
| parent spec       | `N/A`                             |
| supersedes        | `N/A`                             |
| current task unit | `历史任务记录迁移与归档`          |
| created / updated | `2026-07-27 / 2026-08-02`         |

## 1. 目标、范围与完成契约

- 背景/问题：该任务使用旧规则集的多文件任务包记录，现需迁移为当前单文件 Spec。
- 最终目标：在同步后的主线完成 0.2.4-beta.1 准备、CI 门禁和不可变 beta 发布。
- 当前任务单元：无损迁移历史记录并关闭旧路径。
- 范围内：发布候选修复、lockfile 规范化、Changelog、main CI、release commit 和 beta tag。
- 范围外：stable、npm/GHCR、商店、Cloudflare、未冻结 upstream 和主工作区其他任务资料。
- Done Contract：当前摘要覆盖任务状态、关键事实、方案、执行、验证、风险和提交；全部旧 Markdown 原文及内容哈希保存在第 10 章；旧目录仅在迁移验证通过后删除。
- 失败或回炉方式：任一源文件未被完整嵌入、哈希不符或格式检查失败时，保留旧目录并重新生成，不以不完整的新 Spec 替代源记录。

### 1.1 最小任务单元判断

- 为什么当前任务单元足够小：单个 task_id 只生成一份对应 Spec，不合并不同任务的事实或授权。
- 验证证据：源文件清单、SHA-256、完整内容包含检查和 Markdown 格式检查。
- 模型可自主决定的范围：章节重组、历史摘要和附录顺序；不得改写历史授权或扩大任务结论。
- 拆分决定：`Accepted`

## 2. 上下文与调研

### 2.1 上下文来源

- 需求来源：用户要求将历史文档转为新规则集格式后删除旧文档。
- 项目事实源：`PROJECT.md`、Git 历史、当前源码以及第 10 章保存的旧任务包。
- Codemap：`N/A`
- Codemap Mode：`N/A`
- Context Bundle：`N/A`
- Context Bundle Level：`N/A`
- 关联任务记录：`N/A`

### 2.2 调研结论

已确认事实：

- 候选先分离 6-file 修复、lockfile 规范化和 Changelog，再修复 modal 快捷键 CI 阻断。
- main CI 18/18 全绿后创建 release commit 4484d62a6。
- 本地 origin/main 当前仍指向该 release commit，历史准备阶段描述已由本迁移摘要收口。

- 未知与开放问题：无影响本次历史迁移的开放问题；动态 refs、外部服务和旧基线只按历史快照理解。
- 风险与约束：旧 Spec 中间阶段和 go-ahead 已消耗，不能作为新的发布授权。
- `grilling` 结论（如使用）：未使用；迁移目标与删除范围已由用户明确。

### 2.3 方案与决策

- 备选方案：保留旧任务包、只写摘要后删除、或在新 Spec 中同时保存摘要和完整原文。
- 已选方案：使用当前模板的 0-9 章保存权威摘要，并在第 10 章无损嵌入全部旧文件。
- 选择理由：消除并行真相源，同时避免删除未提交历史记录造成证据丢失。

历史任务关键决策：

- 发布准备提交按职责拆分，tag 只在全部门禁通过后创建。
- lockfile 使用固定 Node/npm 在 clean worktree 中验证幂等，不接受系统 Node 产生的 churn。

### 2.4 下一步动作

- 下一步唯一动作：N/A；后续版本使用当前 release Skill 和新任务。

## 3. 计划与执行前检查点

### 3.1 文件变化

| 项目/子项    | 文件或子 Spec                     | 计划变化                     | 原因                     |
| ------------ | --------------------------------- | ---------------------------- | ------------------------ |
| 历史任务记录 | `mydocs/tasks/0014_发布新版本/`   | 合并到当前单文件 Spec 后删除 | 消除旧规则集多文件真相源 |
| 当前 Spec    | `mydocs/specs/0014_发布新版本.md` | 新建                         | 使用当前 Heavy Spec 模板 |

### 3.2 签名与契约

| 项目/子项 | 接口、类型或签名     | 计划变化 | 兼容性         |
| --------- | -------------------- | -------- | -------------- |
| 文档迁移  | 运行时接口与数据契约 | 无变化   | 仅迁移历史记录 |

### 3.3 子 Spec 索引

N/A；该历史 task_id 迁移为单个 Spec。

### 3.4 执行清单

- [x] 1. 读取并归档旧任务包全部 Markdown 文件。
- [x] 2. 建立当前模板要求的状态、目标、上下文、执行、验证和提交摘要。
- [x] 3. 记录源文件 SHA-256 并验证完整内容已嵌入。
- [x] 4. 在新 Spec 验证通过后删除旧目录。

### 3.5 执行前检查点

- 当前目标与任务单元：历史任务单文件迁移。
- 当前 phase：`Review`
- approval status / source：`Plan Approved / User`；用户已明确授权转换并删除旧文档。
- 下一步：验证格式、内容哈希和旧路径引用。
- 风险与回退：验证失败时不删除旧目录；Git 可恢复原 tracked 文件，未跟踪源内容已完整嵌入第 10 章。
- 验证方式：生成器内容包含检查、格式检查、旧路径检索和 Git 范围审查。
- TDD 判定、测试 seam 与验收行为：`N/A`；当前改动只迁移历史文档，不改变运行时行为。
- seam 确认：`N/A`

## 4. 跨项目扩展

N/A；单项目历史文档迁移。

## 5. 执行记录

| 步骤/子项 | 实际变化或子 Spec 锚点                                           | 状态   | 偏差与处理                   |
| --------- | ---------------------------------------------------------------- | ------ | ---------------------------- |
| 1         | 重放并验证 6-file 修复，生成规范化 lockfile 与 Changelog。       | 已记录 | 详细时间线见第 10 章原始记录 |
| 2         | 修复 provider settings 中 modal 快捷键 CI 阻断并重新跑 main CI。 | 已记录 | 详细时间线见第 10 章原始记录 |
| 3         | 完成 0.2.4-beta.1 release commit/tag。                           | 已记录 | 详细时间线见第 10 章原始记录 |
| 文档迁移  | 旧任务包完整原文进入第 10 章                                     | 已完成 | 不改写原始记录               |

## 6. 验证

| 项目/验收项 | 命令或步骤                            | 结果   | 证据           | 未验证原因 |
| ----------- | ------------------------------------- | ------ | -------------- | ---------- |
| 候选        | 目标单测/E2E、format、lint、typecheck | `PASS` | 历史任务记录   | `N/A`      |
| 准备        | 固定 Node/npm release:prepare 幂等    | `PASS` | 历史任务记录   | `N/A`      |
| CI          | main@2c7651f21 18/18 jobs             | `PASS` | 历史任务记录   | `N/A`      |
| 发布        | 0.2.4-beta.1 release commit/tag       | `PASS` | 历史任务记录   | `N/A`      |
| 迁移完整性  | 源文件 SHA-256 与完整内容包含检查     | `PASS` | 第 10.1 节清单 | `N/A`      |

- 集成验证：当前迁移不改变产品代码；历史产品验证结果按上表和第 10 章保留。
- 剩余风险：旧 Spec 中间阶段和 go-ahead 已消耗，不能作为新的发布授权。
- Done Contract 是否由证据满足：是

## 7. 评审（Review）

| 评审轴             | 结论   | 证据或阻塞问题                                             |
| ------------------ | ------ | ---------------------------------------------------------- |
| 目标与 Spec 完成度 | `PASS` | 历史结论和完整原文已迁移                                   |
| Spec 与执行一致性  | `PASS` | 新摘要不扩大旧授权，原文无损保留                           |
| 实现质量与风险     | `PASS` | 旧 Spec 中间阶段和 go-ahead 已消耗，不能作为新的发布授权。 |

- Overall Verdict：`PASS`
- Blocking Issues：None
- Cross-project consistency：`N/A`

### 7.1 回归风险

| project_id | Regression risk | 依据                              |
| ---------- | --------------- | --------------------------------- |
| `paseo`    | `Low`           | 当前只迁移 Markdown，不改变运行时 |

### 7.2 Touched Projects

N/A。

- Orphan changes：`None`

## 8. 偏差、变更与反向同步

- Plan-Execution Diff：历史任务的计划与执行偏差按第 10 章原记录保存；本迁移不重新裁决。
- Change Log：2026-08-02 将旧多文件任务包迁移为当前单文件 Heavy Spec。
- 用户决策：转换历史文档，验证后删除旧目录；0031 由其他会话处理，不在本迁移范围。
- Spec 反向同步结果：第 0-9 章成为当前权威摘要，第 10 章保存只读历史原文。

## 9. 恢复、长期知识与提交关联

- 状态说明：`已收口`
- 当前卡点：`N/A`
- 下一步唯一动作：N/A；后续版本使用当前 release Skill 和新任务。
- Resume / Handoff 锚点：本文第 0 章；详细历史见第 10 章。
- Project Sync Candidates：无；本迁移不从一次性历史记录推导新的长期规则。
- 长期文档同步：仅更新旧任务路径登记，不改变产品知识。

### 提交记录

| 提交信息（Commit Message）                                                                                     | 提交脚注（Commit Footer） | 关联项目 / 改动或阶段   | 文档同步状态 | 备注           |
| -------------------------------------------------------------------------------------------------------------- | ------------------------- | ----------------------- | ------------ | -------------- |
| `fix: apply live typography updates and stabilize release tests`（`a3dbaa27a13c2e45eb28df488194417860485bf2`） | `N/A`                     | `paseo / 发布候选修复`  | 已同步       | 历史提交已存在 |
| `chore: normalize release lockfile`（`c6826cc3006c77a12ed70d8e9163f6872025c197`）                              | `N/A`                     | `paseo / lockfile 准备` | 已同步       | 历史提交已存在 |
| `docs: add 0.2.4-beta.1 changelog`（`21b579f16092bab6e06cb2ee13189ece6ab73ed0`）                               | `N/A`                     | `paseo / Changelog`     | 已同步       | 历史提交已存在 |
| `fix(app): keep shortcut help available in modals`（`2c7651f21f64bf1feb2f3652c02be32e2ac6111b`）               | `N/A`                     | `paseo / F18 CI 修复`   | 已同步       | 历史提交已存在 |
| `chore(release): cut 0.2.4-beta.1`（`4484d62a60ba9cc2ee88c26d77633304b6217981`）                               | `N/A`                     | `paseo / beta release`  | 已同步       | 历史提交已存在 |

## 10. 历史原始记录

### 10.1 源文件完整性清单

以下哈希按 LF 规范化且去除文件尾空白后计算。

| 源文件         | 规范化字节数 | SHA-256                                                            |
| -------------- | -----------: | ------------------------------------------------------------------ |
| `SPEC.md`      |        31409 | `18ec5bc74aadcd7d5cc155063e3a424c11e7d1e09330d5c032089f87196a4647` |
| `findings.md`  |        17576 | `63c945dade11ee3d74756074cfc5a987170dcc95d7e02c1989cf1a4bc5dadae1` |
| `progress.md`  |        23550 | `00e62215bed9ff82d971fed28980e6a9a0d634fa56b722a72e3b6a4248fb1f4f` |
| `task_plan.md` |         1505 | `38c642a331caf201ddde4be3499e3c48a528c241538780cfea5d5fb2c498faaf` |

### 10.2 原 `SPEC.md`

````text
# 发布新版本 Spec

> `2026-07-28` 当前权威合同：本节覆盖下方 `7bc87b12` / `0.2.0-beta.6` 历史记录。历史内容仅保留调查追溯，不再授权任何执行。

## 2026-07-28 当前发布合同

- Spec 层级：Feature Spec；任务深度：`complex`；当前阶段：`Execute / Preparation`。
- 当前目标：把已完成上游同步与 P3 修正的本地 `main@28046d7f49113285fb7573baa4389d7456b1d405` 准备并推送到 `origin/main`，随后发布当前唯一启用的 GitHub beta prerelease。
- 当前候选版本：`0.2.4-beta.1`。仓库脚本从统一 manifests `0.2.3` 计算 `release:beta:patch` 的结果即为该版本；`v0.2.3` 是当前 `main` 的已采用 upstream 基线，第一个 Reforged-owned feature line 按 fork 政策做 patch beta。
- stable 不可执行：`scripts/set-release-version.mjs`、Desktop Release、Android APK Release 与 Release Notes Sync 的 signing/notarization/store gates 仍明确拒绝 stable tag。
- `origin` 精确为 `https://github.com/staoran/paseo-reforged.git`；远端 `main` 仍为 `7bc87b12`，仅有 `v0.2.0-beta.4` 与 `v0.2.0-beta.5` 两枚 `v0.2.*` Reforged tags，`v0.2.4-beta.1` 尚不存在。
- 本地 `main` ahead `origin/main` 90 commits；主工作树仍只有既有 `mydocs/**` WIP，发布流程不得 stash、reset、clean、提交或覆盖这些文件。
- 旧 release clone `E:\Code\paseo-release-0014` 保留两枚未推提交：过期 Changelog `51a48ca88` 与 6-file 修复 `27d90d843`。旧 Changelog 不得移植；6-file 修复在新主线仍全部缺失且对应上下文仍成立，必须按当前 tree 重新应用和验证，不盲目复用旧通过结论。

## 当前范围

### In Scope

- 在 clean 隔离分支重新应用 `27d90d843` 的 6 个已批准最小修复：live typography web 应用、3 个 E2E locator/assertion、Windows project basename helper、MCP fixture 字段。
- 将现有多个未完成 beta 条目合并为单一 `0.2.4-beta.1 - 2026-07-29` 顶部条目；Changelog 只写用户可感知结果，并保留外部贡献归属。
- 运行对应 MCP 单测、3 个 App E2E 文件、format、lint、typecheck、ACP drift、release preparation 与 Git 完整性门。
- 将验证后的本地 `main` fast-forward 并 push 到 `origin/main`，等待新 main CI 全绿。
- CI 全绿且用户再次给出最终 go-ahead 后，使用 clean `main` checkout 执行 `npm run release:beta:patch`，原子推送 release commit 与 `v0.2.4-beta.1`。
- 监控 Desktop Release、Android APK Release、Release Notes Sync，直到全部成功或出现需裁决的失败。

### Out of Scope

- stable/promote、npm、GHCR/Docker、App Store、Play Store、Cloudflare 或 relay 部署。
- 查询或合并冻结点 `cbbf6c168` 之后的新 upstream tip。
- 更新当前 ACP pins；若 drift gate 发现新事实，只记录并暂停裁决。
- 主工作树现有 `mydocs/**` WIP、旧 `0.2.0-beta.6` Changelog commit、无关代码修复或全量本地测试。

## 当前 Changelog 草案

批准后将 `CHANGELOG.md` 中 `0.2.0-beta.5`、`0.2.0-beta.2` 与 `0.2.0-beta.1` 三个未完成 beta 条目合并为以下唯一顶部条目；`0.1.110` 及更早 stable 历史保持不变：

`2026-07-28` 完整性复核：草案必须保留既有三个 Reforged beta 条目，并纳入 upstream `0.2.0` 至 `0.2.3` 已发布的用户可见说明；不能只列同步后的新增重点，否则会违反 `previous stable -> HEAD` 范围。以下正文已按该结论补齐并去重。

```markdown
## 0.2.4-beta.1 - 2026-07-29

### Added

- Paseo Reforged now has its own desktop installation, mobile package, update channel, and support links ([a4172e9](https://github.com/staoran/paseo-reforged/commit/a4172e995dae47a83516da9568dcfc59cc5ad910) by [@staoran](https://github.com/staoran))
- Edit files directly in the web and desktop apps ([#2270](https://github.com/getpaseo/paseo/pull/2270), [#2309](https://github.com/getpaseo/paseo/pull/2309), [#2277](https://github.com/getpaseo/paseo/pull/2277), [#2382](https://github.com/getpaseo/paseo/pull/2382) by [@dwyanewang](https://github.com/dwyanewang))
- Work with pull requests and merge requests from GitLab, Gitea, Forgejo, and Codeberg ([#1913](https://github.com/getpaseo/paseo/pull/1913) by [@nllptrx](https://github.com/nllptrx))
- Use Oh My Pi as a native agent provider ([#2067](https://github.com/getpaseo/paseo/pull/2067) by [@ebg1223](https://github.com/ebg1223))
- Import a session from the current project while creating a workspace ([feb4e82](https://github.com/staoran/paseo-reforged/commit/feb4e82d5f0e8ddacf85217664941a9e27ab7dc6) by [@staoran](https://github.com/staoran))
- Set a separate font family and size for each workspace ([1a00edd](https://github.com/staoran/paseo-reforged/commit/1a00eddcd23cf5eb033586ceeae678c56b240468) by [@staoran](https://github.com/staoran))
- See Codex retry status live while a request is retried ([ae4af3d](https://github.com/staoran/paseo-reforged/commit/ae4af3dc1ba90b18bda8a94c7b9828f0ef82690f) by [@staoran](https://github.com/staoran))
- Manage workspace scripts from the CLI and agent tools ([#1992](https://github.com/getpaseo/paseo/pull/1992) by [@mcowger](https://github.com/mcowger))
- Choose which local files are copied or shared when Paseo creates a worktree ([#2419](https://github.com/getpaseo/paseo/pull/2419) by [@michaelmwu](https://github.com/michaelmwu))
- Open project and workspace folders from the desktop sidebar ([#2491](https://github.com/getpaseo/paseo/pull/2491) by [@PTK030](https://github.com/PTK030))
- Switch projects from New Workspace with `Cmd/Ctrl+P` ([#2110](https://github.com/getpaseo/paseo/pull/2110) by [@turtleDev](https://github.com/turtleDev))
- PR comments preserve safe formatting such as links, images, and tables ([#2432](https://github.com/getpaseo/paseo/pull/2432) by [@mcowger](https://github.com/mcowger))
- Codex discovers project skills from the current workspace ([#2423](https://github.com/getpaseo/paseo/pull/2423) by [@dwyanewang](https://github.com/dwyanewang))
- Open the complete Changes view as a workspace tab ([#2298](https://github.com/getpaseo/paseo/pull/2298) by [@nikuscs](https://github.com/nikuscs))
- Add files to chat directly from Files and Changes ([#2275](https://github.com/getpaseo/paseo/pull/2275) by [@nikuscs](https://github.com/nikuscs))
- Browse workspace commit history and open individual commit diffs from Changes ([#1534](https://github.com/getpaseo/paseo/pull/1534), [#2146](https://github.com/getpaseo/paseo/pull/2146), [#2312](https://github.com/getpaseo/paseo/pull/2312) by [@adradr](https://github.com/adradr))
- Switch models from the Command Center for active agents and new drafts ([#2147](https://github.com/getpaseo/paseo/pull/2147) by [@kedrzu](https://github.com/kedrzu))
- Open existing agents from Paseo links or the CLI ([#2324](https://github.com/getpaseo/paseo/pull/2324))
- Configure workspace service ports with a fixed range or external allocator ([#2165](https://github.com/getpaseo/paseo/pull/2165) by [@mcowger](https://github.com/mcowger))
- Search keyboard shortcuts by action, note, or key combination ([#2160](https://github.com/getpaseo/paseo/pull/2160))
- Turn thinking off for supported Claude models ([#2257](https://github.com/getpaseo/paseo/pull/2257))
- Use Pi's Max thinking level ([#2267](https://github.com/getpaseo/paseo/pull/2267) by [@ByteTrue](https://github.com/ByteTrue))
- Copy terminal IDs from terminal tab menus ([#2371](https://github.com/getpaseo/paseo/pull/2371))
- Wrap long Markdown lines by default in the file editor ([#2459](https://github.com/getpaseo/paseo/pull/2459))
- Open workspace files in more installed editors and file managers ([#2119](https://github.com/getpaseo/paseo/pull/2119))
- Remove individual custom providers from Settings ([#1951](https://github.com/getpaseo/paseo/pull/1951))
- Use Claude Opus 5 ([7ef3376](https://github.com/getpaseo/paseo/commit/7ef3376b62837f4051df711232e1a72c9d3e4fde))
- Use Oh My Pi's Write Approval mode to allow reads while requiring approval for changes ([#2228](https://github.com/getpaseo/paseo/pull/2228) by [@theslava](https://github.com/theslava))

### Improved

- Desktop stops its managed daemon when you quit unless keeping it running is enabled ([#2454](https://github.com/getpaseo/paseo/pull/2454))
- Remote terminal and file traffic uses less bandwidth over encrypted connections ([#2480](https://github.com/getpaseo/paseo/pull/2480))
- Mobile model selection is more straightforward ([#2361](https://github.com/getpaseo/paseo/pull/2361))
- Selector popovers stay readable on iPad ([#2360](https://github.com/getpaseo/paseo/pull/2360) by [@yzim](https://github.com/yzim))
- Projects, workspaces, and chats sync more efficiently ([#2028](https://github.com/getpaseo/paseo/pull/2028), [#2185](https://github.com/getpaseo/paseo/pull/2185), [#2196](https://github.com/getpaseo/paseo/pull/2196), [#2206](https://github.com/getpaseo/paseo/pull/2206), [#2259](https://github.com/getpaseo/paseo/pull/2259), [#2263](https://github.com/getpaseo/paseo/pull/2263))
- CLI and agent tools manage workspaces, agents, and schedules more consistently ([#2186](https://github.com/getpaseo/paseo/pull/2186))
- Pasted PR and MR links are selected automatically as checkout options ([#2290](https://github.com/getpaseo/paseo/pull/2290))
- Project creation is more explicit ([#2098](https://github.com/getpaseo/paseo/pull/2098), [#2187](https://github.com/getpaseo/paseo/pull/2187))
- Idle agents release processes automatically and resume when needed ([#2203](https://github.com/getpaseo/paseo/pull/2203), [#2209](https://github.com/getpaseo/paseo/pull/2209))
- New Claude and Codex agents use safer automatic approval modes when supported ([#2213](https://github.com/getpaseo/paseo/pull/2213))
- Permission and thinking changes made during a turn show when they take effect ([#2201](https://github.com/getpaseo/paseo/pull/2201))
- Workspace search shows and matches project names ([#2345](https://github.com/getpaseo/paseo/pull/2345) by [@cleiter](https://github.com/cleiter), [@claude](https://github.com/claude))
- Claude usage shows model-specific weekly limits ([#2303](https://github.com/getpaseo/paseo/pull/2303) by [@cleiter](https://github.com/cleiter), [@claude](https://github.com/claude))
- Oh My Pi exposes injected Paseo tools directly ([#2418](https://github.com/getpaseo/paseo/pull/2418) by [@perezd](https://github.com/perezd))
- Oh My Pi shows only the thinking levels each model supports ([#2171](https://github.com/getpaseo/paseo/pull/2171) by [@bendavid](https://github.com/bendavid))
- Oh My Pi supports Max thinking in imported and new sessions ([#2191](https://github.com/getpaseo/paseo/pull/2191) by [@mvanhorn](https://github.com/mvanhorn))
- Oh My Pi advisor results retain their structure and severity ([#2219](https://github.com/getpaseo/paseo/pull/2219) by [@ebg1223](https://github.com/ebg1223))
- Usage bars warn as provider limits approach ([#2322](https://github.com/getpaseo/paseo/pull/2322) by [@cleiter](https://github.com/cleiter))
- Workspace focus mode stays confined to the active workspace with a visible exit control ([#2151](https://github.com/getpaseo/paseo/pull/2151))
- Desktop installs the newest available update instead of a cached older release ([#2149](https://github.com/getpaseo/paseo/pull/2149))
- Remote daemon update failures show specific recovery steps ([#2120](https://github.com/getpaseo/paseo/pull/2120))
- Agent history errors appear immediately instead of after a timeout ([#2124](https://github.com/getpaseo/paseo/pull/2124))

### Fixed

- Imported sessions retain their original titles ([084dca0](https://github.com/staoran/paseo-reforged/commit/084dca00b7bff618b09458082d878decfdd40918) by [@staoran](https://github.com/staoran))
- Reasoning details react immediately to the auto-expand setting ([432f4fd](https://github.com/staoran/paseo-reforged/commit/432f4fd6e755c5b71ebd30a180ce5559569412f9) by [@staoran](https://github.com/staoran))
- Windows file links open at the referenced line ([14f8ed7](https://github.com/staoran/paseo-reforged/commit/14f8ed70e5bae181be0a47a584c2034ab61fe65e) by [@staoran](https://github.com/staoran))
- Terminal pairing QR codes remain scannable in narrow terminals ([#2381](https://github.com/getpaseo/paseo/pull/2381))
- Workspace creation stays responsive with many active or archived workspaces ([#2355](https://github.com/getpaseo/paseo/pull/2355), [#2379](https://github.com/getpaseo/paseo/pull/2379))
- Failed agent starts no longer leave provider processes running ([#2348](https://github.com/getpaseo/paseo/pull/2348) by [@dwyanewang](https://github.com/dwyanewang))
- ACP image prompts no longer appear twice ([#2363](https://github.com/getpaseo/paseo/pull/2363))
- Web chats stay pinned to the latest message at non-default browser zoom ([#2368](https://github.com/getpaseo/paseo/pull/2368))
- Grouped tool-call loading animations display correctly ([#2369](https://github.com/getpaseo/paseo/pull/2369))
- Large file views no longer disconnect the session ([#2482](https://github.com/getpaseo/paseo/pull/2482))
- Reaching the top of a chat loads the complete older history ([#2481](https://github.com/getpaseo/paseo/pull/2481))
- Parent agents stay available while child agents are working ([#2458](https://github.com/getpaseo/paseo/pull/2458))
- Stale client connections no longer exhaust daemon memory ([#2169](https://github.com/getpaseo/paseo/pull/2169))
- Oh My Pi opens models that omit a context-window limit ([#2406](https://github.com/getpaseo/paseo/pull/2406) by [@astartsky](https://github.com/astartsky))
- Self-hosted forge links preserve custom web ports ([#2478](https://github.com/getpaseo/paseo/pull/2478) by [@claude](https://github.com/claude), [@muzhi1991](https://github.com/muzhi1991))
- Completed turns no longer remain stuck as running ([#2261](https://github.com/getpaseo/paseo/pull/2261), [#2282](https://github.com/getpaseo/paseo/pull/2282), [#2336](https://github.com/getpaseo/paseo/pull/2336), [#2484](https://github.com/getpaseo/paseo/pull/2484) by [@isac322](https://github.com/isac322), [@mcowger](https://github.com/mcowger))
- Provider settings open correctly above the model selector ([#2476](https://github.com/getpaseo/paseo/pull/2476))
- Image uploads preserve the correct image format ([#2380](https://github.com/getpaseo/paseo/pull/2380))
- Notifications open the correct workspace and agent ([#2331](https://github.com/getpaseo/paseo/pull/2331))
- Archived agents can be restored directly from History ([#2316](https://github.com/getpaseo/paseo/pull/2316))
- CLI agent runs stay in the current workspace unless a new workspace is requested ([#2315](https://github.com/getpaseo/paseo/pull/2315))
- Reused branches no longer attach an unrelated merged or closed pull request ([#2172](https://github.com/getpaseo/paseo/pull/2172) by [@nllptrx](https://github.com/nllptrx))
- Pi compaction waits for long summaries instead of reporting a false timeout ([#2181](https://github.com/getpaseo/paseo/pull/2181) by [@jasonhnd](https://github.com/jasonhnd))
- Pi chats keep new messages aligned with the correct history after an idle agent resumes ([#2313](https://github.com/getpaseo/paseo/pull/2313))
- OpenCode follow-ups triggered by completed background work remain visible ([#2258](https://github.com/getpaseo/paseo/pull/2258))
- Codex no longer shows the parent agent as a phantom subagent ([#2214](https://github.com/getpaseo/paseo/pull/2214))
- Oh My Pi background notices appear as task notifications instead of raw system text ([#2218](https://github.com/getpaseo/paseo/pull/2218) by [@ebg1223](https://github.com/ebg1223))
- Oh My Pi slash commands include commands from every supported source ([#2175](https://github.com/getpaseo/paseo/pull/2175) by [@bendavid](https://github.com/bendavid))
- Local dictation works in Nix-packaged installations ([#1587](https://github.com/getpaseo/paseo/pull/1587) by [@yhori991](https://github.com/yhori991))
- The composer remains visible after submitting dictated text and returning to the app ([#2194](https://github.com/getpaseo/paseo/pull/2194))
- Desktop's dictation shortcut remains responsive after finishing a recording ([#2268](https://github.com/getpaseo/paseo/pull/2268))
- Projects can be renamed before their first workspace ([#2252](https://github.com/getpaseo/paseo/pull/2252) by [@albertodeago](https://github.com/albertodeago))
- Settings keep showing a connected remote host when the local daemon is stopped ([#1749](https://github.com/getpaseo/paseo/pull/1749) by [@dwyanewang](https://github.com/dwyanewang))
- Pinned workspaces no longer disappear briefly when reopening the compact sidebar ([#2210](https://github.com/getpaseo/paseo/pull/2210))
- Terminal panes no longer remain at 80x24 after focus or visibility changes ([#2059](https://github.com/getpaseo/paseo/pull/2059), [#2154](https://github.com/getpaseo/paseo/pull/2154) by [@cleiter](https://github.com/cleiter))
- Sign-in popups in the desktop browser complete successfully ([#2137](https://github.com/getpaseo/paseo/pull/2137))
- Browser typing and shortcuts no longer submit the active Paseo prompt ([#1982](https://github.com/getpaseo/paseo/pull/1982))
- Agent browser tabs remain controllable after switching workspaces ([#2156](https://github.com/getpaseo/paseo/pull/2156))
- Archived workspaces show the correct Unarchive or Restore action ([#2002](https://github.com/getpaseo/paseo/pull/2002))
- Archived sessions can be reimported into the current workspace ([#2123](https://github.com/getpaseo/paseo/pull/2123), [#2265](https://github.com/getpaseo/paseo/pull/2265) by [@nikuscs](https://github.com/nikuscs))
- Browser shortcuts no longer appear where browser tabs are unavailable ([#2116](https://github.com/getpaseo/paseo/pull/2116) by [@jasonhnd](https://github.com/jasonhnd))
- Pin and unpin shortcuts work when sidebar sections are collapsed ([#2299](https://github.com/getpaseo/paseo/pull/2299) by [@cleiter](https://github.com/cleiter))
- `Shift+Tab` no longer changes a background agent's permission mode ([#1848](https://github.com/getpaseo/paseo/pull/1848) by [@cleiter](https://github.com/cleiter))
- Proxied services preserve ports in redirects ([#2288](https://github.com/getpaseo/paseo/pull/2288) by [@cleiter](https://github.com/cleiter))
- Clicking the file editor focuses its pane ([#2457](https://github.com/getpaseo/paseo/pull/2457))
- Claude 5 models use the correct context windows ([c9fb31f](https://github.com/getpaseo/paseo/commit/c9fb31f709a771a18cae279c8506e24c5b6fb7ab))
- Android beta APK releases use the free EAS build tier ([9739d20](https://github.com/staoran/paseo-reforged/commit/9739d20d335f4279fa8aa9df90969cc481b833b9) by [@staoran](https://github.com/staoran))
```

## 更新后的执行计划

1. 已批准：用户回复 `Plan Approved：按 0.2.4-beta.1 执行准备与 push，直接打tag发版本`，同时授权 beta 类型、上述 Changelog、6-file 修复重放、验证、push preparation，以及全部既定门禁通过后的 release commit、tag 与 GitHub prerelease；该批准不允许跳过检查或自动吸收新 drift/额外变更。
2. 在 clean `codex/sync-upstream-2026-07-27@28046d7f` worktree 按当前 tree 重新应用 6-file 修复，运行 MCP 单测与三个目标 E2E 文件，再运行 format、lint、typecheck 与 Git checks；失败立即暂停。
3. 运行 `npm run acp:version-drift:check` 与 `npm run release:prepare`；F16 的 6 项 ACP drift 已按 `cbbf6c168` 同源基线批准冻结，不修改 catalog；其他新 drift 或 lockfile 真实变化仍先形成证据并重新批准。
4. 将唯一 beta Changelog 写入并单独提交；最终审查确认 fix commit 与 Changelog commit 分离、隔离 worktree clean。
5. 将本地主工作树 `main` 以 `--ff-only` 前进到 preparation tip，保持 `mydocs/**` WIP；push `main` 到正确 origin，不推 tag，随后监控新 main CI 至全绿。
6. CI 全绿后复核 release tip、最终版本和 tag 状态；若与当前合同一致，使用本次合并授权继续，不再请求第二次确认。
7. 从 `origin/main` 建立 clean `main` checkout，执行 `npm run release:beta:patch`；验证 release commit 与不可变 `v0.2.4-beta.1` 原子推送。
8. 持续监控 Desktop Release、Android APK Release、Release Notes Sync；任一失败保留 tag 不动，只按文档选择同 tag 基础设施重试或下一 beta 源码修复。

## 当前执行门

- Phase：`Execute / Preparation`。
- Approval：`2026-07-28` 用户已批准当前 `0.2.4-beta.1` 计划，并明确要求全部既定门禁通过后直接打 tag 发版本；该回复合并满足 preparation/push 与最终 release go-ahead。
- 自动继续条件：6-file 修复、Changelog、F16 同源冻结、release preparation、format、lint、typecheck、定向测试、Git 检查和新 main CI 全部与当前合同一致且通过。
- 暂停条件：出现新 ACP drift、lockfile 真实变化、测试/CI 失败、远端前进、版本/tag 冲突、Changelog 或 release diff 超出当前合同，必须先形成证据并重新裁决。
- F15 裁决：用户接受未修改基线也会触发的 MCP Windows 5 秒超时差分证据，要求保持原 6-file diff、不修改测试超时并继续；本次 fixture 直接覆盖测试已通过，新 main CI 仍必须全绿。
- App E2E 证据：`appearance-typography.spec.ts` 为 `1 passed`（约 2 分钟）；`appearance-reasoning.spec.ts` 为 `1 passed (1.2m)`；`agent-stream-ui.spec.ts` 以退出码 `0` 完成（约 4 分 42 秒）。三次均使用隔离 daemon/relay/Metro，未触碰 `6767`。
- F16 ACP 裁决：`cbbf6c168`、当前下游 `main` 与当前 `upstream/main` 的 catalog blob 均为 `eab70de7012c`，drift checker blob 均为 `a6b31a955e3e`；用户明确批准按该上游同源基线冻结当前 6 个 stale pin，不修改 catalog，并继续直接发布 `0.2.4-beta.1`。F16 已解除；后续仍以 lockfile、额外 drift、CI 或 tag 冲突为暂停条件。
- F17 lockfile 裁决：clean `28046d7f` 在已验证的 Node `22.20.0` / npm `11.17.0` 下运行 `release:prepare`，稳定产生仅 `package-lock.json` 的 `25 insertions / 419 deletions`；第二次运行 hash 仍为 `50bb9f05c6faf42261b671e3105ed1d625beb8a6`，证明规范化幂等。没有依赖版本升级，`packages/cli/bin/paseo` 仅为 stat 假阳性且 blob 未变。用户明确批准将该 clean 结果作为独立 lockfile preparation commit，并在复核幂等与既定门禁后继续直接发布 `0.2.4-beta.1`；F17 已解除。
- F18 CI 交互门禁：`main@21b579f16` 的 CI run `30423908337` 有 17/18 jobs 成功，唯一失败为 `provider-settings-refresh.spec.ts` 的 selector 内 `Shift+?`。同一目标用例在本地热缓存下确定性复现。根因是上游 `2acb10fce` 明确要求 keyboard shortcuts dialog 与 Command Center 可覆盖仍打开的 model selector，而下游 `b7db6ec` 新增的 `aria-modal -> modal` focus scope 使 `Shift+?` 的 `focusScope: "other"` 绑定被过滤；项目切换等背景动作仍必须在 modal 中禁用。用户已批准最小 2-file 修复：仅让 keyboard shortcuts dialog 绑定接受 `other`/`modal`，并增加对应纯逻辑正向用例，保留现有 editable/message-input 负向门禁与上游整条 E2E。验证、独立提交、新 main CI 全绿后继续既有直接发布授权。

---

## 历史记录：旧 `7bc87b12` / `0.2.0-beta.6` 计划（已失效）

## 任务状态

- Task ID：`0014`
- Spec 层级：Feature Spec
- 当前阶段：Execute / Preparation
- Approval：beta `0.2.0-beta.6`、完整 Changelog、clean-checkout preparation 与 ACP pin 有意冻结已于 2026-07-27 获批；4 项最小临发布测试修复、F9 的同文件最小 locator 修复及 F10 的最小 typography 产品修复已于 2026-07-28 获批；tag/GitHub prerelease Execute 仍待最终 go-ahead
- Source：用户要求将当前 `main` 推送到远程并发布一个版本

## 目标与完成合同

- 将已验证的当前 Reforged `main` 发布为当前唯一启用的 GitHub beta prerelease。
- 发布 tag 必须指向包含获批 changelog 和统一 workspace 版本的不可变提交。
- Desktop Release、Android APK Release、Release Notes Sync 必须成功；npm、Docker、商店和 relay 均不得发布。
- 当前工作树中的其他任务 WIP 不得被 stash、清理、提交或带入发布。

以下任一情况仍算未完成：未获最终 go-ahead 就打 tag、发布 stable、复用或移动已有 tag、ACP drift 未解释、CI/发布工作流未绿、或把上游同步及其他 WIP 混入 release commit。

## 范围

### In Scope

- 已推送的 `main` `7bc87b12cb1049b0d268a6bd4d97540fedba8ba0`。
- 候选 `0.2.0-beta.6`、当前 beta changelog 的原位更新、统一 workspace 版本和 beta tag。
- clean release checkout 中的 format、lint、typecheck、ACP drift、release check 与 GitHub beta workflows。
- 只修复已验证的 interface typography 设置未应用到 web computed style 的产品路径，并先重跑对应 Typography E2E。

### Out of Scope

- `0010` 的上游 `0.2.2` merge；它仍需独立 Execute Approval。
- stable/promote；macOS signing/notarization gate 尚未解除。
- npm、GHCR/Docker、App Store、Play Store、Cloudflare relay 部署。
- 当前主工作树的其他 tracked/untracked WIP。

## 已确认事实与版本决策

- `origin` 精确为 `https://github.com/staoran/paseo-reforged.git`，GitHub 认证有效。
- `main` 已从 `b64f4f357` 快进推送到 `7bc87b12c`，没有推送 tag。
- 当前 manifests 与最新 Reforged prerelease 都是 `0.2.0-beta.5`。
- 上游最新版本为 `0.2.2`，但 `v0.2.2` 和 `upstream/main` 都不是当前 `HEAD` 的祖先；`0010` 明确把同步作为独立、尚未批准的复杂任务。
- stable 是文档明确阻断的路径。若当前代码立即发布，只能继续现有 beta 线；推荐 `release:beta:next` 生成 `0.2.0-beta.6`。
- `0.2.3-beta.1` 仅适用于先完成 `0010`、采用上游 `0.2.2` 后的下一 patch beta，不得在当前未同步 tree 上伪造该基线。

## Changelog 草案

批准后原位替换当前 `0.2.0-beta.5` 顶部条目，不新增第二个同基线条目：

```markdown
## 0.2.0-beta.6 - 2026-07-27

### Added

- Paseo Reforged now has its own desktop installation, mobile package, update channel, and support links ([a4172e9](https://github.com/staoran/paseo-reforged/commit/a4172e995dae47a83516da9568dcfc59cc5ad910) by [@staoran](https://github.com/staoran))
- Import a session from the current project while creating a workspace ([feb4e82](https://github.com/staoran/paseo-reforged/commit/feb4e82d5f0e8ddacf85217664941a9e27ab7dc6) by [@staoran](https://github.com/staoran))
- Set a separate font family and size for each workspace ([1a00edd](https://github.com/staoran/paseo-reforged/commit/1a00eddcd23cf5eb033586ceeae678c56b240468) by [@staoran](https://github.com/staoran))
- See Codex retry status live while a request is retried ([ae4af3d](https://github.com/staoran/paseo-reforged/commit/ae4af3dc1ba90b18bda8a94c7b9828f0ef82690f) by [@staoran](https://github.com/staoran))
- Open the complete Changes view as a workspace tab ([#2298](https://github.com/getpaseo/paseo/pull/2298) by [@nikuscs](https://github.com/nikuscs))
- Add files to chat directly from Files and Changes ([#2275](https://github.com/getpaseo/paseo/pull/2275) by [@nikuscs](https://github.com/nikuscs))
- Open existing agents from Paseo links or the CLI ([#2324](https://github.com/getpaseo/paseo/pull/2324))
- Use Oh My Pi's Write Approval mode to allow reads while requiring approval for changes ([#2228](https://github.com/getpaseo/paseo/pull/2228) by [@theslava](https://github.com/theslava))

### Improved

- Usage bars now warn as provider limits approach ([#2322](https://github.com/getpaseo/paseo/pull/2322) by [@cleiter](https://github.com/cleiter))
- Oh My Pi advisor results now retain their structure and severity ([#2219](https://github.com/getpaseo/paseo/pull/2219) by [@ebg1223](https://github.com/ebg1223))

### Fixed

- Imported sessions retain their original titles ([084dca0](https://github.com/staoran/paseo-reforged/commit/084dca00b7bff618b09458082d878decfdd40918) by [@staoran](https://github.com/staoran))
- Reasoning details react immediately to the auto-expand setting ([432f4fd](https://github.com/staoran/paseo-reforged/commit/432f4fd6e755c5b71ebd30a180ce5559569412f9) by [@staoran](https://github.com/staoran))
- Windows file links open at the referenced line ([14f8ed7](https://github.com/staoran/paseo-reforged/commit/14f8ed70e5bae181be0a47a584c2034ab61fe65e) by [@staoran](https://github.com/staoran))
- Android beta APK releases now use the free EAS build tier
- Notifications now open the correct workspace and agent ([#2331](https://github.com/getpaseo/paseo/pull/2331))
- Archived agents can be restored directly from History ([#2316](https://github.com/getpaseo/paseo/pull/2316))
- CLI agent runs stay in the current workspace unless a new workspace is requested ([#2315](https://github.com/getpaseo/paseo/pull/2315))
- Oh My Pi slash commands now include commands from every supported source ([#2175](https://github.com/getpaseo/paseo/pull/2175) by [@bendavid](https://github.com/bendavid))
- Oh My Pi chats no longer stay stuck as running after delayed or incomplete completion events ([#2261](https://github.com/getpaseo/paseo/pull/2261), [#2282](https://github.com/getpaseo/paseo/pull/2282) by [@isac322](https://github.com/isac322))
```

`7bc87b12c` 只修复 E2E harness，没有用户可感知变更，因此不进入 changelog；beta.5 后的两个 Android release 修复折叠到已有 APK 条目，不拆成内部迭代。

## 执行计划

1. 用户批准 beta 类型、`0.2.0-beta.6` 和完整 changelog 草案。
2. 从已推送 `origin/main` 创建独立 clean release checkout，避免触碰当前 WIP。
3. 在 clean checkout 运行 `npm run format`、`npm run lint`、`npm run typecheck` 与 `npm run acp:version-drift:check`；任何文件变化或真实 drift 都先形成独立 diff 并重新批准，不混入 changelog commit。
4. 确认 main CI 绿色后，写入并提交获批 changelog；再次请求最终 go-ahead。
5. 最终 go-ahead 后运行 `npm run release:beta:next`，验证远端 `main` 与不可变 `v0.2.0-beta.6`。
6. 持续监控 Desktop Release、Android APK Release、Release Notes Sync，直到全部成功或出现需用户裁决的失败。

## 风险与回退

- 当前主工作树不干净：使用独立 clean checkout，不 stash/reset/clean 当前 WIP。
- ACP check 当前有一个确认 drift 和多个 registry 不可用项：发布命令前必须重新验证；需要修改时先暂停并获取批准。
- 上游 tag 与 Reforged `v0.2.0-beta.4` 同名异指向：不执行 `fetch upstream --tags`、不 force、不移动任何 tag。
- release tag 创建后不可回退；源代码问题只能修复后发布下一 beta，基础设施瞬态失败只重试同一不可变 tag。

## 当前批准门

- 已批准：beta `0.2.0-beta.6`、上述 changelog 草案、独立 clean-checkout preparation。
- 已决定：本次 beta 有意冻结 preparation 发现的 8 个 ACP pin，不修改 catalog。
- 最终门禁：preparation 全部通过并提交获批 changelog 后，用户明确 go-ahead 才能创建 tag 和 GitHub prerelease。
````

### 10.3 原 `findings.md`

```text
# 发布新版本 Findings

## 2026-07-28 当前发布基线（覆盖 F1-F4 的旧发布假设）

- 来源：`git rev-parse`、`git ls-remote origin`、统一 workspace manifests、`docs/release.md`、`release-beta`、旧 release checkout 状态。
- 当前主线：本地 `main@28046d7f49113285fb7573baa4389d7456b1d405`，远端 `origin/main@7bc87b12cb1049b0d268a6bd4d97540fedba8ba0`，本地 ahead 90；冻结的 `cbbf6c168` 与 `v0.2.3` 都是当前 `main` 的祖先。
- 远端 tag：`origin` 只存在 `v0.2.0-beta.4` 与 `v0.2.0-beta.5`；本地还含 upstream 的其他 `v0.2.*` tags，因此发布存在性判断必须以 `git ls-remote origin` 为准，不能以本地 tag 列表为准。
- 版本：根及 10 个发布 workspace manifests 统一为 `0.2.3`；当前已包含 Reforged-owned 功能，按 fork patch-beta 政策使用 `release:beta:patch`，目标为 `0.2.4-beta.1`。
- 发布路径：stable 仍被本地脚本和 GitHub workflow 门禁阻断；当前只允许 GitHub beta prerelease，不包含 npm、Docker/GHCR、商店、Cloudflare 或 relay 发布。
- 旧 release checkout：`E:\Code\paseo-release-0014` 仍 clean 且停在 `27d90d843`，其父提交 `51a48ca88` 是过期 `0.2.0-beta.6` Changelog；只允许按当前 tree 重放 `27d90d843` 的 6-file 修复，不得移植旧 Changelog。
- 当前门禁：尚未在 `28046d7f` 上重放修复、更新 Changelog、复核 ACP drift、运行 preparation 或触发新 main CI；这些结论不能继承旧 `7bc87b12` 的通过状态。

## F14：原 `0.2.4-beta.1` Changelog 草案不满足完整范围

- 来源：当前 `CHANGELOG.md` 的三个 Reforged beta 条目、upstream `5d397754b:CHANGELOG.md` 中 `0.2.0` 至 `0.2.3` 的稳定版说明，以及 `v0.1.110..28046d7f` 的 243 个提交。
- 事实：原草案只保留后续同步的重点项；若据此替换三个 beta 条目，会丢失已经公开的 forge、OMP、Changes、Command Center、服务端口、快捷键、移动选择器、同步与多组修复等用户可见行为。
- 归属复核：当前新增 PR 的 commit author 与草案基本一致；`#2345`、`#2303` 还包含 `@claude`，已补为共同作者；`#2454`、`#2480` 等无署名项只有 `@boudra`，按发布规则继续省略。
- 结论：`SPEC.md#当前-changelog-草案` 已扩充为 `previous stable -> HEAD` 的单一完整条目，保留既有 beta 内容、纳入 upstream 稳定版说明并合入 Reforged 新增项；执行时不得退回此前的精简草案。

## F15：新主线 MCP 文件存在与 6-file 修复无关的 Windows 5 秒超时

- 来源：隔离分支与未修改 `main@28046d7f` 上的定向 Vitest；`mcp-server.test.ts` 的真实 Git/worktree 场景 `keeps a manual workspace title when agent-created worktree naming finishes later`。
- 事实：隔离分支整文件在该场景精确 5 秒超时；同场景单跑仍精确 5 秒超时；未修改的 `main@28046d7f` 也同样失败。诊断性 `--testTimeout=20000` 下场景通过，测试体耗时约 8.35 秒。
- 差分：当前 server patch 只在 `createManagedAgent()` fixture 增加 `providerRetryMessage: null`；其直接覆盖的 `returns full-detail snapshots from get_agent_status` 单跑通过（1 passed，约 69ms）。
- 结论：这不是 6-file patch 引入的行为回归，而是新主线真实 Git 测试在当前 Windows 环境超过 Vitest 默认 5 秒。继续路径需裁决：保留最小 diff 并由新 main CI 裁决，或额外批准在同一测试文件给该场景设置 15 秒超时。
- 用户裁决：接受差分证据，保持原 6-file diff，不修改超时并继续全部发布门禁；新 main CI 若复现失败仍须暂停。

## F16：同步后的 ACP catalog 出现新的 6 项真实 drift

- 来源：新主线隔离分支的 `npm run acp:version-drift:check`，以及对 13 个被 Windows 误报为 `unavailable` 的 npm 包逐项执行只读 `npm view <package> version --json`。
- Windows 事实：原检查仍不能直接启动 `npm`，因此 13 个 `unavailable` 不是 registry 事实；逐项查询均成功，其中 7 项已与 catalog 一致。
- 真实漂移：`dimcode 0.2.36 -> 0.2.38`、`dirac-cli 0.4.22 -> 0.4.28`、`droid 0.179.0 -> 0.181.0`、`fast-agent-acp 0.9.22 -> 0.9.26`、`@qoder-ai/qodercli 1.1.4 -> 1.1.7`、`@qwen-code/qwen-code 0.20.1 -> 0.21.0`。
- 与 F4 的差异：同步后的 catalog 已吸收 F4 的部分旧更新，当前 pin 和 registry latest 也继续前进；因此不能把 F4 对旧 8 项的冻结授权自动解释为当前 6 项的新授权。
- 同源证据：`cbbf6c168`、当前下游 `main` 与当前 `upstream/main` 的 catalog blob 均为 `eab70de7012c`，drift checker blob 均为 `a6b31a955e3e`；上游当前 tip 也未吸收 registry 后续版本。
- 用户裁决：按 `cbbf6c168` 上游同源基线有意冻结当前 6 项，不修改 ACP catalog；继续 `release:prepare`、提交、push、CI 与直接发布 `0.2.4-beta.1`。F16 不再阻断。

## F17：release preparation 会幂等规范化 lockfile

- 来源：候选 worktree 的首次 `release:prepare`，以及从未修改 `28046d7f` 建立的 clean worktree；clean 验证使用此前 release preflight 已验证的 Node `22.20.0` / npm `11.17.0`。
- 工具链排除：系统 Node `26.4.0` / npm `12.0.1` 会产生更大的 `25 insertions / 1028 deletions`，不得作为发布输入；Node 22/npm 11 clean 基线稳定收敛到 `25 insertions / 419 deletions`。
- 内容：真实变化仅为 `package-lock.json`，主要移除不再由当前 workspace resolution 保留的 optional/peer 与重复 workspace 测试节点，并规范化 Rollup optional package 的 `peer` 标记；没有 manifest 或依赖版本升级。
- 幂等证据：连续第二次 `release:prepare` 后 lockfile hash 保持 `50bb9f05c6faf42261b671e3105ed1d625beb8a6`。`packages/cli/bin/paseo` 虽被 Windows stat 标记，工作树 blob 与 HEAD 均为 `e4f527c6f0ef06e5353091abc0b855543588b605`，不构成 diff。
- 影响：release 命令会再次运行 `release:prepare`；若不先提交该规范化，后续版本命令会因 dirty tree 失败。
- 用户裁决：批准采用 clean Node 22/npm 11 的幂等结果并独立提交；同步到候选后重跑 `release:prepare` 验证 hash 不变，再继续既定门禁与 `0.2.4-beta.1` 直接发布。F17 不再阻断。

## F18：modal focus scope 误伤上游全局快捷键帮助

- 来源：Reforged CI run `30423908337`、失败 job `90486091259`、本地目标 Playwright 复现、`2acb10fce`/`b7db6ec` 历史与当前 shortcut/focus-scope 源码。
- 失败证据：CI 为 79 passed / 1 failed，初跑与 retry 均在 `provider-settings-refresh.spec.ts:143` 等待 `keyboard-shortcuts-dialog`；同一测试本地热缓存运行到相同行并失败。其余 17 个 CI jobs，包括 Windows/Linux server、其他三个 Playwright shard、format/lint/typecheck 均成功。
- 合同冲突：上游 `2acb10fce` 同时为 model selector 加入 `role="dialog"`/`aria-modal` 与覆盖式 keyboard shortcuts/Command Center E2E；下游 `b7db6ec` 为阻止 project picker 穿透 modal 增加统一 `modal` focus scope，但 `Shift+?` 仍只接受 `other`，因此把上游明确允许的只读全局帮助一并阻断。
- 排除项：失败焦点不在 search input；失败快照显示 selector settings button 已 active，selector 本身仍可见。Command Center binding 没有相同 scope 限制，事件 listener 与 overlay host 也由同一上游用例覆盖，故不需要移除 modal scope或重写 overlay。
- 最小修复：把 `shortcuts-dialog-toggle-question-mark` 的允许 scope 从 `other` 扩为 `other`/`modal`，在 `keyboard-shortcuts.test.ts` 加 modal 正向用例；保留 project picker 的 `other`/`message-input` 白名单，以及 editable/terminal/Command Center/modal 负向用例。现有 provider settings E2E 作为真实回归 seam。
- 当前裁决：用户已批准该 2-file last-minute change；按现有 unit/E2E seam 完成验证、独立提交并等待新 main CI 全绿后，继续既有直接发布授权。

## F1：当前唯一启用路径是 beta

- 来源：`docs/release.md`、`release-beta`、`release-stable`。
- 事实：stable 同时被本地流程和 CI 的 macOS signing/notarization gate 阻断；npm、Docker 和商店发布也不在当前合同内。
- 影响：用户未指定发布类型时不能静默执行 stable；当前可执行候选只能是 GitHub beta prerelease。

## F2：上游 `0.2.2` 尚未被当前 main 采用

- 来源：`git merge-base --is-ancestor v0.2.2 HEAD`、`git merge-base --is-ancestor upstream/main HEAD` 均失败；`0010` Spec。
- 事实：当前 manifests 为 `0.2.0-beta.5`；上游 manifests 为 `0.2.2`，同步计划尚未获 Execute Approval。
- 影响：当前 release 不能声称采用 `0.2.2`，也不能直接发布 `0.2.3-beta.1`。

## F3：tag namespace 存在同名异指向

- 来源：`git fetch upstream main --tags`。
- 事实：上游和 Reforged 的 `v0.2.0-beta.4` 指向不同提交，fetch 拒绝覆盖；`upstream/main` 本身已成功刷新。
- 影响：发布与同步都不得 force-fetch、移动或复用 tag。

## F4：ACP drift gate 确认有 8 个真实漂移

- 来源：clean checkout 中的 `npm run acp:version-drift:check`；Windows 原始结果的 `--json` 诊断；仓库外临时 preload 修正 `execFile("npm")` 后的同脚本复查。
- Windows 事实：脚本对 13 个 npm 包返回 `spawn npm ENOENT`，原因是 `execFile("npm")` 不能直接启动 Windows 的 `npm.cmd`；手工 `npm view` 正常，非 registry 不可用。
- 真实漂移：`agoragentic-mcp 1.3.3 -> 1.3.6`、`dimcode 0.2.35 -> 0.2.36`、`dirac-cli 0.4.22 -> 0.4.27`、`droid 0.177.0 -> 0.180.0`、`fast-agent-acp 0.9.21 -> 0.9.25`、`@google/gemini-cli 0.51.0 -> 0.52.0`、`@qoder-ai/qodercli 1.1.3 -> 1.1.6`、`@qwen-code/qwen-code 0.20.1 -> 0.21.0`。
- 影响：用户需决定单独批准 catalog 更新，或将这些 pin 明确记录为本次 beta 的有意冻结项；决定前不提交 changelog、不创建 tag。
- 用户决定：本次 beta 有意冻结上述 8 个 pin，不扩大 release diff；后续升级另行验证。

## F5：Server CI 失败来自 MCP 测试 fixture 漏字段

- 来源：GitHub Actions run `30277605926` 的 Linux/Windows server jobs；本地定向复现；`packages/server/src/server/agent/mcp-server.test.ts` 的 `createManagedAgent()` 与生产 `AgentManager` 初始化对比。
- 事实：测试 fixture 缺少 `providerRetryMessage: null`，投影经 JSON 标准化后形成协议 schema 不接受的值；生产 `AgentManager` 已正确初始化该字段，协议拒绝 wire `null` 的行为也正确。
- 结论：这是测试 fixture 漂移，不是生产协议回归。最小候选修复只在 fixture 补齐字段。

## F6：Provider retry E2E 在 agent remove 后断言了已卸载视图

- 来源：`packages/app/e2e/agent-stream-ui.spec.ts:95` 的稳定本地复现与失败 DOM。
- 事实：测试发送 `agent_update kind=remove` 后，页面合法进入 `Workspace unavailable`，随后仍要求已卸载的 `turn-working-indicator` 可见。
- 结论：retry message 消失的目标断言已经完成；后续 working indicator 断言过时。最小候选修复是删除该断言，不改产品状态逻辑。

## F7：Reasoning E2E 的最终断言没有绑定被点击的 block

- 来源：`appearance-reasoning.spec.ts` 的 `--repeat-each=3` 结果与 repeat 2 Playwright trace 点击前后帧。
- 事实：mock stream 产生 4 个 cycle，每个 cycle 都包含相同 reasoning 文本；点击后 Cycle 1 确实收起，但全局 `getByText(...).first()` 继续匹配 Cycle 2-4，因此仍返回 count 1。
- 结论：组件手动折叠正常，失败来自 locator 范围错误。最小候选修复只把最终“已收起”断言限定到被点击的第一个 Thinking badge。

## F8：Typography E2E 等待持久化，但没有等待主题重绘

- 来源：`appearance-typography.spec.ts:51-62,149-154` 的稳定本地原始复现；`saveAppSettings`、`ProvidersWrapper` 与 `applyAppearance` 调用链。
- 事实：测试 helper 只轮询 localStorage。设置写入先更新 query data，再持久化；React effect 随后调用 `applyAppearance`，Unistyles repaint 仍是异步 UI 状态。原始失败中 localStorage 已为 `uiFontSize=20`，header 当次 computed style 仍为 `16px`。
- 置信度：高概率为测试同步缺口，但尚缺“computed style 最终达到 20px”的直接复证。后续一次原文件重跑被 relay/初始 timeline 超时挡在更早步骤，不能用于确认或反驳该结论。
- 最小验证性修复：在现有 header 断言处用 `expect.poll` 等待 computed font size 为 `20px`；若 polling 仍失败，再升级为产品回归调查，不能预先改产品实现。

## F9：Typography E2E 的初始 assistant locator 也会随 stream 漂移

- 来源：批准 F8 修复后的本地单文件 Playwright trace 与失败截图。
- 事实：测试在第 147 行、进入新增 polling 前超时。目标文本实际存在于 trace，但 `workspaceTypographyLocators()` 先选择动态 `.last()` assistant block；新 cycle 到达后 `.last()` 指向不含目标文本的后续 block。trace 中目标文本存在，且记录了多个 assistant message snapshot。
- 影响：当前本地运行无法到达 F8 的 polling，因此既没有触发用户设定的“polling 失败即暂停”，也不能证明 polling 已通过。重复盲跑只是在赌 stream 时序。
- 最小候选修复：让 assistant 文本和 code locator 在全部 `assistant-message` block 中定位各自目标，不再把两者限定到动态 `.last()` block；仍只改 `appearance-typography.spec.ts`，不改产品实现。

## F10：Typography computed-style polling 直接证实 UI 未应用 interface size

- 来源：F9 locator 修复后的 `appearance-typography.spec.ts` 定向 Playwright 运行。
- 事实：测试已到达第 154 行新增 polling；在 10 秒内 computed `fontSize` 始终为 `16`，预期 `20`。该运行使用隔离 daemon、Metro 和动态端口，teardown 后没有残留 `paseo-release-0014` 测试进程。
- 影响：F8 不再只是测试同步假设，当前证据表明 UI appearance 主题没有应用 interface size。用户明确要求 polling 失败立即暂停；不得继续其余修复验证、commit 或 release，需先获产品回归调查范围授权。

## F11：Unistyles web CSSVars 模式不会重绘数字主题 token

- 来源：`react-native-unistyles@3.2.4` 本地安装源码 `src/web/css/state.ts`、`src/web/shadowRegistry.ts`、`src/web/runtime.ts`，以及可移除的浏览器 computed-style/CSS 探针。
- 事实：CSS theme 生成器只把字符串主题值写成 CSS 变量；数字 `fontSize` token 被直接固化在生成 class 中。启用 `CSSVars` 时，shadow registry 又会过滤 `Theme` 依赖，因此 `UnistylesRuntime.updateTheme()` 虽更新内存主题，已挂载节点的数字字号 class 不会重新生成。探针中 header class 始终保留 `font-size: 16px`。
- 结论：状态保存、React effect 与 `applyAppearance()` 均已执行，失效边界在 Unistyles web 样式更新策略。最小产品修复是在既有全局 `StyleSheet.configure()` 中设置 `CSSVars: false`，让 theme change 重新应用样式；无需新增 remount boundary 或修改 appearance 状态流。
- 验证：删除探针后，`appearance-typography.spec.ts` 单文件通过，运行时 interface header computed size 达到 `20px`，workspace/code 字体独立性与 reload/mobile 断言也全部通过。

## F12：Agent stream 整文件剩余失败停在无关 workspace setup

- 来源：修复后的 `agent-stream-ui.spec.ts` 整文件运行、delayed-history 场景单跑、页面截图和 `workspace-setup.ts:75` 失败位置。
- 事实：整文件 8 个场景中 6 个通过，本次修改的 provider retry 场景通过；剩余 delayed authoritative history 与 first app-created timer 均在进入目标行为前失败，页面显示 `No projects yet`。单跑 delayed-history 仍在同一点失败。
- 差分证据：临时移除 `CSSVars: false` 后同一 setup 失败仍可复现，恢复产品修复后工作树回到获批 5-file diff；因此该失败不是本次 typography 修复引入。
- 影响：本次 diff 的定向行为已有通过证据，但 `agent-stream-ui.spec.ts` 整文件尚未全绿。继续修改共享 `withWorkspace` helper 会扩大临发布修复范围，需单独批准；当前严格门禁下不应据此自动提交或发布。

## F13：Windows 项目名提取只识别正斜杠

- 来源：失败 Playwright trace、`fetch_workspaces_response`、失败截图、fixture cleanup 时序和 `projectNameFromPath()` 直接探针。
- 事实：daemon 已返回正确项目，侧栏也显示末级目录名；共享 helper 只按 `/` 分割，在 Windows 上却把完整 `C:\\Users\\...` 路径交给 locator。失败报告中的 `No projects yet` 出现在超时后的 fixture cleanup 删除项目之后，不是初始失败状态。
- 结论：根因位于 `workspace-setup.ts` 的共享路径 helper。最小修复使用 Node 标准库 `basename(repoPath) || repoPath`，同时覆盖它的 sidebar row、create button 和 hover 三个调用点，不改产品实现。
- 验证：两个原失败场景分别单跑通过，随后 `agent-stream-ui.spec.ts` 整文件 `8 passed`（约 5.2 分钟）；`npm run format`、`npm run lint`（0 warnings / 0 errors）、`npm run typecheck` 和 `git diff --check` 均通过。G7 已解除。
```

### 10.4 原 `progress.md`

```text
# 发布新版本进度

## 2026-07-27 发布准备启动

- 用户要求：推送当前 `main` 并发布一个版本。
- Push：`main` 从 `b64f4f357` 快进到 `7bc87b12c`，已成功推送到正确的 `origin`；未推送 tag。
- 发布路径：stable gate 未解除，当前只能准备 beta；推荐 `0.2.0-beta.6`，等待用户批准。
- 上游：`upstream/main` 已刷新；带 tags 的 fetch 因 `v0.2.0-beta.4` 同名异指向拒绝覆盖，未强制修改任何 tag。
- Preflight：ACP drift check 未通过，确认 `fast-agent 0.9.21 -> 0.9.25`，另有 registry unavailable；format/lint/typecheck 与 clean checkout preparation 尚未开始。
- 工作树：保留所有既有 tracked/untracked WIP，不 stash、不清理、不混入 release。
- 下一步：用户批准 beta/version/changelog 后，在独立 clean checkout 执行 preparation；任何 ACP catalog 修改先返回批准门。

## 2026-07-27 Preparation 获批

- 用户明确批准 beta `0.2.0-beta.6`、`SPEC.md#changelog-草案` 全文和独立 clean-checkout preparation。
- 计划状态记录为 `Plan Approved`；批准不包含 ACP catalog 修改，也不包含创建 tag 或公开 GitHub prerelease。
- 下一步：从 `origin/main@7bc87b12c` 创建独立 clean checkout，执行 format、lint、typecheck、ACP drift 与 main CI 核验。

## 2026-07-27 Clean-checkout preflight

- 隔离路径：`E:\Code\paseo-release-0014`，分支 `main`，`origin` URL 与 `HEAD=7bc87b12c` 均已确认。
- 运行时：仓库要求 Node `22.20.0`；使用仓库外临时 Node 22/npm 11，不修改系统 Node 或仓库 manifest。
- `npm ci`：成功；安装钩子造成的单文件 mixed EOL 已在隔离 clone 恢复为相同 Git blob。
- `npm run format`：成功且无真实 diff；Windows clone 的 CRLF 伪 diff 已证明仅为 EOL 后恢复并重跑。
- `npm run lint`：成功，0 warnings / 0 errors。
- `npm run typecheck`：首次因 clean clone 缺 workspace declarations 失败；按项目规则执行 `npm run build:server` 后重跑成功。
- ACP：Windows 原始脚本因 `spawn npm ENOENT` 误报 13 个 unavailable；仓库外临时 preload 复查确认 8 个真实漂移，详见 `findings.md#f4acp-drift-gate-确认有-8-个真实漂移`。
- checkout 当前未修改 catalog；等待用户决定更新或有意冻结。main CI 尚未触发，避免在候选 commit 未确定时重复运行。

## 2026-07-27 ACP 决策与 CI

- 用户明确决定本次 beta 有意冻结 8 个 ACP pin，不修改 catalog。
- 用户批准继续 CI 与 changelog commit；tag/GitHub prerelease 仍未获最终 go-ahead。
- 已从 `origin/main@7bc87b12c` 触发 CI：`https://github.com/staoran/paseo-reforged/actions/runs/30277605926`。
- 已按获批草案原位更新隔离 checkout 的顶部 changelog 条目为 `0.2.0-beta.6 - 2026-07-27`；待格式验证和独立 commit。
- Changelog 定向格式化、`git diff --check`、lint、typecheck 均通过；pre-commit 又复核 format/typecheck 通过。
- Changelog 已作为唯一文件提交：`51a48ca88 docs: add 0.2.0-beta.6 changelog`；尚未 push，隔离 `main` 比 `origin/main` ahead 1。

## 2026-07-27 CI failure 与临发布修复 checkpoint

- Main CI run `30277605926` 在 `origin/main@7bc87b12c` 完成并失败：server Linux、server Windows、Playwright shard 1/4 未通过；format、lint、typecheck、SDK、CLI、relay、desktop、app unit tests 与其余 Playwright shards 通过。
- 已确认三个测试侧根因：MCP `createManagedAgent()` fixture 漏 `providerRetryMessage`；provider retry 场景在 agent remove 后继续断言已卸载 working indicator；reasoning 最终断言跨 cycle 匹配了另一个同文本 block。证据见 `findings.md#f5server-ci-失败来自-mcp-测试-fixture-漏字段` 至 `F7`。
- Typography 原始失败高度符合 effect/repaint 等待缺失；一次复跑被 relay/初始 timeline 超时挡在更早步骤，未产生反证。修复时须先用 computed-style polling 直接复证，失败则暂停并按产品回归处理。
- 复跑使用动态端口，未触碰 `6767`；teardown 后已核对没有残留 `paseo-release-0014` 测试进程。
- Changelog commit `51a48ca88` 保持为 release checkout 唯一 ahead 提交；没有 push、没有运行 `release:beta:next`、没有创建 tag 或 GitHub prerelease。
- 下一步：等待用户批准 4 个最小临发布测试修复；修复必须使用独立 commit，并运行对应 MCP 单测、三个定向 Playwright 文件、format、lint、typecheck，再等待新 main CI 全绿。

## 2026-07-28 最小修复执行与 typography 前置阻塞

- 用户批准 findings F5-F8 的 4 项最小测试修复并要求独立提交；明确要求 typography polling 失败时立即暂停。
- release checkout 已只修改 4 个测试文件，定向 `format:files` 与 `git diff --check` 通过；Changelog commit `51a48ca88` 未改写。
- MCP 定向单测通过：`mcp-server.test.ts` 1 file、113 tests 全绿。
- Typography 单文件 Playwright 在第 147 行等待初始 assistant 文本时超时，尚未执行新增 polling；trace 证明目标文本存在，但动态 `.last()` 已指向另一个 stream block，详见 findings F9。
- teardown 后没有残留 `paseo-release-0014` 测试进程。按 last-minute change 规则，未继续改 locator、未运行剩余 E2E/lint/typecheck、未 stage、未 commit、未 push。
- 下一步：等待用户批准 F9 的同文件最小 locator 修复；获批后先重跑 typography，polling 失败则立即暂停，成功后再完成其余验证与独立 commit。

## 2026-07-28 F9 locator 修复获批

- 用户批准 findings F9 的同文件最小 locator 修复，并要求继续原门禁验证后独立提交。
- 执行边界保持不变：不改产品实现；先单跑 typography，computed-style polling 失败时立即暂停；通过后才运行其余定向验证、lint、typecheck、format 与最终 diff 检查。

## 2026-07-28 Typography polling 失败并暂停

- F9 修复后，`packages/app/e2e/appearance-typography.spec.ts` 已在 `packages/app` workspace 单独运行，成功到达新增 computed-style polling。
- 失败证据：第 154 行 polling 在 10 秒后仍收到 `fontSize=16`，期望 `20`。这不是 locator 或测试命令失败。
- 按用户门禁立即停止：未运行 reasoning/agent-stream E2E、lint、typecheck、全量 format，也未 stage、commit、push 或创建 release。
- teardown 已确认没有包含 `paseo-release-0014` 的 node/Electron/Chrome/Wrangler 残留进程；release checkout 仍只修改 4 个测试文件，Changelog commit `51a48ca88` 保持独立且未 push。
- 下一步：等待用户批准 interface typography 产品回归调查与修复范围。

## 2026-07-28 Typography 产品修复获批

- 用户批准最小产品修复，并要求修复后第一项只重跑 `appearance-typography.spec.ts`。
- 当前范围限于已验证的 interface typography web 应用链路；先用现有红色 E2E 和单个可移除 DOM/CSS 探针区分状态、effect 与样式更新边界，再修复被证实的最小位置。

## 2026-07-28 Typography 产品修复与门禁复核

- 本地 Unistyles 源码与浏览器探针确认根因见 findings F11；最小产品修复只在 `packages/app/src/styles/unistyles.ts` 设置 `CSSVars: false`，所有临时 debug 探针均已移除。
- 按用户要求，修复后的第一项验证为 `appearance-typography.spec.ts` 单文件，结果 `1 passed`（约 2.0 分钟）。随后 `appearance-reasoning.spec.ts` 单文件 `1 passed`（约 2.1 分钟）。
- MCP 定向单测保持通过：`mcp-server.test.ts` 1 file、113 tests；`npm run format`、`npm run lint`（0 warnings / 0 errors）、`npm run typecheck` 与 `git diff --check` 均通过。
- `agent-stream-ui.spec.ts` 整文件结果为 6 passed / 2 failed；本次修改的 provider retry 场景通过。两个失败均停在 `workspace-setup.ts:75`，页面显示 `No projects yet`，未进入各自目标行为；单跑与移除产品修复的差分复核仍在同一点失败，详见 findings F12。
- 当前 release checkout 仍只有 5 个获批文件未提交修改，Changelog commit `51a48ca88` 保持独立；没有 staged 内容，没有 push、tag 或 GitHub prerelease。
- 下一步：先完成最终 5-file diff 审查。严格原门禁仍被 agent-stream 的无关 setup 失败挡住；未经用户接受该残余 blocker，不 stage 或提交，也不扩大到 `withWorkspace` helper。

## 2026-07-28 最终修复 diff 审查

- 以 release checkout `HEAD=51a48ca88` 的未提交 5-file diff 为固定点，按仓库 coding/testing/Unistyles 规则与 0014 Spec/F5-F11 两轴审查；未发现新的标准或规格问题。
- 变更保持 `14 insertions / 12 deletions`：一个全局 Unistyles 设置修复、三个 E2E locator/断言同步修复、一个 MCP fixture 字段补齐；没有 debug 代码、无关重构、依赖或协议变更。
- 残余风险仅为 findings F12：`agent-stream-ui.spec.ts` 整文件的两个无关 workspace setup 失败。当前保持未 stage、未 commit、未 push；等待用户决定接受差分证据继续独立提交，或另行调查共享 setup helper。

## 2026-07-28 Windows workspace name helper 修复与验证

- 用户先要求暂停提交并只读调查 `withWorkspace` 的 `No projects yet`，随后批准 `node:path.basename` 最小修复，并要求先分别单跑两个失败场景。
- 调查确认 daemon 与侧栏均已有正确项目；`projectNameFromPath()` 只按 `/` 分割，导致 Windows 完整路径无法匹配侧栏末级目录名。`No projects yet` 是超时后 fixture cleanup 删除项目的截图状态，详见 findings F13。
- 最小修复只改 `packages/app/e2e/helpers/workspace-setup.ts`：导入 `node:path.basename` 并替换手写路径分割，不新增抽象或测试文件。
- `keeps the viewport fixed when delayed authoritative history arrives after scroll-away` 单场景通过：`1 passed`（约 1.8 分钟）。
- `shows elapsed timer on first app-created running turn` 单场景通过：`1 passed`（约 1.3 分钟）。两项都使用隔离 daemon、Metro、relay 和动态端口，未触碰主 daemon `6767`。
- 按后续确认重跑 `agent-stream-ui.spec.ts` 整文件，结果 `8 passed`（约 5.2 分钟）；此前 6/2 的验证阻塞已完整解除。
- 后续门禁全部通过：`npm run format`；`npm run lint` 为 0 warnings / 0 errors；全仓 `npm run typecheck`；`git diff --check`。
- release checkout 的 6 个 tracked 文件已按显式路径全部暂存，cached diff 为 `16 insertions / 13 deletions`，未暂存区为空且 `git diff --cached --check` 通过；`main` 仍只 ahead `origin/main` 一个 Changelog commit `51a48ca88`。修复 commit message 已生成但尚未提交，没有 push、tag 或 GitHub prerelease。

## 2026-07-28 独立修复提交

- 用户批准使用已生成的 commit message 提交当前 6-file staged 内容。
- 独立修复 commit 已创建：`27d90d843 fix: apply live typography updates and stabilize release tests`，内容为 6 files、16 insertions / 13 deletions。
- pre-commit hook 全部通过：6-file format check、lint（0 warnings / 0 errors）和全仓 typecheck。
- release checkout 工作树 clean；`main` 比 `origin/main` ahead 2，顺序为 Changelog commit `51a48ca88`、修复 commit `27d90d843`。尚未 push、tag 或创建 GitHub prerelease。

## 2026-07-28 主线更新后的发布合同恢复

- 上游同步与后续 P3 修正已使本地主线前进到 `main@28046d7f`；远端只读复核确认 `origin/main` 仍为 `7bc87b12`，本地 ahead 90。
- 当前统一 manifests 为 `0.2.3`，远端 Reforged tags 只有 `v0.2.0-beta.4` 与 `v0.2.0-beta.5`；按当前 fork 政策，候选调整为 `0.2.4-beta.1`。
- `SPEC.md` 顶部已建立覆盖旧计划的新合同：重放 6-file 修复、合并当前 beta Changelog、重新执行全部 preparation、push `main`、等待新 main CI，并在 tag 前再次确认。
- Changelog 完整性复核发现原草案会在合并旧 beta 条目时丢失多组已公开的用户可见变化；已用既有 Reforged beta 条目和 upstream `0.2.0..0.2.3` 稳定版说明补齐同一草案，并修正 `#2345`、`#2303` 的共同作者归属。
- 本次恢复只同步 `task_plan.md`、`findings.md` 与 `progress.md`；没有修改代码或 Changelog，没有移动分支、提交、push、tag 或发布。
- 当前阻塞：新的 Plan 尚未获批。下一步等待用户精确回复 `Plan Approved：按 0.2.4-beta.1 执行准备与 push；tag 前再次确认`。

## 2026-07-28 当前计划与发布合并授权

- 用户回复：`Plan Approved：按 0.2.4-beta.1 执行准备与 push，直接打tag发版本`。
- 授权解释：允许立即执行 clean-isolation preparation 和 `main` push；既定测试、ACP、release preparation、Git 与新 main CI 全绿后，允许不再二次询问，直接运行 `release:beta:patch` 并发布 `v0.2.4-beta.1`。
- 边界保持：不得跳过门禁，不发布 stable/npm/Docker/商店/Cloudflare/relay；新 drift、lockfile 真实变化、失败、远端前进或 release diff 超出合同仍需暂停裁决。
- 下一步：在 `codex/sync-upstream-2026-07-27@28046d7f` clean worktree 重放并审查 6-file 修复。

## 2026-07-29 中断恢复

- 中断前只完成批准状态回写，尚未修改隔离分支代码、Changelog、Git refs 或远端。
- `codex/sync-upstream-2026-07-27@28046d7f` 与旧 `E:\Code\paseo-release-0014@27d90d843` 均保持 clean。
- 实际发布日已变为 `2026-07-29`；同一获批版本的 Changelog heading 已机械更新为 `0.2.4-beta.1 - 2026-07-29`，内容与范围不变。
- 下一步保持不变：重放 6-file 修复并完成全部 preparation 门禁。

## 2026-07-29 6-file 重放与 MCP 门禁暂停

- 6-file patch 已在 clean 隔离分支按当前 tree 重放，diff 与旧候选精确一致：6 files、16 insertions / 13 deletions；定向 formatter 与 `git diff --check` 通过。
- MCP 整文件首跑在无关的真实 Git/worktree 场景 5 秒超时；该场景单跑和未修改基线均同样失败，诊断性 20 秒上限下约 8.35 秒通过，详见 findings F15。
- 本次 fixture 修复直接覆盖的 `returns full-detail snapshots from get_agent_status` 单跑通过（1 passed，约 69ms）。
- 按当前暂停门，尚未运行三个 App E2E、Changelog 写入、lint、typecheck、ACP、release preparation、commit 或 push。
- 下一步：等待用户接受差分证据继续，或批准在同一测试文件为该真实 Git 场景设置 15 秒单测超时。

## 2026-07-29 F15 差分证据获准继续

- 用户接受 F15 差分证据，明确要求保持原 6-file diff、不修改测试超时，并继续全部发布门禁后直接发布。
- 本地 MCP 证据归档为：直接覆盖测试通过；整文件仅有未修改基线也存在的 Windows 默认 5 秒超时；新 main CI 保留最终裁决权。
- 下一步：串行运行 Typography、Reasoning 与 Agent Stream 三个 App E2E。

## 2026-07-29 App E2E 门禁通过

- `appearance-typography.spec.ts`：`1 passed`（约 2 分钟），隔离 daemon 正常停止。
- `appearance-reasoning.spec.ts`：`1 passed (1.2m)`；目标 reasoning 自动展开流程通过，隔离 daemon 正常停止。
- `agent-stream-ui.spec.ts`：退出码 `0`（约 4 分 42 秒）；整文件通过，隔离服务完成清理。
- 三次运行均使用动态端口，未触碰主 daemon `6767`；保持原 6-file diff，未修改测试超时。
- 下一步：写入已批准的唯一 `0.2.4-beta.1` Changelog，运行 format、lint、typecheck、ACP drift、release preparation 与 Git 完整性门。

## 2026-07-29 Changelog 与静态门禁完成，ACP 暂停

- `CHANGELOG.md` 已用 Spec 中获批的 `0.2.4-beta.1 - 2026-07-29` 唯一条目替换三个旧 beta 条目；逐行比较为 `MATCH`，`0.1.110` 及更早历史保持不变。
- `npm run format` 未扩散到合同外文件；`npm run lint` 为 0 warnings / 0 errors；`npm run typecheck` 与 `git diff --check` 通过。
- `npm run acp:version-drift:check` 失败。排除 13 个 Windows `unavailable` 误报后，确认 F16 的 6 个真实 stale pin，已超出 F4 对旧 8 项的冻结事实。
- 当前隔离分支仍只有 6-file 修复与 `CHANGELOG.md` 共 7 个修改文件；未运行 `release:prepare`，未 stage、commit、push 或创建 tag。
- 下一步：等待用户决定冻结当前 6 项继续，或批准更新 ACP catalog；前者不改变当前 release diff，后者需要新增 diff 与验证。

## 2026-07-29 F16 同源冻结获批

- 用户批准按 `cbbf6c168` 上游同源基线冻结 F16 的 6 项 ACP drift，并继续直接发布 `0.2.4-beta.1`。
- 调查确认冻结点、当前下游与当前上游 tip 的 catalog/checker blob 分别完全相同；本次冻结保持上游快照，不制造下游 catalog 差异。
- G9 已解除；下一步运行 `release:prepare`，核对 lockfile 与文件范围后分离提交 6-file 修复和 Changelog。

## 2026-07-29 F17 lockfile 规范化暂停

- 用户批准 F16 后继续运行 `release:prepare`；首次误用系统 Node 26/npm 12 产生过大 churn，未提交。
- 使用 clean `28046d7f` 与既有 release runtime Node `22.20.0` / npm `11.17.0` 重新验证，真实最小变化为 `package-lock.json` 的 `25 insertions / 419 deletions`，且连续第二次运行 hash 不变。
- CLI launcher 的工作树标记为 stat 假阳性，blob 与 HEAD 相同；没有第二个真实文件，也没有依赖版本升级。
- 当前候选分支尚未吸收正确的 clean lockfile，仍未 stage、commit、push 或创建 tag；clean 诊断 worktree 保留规范化结果作为证据。
- 下一步：等待用户批准独立 lockfile preparation commit；获批后把 clean 结果精确应用到候选，重新运行 format/lint/typecheck 与 `release:prepare` 幂等检查，再分离提交并继续发布。

## 2026-07-29 F17 lockfile 规范化获批

- 用户批准采用 clean Node `22.20.0` / npm `11.17.0` 的 lockfile 规范化结果并建立独立提交，随后继续直接发布 `0.2.4-beta.1`。
- G12 已解除；候选分支当前 lockfile hash 仍为污染版本 `e9b2608d3b873a153f1a877fe39569188f056143`，clean 诊断结果为 `50bb9f05c6faf42261b671e3105ed1d625beb8a6`，尚未同步。
- 下一步：精确同步 clean `package-lock.json`，用同一 Node/npm 运行 `release:prepare` 验证 hash 不变，再完成静态/Git 门禁与三个独立提交。

## 2026-07-29 F17 应用与候选提交完成

- clean lockfile 已精确同步到候选；hash 为 `50bb9f05c6faf42261b671e3105ed1d625beb8a6`，差分为 `25 insertions / 419 deletions`。固定 Node `22.20.0` / npm `11.17.0` 重跑 `release:prepare` 后 hash 不变且没有额外 tracked 文件。
- `npm run format`、lint（0 warnings / 0 errors）、全仓 typecheck 与 `git diff --check` 通过；此前获批的 MCP/App 定向测试证据保持有效。
- 三个独立提交已创建：`a3dbaa27a`（6-file 修复）、`c6826cc30`（lockfile 规范化）、`21b579f16`（`0.2.4-beta.1` Changelog）。候选 worktree clean。
- 远端复核：origin URL 精确为 Reforged 仓库，`origin/main` 仍为 `7bc87b12`，`v0.2.4-beta.1` 不存在；候选严格从 `main@28046d7f` 快进。
- 下一步：在保留主工作树 `mydocs/**` WIP 的前提下将 `main` fast-forward 到 `21b579f16`，push 后等待新 main CI 全绿。

## 2026-07-29 main push 与 F18 CI 阻断

- 主工作树在保留全部既有 `mydocs/**` WIP 的情况下 fast-forward 到 `21b579f16`，并成功 push `origin/main`；三个准备提交顺序与候选一致，目标 tag 仍不存在。
- Reforged push 未自动生成 run，按仓库既有方式手动 dispatch CI `30423908337` 到 `main@21b579f16`。最终 17/18 jobs 成功，唯一失败为 Playwright shard 3/4 的 provider settings overlay 流程。
- CI 与本地目标 Playwright 均在 model selector 内按 `Shift+?` 后无法打开 keyboard shortcuts dialog；本地冷启动曾先在 composer/page load 超时，不计作目标反证，热缓存运行已精确复现同一行。
- 临时 `[DEBUG-f18-focus]` 探针已删除，候选 worktree 恢复 clean；没有新 commit、push、tag 或 release。
- 根因与最小方案见 findings F18。下一步等待用户批准 2-file 修复；获批后在隔离分支按现有 unit/E2E seam 验证、独立提交、fast-forward/push main，并重新要求整条 main CI 全绿后继续既有直接发布授权。

## 2026-07-29 F18 2-file 修复获批

- 用户批准 F18 的 2-file 修复，并要求验证通过后继续直接发布 `0.2.4-beta.1`。
- 执行范围限于 keyboard shortcuts dialog 的 modal focus scope 与对应纯逻辑回归用例；复用既有 provider settings E2E，不修改 modal 判定、project picker 或其他快捷键。
- 下一步：在隔离分支完成单个 red/green slice，运行定向 unit/E2E、format、lint、typecheck 与 Git 检查，独立提交并 push 新 main CI。

## 2026-07-29 F18 修复验证与独立提交

- 在 `codex/release-f18-0.2.4-beta.1` 上先增加 modal scope unit case，现有实现按预期红色失败；随后仅把 `shortcuts-dialog-toggle-question-mark` 的 focus scope 扩为 `other`/`modal`，unit 转绿为 82/82。
- 目标 `provider-settings-refresh.spec.ts` 首次因 clean worktree 缺跨包声明失败，按项目规则完成 `build:server`；后续两次冷缓存运行分别被 Metro OOM 与首包 60 秒前置超时阻断，未进入目标断言。Metro 缓存完成后的原命令通过：1 passed（约 1.4 分钟），未修改测试超时。
- `npm run format`、lint（0 warnings / 0 errors）、全仓 typecheck、`git diff --check` 与提交前 hook 全部通过；固定 Node `22.20.0` / npm `11.17.0` 的 `release:prepare` 幂等，lockfile blob 保持 `50bb9f05c6faf42261b671e3105ed1d625beb8a6`。
- 独立提交：`2c7651f21 fix(app): keep shortcut help available in modals`，仅 2 files、7 insertions / 1 deletion；父提交精确为 `21b579f16`。
- 下一步：确认远端 main 未前进且目标 tag 不存在，将主工作树 fast-forward/push 到 `2c7651f21`，手动 dispatch 并等待新 main CI 全绿。

## 2026-07-29 F18 main push 与新 CI

- 远端复核确认 `origin/main` 仍为父提交 `21b579f16`、origin URL 精确为 Reforged 仓库且目标 tag 不存在；主工作树在保留全部既有 `mydocs/**` WIP 的前提下 fast-forward 到 `2c7651f21`。
- 首次 push 遇到 transient Schannel `missing close_notify`，只读确认远端未更新后重试同一非强制 push 成功；`origin/main` 现为 `2c7651f21`。
- Push 未自动生成 CI；已手动 dispatch Reforged `CI` run `30439455336` 到 `main@2c7651f21`。
- 下一步：等待该 run 全部 jobs 成功；全绿后再次核对 main/tag，再执行已授权的 `npm run release:beta:patch`。

## 2026-07-29 F18 新 main CI 全绿

- Reforged CI run `30439455336` 在 `main@2c7651f21` 完成并成功；18/18 jobs 全绿，包括原 F18 所在 Playwright shard 3/4、其余 Playwright shards、Windows/Linux server、CLI、desktop、app、format、lint、typecheck、SDK 与 relay。
- CI 仅报告 GitHub Actions Node 20 deprecation annotations，不影响 job 结论，也不构成本次代码或发布 blocker。
- G11 已解除；下一步从新的干净 `main` clone 再次复核 origin/main 与目标 tag，使用固定 Node `22.20.0` / npm `11.17.0` 执行已授权的 `npm run release:beta:patch`。
```

### 10.5 原 `task_plan.md`

```text
# 发布新版本任务计划

## 任务绑定

| 字段     | 值            |
| -------- | ------------- |
| task_id  | `0014`        |
| Spec     | `SPEC.md`     |
| Findings | `findings.md` |
| Progress | `progress.md` |

## 当前操作状态

- 当前阶段：`Execute / Preparation`
- 批准状态：`PLAN APPROVED + RELEASE GO-AHEAD`；全部既定门禁通过后直接发布，不再请求第二次确认
- 当前责任：从干净 main checkout 执行已授权的 `release:beta:patch` 并验证发布工作流
- 最近进度：`progress.md#2026-07-29-f18-新-main-ci-全绿`

## 阶段索引

| 阶段     | 状态        | 依赖                                                                                         |
| -------- | ----------- | -------------------------------------------------------------------------------------------- |
| Research | completed   | `main@28046d7f`、`origin/main@7bc87b12`、manifests `0.2.3`、远端 tags 与 stable gates 已复核 |
| Plan     | completed   | `0.2.4-beta.1`、当前 Changelog、6-file 修复重放、preparation/push 与条件式直接发布均已批准   |
| Execute  | in progress | F18 与新 main CI 已完成；从干净 main checkout 执行已授权的 beta release                       |
| Review   | pending     | 新 main CI、release tip/tag 与三项 GitHub release workflow                                   |

## 阻塞

当前无阻塞；release 命令仍须在 tag 前复核远端 main、目标 tag 与干净工作树。
```
