# 上游 v0.2.5 差异与冲突复检 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                    |
| ------------------ | ----------------------------------------------------- |
| task_id            | `0031`                                                |
| spec layer         | `Feature Spec`                                        |
| task status        | `已收口`                                              |
| document status    | `Completed`                                           |
| depth              | `deep`                                                |
| phase              | `Review`                                              |
| Execution Approval | `Approved`                                            |
| Approval Source    | `User`                                                |
| file path          | `mydocs/micro_specs/0031_上游v0.2.5差异与冲突复检.md` |
| parent spec        | `N/A`                                                 |
| superseded by      | `N/A`                                                 |
| created / updated  | `2026-08-01 22:28`                                    |

## 1. 目标与完成契约

- 当前理解：固定比较上游正式 tag `v0.2.5` 与本地 `main`，先保存已完成的初步分析；等待 Codex 任务 `019fb91b-4888-7850-9171-bff394c3648d` 完成后，以新的提交和工作区状态再分析一轮，重点识别新增、消失或发生变化的冲突点。
- 核心目标：给出上游 `v0.2.5` 相对复检时本地主分支的变更记录、Git 内容冲突、自动合并但存在语义重叠的路径，以及每个冲突的双方意图和建议方案。
- Done Contract：目标任务已完成；已重新采集 HEAD、merge base、分叉计数、路径差异、只读 `merge-tree` 结果和工作区重叠；已将前后冲突增量、双方对比、解决方案和验证证据回写本文档。未执行真实 merge、rebase、stash、reset、clean、分支、commit、push 或发布。

## 2. 范围与事实

- 范围内：`upstream` 正式 tag `v0.2.5` 与本地 `main` 的提交级比较；当前工作区改动与上游变更路径的重叠；冲突意图分析与解决建议；等待任务完成后的增量复检。
- 范围外：执行真实合并或解决 index 冲突；修改产品源码；验证 `upstream/main` 在 tag 之后的 19 个提交；发布与提交。
- 当前任务单元：初步结论持久化 -> 等待指定任务 -> 二次采集 -> 前后对比 -> 回写与交付。
- 轻量评估：`升级 deep`。原因是本地与上游长期分叉、共改路径多、存在自动合并后的语义风险，并且分析基线会被并发任务改变。
- `grilling` 结论（如使用）：未使用；tag、目标任务和操作边界均已明确。
- TDD 判定：`N/A`；本任务只做 Git 与源码只读分析，不改变可观察行为。

### 初步分析基线（任务完成前）

| 项目                      | 已确认值                                                              |
| ------------------------- | --------------------------------------------------------------------- |
| 上游 annotated tag object | `8d143abc43e2b257c4fc2f77606bb11734ba3da4`                            |
| 上游 tag peeled commit    | `6fc491e6220fba6543bbbe4bf1b1f58cfe59228b`                            |
| 上游发布时间              | `2026-07-30`                                                          |
| 本地 `main`               | `94cb6b816de8c239a5732bd6b7189b551cdbf2d2`                            |
| merge base                | `cbbf6c1684fb0415b7949e684d152f5f7453e769`                            |
| 分叉计数                  | 本地独有 44 commits；上游独有 27 commits                              |
| 上游自 merge base 净变化  | 248 files，`+6902/-9135`                                              |
| 本地自 merge base 净变化  | 438 files，`+25339/-3304`                                             |
| 双方共同修改              | 63 paths                                                              |
| 只读合并预演              | 18 个 Git 内容冲突；45 个共同修改路径可文本自动合并但仍需语义审计     |
| 结构性冲突                | 未发现 rename/delete、目录/文件或子模块冲突                           |
| 仓库操作状态              | 无进行中的 merge/rebase/cherry-pick/revert；index 无 unmerged entries |

初步分析期间，tracked WIP 与上游 248 个变更路径有 14 个重叠：`package-lock.json`、`packages/app/package.json`、`packages/app/src/agent-stream/view.tsx`、`packages/app/src/git/diff-pane.tsx`、8 个 locale 文件、`packages/app/src/panels/agent-panel.tsx`、`packages/app/src/panels/diff-panel.tsx`。未跟踪文件当时与上游变更路径无重叠。工作区持续变化，因此该清单只作为复检前快照，不能作为直接 merge 的安全依据。

### 初步变更记录

- 上游发布基线统一提升到 `0.2.5`，并更新根目录及各 workspace manifest 和 lockfile。
- 上游发布文档加入 npm `latest` / `beta` dist-tag 流程；本地 Reforged 分支保留禁用 npm/GHCR 发布和 stable gate 的独立策略。
- 上游引入跨 host project grouping 与相应 workspace 导航、local project path、host-local `projectId` 路由调整。
- 上游 `#2596` 回退 `#2490` 与 `#2484` 的时间线实现，已知原因包括首条消息跳动、长时间线高度塌缩和消息左对齐。
- 上游 tag 删除 `.worktreeinclude` 恢复能力；但 `upstream/reland-worktree-includes-2419` 的 `668204cd6` 与原实现 patch-id 同为 `eaa68558...`，存在上游重新落地的直接证据。
- Linux `#2439` 与 `#2654` 精确互相抵消，相对本次 merge base 没有 Linux sandbox 净代码变化。
- `upstream/main=de44c8cd6` 比 `v0.2.5` 多 19 commits；本任务固定 tag，不把这些提交混入结论。

### 初步 18 个 Git 内容冲突及建议

| 冲突组                  | 文件                                                                                                                                             | 本地意图                                                                                                   | 上游意图                                                                                                          | 初步解决方案                                                                                                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 版本与依赖（12）        | `package-lock.json`；`package.json`；`packages/{app,cli,client,desktop,expo-two-way-audio,highlight,protocol,relay,server,website}/package.json` | 当前版本为 `0.2.4-beta.1`，并包含 Reforged 品牌、发布脚本、Mermaid 与 `@panzoom/panzoom@^4.6.2` 等本地扩展 | 发布 `0.2.5` 并同步 workspace 版本与依赖图                                                                        | 以 `0.2.5` 为源码基线，逐项保留仍有效的 Reforged 品牌、发布脚本、Mermaid 和 Panzoom 依赖；manifest 全部定稿后用项目既有 npm 流程重建 lockfile，不手工拼 lockfile 冲突 |
| 发布策略（1）           | `docs/release.md`                                                                                                                                | 禁用 npm/GHCR 发布并要求 Reforged stable gate                                                              | 增加 npm `latest` / `beta` dist-tag 发布流程                                                                      | 保留本地发布策略；移除上游全部 24 行 npm 相关内容，包括虽无 marker 但自动合入的片段，避免形成自相矛盾的发布文档                                                       |
| 变更日志（1）           | `CHANGELOG.md`                                                                                                                                   | 维护 Reforged 自己的产品发布历史                                                                           | 插入上游 `0.2.4` / `0.2.5` 发布块                                                                                 | 保留 Reforged changelog；本次不直接插入上游发布块，下一次 Reforged release 再按实际合入净变化整理                                                                     |
| Workspace 导航（1）     | `packages/app/src/components/sidebar-workspace-list.tsx`                                                                                         | 项目与 Workspace 右键菜单                                                                                  | 跨 host project grouping、`settingsTarget`、local project path、host-local `projectId` 路由和 accessibility group | 保留本地 context menu，同时采用上游 host/project 数据与路由语义，并补两套行为的组合测试                                                                               |
| Agent 时间线（1）       | `packages/app/src/types/stream.ts`                                                                                                               | `AgentMessagePhase` 与 activity folds                                                                      | 因明确 UI 回归而回退新时间线实现                                                                                  | 接受上游回退后的 reducer 基线，再重新移植 phase；重点覆盖 canonical replace、分页拼接、Markdown block promotion 和 final/activity fold                                |
| Workspace recovery（2） | `packages/server/src/server/session/workspace-recovery/workspace-recovery-service.ts` 及其 `.test.ts`                                            | 已发布 `.worktreeinclude` 能力，并有 `28046d7f4` logger seam                                               | tag 内删除该能力                                                                                                  | 保留 Reforged 能力：在 tag 合并结果上重放与 re-land 分支 patch-id 一致的补丁，再保留本地 logger seam；回归验证 `.worktreeinclude` 涉及的 13 个路径                    |

### 自动合并语义风险

- 其余 45 个共同修改路径没有 Git marker，不代表没有行为冲突。复检时必须单独审计 host/runtime、workspace 打开与恢复、agent stream、diff UI、locale、协议消息和发布链路等路径。
- 当前 WIP 已与其中多条路径重叠；直接在现工作区 merge 会把未提交编辑与提交级冲突混在一起。后续如获实际同步授权，应使用隔离 worktree/branch，且不得自行 stash、reset、clean 或覆盖 WIP。

### 上游独有 27 个提交的变更记录

| 分类                       | 提交                                                                                                   | tag 最终净意图                                                                                                                                              |
| -------------------------- | ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Agent / provider / session | `f91a98434`、`f0d7eeb98`、`963d4f924`、`bbf3d0f9c`、`fab975a05`、`ab2407007`、`b897c59e9`、`891dd5a81` | Claude 1M 模型展示、Grok quota、CLI thinking、最新 plan approval、后台 agent 保活、Pi interrupt 降噪、usage 缺失时保留 context window、向 provider 暴露 cwd |
| App UX 与稳定性            | `e241e02af`、`504b687f8`、`acb8506d1`、`68a215d40`、`9e5accee3`、`20e46e3e3`                           | 快速上滑收键盘、恢复文件树防启动崩溃、拒绝超大 checkout diff、跨 host 项目分组；`#2596` 最终回退 `#2490/#2484` 的 timeline optimistic/pagination 方案       |
| CI 与兼容                  | `76e336a1b`、`fdee3236f`、`ee3a9b013`、`16bbead5a`                                                     | PR 只跑相关 CI、加速 server CI、兼容旧 GitHub CLI 搜索、修复 Windows stderr timeout flaky                                                                   |
| Worktree recovery          | `c6df5ffd4`                                                                                            | tag 内回退 `#2419`，删除 `.worktreeinclude` 携带逻辑；上游另有 patch-id 相同的 re-land 分支，但不属于 tag                                                   |
| Linux desktop              | `fd7061a8b`、`b345e53a9`                                                                               | 两个提交精确互相抵消，相对 merge base 无 Linux sandbox 净代码变化                                                                                           |
| Release                    | `8f8a373ee`、`2f913736f`、`d1ce2b77f`、`4d7d6a7d1`、`95257a635`、`6fc491e62`                           | 0.2.4/0.2.5 changelog、版本切换、lock/Nix 签名与最终 `v0.2.5` 发布                                                                                          |

### 0030 完成后的二次复检

目标 Codex 任务 `019fb91b-4888-7850-9171-bff394c3648d` 于本轮等待后进入 `completed`；0030 本身按其验收合同保持 `待手工验收 / Review`，但实现修改已停止并完成 Reverse Sync。随后冻结的 Git 快照如下：

| 检查项                  | 初步快照                                      | 二次快照                                                                  | 增量结论                           |
| ----------------------- | --------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------- |
| 最新远端正式 tag        | `v0.2.5`                                      | `git ls-remote --refs --sort=-version:refname upstream` 首项仍为 `v0.2.5` | 无变化                             |
| `HEAD/main`             | `94cb6b816de8c239a5732bd6b7189b551cdbf2d2`    | 同一提交                                                                  | 0030 未提交，因此提交级基线未变    |
| merge base / 分叉       | `cbbf6c1...`；44/27                           | 相同                                                                      | 无变化                             |
| 双方净变化              | 上游 248 files；本地 438 files；共同 63 paths | 相同                                                                      | 无变化                             |
| `merge-tree`            | 18 个 content conflicts；无结构性冲突         | 同 18 个文件；0 个非 content conflict                                     | 新增 0、消失 0、变化 0             |
| 当前 WIP 与上游路径重叠 | 14 paths                                      | 同一组 14 paths                                                           | 新增 0、移除 0                     |
| 当前工作区规模          | 持续变化，未作为稳定计数                      | 37 个 tracked WIP、69 个 untracked paths                                  | 仅用于说明工作区仍不适合直接 merge |

0030 实际涉及 23 个产品/测试路径，其中 10 个路径也被上游 tag 修改：

| 0030 与上游重叠组                                    | 0030 一侧                                                                     | 上游 `v0.2.5` 一侧                                           | 冲突判断与方案                                                                                                   |
| ---------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| `packages/app/package.json`、`package-lock.json`     | 新增 `@panzoom/panzoom@^4.6.2`；lock 增加 `4.6.2` 包记录与 app workspace 依赖 | 将 app 及所有 workspace 从 `0.2.3` 提升到 `0.2.5`            | 两者已经属于原 18 个内容冲突，不新增冲突文件；解析 manifest 时同时保留 Panzoom 和 `0.2.5`，最后统一重建 lockfile |
| 8 个 `packages/app/src/i18n/resources/*.ts`          | 在 `message.actions` 增加 6 个 Mermaid fit/pan/reset/select/zoom 标签         | 在 `navigation.diff` 增加 `previewTooLargeTitle/Description` | 键名与对象位置分离，可同时保留；属于自动合并后的验证项，不构成新语义冲突                                         |
| 其余 13 个 Mermaid/controller/bridge/asset/stub 路径 | 完成 Web/Native Panzoom controller、bridge、测试和生成 asset                  | tag 未修改这些路径                                           | 无路径冲突；后续移植 0030 时整体保留，并在依赖/locale 合并后重建 asset                                           |

最终增量结论：0030 完成后没有新增 Git 内容冲突、结构性冲突、路径级 WIP 重叠或不可兼容语义。唯一变化是原 manifest/lockfile 解决方案必须显式保留 Panzoom，8 个 locale 自动合并后必须同时检查上游 diff-too-large 标签和本地 Mermaid 标签。

### 当前 main 以 v0.2.5 为快照基点的复检（2026-08-01 20:53）

本轮将上游 `v0.2.5` 固定为快照比较基点；由于该 tag 尚未成为本地 `main` 的祖先，冲突判断仍必须使用双方真实 merge base `cbbf6c1684fb0415b7949e684d152f5f7453e769` 做三方分析，不能把两端快照 diff 误当作 merge 结果。

| 检查项                    | 上一轮                    | 当前结果                                                      | 变化                                          |
| ------------------------- | ------------------------- | ------------------------------------------------------------- | --------------------------------------------- |
| 远端 tag                  | `v0.2.5 -> 6fc491e62...`  | 远端 object/peeled commit 仍为 `8d143abc...` / `6fc491e62...` | 无                                            |
| 本地 `main`               | `94cb6b816...`            | `0999088499b97877bde0ea65be4a5da5b5657ca0`                    | 新增 `451b35a52` 与 `099908849` 两个提交      |
| 分叉                      | 本地 44 / 上游 27         | 本地 46 / 上游 27                                             | 本地 +2                                       |
| 本地自 merge base 净变化  | 438 files，`+25339/-3304` | 447 files，`+27242/-3326`                                     | +9 paths，净增约 1881 行                      |
| `v0.2.5 -> main` 快照差异 | N/A                       | 631 files，`+36326/-10177`                                    | 仅表示最终快照差异，不用于冲突计数            |
| 双方共同修改              | 63 paths                  | 64 paths                                                      | 新增 `packages/app/src/panels/diff-panel.tsx` |
| 三方预演                  | 18 个 content conflicts   | 同一 18 个 content conflicts；0 个结构性冲突                  | 新增 0、消失 0、变化 0                        |

两个新提交对合并的影响：

| 本地提交                                                 | 与上游重叠                                                                                           | 自动合并后的语义结论                                                                                                                                        | 合并后验证                                                                                               |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `451b35a52 fix(app): avoid eager working diff rendering` | 新增共同路径 `packages/app/src/panels/diff-panel.tsx`；同时与上游 `#2488` 的 oversized diff 链路相交 | 合并结果先以 `workingDiff.diffTooLarge` 显示上游状态，再对正常 diff 使用本地 `focusPath/expandedPaths` 延迟渲染；状态优先级和写入彼此独立，不构成新语义冲突 | 覆盖 oversized diff 短路、正常 diff 默认折叠、定向 `focusPath`、expand all 和 refresh                    |
| `099908849 feat(app): add Mermaid pan and zoom controls` | `package-lock.json`、`packages/app/package.json` 和 8 个 locale 已从 WIP 进入 `main`                 | 仍只影响原有 2 个内容冲突文件和 8 个可自动合并 locale；合并树已同时保留 Mermaid 与 `previewTooLarge*` 标签                                                  | manifest 保留 `@panzoom/panzoom@^4.6.2`；全部 manifest 定稿后重建 lockfile，并重建 Mermaid WebView asset |

当前工作区仍有 13 个 tracked 与 62 个 untracked paths；其中只有以下 2 个未提交路径与上游 tag 重叠：

| WIP 路径                                  | 本地未提交意图                                                                                                       | 上游意图                                                                              | 移植方案                                                                                                           |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `packages/app/src/agent-stream/view.tsx`  | 接入 `useTurnChanges`、轮次文件变更卡片和 working diff 文件跳转；当前仍以 `pendingMessageSubmissions` 辅助判断运行态 | 删除 `pendingMessageSubmissions` 链路，统一使用 `baseRenderModel.turnTiming.isActive` | 合并后重新接入 `useTurnChanges`，但 `isTurnRunning` 必须取上游 canonical timing；不得恢复已删除的 submission model |
| `packages/app/src/panels/agent-panel.tsx` | 为变更卡片增加 `openTab({ kind: "working_diff", focusPath, focusRequestId })` 路由                                   | 删除 pending submission selector 和向 stream view 传递旧 prop                         | 保留 working diff 路由 handler；删除旧 pending submission 依赖，只向新 stream contract 增加文件跳转 callback       |

本轮最终结论：以当前已提交 `main` 重新分析，Git 内容冲突仍是原来的 18 个，解决方案不变；新增的 `diff-panel.tsx` 属于兼容的自动合并路径，不升级为冲突。当前未提交的轮次变更卡片存在一项明确的上游契约适配，但不计入 `main` 的 18 个 Git 冲突。

### 非代码层冲突审计（2026-08-01 22:28）

本节的“冲突”指双方的产品合同、状态所有权或用户可观察行为不一致，不等同于 Git conflict marker。源码级核对后的结论为 5 类明确冲突、2 类条件冲突，以及 3 组无直接冲突但必须组合验证的交互。

| 分级     | 主题                                    | 上游 `v0.2.5` 意图                                                                                                             | 本地意图                                                                                                            | 用户可观察后果                                                                                                       | 解决方案                                                                                                                                                                                                  |
| -------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 明确冲突 | 超大 diff 安全限制与定向查看            | `#2488` 在 checkout diff 超限时返回 `files: []`、`diffTooLarge: true` 和 error，Changes 直接显示 `DiffTooLargeState`           | 0026/0029 通过当前 diff 生成轮次文件卡片，并用 `focusPath` 只展开目标文件                                           | 合并后的 `useTurnChanges` 会因 payload error 隐藏卡片；即使已有 `focusPath`，服务端也没有目标文件正文可供展示        | 短期以上游资源上限为准，超限时不显示失效卡片/链接；若仍要求单文件可查看，另行设计 path-scoped diff RPC 或轻量文件状态协议，不能只改前端展开状态                                                           |
| 明确冲突 | 时间线状态所有权                        | `#2596` 回退 optimistic timeline/pagination 和 `pendingMessageSubmissions`，运行态统一取 canonical `turnTiming.isActive`       | 本地保留 phase/activity fold、retry footer；当前 0026 WIP 仍用 `pendingMessageSubmissions` 辅助轮次运行态           | footer、Activity 折叠和轮次卡片可能在提交中、重连或分页边界显示错误状态                                              | 以上游 reducer、消息身份和 `TurnTiming` 为唯一基线；phase、Activity、retry 和轮次卡片只做派生投影；WIP 的 `isTurnRunning` 改取 `turnTiming.isActive`，不恢复旧 submission model                           |
| 明确冲突 | 导航中的 checkout 增删统计              | tag 的 sidebar Workspace row 仍渲染 `DiffStat`                                                                                 | 0020 明确要求 Project/Workspace 导航和 Workspace 顶部均不显示 checkout `+N/-N`，因为它不代表会话贡献                | 解析跨 host sidebar 冲突时若直接采用上游 row，用户会看到已明确移除的统计重新出现                                     | 采用上游跨 host 数据/分组结构，但保留本地无 `DiffStat` 的渲染和 action-slot 条件；底层统计继续供 hover、源码控制和归档判断使用                                                                            |
| 明确冲突 | Worktree 隔离与本地状态连续性           | tag 中 `c6df5ffd4` 回退 `.worktreeinclude`，新 worktree 不再自动 materialize 本地文件/共享目录                                 | 本地保留已经发布的 `.worktreeinclude` copy/symlink 行为                                                             | 采用 tag 行为会让依赖、缓存或本地配置不再随 worktree 建立；保留行为则引入显式共享边界                                | 保留本地能力及其 opt-in、路径约束、失败降级和 symlink 安全说明；上游 `668204cd6` 的同 patch-id re-land 说明 tag 删除更像快照状态，不能据此推断上游长期否定该能力                                          |
| 明确冲突 | 发布与产品身份治理                      | 上游文档和 changelog 使用 npm `latest`/`beta` dist-tag 及上游 `0.2.4/0.2.5` 发布历史                                           | Reforged 禁用 npm/GHCR，使用自己的 stable gate、品牌和 changelog                                                    | 机械合入会形成自相矛盾的发布说明，并把上游版本历史误作 Reforged 已发布能力                                           | 代码以 `0.2.5` 为同步基线，发布流程、品牌和 changelog 服从本地治理；不恢复 npm 流程，也不直接复制上游 changelog                                                                                           |
| 条件冲突 | 跨 host 项目分组与右键菜单归属          | 逻辑项目按 `projectKey` 跨 host 分组，但 settings 使用 host-local `serverId/projectId`；上游 kebab 默认取一个 `settingsTarget` | 0017/0024 要求项目、project workspace、status workspace 的右键菜单与 kebab 完全同构，并保留本地文件管理器动作       | 同一项目存在于多个 host 时，单个聚合 row 的“设置/打开目录”可能指向不同 host；沿用旧 `projectKey` 路由会打开错误目标  | 先采用上游 host-aware identity，并让 context/kebab 共用同一 `settingsTarget` 和条件定义；文件管理器动作只在本地主机路径存在时显示。多 host 目标选择若需显式化，作为独立 UI 决策，不在冲突解析中暗选新行为 |
| 条件冲突 | 后台 agent 保活与“最新轮次变化”时间边界 | `#2590` 让 idle runtime 和后台工作持续存在，显式生命周期动作才关闭                                                             | 0026 卡片只归属最新完成的前台 turn，文件集合来自该 turn 的结构化 edit/write，但行数来自持续变化的 live working diff | final answer 后若后台工作继续，同文件统计可能漂移，后台独有文件不会进入卡片；“本轮”可能被误读为整个 runtime 生命周期 | 明确卡片只表示最新前台 turn 中曾编辑且当前仍有 diff 的文件，不承诺精确归因；后台状态继续由 workspace/subagent 状态呈现。若要求精确 turn/agent 归因，需要服务端快照或带 owner 的协议事件                   |

以下组合没有发现直接意图冲突：

- 上游快速上滑收键盘只在 Native `FlatList` 的现有 scroll lifecycle 上分类，不新增竞争手势；0019 的三按钮文字选择仅在 Web/Electron 启用，0030 的展开 Mermaid 位于独立 `Modal`，viewport 模式下 Native WebView 关闭自身 scroll。因此没有跨平台手势所有权冲突，但应回归“展开 modal 不带动底层列表”和 select mode。
- Activity fold 只折叠 stream item；pending permission/plan approval 由 `renderLiveAuxiliaryNode()` 在 fold 外渲染。上游“只保留最新 proposal”不会被折叠隐藏。需要验证的是 canonical timing，而不是把权限卡移入 fold。
- 跨 host 合并后的 New Workspace 仍把当前 `selectedServerId`、对应 `client` 和 `cwd` 传给本地导入入口；切换 host 后导入归属在合成树中保持一致。该路径不是产品冲突，只需补 host A -> host B -> import 的组合回归。

## 3. 涉及文件与计划

| 文件                                                  | 计划变化                             | 事实源                               |
| ----------------------------------------------------- | ------------------------------------ | ------------------------------------ |
| `mydocs/todolist.md`                                  | 登记 0031 及等待/复检状态            | 项目任务总表规则                     |
| `mydocs/micro_specs/0031_上游v0.2.5差异与冲突复检.md` | 保存初步结论、二次证据与最终冲突增量 | 用户要求、Git 对象与源码审计         |
| 产品源码                                              | 不修改                               | 用户仅授权分析；当前工作区有并发 WIP |

1. 先持久化固定 tag、初始提交基线、冲突分组和建议。
2. 等待 Codex 任务 `019fb91b-4888-7850-9171-bff394c3648d` 完成，不干预其实现。
3. 完成后重新采集提交级差异和 WIP 级路径/语义重叠，并与初步快照做集合差分。
4. 回写新增、消失、变化的冲突点及最终建议；同步任务总表状态。

## 4. 执行前检查点

- 当前目标：在并发任务结束后识别相对初步结论新增的冲突，而不是执行上游合并。
- 当前进度：目标任务已完成，二次提交级与 WIP 级复检完成，结论已回写。
- 当前动作是否仍服务核心目标：是。等待可避免在持续变化的工作区上产出过期结论。
- 下一步：本分析任务收口；如后续获实际同步授权，从隔离 worktree/branch 执行并验证合并。
- 风险与回退：并发任务可能修改本总表或源码；每次写入前重新读取相关文件并只追加最小变更。任何产品 WIP 均不回退。
- 验证方式：`git status --short --branch`、`git rev-parse`、`git merge-base`、`git rev-list --left-right --count`、`git diff --stat/--name-only`、`git merge-tree --write-tree --messages`，以及前后路径集合和关键语义 diff 审计。
- TDD 判定、测试 seam 与验收行为：`N/A；只读分析，不实现行为变更`。
- seam 确认：`N/A`。
- Execution Approval / Source：`Approved / User`；用户明确要求落盘、等待和复检。该授权不包含实际 merge 或任何提交/发布操作。

## 5. 执行与变更记录

- 实际改动：创建本 micro-spec，并在任务总表登记 0031。
- 偏差与用户决策：用户要求先落盘，再等待指定会话完成后复检；因此没有在首轮结论后直接收口。
- Change Log：
  - `2026-08-01 01:43`：记录初步基线、18 个 Git 内容冲突、45 个自动合并语义风险路径和解决建议；进入等待状态。
  - `2026-08-01 02:51`：目标任务完成后冻结二次快照；确认提交级冲突仍为 18 个、WIP 重叠仍为 14 个，0030 没有新增冲突文件；更新 Panzoom 与 locale 解决方案并收口。
  - `2026-08-01 20:53`：以包含 0029/0030 提交的当前 `main` 重新分析；共同路径增至 64，但 18 个内容冲突不变；确认新增 `diff-panel.tsx` 可语义兼容合并，并记录剩余两个 WIP 路径的上游契约适配。
  - `2026-08-01 22:28`：补做产品意图、状态所有权、交互和界面审计；识别 5 类明确冲突、2 类条件冲突，并确认移动端收键盘/选择、Activity/权限卡和跨 host 导入三组仅需组合验证。

## 6. 验证与完成判断

| 验收项          | 命令或步骤                                                                 | 结果                     | 证据                                                                                          |
| --------------- | -------------------------------------------------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------- |
| 固定上游 tag    | tag object/peeled commit 核验                                              | 通过                     | `v0.2.5 -> 6fc491e6220fba6543bbbe4bf1b1f58cfe59228b`                                          |
| 初步提交级冲突  | `git merge-tree --write-tree --messages 94cb6b8... 6fc491e...`             | 通过（预期返回冲突状态） | 18 个 content conflicts；无结构性冲突                                                         |
| 目标任务状态    | Codex thread coordination 等待完成态                                       | 通过                     | thread 为 `completed`；0030 已停止修改并完成 Reverse Sync                                     |
| 二次提交级复检  | 重跑 HEAD/base/count/stat 与 `merge-tree`                                  | 通过                     | HEAD/base/count/stat 均不变；同一 18 个 content conflicts；无结构性冲突                       |
| WIP 集合差分    | 比较前后 WIP 与上游 248 paths 的交集                                       | 通过                     | 前后均为同一组 14 paths；新增 0、移除 0                                                       |
| 0030 语义复检   | 审计 23 个任务路径与上游集合、manifest/lock/locale diff                    | 通过                     | 10 个既有重叠：2 个原冲突文件需保留 Panzoom，8 个 locale 修改可并存；其余 13 个路径上游未修改 |
| 远端 tag 时效性 | `git ls-remote --refs --sort=-version:refname upstream`                    | 通过                     | 远端最新正式 tag 仍为 `v0.2.5`，object `8d143abc...`                                          |
| 当前 main 复检  | `rev-parse`、`rev-list`、两侧 path 集合与 `merge-tree`                     | 通过                     | `main=099908849...`；46/27；64 个共同路径；同一 18 个 content conflicts                       |
| 新共同路径语义  | 审计本地 `451b35a52`、上游 `9e5accee3` 与 synthetic merge tree             | 通过                     | `diffTooLarge` 短路与正常 diff 延迟展开均保留，无状态覆盖                                     |
| 当前 WIP 重叠   | HEAD diff/untracked 与上游 248 paths 做集合交集                            | 通过                     | 75 个 WIP paths 中仅 `agent-stream/view.tsx`、`agent-panel.tsx` 重叠；已给出移植方案          |
| 非代码层审计    | 上游提交意图、任务 0016/0017/0019/0020/0026/0029/0030 与合成树源码交叉核对 | 通过                     | 5 类明确冲突、2 类条件冲突；3 组交互无直接冲突但需组合验证                                    |

- 未验证项与原因：未执行真实 merge、构建或合并后测试；这些操作不属于本次只读分析授权。
- 剩余风险：工作区仍有 13 个 tracked WIP 和 62 个 untracked paths，其中 2 个与上游变更重叠；直接 merge 仍可能遇到本地改动保护或把未提交任务混入解决过程。实际同步应在隔离 worktree/branch 进行，并在所有 manifest 定稿后重建 lockfile。
- Done Contract 是否由证据满足：是；目标任务完成后的提交级、路径级和语义级复检均已完成并落盘，且未执行真实 merge 或改变用户 WIP。

## 7. 恢复与同步

- 状态说明：已按包含 0029/0030 提交的当前 `main` 完成第三次复检，0031 保持已收口。
- 当前卡点：无分析卡点；实际同步尚未授权，且当前脏工作区不适合作为 merge 场地。
- 下一步唯一动作：仅在用户另行授权实际同步后，创建隔离 worktree/branch 并按本文 6 个冲突组逐项解决和验证。
- Resume / Handoff：恢复实际同步时先读取本文件，重新确认 tag/HEAD/工作区状态；把 18 个内容冲突与 0030 的 Panzoom/locale 保留要求作为合并验收清单。
- Project Sync Candidates：`无`；本次 tag 与一次性冲突结论留在本任务文档。
- 长期文档同步：不更新 `PROJECT.md`。

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联改动或阶段     | 文档同步状态 | 备注              |
| -------------------------- | ------------------------- | ------------------ | ------------ | ----------------- |
| `N/A`                      | `N/A`                     | 只读分析与任务记录 | 未提交       | 用户未授权 commit |
