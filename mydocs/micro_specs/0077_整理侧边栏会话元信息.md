# 整理侧边栏会话元信息 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                |
| ------------------ | ------------------------------------------------- |
| task_id            | `0077`                                            |
| spec layer         | `Feature Spec`                                    |
| task status        | `已收口`                                          |
| document status    | `Completed`                                       |
| depth              | `standard`                                        |
| phase              | `Review`                                          |
| Execution Approval | `Approved`                                        |
| Approval Source    | `User`                                            |
| file path          | `mydocs/micro_specs/0077_整理侧边栏会话元信息.md` |
| parent spec        | `N/A`                                             |
| superseded by      | `N/A`                                             |
| created / updated  | `2026-08-17 11:29 / 2026-08-17 11:52`             |

## 1. 目标与完成契约

- 当前理解：整理侧边栏会话行的第二行信息，让 Agent 标识位置稳定；桌面 hover card 明确显示会话所属项目；桌面行内不再重复显示项目名，compact / 移动端继续显示。
- 核心目标：在不改变会话分组、状态、导航和数据契约的前提下，让桌面与移动端的会话归属信息各自清晰且布局整齐。
- Done Contract：Agent 标识脱离动态中点分隔序列并固定贴齐元信息行最右侧，不随其他元信息长度改变位置；桌面 hover card 可见准确项目名；状态分组的桌面会话行不显示标题下项目名、compact 会话行仍显示；相关定向单测、Playwright、typecheck、lint、format 与差异检查给出通过证据。

## 2. 范围与事实

- 范围内：共享会话元信息行的 Agent 顺序、共享会话行的 responsive 项目名显示、桌面 workspace hover card 的项目名，以及精确回归测试。
- 范围外：侧边栏分组与排序、项目图标和状态图标语义、会话标题来源、移动端 hover card、协议与 daemon 数据。
- 当前任务单元：三个共享侧边栏呈现调整及其既有测试 seam，能够独立实现和验证。
- 轻量评估：`standard`；涉及多个共享 UI 文件和 desktop/compact 行为，但不改公共契约、数据或架构。
- 已确认事实：`WorkspaceMetaRow` 当前把 resident Agent 指示器追加在动态项目/host/PR/check/service 信息末尾；`WorkspaceHoverCard` 已持有 `SidebarWorkspaceEntry.projectName`；状态分组通过 `metaProjectName` 在共享 row content 中显示项目名，现有 `sidebar-model-b.spec.ts` 已同时覆盖 desktop 与 390px compact。
- `grilling` 结论（如使用）：`N/A`；用户明确选择 Agent 标识固定在元信息行最右侧，并批准按该方向执行。
- 风险与未知：Agent 右对齐不能在左侧 metadata 后遗留孤立分隔符，且左侧长文本仍须正确收缩；desktop 隐藏项目名不能影响 accessible name；hover card 新行不能造成卡片溢出或遮挡。

## 3. 涉及文件与计划

| 文件                                                                           | 计划变化                                                                   | 事实源                               |
| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------- | ------------------------------------ |
| `packages/app/src/components/sidebar/workspace-meta-row/index.tsx`             | 将 resident Agent 指示器固定贴齐元信息行最右侧，并从中点分隔序列中独立出来 | 当前 metadata 渲染顺序               |
| `packages/app/src/components/sidebar/sidebar-workspace-row-content.tsx`        | 仅在 compact form factor 向 meta row 传入项目名                            | 现有 responsive hook 与状态分组入口  |
| `packages/app/src/components/workspace-hover-card.tsx`                         | 在桌面 hover card 增加当前项目名信息行                                     | `SidebarWorkspaceEntry.projectName`  |
| `packages/app/src/components/sidebar/sidebar-workspace-activity-time.test.tsx` | 回归 Agent 标识保持 DOM 末项且不渲染专属分隔点                             | 现有共享 row jsdom seam              |
| `packages/app/e2e/browser/sidebar-model-b.spec.ts`                             | 回归 desktop 隐藏、compact 保留项目名                                      | 现有 status grouping responsive seam |
| `packages/app/e2e/browser/sidebar-workspace.spec.ts`                           | 回归 hover card 显示项目名                                                 | 现有 hover card E2E seam             |
| `mydocs/todolist.md`、本 micro-spec                                            | 维护批准、变更、验证和恢复记录                                             | 项目工作流参数                       |

1. 先更新三个既有测试 seam，并取得旧实现不满足新行为的 RED 证据。
2. 最小调整 metadata 顺序、responsive 传值和 hover card 信息行，不改变 view model 或协议。
3. 运行受影响单文件/精确 Playwright 与静态检查，核对 desktop/compact 布局后回写结论。

## 4. 执行前检查点

- 当前目标：让 Agent 标识、项目归属和 responsive 信息密度在侧边栏会话行中稳定、清楚。
- 当前进度：用户已选择 Agent 固定最右侧并批准执行；项目规则、hover/floating 文档、相关历史 spec、生产渲染链路及现有测试 seam 已核对，尚未修改代码。
- 当前动作是否仍服务核心目标：`是；计划只触及三个现有呈现入口及其精确测试。`
- 下一步：先建立三个用户行为的 RED，再实施最小改动并验证。
- 风险与回退：Agent 通过自动左边距贴齐行尾，左侧信息继续沿用现有收缩合同且不保留 Agent 前分隔点；若 hover card 高度影响定位，复用现有 InfoRow 和测量路径，不另建浮层。
- 验证方式：受影响 Vitest 文件 `--bail=1`；两个 Playwright 文件按精确标题 `--grep`；`npm run typecheck`、`npm run lint`、目标文件 format/check、`git diff --check`。
- TDD 判定、测试 seam 与验收行为：`TDD；Seam A = 共享 workspace metadata 行，Agent 指示器保持 DOM 末项、没有专属前置分隔点，并由浏览器几何验证贴齐行右边界；Seam B = 状态分组 desktop/390px compact 的精确项目名可见性；Seam C = 桌面 workspace hover card 的项目名信息行。`
- seam 确认：`N/A；复用已存在且直接观察三项用户行为的测试 seam，不新增架构 seam。`
- Execution Approval / Source：`Approved / User；用户于 2026-08-17 11:34 明确选择“固定最右侧”并批准执行 0077。`

## 5. 执行与变更记录

- 实际改动：`WorkspaceMetaRow` 移除 Agent 前的中点分隔符，并用自动左边距将指示器稳定贴齐最右侧；共享 row 只在 Native 或 compact form factor 传入项目名；桌面 hover card 新增带独立项目图标的项目名信息行。
- 偏差与用户决策：用户在执行前 checkpoint 将 Agent 位置从计划的固定首项改为固定最右侧；spec、测试 seam 与实现计划已同步后再进入 Execute。
- Change Log：`2026-08-17 11:29` 完成任务登记、事实核对、实现边界和执行前 checkpoint，等待用户批准。
- Change Log：`2026-08-17 11:34` 用户选择 Agent 标识固定最右侧并批准执行 0077；任务进入 Execute。
- Change Log：`2026-08-17 11:41` 三个新合同均取得 RED：Agent 前仍为 `·`、desktop 项目名数量仍为 1、hover card 项目名节点不存在。
- Change Log：`2026-08-17 11:52` 最小生产改动、GREEN、根静态门禁、格式与资源回收均完成；任务进入 Review 并收口。
- Change Log：`2026-08-17 12:00` 用户授权按 0077 精确文件边界创建本地提交，不 push；提交信息登记为 `feat(app): refine sidebar workspace metadata`。

## 6. 验证与完成判断

| 验收项                    | 命令或步骤                                                               | 结果   | 证据                                                                           |
| ------------------------- | ------------------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------ |
| Agent RED                 | 目标 Vitest `-t "keeps resident Agent status" --bail=1`                  | `RED`  | 期望前项为 `web`，实际为孤立分隔符 `·`                                         |
| responsive / hover RED    | 两个精确 Playwright `--grep`                                             | `RED`  | desktop 项目名期望 0 实际 1；`hover-card-workspace-project` 不存在             |
| Agent GREEN               | `sidebar-workspace-activity-time.test.tsx --bail=1`；lint 修正后精确复跑 | `通过` | 完整文件 `6/6`；最终精确用例 `1 passed / 5 skipped`                            |
| desktop / compact / hover | 两个目标 Playwright 文件合并运行精确用例，`--workers=1`                  | `通过` | `2 passed`；390px compact 保留项目名，desktop 隐藏，Agent 右边界误差不超过 1px |
| Typecheck                 | 根 `npm run typecheck`                                                   | `通过` | 两次退出码 0；最终代码生成链路无差异                                           |
| Lint                      | 根 `npm run lint`                                                        | `通过` | 3516 files，`0 warnings / 0 errors`                                            |
| 格式与差异                | 8 个目标文件 `format:check:files`；根 `git diff --check`                 | `通过` | formatter 全通过；无 whitespace error                                          |
| 资源回收                  | 核对两轮 Metro/隔离 daemon 端口及 `6767`                                 | `通过` | 任务端口均未监听；仅任务前已有的主 daemon `127.0.0.1:6767` 保留且未触碰        |

- 未验证项与原因：未做 Android/iOS 真机截图；移动布局由 390px compact Playwright 覆盖，Native 路径由 `isNative || isCompact` 与根 typecheck 提供替代证据。
- 剩余风险：Native 的具体字体度量和极窄横向空间尚无真机视觉证据；左侧文本继续使用现有收缩合同，Agent 自身 `flexShrink: 0`。
- Done Contract 是否由证据满足：`是；三个用户行为及静态、格式和资源回收门禁均已有证据。`

## 7. 恢复与同步

- 状态说明：`Review / 已收口 / Completed`
- 当前卡点：无。
- 下一步唯一动作：按用户授权创建 0077 精确边界本地提交，不 push。
- Resume / Handoff：0077 已完成；恢复时只需核对本 micro-spec 与 6 个 App 目标文件，继续保留 0075/0076 等并行用户改动。
- Project Sync Candidates：`无；本次复用既有 responsive、hover 与 floating 约束，没有形成新的跨任务项目事实。`
- 长期文档同步：`N/A`

### 提交记录

| 提交信息（Commit Message）                     | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态   | 备注                      |
| ---------------------------------------------- | ------------------------- | -------------- | -------------- | ------------------------- |
| `feat(app): refine sidebar workspace metadata` | `N/A`                     | `0077`         | `随本提交同步` | 用户授权本地提交；不 push |
