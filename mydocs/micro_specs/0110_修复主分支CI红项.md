# 修复主分支 CI 红项 Micro Spec

## 0. 状态与索引

| 字段               | 值                                            |
| ------------------ | --------------------------------------------- |
| task_id            | `0110`                                        |
| spec layer         | `Feature Spec`                                |
| task status        | `验证中`                                      |
| document status    | `Active`                                      |
| depth              | `standard`                                    |
| phase              | `Validate`                                    |
| Execution Approval | `Approved`                                    |
| Approval Source    | `User；2026-09-12`                            |
| file path          | `mydocs/micro_specs/0110_修复主分支CI红项.md` |
| parent spec        | `N/A`                                         |
| superseded by      | `N/A`                                         |
| created / updated  | `2026-09-12`                                  |

## 1. 目标与完成契约

- 当前理解：`main` 合并提交 `5ec05ee28` 的 GitHub CI `34623549506` 失败，父提交 `0096ecf71` 也有同类红项；用户要求将 CI 修复从 beta 发布中独立出来，修复后再提交、推送并发布 `0.7.2-beta.7`
- 核心目标：恢复 `main` 当前 CI 合同，使 App、Server、Desktop、Playwright 与 Nix 相关 checks 从同一提交绿灯通过，不改变产品协议或无关功能
- Done Contract：每类失败有已运行、可复现且最小的反馈命令；根因经证据确认并有回归覆盖；目标定向测试、typecheck、lint、格式检查通过；推送后精确提交的 GitHub CI 全绿；`0109` 才可继续 beta 发布

## 2. 范围与事实

- 范围内：当前 `main` CI `34623549506` 的 App unit test、双平台 Server test、Ubuntu Desktop browser E2E、四个 App Playwright shard 与 Nix update-hash 的失败 test/runtime contracts
- 范围外：新产品功能、WebSocket 协议、server 业务行为重设计、发布版本修改、npm/Docker/商店发布；`0109` 的 changelog 和 release commit 保持独立
- 当前任务单元：对失败 job 建立可重复 feedback loop，按共享根因分组做最小 RED-GREEN 修复，再由完整 CI 收口
- 轻量评估：`standard`；涉及多个现有测试表面，但已有具体 failure job 和执行 seam，不需要公共接口变更
- 已确认事实：`34623549506` 的 format、lint、typecheck、SDK、relay、CLI 和 Windows Desktop checks 已通过；App、双平台 Server、Ubuntu Desktop、四个 Playwright shard 与 Nix update-hash 均失败
- 已确认事实：父提交 `0096ecf71` 的 `34559111051` 在 App、Server、Ubuntu Desktop 与四个 Playwright shard 已失败，因此不能直接将红项归因于 0105 至 0108 的功能提交
- 已确认事实：App failure 报出 `expo-modules-core` 导入 `TurboModuleRegistry` 时，`react-native-web/dist/index.js` 不提供该 export；发生在 Vitest dependency optimization
- 风险与未知：失败可能共享测试环境根因，也可能是多个上游实现/测试漂移。不得通过 skip、重试、放宽断言或忽略 required job 取绿

## 3. 涉及文件与计划

| 文件或区域                                    | 计划变化                                              | 事实源                                     |
| --------------------------------------------- | ----------------------------------------------------- | ------------------------------------------ |
| `packages/app/vitest.config.ts`、对应定向测试 | 仅在证实后修正 Vitest 的 React Native / Expo 解析合同 | App job `103343155654`                     |
| `packages/server/src/**`、对应失败测试        | 对齐已采用 upstream 行为与断言，或修复实际回归        | Server jobs `103343155365`、`103343155421` |
| `packages/desktop/**`、对应 browser E2E       | 修复真实 isolated daemon/Metro/Electron 合同          | Ubuntu Desktop job `103343155298`          |
| `packages/app/e2e/**`、共享支持或被测实现     | 修复真实浏览器失败，不减少 E2E 覆盖                   | Playwright jobs `103343155508/621/691/703` |
| `.github/**`、Nix/lock 相关文件（仅必要时）   | 修复 `update-hash` 的确定性失败                       | Nix Update Hash job `103343058570`         |
| 本文件、`mydocs/todolist.md`                  | 记录反馈 loop、根因、验证与提交证据                   | 项目工作流                                 |

1. 提取每个 failed job 的精确症状，按共享根因分组，并运行对应最小 command
2. 为确认根因列出可证伪假设；在真实 test seam 上先 RED 后 GREEN
3. 完成目标测试、根静态检查与格式检查；提交并推送修复，等待精确 GitHub CI 全绿
4. 回写证据，恢复 `0109` 发布任务并按获批 changelog 发布 `0.7.2-beta.7`

## 4. 执行前检查点

- 当前目标：恢复 `main` 现有 CI 合同后再发布 beta
- 当前进度：已建立独立任务，确认完整 failure matrix 与 App dependency optimization 症状；其余 job 的最小 repro 和根因待收集
- 当前动作是否仍服务核心目标：是；先将远端 failure logs 转换为局部可重复 feedback loop，避免以全量 CI 输出替代诊断
- 下一步：提取失败 job 日志并运行最小 RED 命令，按共享根因排序
- 风险与回退：不通过跳过、重试或放宽断言掩盖红项；每项代码改动可由单独回归测试和 Git diff 回退
- 验证方式：单文件 Vitest / 目标 Playwright / Desktop E2E、根 typecheck/lint/format、精确 GitHub CI
- TDD 判定、测试 seam 与验收行为：`TDD；每个 CI failure 的真实 test command 是 feedback seam，先确认 RED 后再实施`
- seam 确认：`User；用户明确要求先修复当前 CI 红项，再继续发布`
- Execution Approval / Source：`Approved / User；2026-09-12 明确授权修复、提交、推送，并在 CI 成功后发布 beta`

## 5. 执行与变更记录

- 实际改动：创建独立 CI 修复任务，`0109` 保持暂停并保留未提交的 beta changelog 草案
- 偏差与用户决策：无
- Change Log：`2026-09-12` 确认完整 CI run `34623549506` 已失败；不把失败视为新功能回归，先建立最小可重复定位链路
- Change Log：`2026-09-12` 已修复 App i18n 插值、durable timeline、Codex fake App Server 与 replica-cache E2E 的既有 CI 合同；对应定向 Vitest 和 archive-cache E2E 已通过
- Root cause：Desktop `appearance-font-size.electron.mjs` 将 `SettingsSection` 的 `Theme` 二级标题误判为 `fontSize.lg`（16px）。`settingsStyles.sectionHeaderTitle` 使用 `fontSize.sm`，默认准确值为 12px；App Playwright 已覆盖 21px -> 18px、12px -> 10px 的同一缩放合同
- Root cause：Nix `update-hash` 在工作流首步依赖未配置的 `PASEO_BOT_APP_ID`。workflow 改为默认只读 token 下的 `update-nix.sh --check` 与 hash/lockfile diff 验证，不再自动写回或提交生成文件
- Change Log：`2026-09-12` durable registration timeline 将已有 epoch 的恢复后事件按后缀 append，保留 committed prefix；为保持 lint complexity 合同，stage/flush/commit 逻辑被提取为私有 helper，顺序、CAS selection 和 rollback ownership 不变
- Change Log：`2026-09-12` 修复提交 `38a6506d1` 的 Nix runner 计算出 `nix/npm-deps.hash` 已过期；使用同一 Ubuntu runner 报告的 `sha256-BB/IpSuub3y1PW2A8URNcPfDiitf/kPpmIfh3AozD6w=` 更新生成物，另行提交以保留只读 workflow 的 failure signal

## 6. 验证与完成判断

| 验收项                   | 命令或步骤                                                                                              | 结果     | 证据                                                                                                                   |
| ------------------------ | ------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------- |
| CI failure matrix        | GitHub Actions `34623549506`                                                                            | 已确认   | App、Server、Ubuntu Desktop、4 Playwright、Nix 红                                                                      |
| App failure 初始反馈信号 | App unit job 日志                                                                                       | 已确认   | `TurboModuleRegistry` export 缺失                                                                                      |
| Desktop 字号初始断言     | `appearance-font-size.electron.mjs`                                                                     | 已修复   | `Theme` section header 默认精确为 `12px`                                                                               |
| Nix token 失败           | GitHub Actions `34623549501`                                                                            | 已修复   | 只读 hash/lockfile 验证替代缺失 App token 的自动提交                                                                   |
| Nix hash 新反馈          | GitHub Actions `34639729624`                                                                            | 已修复中 | runner 发现 stale hash，正确值已提交等待下一轮验证                                                                     |
| App unit / Server 回归   | `resources.test.ts`、`agent-manager.test.ts`、`rewind.test.ts`、`codex-app-server-agent.test.ts`        | 通过     | `37/37`、`236/236`、`6/6`、`163/163`                                                                                   |
| App browser 回归         | `workspace-archive-cache.spec.ts`、`rewind-menu.ui-contract.spec.ts`、`appearance-theme-picker.spec.ts` | 通过     | `1/1`、`4/4`、`4/4`                                                                                                    |
| Desktop browser 回归     | `npm run test:e2e:browser-tabs --workspace=@getpaseo/desktop`                                           | 环境受限 | Windows Node `v26.7.0` 的 dev runner `spawn npx ENOENT` 后 CDP timeout；未进入字号断言，Ubuntu required job 为最终证据 |
| 静态检查                 | `npm run format`、`npm run typecheck`、`npm run lint`                                                   | 通过     | format exit 0；typecheck exit 0；lint `0 warnings and 0 errors`                                                        |
| diff 结构检查            | `git diff --check`                                                                                      | 通过     | exit 0                                                                                                                 |
| 精确 main CI             | 推送提交的 GitHub Actions                                                                               | 待执行   | 必须全部 required checks 绿                                                                                            |

- 未验证项与原因：Windows 本机无法运行 Linux Desktop browser E2E；必须由推送提交的 Ubuntu required job 验证。完整 CI 尚未复跑
- 剩余风险：在所有 CI contract 通过前，`0109` 不得创建 release commit 或 tag
- Done Contract 是否由证据满足：否；诊断与修复进行中

## 7. 恢复与同步

- 状态说明：本地修复与静态/定向验证已完成；发布任务 `0109` 继续暂停
- 当前卡点：等待更新 Nix hash 后的精确 GitHub CI。Ubuntu Desktop E2E 只能由 required job 作为最终证据
- 下一步唯一动作：推送 hash 修复并等待精确 CI 全绿
- Resume / Handoff：从 hash 修复提交的 GitHub Actions 继续；不要暂存 `CHANGELOG.md` 或 `0109`，直到本任务的精确 CI 绿
- Project Sync Candidates：`无；若确认 CI runner 解析合同或测试隔离规则是稳定事实，再同步至既有 docs`
- 长期文档同步：`N/A`

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态 | 备注 |
| -------------------------- | ------------------------- | -------------- | ------------ | ---- |
| `<待提交>`                 | `N/A`                     | CI 修复        | 待填写       |      |
