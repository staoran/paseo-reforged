# 发布 0.2.6-beta.1 测试版 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                  |
| ------------------ | --------------------------------------------------- |
| task_id            | `0032`                                              |
| spec layer         | `Feature Spec`                                      |
| task status        | `执行中`                                            |
| document status    | `Active`                                            |
| depth              | `standard`                                          |
| phase              | `Execute`                                           |
| Execution Approval | `Approved`                                          |
| Approval Source    | `User`                                              |
| file path          | `mydocs/micro_specs/0032_发布0.2.6-beta.1测试版.md` |
| parent spec        | `N/A`                                               |
| superseded by      | `N/A`                                               |
| created / updated  | `2026-08-02`                                        |

## 1. 目标与完成契约

- 当前理解：在上游 `v0.2.5` 已合入并推送 Reforged `main` 后，按仓库 `release-beta` 流程发布首个 Reforged 独立 patch beta。
- 核心目标：从干净的 `main` 发布 `v0.2.6-beta.1` GitHub prerelease，产出 Windows desktop 与 Android APK 测试资产，并同步用户向 release notes。
- Done Contract：目标版本和 changelog 经用户确认；`release:beta:patch` 成功原子推送 `main` 与不可变 tag；Desktop Release、Android APK Release 和 Release Notes Sync 全部成功；npm、Docker、商店与稳定渠道均未发布。

## 2. 范围与事实

- 范围内：beta 版本判断、changelog、全仓格式化、lint、typecheck、ACP catalog drift、`release:check`、版本提交与 tag、GitHub prerelease 工作流核验。
- 范围外：stable promotion、npm/GHCR 发布、App Store/Play Store 提交、Cloudflare 部署、强制推送、移动既有 tag、Linux/Nix 修复。
- 当前任务单元：本地准备与 changelog 审批 -> beta release -> GitHub 产物与 release notes 验证。
- 轻量评估：`升级 standard`；发布路径已由仓库脚本固定，但 tag 不可变且包含公开写入和多工作流验收。
- 已确认事实：`main=70613999af5e7ecd8e39f03f5e640a9c94243568` 已推送 `origin`；采用上游基线 `0.2.5`；当前 fork 已包含独立功能，按 patch 策略目标为 `0.2.6-beta.1`。
- `grilling` 结论（如使用）：未使用；目标渠道、远端、版本策略和禁止项均有权威流程。
- 风险与未知：GitHub 构建依赖外部 runner 与凭证；macOS 资产可能未签名/未公证；Nix hash 未重算的风险已由用户明确接受且不作为 Windows beta 门禁。

## 3. 涉及文件与计划

| 文件                                               | 计划变化                                             | 事实源                             |
| -------------------------------------------------- | ---------------------------------------------------- | ---------------------------------- |
| `CHANGELOG.md`                                     | 将当前 beta 条目原位更新为 `0.2.6-beta.1` 用户向记录 | `docs/release.md#Changelog policy` |
| 根与 workspace `package.json`、`package-lock.json` | 由 `release:beta:patch` 统一更新到 `0.2.6-beta.1`    | 版本脚本与 release flow            |
| `mydocs/todolist.md`、本 micro-spec                | 登记审批、发布和验收证据                             | 项目任务记录规则                   |

1. 运行 format、lint、typecheck、ACP drift 和 `release:check`，确认 `main` CI。
2. 从 `v0.2.5..HEAD` 更新当前 beta changelog 条目，展示目标版本和草案并取得用户确认。
3. 单独提交 changelog 与任务记录，执行 `npm run release:beta:patch`。
4. 核验远端 branch/tag、GitHub prerelease、Windows desktop、Android APK 和 release notes。

## 4. 执行前检查点

- 当前目标：完成 `0.2.6-beta.1` 的本地准备并提交用户审批。
- 当前进度：上游 merge 已提交，最新并行提交已合入，`main` 已推送且发布 worktree 干净。
- 当前动作是否仍服务核心目标：是；正在执行 beta 的强制准备门禁。
- 下一步：格式化、静态检查、ACP drift、release check、CI 核验和 changelog 草案。
- 风险与回退：tag 创建前可通过普通提交修正；tag 创建后不可移动，源代码问题必须发布下一 beta。保护分支保留主工作区既有 WIP。
- 验证方式：本地命令退出码、Git refs、GitHub Actions 状态、release API 与资产清单。
- TDD 判定、测试 seam 与验收行为：`N/A；发布只使用既有脚本和工作流，不新增产品行为`。
- seam 确认：`N/A；无新增测试 seam`。
- Execution Approval / Source：`Approved / User / 2026-08-02`；用户明确要求推送 `main` 并发布测试版。具体 changelog 仍按 release flow 展示并等待确认。

## 5. 执行与变更记录

- 实际改动：待执行。
- 偏差与用户决策：用户明确主用 Windows，不以 Linux/Nix 验证阻塞本次测试版。
- Change Log：
  - `2026-08-02`：确定采用 `v0.2.5` 上游基线，Reforged 独立功能按 patch beta 分类，目标 `0.2.6-beta.1`。
  - `2026-08-02`：`main` 在合入前新增 `9e5843dc8`，已与 `d77c25d96` 一并合入 `70613999a` 并推送 origin。

## 6. 验证与完成判断

| 验收项          | 命令或步骤                                        | 结果   | 证据                                                    |
| --------------- | ------------------------------------------------- | ------ | ------------------------------------------------------- |
| main 合入与推送 | Git parents、clean status、`git push origin main` | 通过   | `70613999a` 已推送，父提交为 `9e5843dc8` 与 `d77c25d96` |
| 本地发布准备    | format、lint、typecheck、ACP drift、release check | 待执行 | N/A                                                     |
| changelog 审批  | 展示 `0.2.6-beta.1` 草案                          | 待执行 | N/A                                                     |
| beta 发布       | `npm run release:beta:patch`                      | 待执行 | N/A                                                     |
| GitHub 产物     | Desktop、Android、Release Notes Sync              | 待执行 | N/A                                                     |

- 未验证项与原因：本地准备、changelog 审批和远端 beta 工作流尚未执行。
- 剩余风险：外部 CI/runner/凭证可能导致构建失败；tag 创建后只能重试同源构建或发布下一 beta。
- Done Contract 是否由证据满足：否，任务执行中。

## 7. 恢复与同步

- 状态说明：`main` 已合并并推送，beta 尚未创建。
- 当前卡点：需完成准备并取得具体 changelog 确认。
- 下一步唯一动作：运行本地发布准备命令并起草 changelog。
- Resume / Handoff：先确认 `main` 与 `origin/main` 均为 `70613999a`，无 `v0.2.6-beta.1` tag，再读取本文件和 `docs/release.md` 的 Beta flow。
- Project Sync Candidates：`无`；发布流程已有长期文档。
- 长期文档同步：不更新 `PROJECT.md` 或 `docs/release.md`。

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联改动或阶段            | 文档同步状态 | 备注           |
| -------------------------- | ------------------------- | ------------------------- | ------------ | -------------- |
| `<待提交>`                 | `N/A`                     | beta changelog 与任务记录 | 待提交       | 不包含代码变更 |
