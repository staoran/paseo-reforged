# 发布 0.2.6-beta.1 测试版 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                  |
| ------------------ | --------------------------------------------------- |
| task_id            | `0032`                                              |
| spec layer         | `Feature Spec`                                      |
| task status        | `执行中`                                            |
| document status    | `Active`                                            |
| depth              | `standard`                                          |
| phase              | `Verify`                                            |
| Execution Approval | `Approved`                                          |
| Approval Source    | `User`                                              |
| file path          | `mydocs/micro_specs/0032_发布0.2.6-beta.1测试版.md` |
| parent spec        | `N/A`                                               |
| superseded by      | `N/A`                                               |
| created / updated  | `2026-08-02`                                        |

## 1. 目标与完成契约

- 当前理解：在上游 `v0.2.5` 已合入并推送 Reforged `main` 后，按仓库 `release-beta` 流程发布首个 Reforged 独立 patch beta。
- 核心目标：从干净的 `main` 发布 `v0.2.6-beta.1` GitHub prerelease，产出 Windows desktop 与 Android APK 测试资产，并同步用户向 release notes。
- Done Contract：目标版本和 changelog 经用户确认；`release:beta:patch` 成功原子推送 `main` 与不可变 tag；Desktop Release、Android APK Release 和 Release Notes Sync 全部成功；npm、Docker、商店与稳定渠道均未发布。

## 2. 范围与事实

- 范围内：beta 版本判断、changelog、全仓格式化、lint、typecheck、ACP catalog drift、`release:check`、版本提交与 tag、GitHub prerelease 工作流核验。
- 范围外：stable promotion、npm/GHCR 发布、App Store/Play Store 提交、Cloudflare 部署、强制推送、移动既有 tag、Linux/Nix 修复。
- 当前任务单元：本地准备与 changelog 审批 -> beta release -> GitHub 产物与 release notes 验证。
- 轻量评估：`升级 standard`；发布路径已由仓库脚本固定，但 tag 不可变且包含公开写入和多工作流验收。
- 已确认事实：`main=origin/main=e463ce5213da72c2fc13f6fe8374dc582ed507fb`；采用上游基线 `0.2.5`；当前 fork 已包含独立功能，按 patch 策略目标为 `0.2.6-beta.1`。
- `grilling` 结论（如使用）：未使用；目标渠道、远端、版本策略和禁止项均有权威流程。
- 风险与未知：手动 CI `30740044113` 暴露的 format、失败轮次 footer 与陈旧 Playwright 契约已在本地修复并验证；修复提交后的 CI 尚未运行。GitHub 构建依赖外部 runner 与凭证；macOS 资产可能未签名/未公证；Nix hash 未重算的风险已由用户明确接受且不作为 Windows beta 门禁。

## 3. 涉及文件与计划

| 文件                                               | 计划变化                                             | 事实源                             |
| -------------------------------------------------- | ---------------------------------------------------- | ---------------------------------- |
| `CHANGELOG.md`                                     | 将当前 beta 条目原位更新为 `0.2.6-beta.1` 用户向记录 | `docs/release.md#Changelog policy` |
| 根与 workspace `package.json`、`package-lock.json` | 由 `release:beta:patch` 统一更新到 `0.2.6-beta.1`    | 版本脚本与 release flow            |
| `.oxfmtrc.json`                                    | 精确排除 Mermaid 生成 HTML 的格式检查                | format CI 失败证据                 |
| `packages/app/src/agent-stream/layout*`            | 恢复失败轮次 footer 并增加回归测试                   | Playwright 与 layout 单测 seam     |
| `packages/app/e2e/*`                               | 同步 UI 契约并修复 Windows 临时路径与 CLI 垫片       | Playwright 失败证据                |
| `mydocs/todolist.md`、本 micro-spec                | 登记审批、发布和验收证据                             | 项目任务记录规则                   |

1. 修复已批准的 format、E2E 和失败轮次 footer 问题，完成定向 Red/Green 与本地发布检查，再提交、推送并确认 CI。
2. 从 `v0.2.5..HEAD` 更新当前 beta changelog 条目，展示目标版本和草案并取得用户确认。
3. 单独提交 changelog 与任务记录，等待用户明确 go-ahead 后执行 `npm run release:beta:patch`。
4. 核验远端 branch/tag、GitHub prerelease、Windows desktop、Android APK 和 release notes。

## 4. 执行前检查点

- 当前目标：完成 `0.2.6-beta.1` 的本地准备并提交用户审批。
- 当前进度：已批准修复与本地发布检查完成；待提交并推送 `main`，再确认 CI。
- 当前动作是否仍服务核心目标：是；取得修复后的绿色 CI 后进入 changelog 审批。
- 下一步：提交本轮独立修复并推送 `main`，重新触发 CI。
- 风险与回退：tag 创建前可通过普通提交修正；tag 创建后不可移动，源代码问题必须发布下一 beta。保护分支保留主工作区既有 WIP。
- 验证方式：本地命令退出码、Git refs、GitHub Actions 状态、release API 与资产清单。
- TDD 判定、测试 seam 与验收行为：`TDD；使用 layoutStream 公共布局输入/输出 seam，error 状态的已完成失败轮次仍提供 auxiliary footer，running 状态继续不提供 completed footer`。
- seam 确认：`User / 2026-08-02`；用户批准失败轮次 footer 修复及对应回归测试。
- Execution Approval / Source：`Approved / User / 2026-08-02`；用户批准 format、E2E 与失败轮次 footer 修复。具体 changelog 与公开发布仍按 release flow 分别等待确认和 go-ahead。

## 5. 执行与变更记录

- 实际改动：排除 Mermaid 生成 HTML 的格式化；恢复 `error` 轮次 footer 并增加 layout 回归测试；同步 Activity 折叠、工具卡点击、`projectKey`、真实 Git 分支标题、Changes 默认折叠和 timeline 最终回答等 E2E 契约；使用系统临时目录与 Windows `.cmd` 垫片修复本地 GitHub PR 夹具。
- 偏差与用户决策：用户明确主用 Windows，不以 Linux/Nix 验证阻塞本次测试版。
- Change Log：
  - `2026-08-02`：确定采用 `v0.2.5` 上游基线，Reforged 独立功能按 patch beta 分类，目标 `0.2.6-beta.1`。
  - `2026-08-02`：`main` 在合入前新增 `9e5843dc8`，已与 `d77c25d96` 一并合入 `70613999a` 并推送 origin。
  - `2026-08-02`：手动 CI `30740044113` 暴露 format 与 Playwright 失败；用户批准生成文件排除、陈旧 E2E 同步和失败轮次 footer 产品修复。
  - `2026-08-02`：ACP catalog 与上游 `v0.2.5` 均固定 `fast-agent-acp 0.9.22`，按用户要求保持跟随上游，不追 registry `0.9.30`。
  - `2026-08-02`：Windows 定向 E2E 发现上游 PR 夹具硬编码 `/tmp`，且无扩展名 `gh` 垫片无法转发多行 GraphQL；改用 `tmpdir()`、`.cmd` 启动器及夹具专用 GraphQL 入口后原始场景通过。
  - `2026-08-02`：Changes 用例旧断言仍假设文件默认展开；改为显式验证混合状态下的 Expand all / Collapse all 交互后通过。

## 6. 验证与完成判断

| 验收项            | 命令或步骤                                        | 结果             | 证据                                                                                                                |
| ----------------- | ------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------- |
| main 合入与推送   | Git parents、clean status、`git push origin main` | 部分通过         | 修复前基线 `main=origin/main=e463ce521`；本轮修复待提交推送                                                         |
| 产品回归          | layout 单测与失败轮次 Playwright                  | 通过             | layout `35 passed`；失败轮次 fork E2E `1 passed`                                                                    |
| 定向 Playwright   | Activity、workspace/header、Changes、startup      | 通过             | Activity 相关场景通过；sidebar/entry `13 passed`；工作区标题、PR worktree、Changes 各通过；startup/empty `4 passed` |
| 本地发布准备      | format、lint、typecheck、release prepare/check    | 通过             | format/check、lint、typecheck、`release:prepare`、`release:check` 均退出 0                                          |
| ACP catalog drift | `npm run acp:version-drift:check`                 | 已检查，有意保留 | 上游 `v0.2.5` 固定 `fast-agent-acp 0.9.22`；不追 registry `0.9.30`，另有 13 个 registry 查询不可用                  |
| 修复后 CI         | GitHub Actions                                    | 待执行           | N/A                                                                                                                 |
| changelog 审批    | 展示 `0.2.6-beta.1` 草案                          | 待执行           | N/A                                                                                                                 |
| beta 发布         | `npm run release:beta:patch`                      | 待执行           | N/A                                                                                                                 |
| GitHub 产物       | Desktop、Android、Release Notes Sync              | 待执行           | N/A                                                                                                                 |

- 未验证项与原因：修复后 CI、changelog 审批和远端 beta 工作流尚未执行。
- 剩余风险：外部 CI/runner/凭证可能导致构建失败；tag 创建后只能重试同源构建或发布下一 beta。
- Done Contract 是否由证据满足：否，任务执行中。

## 7. 恢复与同步

- 状态说明：已批准修复与本地发布检查完成，beta 尚未创建。
- 当前卡点：需提交并推送修复，取得绿色 CI 后展示 changelog 草案。
- 下一步唯一动作：提交本轮修复并推送 `main`。
- Resume / Handoff：先确认无 `v0.2.6-beta.1` tag，读取本文件和 `docs/release.md` 的 Beta flow，再从修复提交与 CI 验证继续。
- Project Sync Candidates：`无`；发布流程已有长期文档。
- 长期文档同步：不更新 `PROJECT.md` 或 `docs/release.md`。

### 提交记录

| 提交信息（Commit Message）                              | 提交脚注（Commit Footer） | 关联改动或阶段                 | 文档同步状态 | 备注                               |
| ------------------------------------------------------- | ------------------------- | ------------------------------ | ------------ | ---------------------------------- |
| `fix(app): restore failed-turn footer and E2E coverage` | `N/A`                     | format、footer、E2E 与任务记录 | 待提交       | 独立于 changelog 和 release commit |
