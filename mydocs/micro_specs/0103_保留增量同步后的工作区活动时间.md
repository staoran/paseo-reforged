# 保留增量同步后的工作区活动时间 Micro Spec

## 0. 状态与索引

| 字段                        | 值                                                          |
| --------------------------- | ----------------------------------------------------------- |
| task_id                     | `0103`                                                      |
| spec layer                  | `Feature Spec`                                              |
| task status                 | 已收口                                                      |
| document status             | `Completed`                                                 |
| depth                       | `fast`                                                      |
| phase                       | `Review`                                                    |
| Execution Approval          | `Approved`                                                  |
| Approval Source             | `User`：2026-09-09 明确要求修复漏传字段并补增量同步回归测试 |
| file path                   | `mydocs/micro_specs/0103_保留增量同步后的工作区活动时间.md` |
| parent spec / superseded by | `N/A`                                                       |
| created / updated           | `2026-09-09`                                                |

## 1. 目标与完成契约

- 当前理解与核心目标：补齐 `projectAgentSnapshot()` 的 `lastMessageAt` 投影，使目录增量同步保留既有的最后消息时间
- Done Contract：全量目录快照之后接受不含该 Agent 的增量响应，Agent 消息时间及侧栏消费的 workspace 活动时间仍等于原始消息时间；无消息时保持 `null`；回归测试先失败后通过

## 2. 范围与事实

- 范围内：App 快照投影和目录同步定向回归测试
- 范围外：历史记录回填、缓存归属、协议或服务端变更、运行实例重启、提交和发布
- 当前任务单元与轻量评估：单字段保留与对应行为验证，足够小
- 已确认事实：`AgentDirectoryReplica.commitChanges()` 将既有 Agent 转回快照；`projectAgentSnapshot()` 漏传 `lastMessageAt`，随后规范化将缺失字段变成 `null`
- 事实源：`docs/data-model.md`、`utils/agent-snapshots.ts`、`runtime/directory-sync/agent-replica.ts`、既有目录同步测试
- grilling：`N/A`
- 风险与未知：旧记录原本缺少消息时间的情况不由本次修复解决

## 3. 涉及文件与计划

| 文件                                                    | 计划变化                                 | 事实源                                              |
| ------------------------------------------------------- | ---------------------------------------- | --------------------------------------------------- |
| `packages/app/src/utils/agent-snapshots.ts`             | 投影最后消息时间，空值保持 null          | Agent 模型和缓存序列化                              |
| `packages/app/src/runtime/directory-sync/index.test.ts` | 全量快照后空增量响应仍保留工作区活动时间 | DirectorySync 公共入口与 session workspace 活动索引 |

1. 新增定向回归，观察时间从有效值变成 null 的失败
2. 补齐投影，运行目标测试及根静态检查并回写结果

## 4. 执行前检查点

- 当前目标：增量同步保留最后消息时间
- 当前进度：前轮诊断已复现漏传，当前工作区干净
- 当前动作是否仍服务核心目标：是
- 下一步：先写目录同步回归，再补齐字段
- 风险与回退：单字段改动可独立撤销；不改变未知时间语义
- 验证方式：单个 Vitest 文件 `--bail=1`、根 `typecheck`、`lint`、目标格式检查
- TDD 判定、测试 seam 与验收行为：`TDD`；`DirectorySync.refreshAgents()` 接受全量和增量结果，观察 Agent 与 workspace 活动时间
- seam 确认：`User`；用户已明确要求增量同步后时间保留的回归测试
- Execution Approval / Source：`Approved / User`，本轮明确修复指令

## 5. 执行与变更记录

- 实际改动：快照投影新增 `lastMessageAt: agent.lastMessageAt?.toISOString() ?? null`；目录同步增加有消息时间和 null 两个回归用例
- 偏差与用户决策：无
- Change Log：2026-09-09 创建任务并记录已有授权；定向回归先在增量响应后收到 null 而失败，修复单字段后通过

## 6. 验证与完成判断

| 验收项       | 命令或步骤                                                                   | 结果 | 证据                                                                                  |
| ------------ | ---------------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------- |
| 增量同步回归 | `npx vitest run src/runtime/directory-sync/index.test.ts --bail=1`，App 目录 | 通过 | 19/19；修复前用 `-t 'preserves workspace activity time'` 确认期望日期实际 null 的 RED |
| 快照回归     | `npx vitest run src/utils/agent-snapshots.test.ts --bail=1`，App 目录        | 通过 | 8/8                                                                                   |
| 静态检查     | 根 `npm run typecheck`、`npm run lint`                                       | 通过 | 退出码均为 0；lint 0 警告和错误                                                       |
| 格式与差异   | 目标文件格式化、`format:check:files`、`git diff --check`                     | 通过 | 目标四个文件格式检查及 diff 检查退出码为 0                                            |

- 未验证项与原因：不执行真实桌面重启，本次为定向逻辑修复
- 剩余风险：旧数据缺字段仍需独立处理
- Done Contract 是否由证据满足：是；全量快照后真实增量同步保持 Agent 与 workspace 活动时间，无消息保持 null

## 7. 恢复与同步

- 状态说明：修复与定向回归、静态检查已完成，未提交
- 当前卡点：无
- 下一步唯一动作：等待用户后续验收或提交指令
- Resume / Handoff：沿用本文件，不创建平行 spec
- Project Sync Candidates：无；最后消息时间语义已由 `docs/data-model.md` 记录
- 长期文档同步：无需新增

### 提交记录

| 提交信息（Commit Message）                                          | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态 | 备注           |
| ------------------------------------------------------------------- | ------------------------- | -------------- | ------------ | -------------- |
| `6039da7` `fix(app): preserve workspace activity across agent sync` | `N/A`                     | 本次修复       | 已同步       | 本地提交已创建 |
