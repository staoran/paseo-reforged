# 混合版本真实环境与文档收口 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                      |
| ------------------ | ------------------------------------------------------- |
| task_id            | `0097`                                                  |
| spec layer         | `Feature Spec`                                          |
| task status        | `待手工验收`                                            |
| document status    | `Review`                                                |
| depth              | `deep`                                                  |
| phase              | `Review`                                                |
| Execution Approval | `Approved`                                              |
| Approval Source    | `User / 2026-08-19；按依赖顺序逐票实施并本地提交`       |
| file path          | `mydocs/micro_specs/0097_混合版本真实环境与文档收口.md` |
| parent spec        | `mydocs/specs/0084_跨端中继二进制加密与状态追平压缩.md` |
| superseded by      | `N/A`                                                   |
| created / updated  | `2026-08-19 / 2026-09-02`                               |

## 1. 目标与完成契约

- 当前理解：本地 RED/GREEN 不能证明 production relay 透明转发、弱网收益、Hermes 解压、混合版本回退或文档事实一致；这些是 Spec 0084 的最终发布门禁。
- 核心目标：在所有实现 tickets 完成后执行真实环境验收、兼容矩阵复核、安全/架构/性能文档同步，并决定是否收口 0084。
- Done Contract：
  - new/new、new/old、old/new、framed Base64/binary、无 codec、confirm mismatch、legacy fallback 在真实/定向环境通过。
  - production relay 只透明转发两种表示；本地 32 MiB 单帧、64 MiB physical/receive budget、错误关闭和顺序门禁通过；hosted relay 的近 32 MiB 两种表示边界需专用环境验证；不修改外部 Elixir relay。
  - 2 Mbps/150 ms、10 Mbps/80 ms 弱网 profile 的 timeline/10 MiB 文本下载达到父 Spec p50/p95 目标；实时 terminal/agent_stream 回归不超过阈值。
  - Hermes 1 MiB/4 MiB decode p95、无 OOM/错序；失败时按 Spec 移除 `deflate-raw` advertisement，而不关闭 framed identity。
  - 更新 `SECURITY.md`、`docs/architecture.md`、`docs/terminal-performance.md`、`public-docs/configuration.md`、配置 JSON schema 和父 Spec/任务总表；记录旧 binary 回退需删除 transport block。
  - 只有全部证据满足后才将 0084 标为完成；外部 relay 不透明或真机门禁失败时保留明确阻塞，不静默放宽策略。

## 2. 范围与事实

- 范围内：定向测试/build/typecheck/lint/format、live relay、混合版本、弱网、Hermes/移动端、文档和父 Spec 收口。
- 范围外：production relay 源码修改、push、PR 和发布；本轮已授权本地 commit。
- 当前任务单元：集成验收和长期事实同步。
- 轻量评估：`deep`；高风险跨 runtime/网络/安全，必须保留失败证据和回滚路径。
- 已确认事实：父 Spec 第 6 节门禁均已有结论；Desktop 与 realtime validation 通过，Hermes CLI 补充环境失败且 release 真机仍阻塞，hosted near-limit Base64/binary 均以 `1009` 失败。production 已按回炉合同关闭 framed advertisement/acceptance。
- 风险与未知：需要真实 relay endpoint、网络 profile 和 Hermes 设备，环境缺失即报告阻塞。

## 3. 涉及文件与计划

| 文件/环境                                                           | 计划变化                                            | 事实源                   |
| ------------------------------------------------------------------- | --------------------------------------------------- | ------------------------ |
| `packages/server/src/server/daemon-e2e/live-relay.real.e2e.test.ts` | 通过真实 hosted relay 分别验证 framed Base64/binary | live relay seam          |
| `packages/server/scripts/measure-live-relay-performance.ts`         | 显式 hosted relay 与弱网 profile 手工测量 CLI       | performance evidence     |
| `packages/server/src/server/test-utils/paseo-daemon.ts`             | 仅向隔离测试 daemon 透传已存在的 transport policy   | test harness seam        |
| 受影响 Vitest/workspace checks                                      | 执行最终定向验证，不扩大到完整本地套件              | PROJECT.md testing rules |
| live relay environment                                              | 验证 opaque Base64/binary、边界和混合版本           | parent Spec 6.1          |
| Hermes/Android/iOS                                                  | 验证 1/4 MiB decode p95、OOM、错序                  | parent Spec 6.2          |
| `SECURITY.md`                                                       | wire、尺寸侧信道、回退和限额                        | security facts           |
| `docs/architecture.md`                                              | 64 MiB high-water、runtime boundaries               | architecture drift       |
| `docs/terminal-performance.md`                                      | realtime/state-sync/bulk-live 分类和 p95            | terminal facts           |
| `public-docs/configuration.md`、website schema                      | encoding/compression config and old-binary caveat   | config contract          |
| `mydocs/specs/0084...`、`mydocs/todolist.md`                        | evidence, status, next/rollback and final verdict   | project workflow         |

1. 先完成所有前置 tickets 的定向验证和静态门禁。
2. 再执行 live relay、弱网和 Hermes；失败按回炉规则分类。
3. 最后同步长期文档、父 Spec 和任务总表，进入 Review。

## 4. 执行前检查点

- 当前目标：验证和收口，不在现场修改外部 relay 或扩大协议范围。
- 当前进度：0087–0095 已逐票完成并本地提交；0095 提交为 `e4497c45d`。当前构建经 production relay 的 framed Base64、framed binary 和默认 Codex workflow 已于 2026-08-21 通过 `3/3`。
- 当前动作是否仍服务核心目标：是；这是发布前唯一综合证据入口。
- 下一步：保持 production legacy wire；只在目标 Hermes release 真机和外部 relay 限额完成对齐后重验对应失败门禁，不重复已完成的 Desktop 或 realtime 正式 5 轮。
- 风险与回退：任何安全/兼容/性能门禁失败都保留 legacy fallback，并在父 Spec 标记阻塞。
- 验证方式：父 Spec 第 6 节全表、真实环境日志和文档 format check。
- TDD 判定、测试 seam 与验收行为：集成环境验收以公开 `createTestPaseoDaemon`、真实 hosted relay 和 `DaemonClient.collectDiagnostics()` 为 seam；先证明强制 Base64/binary 的 negotiated mode，再最小透传测试配置，不读取内部状态。
- seam 确认：`User；父 Spec 已批准真实 relay、弱网、Hermes 和文档门禁`。
- Execution Approval / Source：`Approved / User / 2026-08-19`。

### 2026-09-01 续接 checkpoint：realtime p95 实际测量路径

- 当前目标：用真实 isolated daemon、hosted relay、WebSocket 和本地弱网 shaper 测量并发 state-sync 期间的 terminal echo/增量 `agent_stream` p95；不以内部 queue 或 event-loop 指标替代端到端结果。
- 已完成：真实 `MockLoadTestAgentClient`、PTY terminal subscription、两路并发 canonical timeline fetch、32 条唯一增量事件、legacy/framed 交替采样、机器可读报告及失败退出码均已接入。
- 诊断收口：measurement `WebSocketLike` 原先只暴露 Node `on`，使 close `(code, reason)` 被 E2EE adapter 当成 CloseEvent 并退化为 `code 0`；新增标准 `addEventListener/removeEventListener`、四路并发分支标签及有界 control/data 生命周期摘要后，定向 Vitest `8/8`、Server typecheck、目标 lint/format/diff check 通过。
- 正式结果：`--runs=5 --realtime-only` 在 hosted relay 上完整退出 0；两档 terminal echo 与增量 `agent_stream` p95 均通过 `max(legacy + 2 ms, legacy * 1.05)` 门禁，且资源全部回收。

### 2026-09-01 续接 checkpoint：发布回滚后的测量真实性

- hosted near-limit 失败触发父 Spec 的发布回滚：production client 不 advertisement、production daemon 不接受 `framedCiphertextV1`；实际 relay wire 保持 legacy Base64/hybrid 且不启用 transport compression。
- validation override 只经 client transport factory 与 isolated daemon dependency 注入，不进入 persisted config、RPC、CLI、env 或普通 `DaemonClientConfig`。
- measurement 已改为显式组装 physical WebSocket + E2EE transport，并在 `fetchAgents`、timeline/file、subscription 或 realtime probe 前等待 authenticated observer；framed 必须为 `framed-v1-binary + deflate-raw`，legacy 必须为 `legacy-hybrid`，任何静默 fallback 都立即失败。
- 连接级静默 fallback RED→GREEN 后，measurement `10/10`、client transport `4/4`、server relay transport `9/9`、relay runtime `3/3`、framed contract `91/91` 通过。本 checkpoint 不重跑 hosted near-limit 或 realtime 正式 5 轮。
- `build:relay`、`build:client`、relay/client/server typecheck、15 文件 lint、26 文件 format check 与 `git diff --check` 均通过；production gate 绕过检索未发现配置、CLI、env、Desktop/App 或普通 client 开启路径。

## 5. 执行与变更记录

- 实际改动：补充 hosted relay 的强制 Base64/binary 兼容矩阵、隔离 daemon 的 transport policy 测试透传，以及本机弱网代理性能测量；统一审查将弱网证据从带环境 skip/网络时延断言的 Vitest 改为必须显式传 `--relay-endpoint` 的手工 CLI，保留 payload 完整性检查和 JSON 汇总，不把易抖动时延阈值放入自动测试；同步安全、架构、终端性能、配置文档和生成 schema。
- 真实证据：production relay `paseo-relay-next.fly.dev:443` TCP 可达；live relay `3/3` 通过（framed Base64、framed binary、默认 Codex workflow），两种 framed 场景均观察到入站 `deflate-raw`；弱网测量以 `--runs=5` 显式执行，两档均完成且 timeline/file payload 的 SHA-256 完整性检查通过。
- 偏差与用户决策：弱网测试首版使用小 todo timeline，2 Mbps timeline 改善为 18%，未放宽 20% 门禁；改为公共 40-item 尾页中包含 1,000,000-byte 完成态文件型 item 后重跑通过。Zod 4.4.3 生成器相对旧 schema 产生 984 行可复现结构漂移，未手改生成物，保留权威生成结果并记录该审查风险。
- Change Log：`2026-08-19` 登记 0097；`2026-08-21` 完成 live/弱网与长期文档同步并进入 Review。当前机器无 WSL distro、Android SDK/adb、Hermes CLI；Docker context 指向外部 `tencent-ssh`，未用于本地弱网验证；这些门禁不以假设代替。
- Change Log：`2026-08-21` 统一审查将弱网性能入口迁为 `measure:live-relay-performance` 显式手工命令，并保留原 5 轮测量结果作为历史证据。
- Change Log：`2026-08-21` 统一审查补齐 framed capability 每数组 16 token/每 token 64 UTF-16 code units 的不可信输入边界、Base64 identity-wire admission reservation、pending reservation 的 `bufferedAmount` 可见性，以及 Hermes/decoder 不可用时不 advertisement `deflate-raw`。
- Change Log：`2026-08-21` 统一审查补入真实双层 FIFO 的 prepared→physical reservation 原子移交回归；现有 live `3/3` 未发送近 32 MiB 帧，因此 hosted relay 单帧边界继续标为未验证。
- Change Log：`2026-08-27` 统一代码审查通过；关闭 Standards findings，并以已发布 `@getpaseo/server@0.2.5` 的真实 Browser E2E 替代 WebSocket transport mock。根 typecheck、lint、两文件 format/diff 与 `pair-device-relay` `4/4` 通过。
- Change Log：`2026-08-31` 从当前工作树重新构建 Windows x64 unpacked artifact，并通过隔离 packaged smoke：真实 renderer/preload bridge、desktop-managed daemon、bundled CLI status 与 terminal hook workflow 均成功。临时 daemon 使用 `127.0.0.1:61505`，结束后已退出，未触碰主 daemon `6767`；artifact 同时包含工作树中尚未提交的并行 UI 改动，因此该证据只归因于本次 packaged 集成门禁，不作为 clean `HEAD` 可复现性证明。
- Change Log：`2026-09-01` 官方 Hermes v0.12.0 Windows DEBUG/HBC 89 补充环境完成 20 轮测量，1 MiB/4 MiB decode p95 为 `354/1465 ms`，超过 `50/150 ms` 门槛；长度、双校验和与顺序正确。该环境不是 RN 0.81 release 真机，真机 OOM、GC、耗电和 UI stall 仍阻塞，当前 Hermes guard 继续不 advertisement `deflate-raw`。
- Change Log：`2026-09-01` hosted relay 近 32 MiB Base64 `33,554,428` wire bytes 与 binary `33,554,431` wire bytes 均收到 close `1009`；这是外部门禁真实失败，不放宽本地限额、不切回 v1 绕过，发布前必须保持 `framedCiphertextV1` advertisement 关闭。
- Change Log：`2026-09-01` realtime 正式 5 轮通过：2 Mbps/150 ms legacy→framed terminal p95 `14341.68→1007.01 ms`、agent-stream p95 `13749→527 ms`；10 Mbps/80 ms 为 `5479.82→617.25 ms`、`5060→307 ms`。
- Change Log：`2026-09-01` hosted near-limit 失败后的 production gate 已关闭；measurement 改为 validation-only 显式 opt-in，并在所有业务请求前对 authenticated negotiated mode fail closed。历史 realtime 数值只代表 validation 路径，不代表 production framed 已发布。

## 6. 验证与完成判断

| 验收项        | 命令或步骤                                                | 结果                                                    | 证据                                         |
| ------------- | --------------------------------------------------------- | ------------------------------------------------------- | -------------------------------------------- |
| compatibility | mixed-version and live relay matrix                       | 通过（live `3/3`；本地兼容矩阵已在 0087–0093 定向通过） | `live-relay.real.e2e.test.ts`                |
| performance   | weak-network p50/p95 and realtime regression              | 通过；弱网追平/文件及 realtime 正式 5 轮均满足门槛      | `scripts/measure-live-relay-performance.ts`  |
| mobile        | Hermes 1/4 MiB decode gate                                | CLI 补充环境失败；RN 0.81 Android/iOS release 真机阻塞  | `scripts/measure-hermes-relay-decode.ts`     |
| desktop       | current-source Windows x64 packaged smoke                 | 通过；隔离 daemon 与 terminal workflow 完整             | `packages/desktop/e2e/packaged-app-smoke.js` |
| hosted limit  | near-32 MiB framed Base64/binary                          | 失败；两种表示均被 hosted relay 以 `1009` 关闭          | `packages/relay/src/live-relay.e2e.test.ts`  |
| docs          | security/architecture/performance/config and parent sync  | 已同步 production legacy / validation-only 发布事实     | 本 ticket diff                               |
| release gate  | production advertisement/acceptance and measurement truth | 生产关闭；measurement 静默 fallback RED→GREEN           | transport/measurement 定向 Vitest            |

- 弱网原始结果：`2Mbps/150ms` timeline legacy `p50/p95=8076/9024ms`、framed `483/832ms`，改善 `94%/91%`；文件 legacy `42980/43413ms`、framed `4231/4410ms`，改善 `90%/90%`。`10Mbps/80ms` timeline legacy `3647/4377ms`、framed `370/398ms`，改善 `90%/91%`；文件 legacy `9290/10442ms`、framed `1091/1383ms`，改善 `88%/87%`。
- 统一审查后的 CLI smoke：`--runs=1` 再次经 production relay 完成两档 SHA-256 payload 校验；2 Mbps/150 ms timeline `7119 ms -> 540 ms`（`92%`）、file `43292 ms -> 4308 ms`（`90%`），10 Mbps/80 ms timeline `3506 ms -> 461 ms`（`87%`）、file `12230 ms -> 1539 ms`（`87%`）。单样本仅证明迁移后入口可执行，不替代上面的 5 轮 p50/p95 历史证据。
- realtime 正式 5 轮原始 p95：`2Mbps/150ms` legacy terminal/agent-stream 为 `14341.68/13749 ms`，framed 为 `1007.01/527 ms`，允许上限为 `15058.77/14436.45 ms`；`10Mbps/80ms` legacy 为 `5479.82/5060 ms`，framed 为 `617.25/307 ms`，允许上限为 `5753.81/5313 ms`。两档均无失败指标。
- 未验证项与原因：RN 0.81 对应 Android/iOS release 真机的 OOM、GC、耗电和 UI stall；官方 Hermes v0.12.0 Windows DEBUG/HBC 89 只提供补充失败证据，不能替代目标 runtime。旧/新本地兼容回归、production relay 普通 payload 透明性、Desktop packaged smoke 和 realtime p95 均已有证据。
- hosted relay 近 32 MiB 门禁已真实失败：Base64 `33,554,428` wire bytes 与 binary `33,554,431` wire bytes 均 close `1009`。在外部 relay 限额与本地契约对齐前，不得发布 `framedCiphertextV1` advertisement。
- 剩余风险：压缩尺寸侧信道、设备解压性能和真实 OS 级调度仍未证实；不因 live relay/弱网通过而关闭 legacy fallback 或宣称 0084 发布完成。
- Done Contract 是否由证据满足：`No；realtime 与 Desktop 已通过，但 Hermes release 真机仍阻塞，hosted relay 近 32 MiB 两种表示已真实失败`。

## 7. 恢复与同步

- 状态说明：前置 0087–0095 已收口；0097 当前可运行的 live relay、弱网、Desktop packaged、Hermes CLI、realtime p95 与 hosted near-limit 门禁均已执行。production 已回滚到 legacy Base64/hybrid；保持待手工验收是因为 release 真机和 hosted limit 失败尚未解除。
- 当前卡点：缺少 RN 0.81 Android/iOS release 真机证据；hosted relay 对近 32 MiB Base64/binary 帧均返回 `1009`，与父 Spec 发布门禁不兼容。
- 下一步唯一动作：在目标 release 真机完成 OOM/GC/耗电/UI stall 门禁，并由外部 relay 所有者对齐单帧限制后重验近 32 MiB；在此之前不启用 Hermes `deflate-raw` 或 `framedCiphertextV1` advertisement。
- Resume / Handoff：从父 Spec 第 6、7.16–7.17、8、9 节和 0095 metrics 输出接续。
- Project Sync Candidates：production gate 与 validation-only measurement 事实已落四份长期文档，无未同步候选。
- 长期文档同步：已按当前 release gate 完成；0084/0097 保留外部门禁明细。

### 提交记录

| 提交信息（Commit Message）                                     | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态 | 备注                                                |
| -------------------------------------------------------------- | ------------------------- | -------------- | ------------ | --------------------------------------------------- |
| `test(relay): validate framed transport rollout`               | `N/A`                     | `paseo / 0097` | `已同步`     | live/弱网通过；设备门禁阻塞                         |
| `test(relay): use real legacy daemon in pairing e2e`           | `N/A`                     | `paseo / 0097` | `已同步`     | 真实旧 daemon gate 与持久化失败 E2E；手工门禁仍阻塞 |
| `fix(timeline): close state sync registration review findings` | `N/A`                     | `paseo / 0097` | `已同步`     | `c9fb23701`；状态追平与 registerSession review-fix  |
| `fix(relay): hold framed transport behind release gate`        | `N/A`                     | `paseo / 0097` | `已同步`     | `559c6502b`；production framed gate 关闭            |
| `docs(relay): document closed framed release gate`             | `N/A`                     | `paseo / 0097` | `已同步`     | `2183e242a`；长期文档同步 production legacy wire    |
