# 加密帧 FIFO 与高水位 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                      |
| ------------------ | ------------------------------------------------------- |
| task_id            | `0090`                                                  |
| spec layer         | `Feature Spec`                                          |
| task status        | `已批准`                                                |
| document status    | `Active`                                                |
| depth              | `standard`                                              |
| phase              | `Plan`                                                  |
| Execution Approval | `Approved`                                              |
| Approval Source    | `User / 2026-08-19；按依赖顺序逐票实施并本地提交`       |
| file path          | `mydocs/micro_specs/0090_加密帧FIFO与高水位.md`         |
| parent spec        | `mydocs/specs/0084_跨端中继二进制加密与状态追平压缩.md` |
| superseded by      | `N/A`                                                   |
| created / updated  | `2026-08-19`                                            |

## 1. 目标与完成契约

- 当前理解：异步压缩/解压会改变完成时序；现有 encrypted relay socket 只按估算长度检查 bufferedAmount，没有 prepared-frame 或 receive reservation。
- 核心目标：把最终 wire bytes、出站 FIFO、入站 FIFO 和物理高水位绑定到同一 transport seam。
- Done Contract：
  - 每个应用帧只 prepare/encrypt/encode 一次；发送顺序与调用顺序严格一致，confirm、pending backlog、实时帧不乱序。
  - 发送前按最终 Base64/binary wire bytes 检查单帧 32 MiB 与物理 64 MiB high-water；异步准备期间按实际待处理 bytes 预留，不以旧估算绕过。
  - 接收入站先按 raw wire bytes 预检，再 decode/decrypt；当前项与等待 FIFO reservation 合计不超过 64 MiB。
  - send/decode/close/error 任一失败释放 reservation、关闭当前连接且不交付半成品；legacy path 保持原行为。
  - direct WebSocket 不受 relay queue hint 影响。

## 2. 范围与事实

- 范围内：`packages/relay/src/encrypted-channel.ts`、`packages/server/src/server/websocket/encrypted-relay-socket.ts`、`packages/server/src/server/relay-transport.ts` 相关测试。
- 范围外：codec 算法本身、traffic classification、UI、外部 relay。
- 当前任务单元：prepared frame 与异步 FIFO/reservation 集成。
- 轻量评估：`standard`；连接生命周期和内存安全高风险，遵守父 Spec 限额。
- 已确认事实：`attachEncryptedSocket` 在 channel Promise resolve 后才 attach，已有 pending message buffer；需避免在新的 confirming/opening 阶段丢帧。
- 风险与未知：高水位对真实 ws bufferedAmount 的差异由 0097 live/弱网验证。

## 3. 涉及文件与计划

| 文件                                                             | 计划变化                                                     | 事实源               |
| ---------------------------------------------------------------- | ------------------------------------------------------------ | -------------------- |
| `packages/relay/src/encrypted-channel.ts`                        | send/receive FIFO、prepared frame、raw budget、close cleanup | GREEN-3/父 Spec D.3  |
| `packages/server/src/server/websocket/encrypted-relay-socket.ts` | 最终 wire high-water 和 reservation                          | 当前 64 MiB 物理限制 |
| `packages/server/src/server/relay-transport.ts`                  | attach/pending message 顺序与 transport adapter 透传         | relay runtime 入口   |
| 对应 tests                                                       | 并发、慢 codec、超限、错误释放、legacy 回归                  | TDD seam             |

1. 先补异步慢准备和超限 RED。
2. 实现出站/入站 reservation 与 FIFO。
3. 验证 old/new 和 direct path 不变。

## 4. 执行前检查点

- 当前目标：只解决顺序、内存和最终长度，不新增业务分类。
- 当前进度：RED-3 已覆盖握手 FIFO，尚未覆盖 codec/receive reservation。
- 当前动作是否仍服务核心目标：是；这是压缩上线的安全门。
- 下一步：0088/0089 完成 prepared frame contract 后执行。
- 风险与回退：任何 reservation 不一致时关闭 framed 连接，legacy 继续使用原路径。
- 验证方式：encrypted socket tests、framed channel tests、定向 typecheck/lint/format。
- TDD 判定、测试 seam 与验收行为：`TDD；Transport、channel events、socket bufferedAmount/close`。
- seam 确认：`User；父 Spec 已批准 FIFO、高水位和 fail-closed 门禁`。
- Execution Approval / Source：`Approved / User / 2026-08-19`。

## 5. 执行与变更记录

- 实际改动：本轮未修改生产代码。
- 偏差与用户决策：无。
- Change Log：`2026-08-19` 从父 Spec 执行清单 6 拆出。

## 6. 验证与完成判断

| 验收项        | 命令或步骤                                 | 结果   | 证据             |
| ------------- | ------------------------------------------ | ------ | ---------------- |
| order         | delayed prepare/decode + concurrent frames | 待执行 | 待新增 tests     |
| budget        | 32/64 MiB boundary and reservation release | 待执行 | 父 Spec D.2      |
| compatibility | legacy and direct WebSocket regression     | 待执行 | 现有 relay tests |

- 未验证项与原因：尚未授权实现。
- 剩余风险：真实 ws implementation 和弱网 profile 留给 0097。
- Done Contract 是否由证据满足：`No；待 Execute`。

## 7. 恢复与同步

- 状态说明：ticket 已登记，依赖 0088、0089。
- 当前卡点：无设计卡点，仅缺执行授权。
- 下一步唯一动作：实现 prepared-frame FIFO 和 reservation。
- Resume / Handoff：从 0089 prepared frame 类型和现有 encrypted socket 测试接续。
- Project Sync Candidates：高水位事实回写 `docs/architecture.md`。
- 长期文档同步：待 0097。

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态 | 备注     |
| -------------------------- | ------------------------- | -------------- | ------------ | -------- |
| `<待提交>`                 | `N/A`                     | `paseo / 0090` | `待填写`     | 未获授权 |
