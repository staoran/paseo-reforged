# 合并远端 bgg v0.8.0 到 Reforged Micro Spec

## 0. 状态与索引

| 字段               | 值                                                        |
| ------------------ | --------------------------------------------------------- |
| task_id            | `0113`                                                    |
| spec layer         | `Feature Spec`                                            |
| task status        | `执行中`                                                  |
| document status    | `Active`                                                  |
| depth              | `standard`                                                |
| phase              | `Execute`                                                 |
| Execution Approval | `Approved`                                                |
| Approval Source    | `User；2026-09-14 明确批准按第 7 节开始实际合并`          |
| file path          | `mydocs/micro_specs/0113_合并远端bgg_v0.8.0到Reforged.md` |
| parent spec        | `N/A`                                                     |
| superseded by      | `N/A`                                                     |
| created / updated  | `2026-09-14`                                              |

## 1. 目标与完成契约

- 当前理解：本地 `main` 在 `8360ced27`，远端 bgg 的 `v0.8.0` peeled commit 为 `b8e24677e12b226c7c38c1c3a40649daa9f1152f`，需要把上游 0.8.0 合入 Reforged，同时保留本地功能、品牌、发布门禁和兼容策略
- 核心目标：完成一次可审计的 `--no-commit --no-ff` 合并，解析 87 个 Git 内容或 add/add 冲突，验证合并后的协议、Server、App、CLI、Desktop 和 Website 契约，创建 merge commit，再安全回放合并前保护 stash
- Done Contract：
  1. `git ls-files -u` 和 `git diff --name-only --diff-filter=U` 均为空，`MERGE_HEAD` 指向 v0.8.0
  2. 87 个冲突路径均按本文件第 2、3 节保留双方必要意图，无残留冲突 marker
  3. `npm run build:protocol`、`npm run build:plugin`、`npm run build:client`、`npm run build:server`、`npm run typecheck`、`npm run lint` 和 `git diff --check` 通过
  4. 受影响的 import sessions、session、workspace、Codex transport、Desktop packaging、workspace activity、Mermaid runtime/host 回归通过
  5. 创建 merge commit 后回放 `stash@{0}`，二次冲突单独解析并再次确认无未合并条目
  6. Nix hash 若无法在当前 Windows 环境重算，必须明确记录为非 Windows 风险，不伪装为已验证

## 2. 范围、双方意图与冲突分析

### 2.1 参与方与基线

| 参与方          | 事实与主要意图                                                                                                                                                                                               |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 本地 Reforged   | `8360ced27`；保留 Paseo Reforged 身份、fork 仓库与发布门禁，并持续演进 ReplicaCache、canonical timeline、Goal、framed relay、Mermaid 交互、workspace activity、导入事务、Codex transport 和 provider catalog |
| 远端 bgg v0.8.0 | `b8e24677e...`，提交主题为 `chore(release): cut 0.8.0`；带来 upstream 0.8.0 版本、Plugin SDK/provider 生命周期、历史搜索分页、导入/回滚、模型和 provider catalog 扩展、插件路由及完整发布资料                |
| 合并机制        | merge base 为 `1f3bb0239b631ea91c9f0f83bc9e511a7a26fd2d`；采用本地意图与 v0.8.0 新能力的合成，不以任一侧整文件覆盖为默认策略                                                                                 |

### 2.2 代码冲突块：发布、依赖和身份

路径：`.github/workflows/android-apk-release.yml`、`CHANGELOG.md`、`CLAUDE.md`、`CONTRIBUTING.md`、`SECURITY.md`、`docs/agent-lifecycle.md`、`docs/hub.md`、`docs/release.md`、`docs/unistyles.md`、`nix/npm-deps.hash`、`package-lock.json`、`package.json`、`packages/*/package.json`、`packages/desktop/electron-builder.yml`、`packages/website/package.json`。

- 本地意图：版本和品牌必须继续指向 Paseo Reforged，仓库、包名、Electron/macOS 门禁、CI path、beta/stable release 流程和已有 fork 文档不能被上游名称覆盖；本地 lockfile 还包含 Mermaid、Panzoom、framed transport 等本地依赖结果
- 上游意图：把所有 workspace 统一到 0.8.0，加入插件、provider、移动商店 changelog、Electron 44、macOS 13 和 v0.8.0 release workflow 约束，并使 lockfile 与新依赖集合一致
- 建议：版本号、上游新增 release 文件和工作流能力合入；品牌、仓库 URL、应用包名、Reforged 发布 gate 以本地为准；lockfile 用合并后 workspace manifest 重新生成，不能机械取一侧；`nix/npm-deps.hash` 当前保留 upstream hash，但 Nix 未安装，交给 CI/Nix 复核

### 2.3 代码冲突块：App 时间线、导入和交互

路径：`packages/app/src/agent-stream/model.test.ts`、`strategy-native.tsx`、`view.tsx`、`client-slash-commands/index.test.ts`、`components/import-session-sheet*`、`components/message.tsx`、`components/question-form-card.tsx`、`components/sidebar*`、`composer/draft/workspace-tab.tsx`、`contexts/session-context.tsx`、`data/push-router.ts`、`desktop/updates/update-callout-source.tsx`、`hooks/use-archive-agent.test.ts`、`hooks/use-settings/storage.test.ts`、`panels/agent-panel.tsx`、`screens/new-workspace-screen.tsx`、`open-project-screen.tsx`、`sessions-screen.tsx`、`screens/settings/appearance/appearance-section.tsx`、`utils/agent-attention.test.ts`、`utils/agent-directory-reconciliation.test.ts`、`utils/agent-directory-sync.ts`、`utils/workspace-agent-activity.test.ts`。

- 本地意图：继续使用 `Agent.turn`/TurnLiveness、canonical timeline、lazy loading、pending submission、activity fold、ReplicaCache、workspace activity 和跨 host 目录同步；通知文本、图片编码、创建 Agent 的 `initialPrompt` 等本地合同必须保持；Mermaid fullscreen/iframe viewport 和移动端交互不能因上游 UI 重构丢失
- 上游意图：引入 v0.8.0 的导入会话界面、插件设置入口、provider retry/attention、移动端 sheet/keyboard 体验、更多 sidebar 行为和新的 Agent/Session fixture 合同
- 建议：以 `Agent.turn` 作为运行态唯一来源，不恢复旧 `activeTurn` 或独立 `agentTurnLiveness`；导入流程保留事务 rollback 和 bounded discovery snapshot；创建请求使用协议字段 `initialPrompt`；通知使用 `notificationStylesheet`；插件设置路由使用 Expo 生成类型；测试 fixture 随当前协议更新，不用放宽类型绕过功能差异
- 交互冲突：导入 sheet 的发现、取消、回滚与 session timeline 初始化存在时序耦合，必须保留取消后的结果丢弃；sidebar 的 workspace/agent activity 不能因为上游排序重写丢掉本地 unread、activity time 和 workspace 归属；移动端键盘、Mermaid fullscreen 和 iframe viewport 属于不同 surface，不能用一个平台分支互相覆盖

### 2.4 代码冲突块：运行时、ReplicaCache、host 和传输

路径：`packages/app/src/runtime/host-runtime.test.ts`、`runtime/host-runtime.ts`、`runtime/replica-cache/index.test.ts`、`runtime/replica-cache/index.ts`、`packages/client/src/daemon-client.ts`、`packages/server/src/server/websocket-server.ts`、`packages/desktop/src/daemon/desktop-packaging.test.ts`。

- 本地意图：ReplicaCache 需要 dirty flush、final flush、drain、lazy load、Goal/timeline ownership、byte budget、并发写保护；relay/framed transport 需要业务流量分类、兼容 gate、FIFO/high-water mark 和旧端回退；host runtime 需要 DirectorySync 与增量 checkpoint 的单一所有权
- 上游意图：引入 v0.8.0 的 websocket/client 生命周期、relay/provider/plugin 连接能力、桌面打包约束以及新的 host reconnect 行为
- 建议：缓存队列、final handshake、timeline canonical replacement 和 framed transport 以本地实现为主，只把上游新 RPC/生命周期接入到现有 seam；不要恢复 row-store 直写或双重 liveness store；每个 reconnect/close 分支都验证 owner、generation、pending write 和旧协议兼容
- 交互冲突：后台、前台、断线重连和 Agent 切换可能同时触发 cache flush 与 timeline fetch，必须保持 `flushDirty`/`drain`/final handshake 顺序；framed 与 legacy transport 不能因上游默认值改变而互相误判

### 2.5 代码冲突块：协议、Server、导入和 provider

路径：`packages/protocol/src/agent-types.ts`、`messages.ts`、`messages.test.ts`、`messages.wire-compat.test.ts`、`packages/server/src/server/agent/agent-manager.ts`、`agent/import-sessions*`、`providers/codex-app-server-agent.ts`、`providers/codex/app-server-transport.test.ts`、`server/session*`、`session/workspace-provisioning/workspace-provisioning-service.ts`、`workspace-git-service.observation.test.ts`、`worktree-session.ts`，以及各 workspace package manifest。

- 本地意图：保持 provider-neutral timeline、Goal state、turn identity、旧 daemon/client wire compatibility、Codex async question/transport、导入事务 rollback 和 workspace automation/observation 合同
- 上游意图：引入 PluginTimelineItem、provider lifecycle/status/retry、历史搜索分页、导入 session discovery、模型/provider catalog、workspace/worktree 新 API 和 v0.8.0 的 provider 生命周期
- 建议：公共 schema 以两侧字段并集为方向，但字段是否 optional、旧端默认值、timeline item discriminant 和 request limit 必须逐项验证；导入 discovery 使用请求 `limit` 的 bounded snapshot，不使用 `limit + importedCount`；Server 内部若保留镜像类型，CLI/server 构建后必须确认两份 `AgentTimelineItem` 可安全传递，不靠 `as any` 掩盖 PluginTimelineItem 差异
- 交互冲突：provider retry、turn completed、attention 和历史导入可能乱序到达；用 turn identity、epoch/seq、transaction rollback 和兼容 gate 约束，不以最后到达消息盲目覆盖 canonical state

### 2.6 代码冲突块：Website、文档和配置 schema

路径：`packages/website/public/schemas/paseo.config.v1.json`、`packages/website/src/components/landing-page.tsx`、`src/downloads.tsx`、`src/routes/hub.tsx`、`public-docs/custom-providers.md`、`public-docs/skills.md`、`scripts/sync-release-notes-from-changelog.mjs`。

- 本地意图：网站继续展示 Reforged 下载入口、平台检测、移动商店 CTA、Hub/产品定位和 fork 文档
- 上游意图：补 v0.8.0 Plugin SDK、provider、Hub plan、下载平台和公开文档导航
- 建议：合并新增文档和 provider/plugin reference；下载 URL、品牌文案和 Reforged 平台命名保留本地；schema 以兼容扩展为主，任何 required 字段变更必须有旧配置解析回归
- 交互冲突：平台检测、下载按钮、Hub plan 和文档导航共同决定用户入口，不能只合并视觉层而丢掉对应路由或 CTA

### 2.7 add/add 冲突块：Unistyles patch

路径：`patches/react-native-unistyles+3.2.4.patch`。

- 本地意图：修复多个 Unistyles Web 样式合并后的 `uni__dependencies` 丢失，确保 runtime theme/font size 在 sidebar、workspace tab、settings 三个 surface 都能响应
- 上游意图：同一依赖版本可能存在另一组 patch 或无 patch 假设
- 建议：保留已验证的依赖去重和 `uni__dependencies` 恢复补丁，重新执行受影响 browser/runtime 检查；不以单组件 workaround 替代共享运行时修复

## 3. 意图冲突与本地功能影响判断

### 3.1 明确会影响本地新增或调整功能的冲突

会影响，主要集中在以下本地能力的 owner 或交互边界，而不是简单文本冲突：

| 本地功能                             | 受影响路径                                                                              | 风险                                                                              | 处理建议                                                                                 |
| ------------------------------------ | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| ReplicaCache 与 timeline final/drain | `runtime/replica-cache/*`、`host-runtime.ts`、`session-context.tsx`                     | 上游生命周期若覆盖 owner 会造成后台丢写、重复写或 canonical timeline 被旧响应覆盖 | 保留本地队列、generation、final handshake 和 drain 语义，只接入上游新事件                |
| Agent.turn 与 lazy timeline          | `session-store.ts`、`agent-directory-sync.ts`、`agent-panel.tsx`、`session-context.tsx` | 旧 `activeTurn` 与新 `turn` 并存会造成运行态、activity fold、projection gate 分裂 | 单一使用 `Agent.turn`，fixture 和目录同步一起迁移                                        |
| 导入 sessions                        | `import-sessions.ts`、`import-session-sheet*`                                           | discovery limit、取消、rollback、snapshot 组合错误会导入过量或留下半成品          | 使用请求 limit 的 bounded snapshot，事务 rollback 在取消和失败路径均执行                 |
| framed relay/兼容 gate               | `daemon-client.ts`、`websocket-server.ts`、relay/desktop manifest                       | 新默认 transport 可能破坏旧客户端或 byte budget/FIFO                              | 保留 legacy fallback、兼容 gate、FIFO 和高水位，做 wire compatibility 回归               |
| Mermaid fullscreen/iframe viewport   | Mermaid host/runtime/新 fullscreen 文件及 e2e                                           | 宿主与 iframe 各自 viewport 状态可能互相覆盖，交互模式下缩放/选择失效             | 宿主只管理公开 bridge 状态，iframe 管理自身 viewport，分别测 fullscreen、fit、pan/select |
| workspace activity 与 Agent 归属     | sidebar、workspace activity、directory sync                                             | 上游目录刷新可能回退 activity time、workspaceId 或 unread 状态                    | 以 canonical directory snapshot + local activity projection 合成，禁止旧快照回退新元数据 |
| Goal/provider lifecycle              | provider protocol、agent manager、App attention                                         | retry/status/Goal event 乱序会错误显示活动或完成态                                | 用 turn/epoch/seq 和 provider-owned Goal projection 约束覆盖                             |
| Reforged 发布身份                    | workflow、package manifest、website/downloads、CHANGELOG                                | 上游整文件覆盖会把包名、仓库和下载链接切回 upstream                               | release metadata 分字段合并，Reforged 取本地，v0.8.0 新 changelog 取上游                 |
| Unistyles 字体与 theme               | patch、docs/unistyles、appearance                                                       | 删除 patch 会使 runtime 字号回退到挂载值                                          | 保留 patch 并跑对应 runtime/browser 用例                                                 |

### 3.2 不属于产品行为冲突的合并后合同缺口

类型错误中的 `activeTurn` fixture、通知样式名称、`initialPrompt` 字段、Expo 路由生成文件和 server/CLI timeline 类型属于代码接口收口，不代表选择了新产品行为。处理原则是跟随当前权威类型、重生成声明栈、再做受影响回归；不能为通过 typecheck 恢复已经删除的旧字段。

## 4. 涉及文件与计划

1. 以本地功能 owner 为准解析 87 个冲突路径，逐组检查 upstream 新能力是否完整进入
2. 运行 protocol/plugin/client/server build，刷新 Expo route types，修复仅由合并组合产生的类型合同
3. 运行 lint、typecheck、git diff check 和受影响单文件测试
4. 暂存并创建 merge commit
5. 回放保护 stash，独立解析二次冲突，再运行最小回归

## 5. 执行前检查点

- 当前目标：在不回滚、不清理、不推送的前提下完成已批准的 v0.8.0 本地合并
- 当前进度：真实 merge 已执行，87 个冲突已解析；格式、构建、静态检查和受影响回归已通过，merge commit 尚未创建
- 当前动作是否仍服务核心目标：是
- 下一步：暂存已验证结果并创建 merge commit，然后回放保护 stash
- 风险与回退：保护 stash 为 `stash@{0}`，名称 `pre-merge-upstream-v0.8.0-2026-09-13`；不执行 `merge --abort`、reset、clean 或 push
- 验证方式：构建、typecheck、lint、diff check、受影响 Vitest/browser/packaging 回归；Nix hash 交给 CI/Nix
- TDD 判定、测试 seam 与验收行为：`N/A；本轮是已批准的 merge conflict resolution，沿用双方已有测试 seam`
- seam 确认：`N/A；采用现有协议、目录、timeline、Mermaid 和 packaging seam`
- Execution Approval / Source：`Approved / User`

## 6. 验证与完成判断

| 验收项              | 命令或步骤                                                               | 结果 | 证据                                                                                                    |
| ------------------- | ------------------------------------------------------------------------ | ---- | ------------------------------------------------------------------------------------------------------- |
| 依赖                | `npm install --ignore-scripts`                                           | 通过 | workspace 依赖已同步                                                                                    |
| 构建                | `npm run build:protocol`、`build:plugin`、`build:client`、`build:server` | 通过 | protocol/plugin/client/server/CLI 构建完成                                                              |
| 静态检查            | `npm run lint`                                                           | 通过 | 0 warning / 0 error                                                                                     |
| 类型检查            | `npm run typecheck`                                                      | 通过 | app、server、CLI 类型合同已收口                                                                         |
| 格式                | `npm run format`、`npm run format:check`                                 | 通过 | 4705 个文件格式检查通过                                                                                 |
| Server/App 定向回归 | import sessions、session、session workspaces、workspace activity         | 通过 | import `28/28`；session `164 passed / 1 skipped`；workspaces `115 passed / 4 skipped`；activity `16/16` |
| Transport/packaging | Codex transport、Desktop packaging                                       | 通过 | `12/12`、`11/11`                                                                                        |
| Mermaid             | runtime browser、host browser                                            | 通过 | `4/4`、`1/1`                                                                                            |
| 合并后补充回归      | ReplicaCache、directory/activity、Codex、plugin、client activity         | 通过 | ReplicaCache `25/25`；App 组合 `48/48`；Codex `177/177`；插件 `4/4`；client activity 组合 `79/79`       |
| 冲突状态            | `git diff --name-only --diff-filter=U`、marker scan                      | 通过 | 当前无 unmerged entries；脚本分隔线误报已排除                                                           |

- 未验证项与原因：`nix/npm-deps.hash` 尚未在本机 Nix 环境重算；当前 Windows 环境无 Nix，这只影响非 Windows/Nix 打包
- 剩余风险：stash 回放可能在本地 ReplicaCache、timeline、E2E helper 和 micro-spec 文件产生二次冲突；merge commit 前后必须分别确认工作树状态
- Done Contract 是否由证据满足：合并解析和本地静态/定向门禁已满足；待 merge commit、stash 回放和二次验证完成后满足全部契约

## 7. 恢复与同步

- 状态说明：`Execute；验证已通过，尚未创建 merge commit`
- 当前卡点：无代码阻塞；待创建 merge commit 和回放 stash
- 下一步唯一动作：创建 merge commit，然后回放 `stash@{0}` 并处理二次冲突
- Resume / Handoff：恢复时先确认 `MERGE_HEAD=b8e24677e...`、`git ls-files -u` 为空；merge commit 后再处理 stash 二次冲突，重点关注 `runtime/replica-cache`、`viewed-timeline-sync`、Mermaid e2e helper、`timeline/replica.test.ts`、未跟踪 micro-spec 和 `.tmp-ci-*` trace
- Project Sync Candidates：无；本轮一次性冲突事实留在本 micro-spec
- 长期文档同步：暂不需要

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态 | 备注                  |
| -------------------------- | ------------------------- | -------------- | ------------ | --------------------- |
| `<待提交>`                 | `N/A`                     | `v0.8.0 merge` | `验证通过`   | merge commit 尚未创建 |
