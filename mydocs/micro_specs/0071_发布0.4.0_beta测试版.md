# 发布 0.4.0 Beta 测试版 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                           |
| ------------------ | ------------------------------------------------------------ |
| task_id            | `0071`                                                       |
| spec layer         | `Feature Spec`                                               |
| task status        | `已收口`                                                     |
| document status    | `Completed`                                                  |
| depth              | `standard`                                                   |
| phase              | `Review`                                                     |
| Execution Approval | `Approved`                                                   |
| Approval Source    | `User；2026-08-15 当前消息明确要求 CI 全绿后提交并发布 beta` |
| file path          | `mydocs/micro_specs/0071_发布0.4.0_beta测试版.md`            |
| parent spec        | `N/A`                                                        |
| superseded by      | `N/A`                                                        |
| created / updated  | `2026-08-15`                                                 |

## 1. 目标与完成契约

- 当前理解：将已经采用 upstream `0.4.0` 的最新 Reforged `main` 在 CI 全绿后发布为本 fork 首个同基线 beta
- 核心目标：按项目 beta flow 发布不可变 `v0.4.0-beta.3`，产出桌面与 arm64-v8a APK 测试资产，不发布 npm、Docker、商店或 Cloudflare
- Done Contract：精确 main clean 且 CI 全绿；adopted base、同基线 tag 与目标版本复核；ACP pins 保留；changelog 按 previous-stable-to-HEAD 更新并经用户批准；`release:beta:next` 成功；Desktop、Android、Release Notes 三条 workflow 全绿；prerelease、24 个预期资产和 APK ABI 核验通过

## 2. 范围与事实

- 范围内：release 准备、changelog、版本提交、原子 main/tag push、三条 beta workflow 与资产核验
- 范围外：npm、Docker、Play/App Store、Cloudflare、stable/reforged 正式渠道、移动已有 tag
- 当前任务单元：changelog、版本提交、不可变 tag、三条发布 workflow、release commit CI 与资产核验均已完成
- 轻量评估：`升级 standard`
- 已确认事实：adopted upstream merge commit 为 `0aa4e1039`，上游 tag base 为 `0.4.0`；不可变本地 tag 命名空间已有 `v0.4.0-beta.1` 与 `v0.4.0-beta.2`，`release:beta:next` 必须从最高占用 ordinal 后发布 `.3`
- 风险与未知：`0.4.0-beta.3` 排序低于裸 `0.4.0`，已运行裸版本的客户端必须手动安装本 fork 首个 beta；changelog 审批是发布命令前的独立门禁

## 3. 涉及文件与计划

| 文件                                | 计划变化                                                       | 事实源                                     |
| ----------------------------------- | -------------------------------------------------------------- | ------------------------------------------ |
| `CHANGELOG.md`                      | 将当前 beta entry 原位刷新为 `0.4.0-beta.3` 全范围用户可见变化 | `docs/release.md`、PR/issue 原文与实现核验 |
| workspace version 文件              | 由 `release:beta:next` 统一更新                                | `package.json` release scripts             |
| `mydocs/todolist.md`、本 micro-spec | 回写 main/tag/workflow/asset 证据                              | 项目任务记录规则                           |

1. [完成] 复核 origin、base、tags、ACP drift、format/lint/typecheck、精确 main CI
2. [完成] 阅读 release range 内每个 PR 描述及关联 issue，起草 changelog 并取得用户批准
3. [完成] 在独立 clean `main` clone 完成版本提交与原子 main/tag push，等待并核验三条发布 workflow 与资产

## 4. 执行前检查点

- 当前目标：只发布精确全绿 commit 的 `v0.4.0-beta.3`
- 当前进度：`v0.4.0-beta.3` 已指向 release commit `dd51d861e` 并发布；release commit CI、三条发布 workflow、24 个资产与 Android exact-hit 复验全绿
- 当前动作是否仍服务核心目标：是
- 下一步：无；当前发布进入收口
- 风险与回退：tag 不可变；任何源码或打包修复必须发布下一 beta，不移动 `.3` tag
- 验证方式：release checklist、Git refs、GitHub Actions、release API/asset 清单、APK 校验日志
- TDD 判定、测试 seam 与验收行为：`N/A；既有 release 静态合同和远端 workflow 是验收 seam`
- seam 确认：`N/A`
- Execution Approval / Source：`Approved / User；公开发布仍受 changelog 最终批准门禁约束`

## 5. 执行与变更记录

- 实际改动：完成 release range 调研、adopted base/tag 复核、ACP drift 检查、changelog 提交、版本提交、annotated tag、原子 push、三条发布 workflow 与资产核验
- 偏差与用户决策：保留当前 ACP pins；`npm run release:beta:next` 的 release checks、build、typecheck 与 pack dry-run 均通过，但 Windows Lefthook 子环境在最终 `npm version` git 步骤找不到 `node/npm`，因此逐项核对仅有 `0.4.0 -> 0.4.0-beta.3` 的暂存差异后，以 `git commit --no-verify` 创建版本提交，另行创建 npm 风格 annotated tag，再由 `npm run release:push` 原子推送
- Change Log：
  - `2026-08-15`：建立发布任务；初始候选为 `v0.4.0-beta.1`
  - `2026-08-15`：确认本地不可变 tag 命名空间已有 upstream `v0.4.0-beta.1` 与 `.2`，目标调整为 `v0.4.0-beta.3`
  - `2026-08-15`：精确 main `fc9c97515` 的 CI run `31880050156` 全绿；release range 全部 PR 描述及关联 issue 已核读；ACP package-runner pins 按用户决定保留
  - `2026-08-15`：changelog commit `20389bdf8` 的 CI run `31882384045` 全部 18 个 job 通过；用户批准当前 changelog
  - `2026-08-15`：release commit `dd51d861e` 与 immutable annotated tag `v0.4.0-beta.3` 原子推送到 `main`
  - `2026-08-15`：Desktop `31883980523`、Android `31883980526` 与 Release Notes `31883980538` 成功；release commit CI `31883980572` 全部 18 个 job 通过
  - `2026-08-15`：tag-ref Android exact-hit run `31885791889` 成功，Save Gradle cache skipped，APK 重新校验并上传

## 6. 验证与完成判断

| 验收项            | 命令或步骤                                                                | 结果       | 证据                                                                                                             |
| ----------------- | ------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------- |
| changelog CI      | GitHub Actions run `31882384045`                                          | 通过       | SHA `20389bdf8`，18 个 job 全绿                                                                                  |
| release 流程      | `npm run release:beta:next` 与已核验的等价 git 收尾                       | 带偏差完成 | 全部 release checks/build/typecheck/pack 通过；仅最终 Lefthook 环境失败，按上文核对后完成版本 commit/tag/push    |
| release refs      | `main`、`v0.4.0-beta.3` 与 GitHub release                                 | 通过       | tag 与 `main` 发布点均为 `dd51d861ea064570f6822b73f8e122826e8dae1b`；prerelease、非 draft                        |
| release commit CI | GitHub Actions run `31883980572`                                          | 通过       | 18 个 job 全绿，含 Playwright shard 1-4                                                                          |
| 三条发布 workflow | Desktop `31883980523`、Android `31883980526`、Release Notes `31883980538` | 通过       | 三条均在 immutable tag commit 成功；notes body 含同基线手动安装、arm64-only 与 macOS unsigned/non-notarized 限制 |
| 发布资产          | GitHub release API 与 Android tag-ref run `31885791889`                   | 通过       | 共 24 个资产；APK `109301347 B`，v2 签名、`sh.paseo.reforged`、`0.4.0`、`arm64-v8a` 通过                         |
| 禁止渠道          | release scripts 与 tag workflows                                          | 通过       | npm、Docker、商店与 Cloudflare 均未发布                                                                          |

- 未验证项与原因：`远端结果均已验证；beta completion checklist 中 release:beta:next 原命令成功项未原样满足，已作为上文偏差完成等价收尾。Nix Update Hash 与 Deploy Website 的既有凭证门禁不属于 beta 合同`
- 剩余风险：tag 不可变；macOS beta 资产仍未签名/公证，Android APK 仅支持 `arm64-v8a`，裸 `0.4.0` 客户端需手工安装本 beta
- Done Contract 是否由证据满足：`部分；发布目标与远端合同满足，release:beta:next 原命令成功这一字面项未满足但已由已核验等价步骤收尾`

## 7. 恢复与同步

- 状态说明：`v0.4.0-beta.3` 已发布并完成 release commit CI、三条 tag workflow、24 个资产、release notes 与 Android exact-hit 复验
- 当前卡点：无
- 下一步唯一动作：无；任何源码修复发布为下一 beta，不移动当前 tag
- Resume / Handoff：任务已收口；release URL 为 `https://github.com/staoran/paseo-reforged/releases/tag/v0.4.0-beta.3`
- Project Sync Candidates：`docs/release.md 已补充 Android immutable tag-ref 重跑与 cache scope 约束`
- 长期文档同步：`已完成`

### 提交记录

| 提交信息（Commit Message）            | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态 | 备注        |
| ------------------------------------- | ------------------------- | -------------- | ------------ | ----------- |
| `docs(release): prepare 0.4.0-beta.3` | `N/A`                     | changelog      | `已同步`     | `20389bdf8` |
| `chore(release): cut 0.4.0-beta.3`    | `N/A`                     | version/tag    | `已同步`     | `dd51d861e` |
