# 发布 0.4.0 Beta 测试版 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                           |
| ------------------ | ------------------------------------------------------------ |
| task_id            | `0071`                                                       |
| spec layer         | `Feature Spec`                                               |
| task status        | `执行中`                                                     |
| document status    | `Active`                                                     |
| depth              | `standard`                                                   |
| phase              | `Plan`                                                       |
| Execution Approval | `Approved`                                                   |
| Approval Source    | `User；2026-08-15 当前消息明确要求 CI 全绿后提交并发布 beta` |
| file path          | `mydocs/micro_specs/0071_发布0.4.0_beta测试版.md`            |
| parent spec        | `N/A`                                                        |
| superseded by      | `N/A`                                                        |
| created / updated  | `2026-08-15`                                                 |

## 1. 目标与完成契约

- 当前理解：将已经采用 upstream `0.4.0` 的最新 Reforged `main` 在 CI 全绿后发布为首个同基线 beta
- 核心目标：按项目 beta flow 发布不可变 `v0.4.0-beta.1`，产出桌面与 arm64-v8a APK 测试资产，不发布 npm、Docker、商店或 Cloudflare
- Done Contract：精确 main clean 且 CI 全绿；adopted base、同基线 tag 与目标版本复核；ACP pins 保留；changelog 按 previous-stable-to-HEAD 更新并经用户批准；`release:beta:next` 成功；Desktop、Android、Release Notes 三条 workflow 全绿；prerelease、24 个预期资产和 APK ABI 核验通过

## 2. 范围与事实

- 范围内：release 准备、changelog、版本提交、原子 main/tag push、三条 beta workflow 与资产核验
- 范围外：npm、Docker、Play/App Store、Cloudflare、stable/reforged 正式渠道、移动已有 tag
- 当前任务单元：等待 0069/0070 集成与精确 main CI 全绿后执行既有 beta flow
- 轻量评估：`升级 standard`
- 已确认事实：adopted upstream merge commit 为 `0aa4e1039`，上游 tag base 为 `0.4.0`；当前最高同基线 beta tag 尚待发布前远端复核
- 风险与未知：`0.4.0-beta.1` 排序低于裸 `0.4.0`，已运行裸版本的客户端必须手动安装首个 beta；changelog 审批是发布命令前的独立门禁

## 3. 涉及文件与计划

| 文件                                | 计划变化                                                       | 事实源                                     |
| ----------------------------------- | -------------------------------------------------------------- | ------------------------------------------ |
| `CHANGELOG.md`                      | 将当前 beta entry 原位刷新为 `0.4.0-beta.1` 全范围用户可见变化 | `docs/release.md`、PR/issue 原文与实现核验 |
| workspace version 文件              | 由 `release:beta:next` 统一更新                                | `package.json` release scripts             |
| `mydocs/todolist.md`、本 micro-spec | 回写 main/tag/workflow/asset 证据                              | 项目任务记录规则                           |

1. 复核 origin、base、tags、ACP drift、format/lint/typecheck、精确 main CI
2. 阅读 release range 内每个 PR 描述及关联 issue，起草 changelog 并展示给用户批准
3. 在独立 clean `main` clone 执行 `release:beta:next`，等待并核验三条发布 workflow 与资产

## 4. 执行前检查点

- 当前目标：只发布精确全绿 commit 的 `v0.4.0-beta.1`
- 当前进度：发布授权已给出；代码修复、CI 与 changelog 门禁尚未完成
- 当前动作是否仍服务核心目标：是
- 下一步：完成 0069/0070 并把精确 main 推到全绿
- 风险与回退：tag 不可变；发布命令前任何代码变化都需重新走 CI；changelog 未批准不得执行发布命令
- 验证方式：release checklist、Git refs、GitHub Actions、release API/asset 清单、APK 校验日志
- TDD 判定、测试 seam 与验收行为：`N/A；既有 release 静态合同和远端 workflow 是验收 seam`
- seam 确认：`N/A`
- Execution Approval / Source：`Approved / User；公开发布仍受 changelog 最终批准门禁约束`

## 5. 执行与变更记录

- 实际改动：`待执行`
- 偏差与用户决策：`保留当前 ACP pins`
- Change Log：`2026-08-15 建立发布任务；目标候选为 v0.4.0-beta.1，待远端 tag 复核`

## 6. 验证与完成判断

| 验收项       | 命令或步骤                      | 结果   | 证据 |
| ------------ | ------------------------------- | ------ | ---- |
| 精确 main CI | GitHub Actions                  | 待执行 |      |
| beta release | `npm run release:beta:next`     | 待执行 |      |
| 发布资产     | 三条 workflow 与 GitHub release | 待执行 |      |

- 未验证项与原因：尚未进入发布准备
- 剩余风险：尚未执行
- Done Contract 是否由证据满足：`否`

## 7. 恢复与同步

- 状态说明：发布任务已落盘，等待 0069/0070
- 当前卡点：精确 main 尚未 CI 全绿；changelog 尚未起草和批准
- 下一步唯一动作：完成并推送 0069/0070
- Resume / Handoff：先读 0069、0070 的完成证据，再从本文件第 3 节动作 1 继续
- Project Sync Candidates：`无；既有 docs/release.md 已覆盖流程`
- 长期文档同步：`不需要`

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联改动或阶段 | 文档同步状态 | 备注 |
| -------------------------- | ------------------------- | -------------- | ------------ | ---- |
| `<待提交>`                 | `N/A`                     | `0071`         | `待回写`     |      |
