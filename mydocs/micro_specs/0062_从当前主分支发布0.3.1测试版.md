# 从当前主分支发布 0.3.1 测试版 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                       |
| ------------------ | -------------------------------------------------------- |
| task_id            | `0062`                                                   |
| spec layer         | `Feature Spec`                                           |
| task status        | `发布中`                                                 |
| document status    | `Active`                                                 |
| depth              | `deep`                                                   |
| phase              | `Execute`                                                |
| Execution Approval | `Approved（CI 修复、提交、推送与公开 beta 发布）`        |
| Approval Source    | `User / 2026-08-13 明确批准 CI 修复并继续发测试版`       |
| file path          | `mydocs/micro_specs/0062_从当前主分支发布0.3.1测试版.md` |
| parent spec        | `N/A`                                                    |
| superseded by      | `N/A`                                                    |
| created / updated  | `2026-08-13`                                             |

## 1. 目标与完成契约

- 当前理解：最新 `main` 已推送；修复该精确提交暴露的 App、Server 与 Playwright CI 失败，推送修复并等待新 HEAD 全绿，再发布一版 Paseo Reforged beta
- 核心目标：以已采用的 upstream `0.3.1` 为三段式基线发布下一可用 beta，并核验不可变 tag、GitHub prerelease、Desktop Release、Android APK Release 与 Release Notes Sync
- Done Contract：目标版本和 changelog 获批；本地发布门禁通过；`main` 与不可变 tag 成功推送；三条发布 workflow 全绿；release notes 与资产核验通过；npm、Docker、商店、Cloudflare 和稳定渠道均未发布

## 2. 范围与事实

- 范围内：任务记录、`CHANGELOG.md` 当前 beta 条目、已批准的 App/Server/Playwright CI 修复、format/lint/typecheck、ACP catalog drift、release prepare/check、版本 manifests、发布提交与 tag、GitHub prerelease 和三条发布 workflow 核验
- 范围外：与 CI 失败无关的产品改动或重构、stable/formal release、npm/GHCR、App Store/Play Store、Cloudflare、移动或强推既有 tag
- 当前任务单元：修复当前 `main` 的发布阻塞并发布单个 beta，失败时保留不可变 tag 规则并按文档选择重试或下一 beta
- 轻量评估：`升级 deep`；范围仍是单项目且失败清单明确，但横跨 App、Server、Windows 与 Playwright 行为，需逐项诊断和分层验证
- 已确认事实：准备开始时 `main=ca36e8308` 且工作区干净，当前仅有受控发布文档改动；刷新后相对 `origin/main` 为 `0 behind / 45 ahead`；`origin` 精确为 `https://github.com/staoran/paseo-reforged.git`；upstream `v0.3.1` 已由合并提交 `948fd4a33` 纳入当前历史，根版本为 `0.3.1`；本地 upstream `v0.4.0-beta.1/.2` 均不是当前 `main` 的祖先，不能作为已采用基线
- `grilling` 结论（如使用）：`N/A；版本和发布范围可由仓库事实收敛`
- 风险与未知：用户已决定本次保留 ACP catalog 的 11 个 drift/stale pins；依赖审计报告 99 项现有漏洞（7 low、33 moderate、52 high、7 critical）；`36859c0` 已推送但主 CI 失败，部分失败是过期测试、部分是真实行为回归；GitHub CLI keyring token 在修复收口时失效，Git 远程读取仍正常；同基线 `0.3.1-beta.1` 的 SemVer 排序低于裸 `0.3.1`，已安装 `0.3.1` 的客户端需手动安装首个 beta

## 3. 涉及文件与计划

| 文件或系统                                         | 计划变化或动作                                         | 事实源                                 |
| -------------------------------------------------- | ------------------------------------------------------ | -------------------------------------- |
| `CHANGELOG.md`                                     | 原位刷新当前 beta 条目为用户向完整发布说明             | `docs/release.md#Changelog policy`     |
| `packages/app`                                     | 修复已批准的 App unit 与 Playwright CI 失败            | CI 工件、定向测试、既有产品合同        |
| `packages/server`                                  | 修复已批准的 Server unit/Windows CI 失败               | CI 日志、定向 Vitest                   |
| 根与 workspace `package.json`、`package-lock.json` | 由 `release:beta:next` 同步为批准后的目标版本          | 版本脚本与 release flow                |
| `mydocs/todolist.md`、本 micro-spec                | 记录门禁、批准、发布与验收证据                         | 项目任务记录规则                       |
| GitHub                                             | 推送 `main` 与不可变 tag，核验 prerelease 和 workflows | `docs/release.md#Completion checklist` |

1. 使用已有失败测试逐项修复 App、Server 与 Playwright CI，产品合同已变化的用例只更新过期断言
2. 运行受影响单文件、format、lint、typecheck，提交并推送独立 CI 修复，等待新精确 HEAD 主 CI 全绿
3. 执行 `npm run release:beta:next`，最后核验远端 refs、prerelease、release notes、资产和三条 workflow

## 4. 执行前检查点

- 当前目标：把已批准的最新 `main` 推送并通过 CI，再发布 `0.3.1-beta.1`
- 当前进度：`main` 与 `origin/main` 均为 `4ff2689`；第一轮 CI 修复已推送，剩余两个 Playwright 失败已完成定向修复，待提交并由新 HEAD 的 Linux CI 验证
- 当前动作是否仍服务核心目标：是；CI 全绿是发布脚本前的强制门禁
- 下一步：逐项完成已批准 CI 修复并推送，等待新精确 HEAD 主 CI 全绿后执行 `npm run release:beta:next`
- 风险与回退：任何最后一分钟文件变化都需单独审阅；tag 创建后不可移动；最新代码需先推送并通过 CI；不触碰 `6767` daemon
- 验证方式：命令退出码、clean status、版本脚本 `--print`、Git refs、GitHub Actions 与 Release API
- TDD 判定、测试 seam 与验收行为：`TDD；沿用已失败的 App/Server 单文件测试和 Playwright 用户流作为 Red，验收为对应行为 Green 且无扩大产品合同`
- seam 确认：`User；2026-08-13 批准已展示的 CI 修复清单，seams 为现有公开 UI 流、daemon 服务接口和测试夹具`
- Execution Approval / Source：`Approved / User；2026-08-13 明确批准上述 CI 修复并继续发测试版；ACP pins 继续保留`

## 5. 执行与变更记录

- `2026-08-13`：本轮已完成已批准的 timeline summary 分页上限、新 epoch 本地用户消息保留、Agent retry 启动竞态、inline code 行高和跨平台测试合同修复；定向测试、`npm run build:client`、`npm run typecheck`、`npm run lint`和 `git diff --check` 已通过
- `2026-08-13`：`npm run acp:version-drift:check` 实际报告 checked 15 / drift 11 / skipped 21；按用户要求保留当前 ACP pins，不运行 `acp:version-drift:update`
- `2026-08-13`：剩余 shard 1 失败定位为 `DraftPanel.handleCreated` 绕过 `upsertAgentReplica`，running create snapshot 未同步到 `agentTurnLiveness`；改用统一 replica 写入后，原始 authoritative hydration Playwright 用例 1/1 通过
- `2026-08-13`：剩余 shard 4 用例在 `4ff2689` 中误删三页分页动作却继续断言第三页内容；已恢复真实分页并把旧 daemon page ownership 合同改为无重复。对应 reducer 单文件 122/122 通过；完整 E2E 在 Windows 于测试前命中既有 `spawnSync npm.cmd EINVAL`，由 Linux CI 复验

- 实际改动：已创建并推送 `0.3.1-beta.1` 用户向 changelog；本轮已完成已批准的 App、Client、Server 和跨平台测试修复；尚未修改版本 manifest、tag 或远端发布 refs
- 偏差与用户决策：用户补充确认必须推送本地 `main` 的最新代码后再发 beta；`36859c0` CI 失败后又明确批准定向 CI 修复、提交、推送和继续公开 beta；ACP pins 按用户决定保持现状
- Change Log：
  - `2026-08-13`：用户要求从当前主分支发布测试版；开始 `release-beta` 两阶段流程
  - `2026-08-13`：确认 adopted upstream base 为 `0.3.1`；本地 upstream `v0.4.0-beta.*` 未合入当前 `main`，不改变本次三段式基线
  - `2026-08-13`：`npm run format` 通过；oxfmt 写回时间戳触发 Git for Windows `diff-files` stat 假阳性，过滤后的 blob 哈希与 `HEAD` 一致，索引重算后仅剩本任务记录变化且无产品文件 diff
  - `2026-08-13`：`npm run lint`、`npm run typecheck` 与 Release Notes Sync 定向测试 4/4 通过；中断安装导致的 React Native 补丁部分应用已通过精确重装受影响依赖恢复，未修改产品代码
  - `2026-08-13`：`npm run release:prepare` 通过，全部仓库补丁成功应用；`npm run release:check` 完整通过，六个发布 workspace 的 typecheck/build/pack dry-run 成功
  - `2026-08-13`：ACP drift 检查确认 15 个可检查项中 11 个 stale pins；依赖审计报告 99 项现有漏洞，均作为发布前发现展示，不自动修改依赖
  - `2026-08-13`：GitHub CLI 登录恢复；`origin/main=59ff692bb` 的 CI 与 Docker 通过，本地 `main=ca36e830` 领先 45 个提交且尚无该精确 HEAD 的远端 CI
  - `2026-08-13`：用户明确回复 `go ahead`，批准 changelog、目标版本、推送最新 `main` 和公开 beta 发布，并决定本次保留 11 个 ACP stale pins
  - `2026-08-13`：发布准备提交 `36859c0` 已推送且与 `origin/main` 一致；精确 HEAD 主 CI run `31698387882` 失败，按发布门禁暂停打 tag
  - `2026-08-13`：定向诊断确认失败同时包含过期测试合同与真实行为回归；用户明确批准已展示的 CI 修复清单并继续发测试版，任务深度升级为 Light `deep`
  - `2026-08-13`：一次误作纯预览的 `release:beta:next -- --print` 在 `release:prepare` 的 `npm install` 阶段被完整终止；未进入版本写入或推送，工作区 clean、无 beta tag、无残留进程
  - `2026-08-13`：第一轮 CI 修复提交 `4ff2689` 已推送；精确 HEAD 主 CI run `31746889748` 仅余 Playwright shard 1/4 两项失败，Docker run `31746889727` 通过
  - `2026-08-13`：剩余两项修复已完成；shard 1 原始 Playwright 1/1、`agent-directory-sync.test.ts` 7/7、`session-stream-reducers.test.ts` 122/122 通过；所有本轮测试进程和端口已回收

## 6. 验证与完成判断

| 验收项      | 命令或步骤                                                | 结果   | 证据                                                              |
| ----------- | --------------------------------------------------------- | ------ | ----------------------------------------------------------------- |
| 分支与远端  | `git status`、`fetch`、divergence、remote URL             | 通过   | 修复前 `main=origin/main=4ff2689`；仅有受控任务改动与用户并行文档 |
| 目标版本    | adopted base、tag 占用、版本脚本 `--print`                | 通过   | 精确输出 `0.3.1-beta.1`                                           |
| 本地门禁    | format、lint、typecheck、ACP drift、release prepare/check | 通过   | 本轮目标文件格式化、lint、typecheck 与定向测试通过；ACP pins 保留 |
| changelog   | 用户向、原位、完整范围、严格标题、用户批准                | 已批准 | 用户明确 go ahead；Release Notes Sync 定向测试 4/4 通过           |
| GitHub 发布 | refs、prerelease、三条 workflow、release notes、资产      | 阻塞   | `4ff2689` 主 CI run `31746889748` 失败；剩余修复待推送验证        |
| 禁止范围    | npm、Docker、store、stable、Cloudflare                    | 待验证 | 发布后核对                                                        |

- 未验证项与原因：旧 daemon 完整 E2E 在 Windows 于测试逻辑前命中 `spawnSync npm.cmd EINVAL`，需 Linux CI 验证；修复后的精确 HEAD CI、最终发布命令和所有公开发布结果尚未验证
- 剩余风险：依赖树有 99 项现有漏洞；本次有意保留 11 个 ACP stale pins；首个同基线 beta 需从裸 `0.3.1` 手动安装
- Done Contract 是否由证据满足：否；当前仅完成事实核对和任务落盘

## 7. 恢复与同步

- 状态说明：公开发布与 CI 修复均已获批准；发布准备和第一轮 CI 修复已推送，剩余两个 Playwright 修复及本地静态检查已完成，尚无版本变更或 tag
- 当前卡点：无；待提交并推送修复后的精确 HEAD
- 下一步唯一动作：提交并推送剩余受控 CI 修复，明确排除用户并行任务 `0063` 与 `mydocs/todolist.md`
- Resume / Handoff：从本文件第 4 节继续；不得把“继续”解释为 release go-ahead
- Project Sync Candidates：`无；当前事实已由 docs/release.md 和发布 Skill 覆盖`
- 长期文档同步：暂不需要

### 提交记录

| 提交信息（Commit Message）                      | 提交脚注（Commit Footer） | 关联改动或阶段          | 文档同步状态 | 备注     |
| ----------------------------------------------- | ------------------------- | ----------------------- | ------------ | -------- |
| `36859c086 docs(release): prepare 0.3.1-beta.1` | `N/A`                     | changelog 与任务记录    | 已同步       | 已推送   |
| `4ff2689f2 fix: unblock beta release CI`        | `N/A`                     | 第一轮 CI 修复          | 已同步       | 已推送   |
| `<待提交>`                                      | `N/A`                     | 剩余 Playwright CI 修复 | 待填写       | 已获批准 |
| `<待提交>`                                      | `N/A`                     | release version 与 tag  | 待填写       | 已获批准 |
