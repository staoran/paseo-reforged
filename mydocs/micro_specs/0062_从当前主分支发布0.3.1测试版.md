# 从当前主分支发布 0.3.1 测试版 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                                   |
| ------------------ | -------------------------------------------------------------------- |
| task_id            | `0062`                                                               |
| spec layer         | `Feature Spec`                                                       |
| task status        | `发布中`                                                             |
| document status    | `Active`                                                             |
| depth              | `deep`                                                               |
| phase              | `Execute`                                                            |
| Execution Approval | `Approved（免费 GitHub runner 的 arm64-only APK 修复与继续发布）`    |
| Approval Source    | `User / 2026-08-14 批准 arm64-v8a GitHub runner 方案并继续当前 beta` |
| file path          | `mydocs/micro_specs/0062_从当前主分支发布0.3.1测试版.md`             |
| parent spec        | `N/A`                                                                |
| superseded by      | `N/A`                                                                |
| created / updated  | `2026-08-13 / 2026-08-14`                                            |

## 1. 目标与完成契约

- 当前理解：`0.3.1-beta.1` 的 release commit、不可变 tag、prerelease、桌面资产与 release notes 已发布；只剩 Android APK 因三次 runner 服务外部取消未产出。用户只接受免费构建，已批准在 GitHub runner 上仅编译 `arm64-v8a` 以缩短 native 构建时间
- 核心目标：以已采用的 upstream `0.3.1` 为三段式基线发布下一可用 beta，并核验不可变 tag、GitHub prerelease、Desktop Release、Android APK Release 与 Release Notes Sync
- Done Contract：目标版本和 changelog 获批；本地发布门禁通过；`main` 与不可变 tag 成功推送；三条发布 workflow 全绿；release notes 与资产核验通过；npm、Docker、商店、Cloudflare 和稳定渠道均未发布

## 2. 范围与事实

- 范围内：任务记录、`CHANGELOG.md` 当前 beta 条目、已批准的 App/Server/Playwright CI 修复、Android workflow Gradle 缓存、format/lint/typecheck、ACP catalog drift、release prepare/check、版本 manifests、发布提交与 tag、GitHub prerelease 和三条发布 workflow 核验
- 范围外：与 CI 失败无关的产品改动或重构、stable/formal release、npm/GHCR、App Store/Play Store、Cloudflare、移动或强推既有 tag
- 当前任务单元：修复当前 `main` 的发布阻塞并发布单个 beta，失败时保留不可变 tag 规则并按文档选择重试或下一 beta
- 轻量评估：`升级 deep`；范围仍是单项目且失败清单明确，但横跨 App、Server、Windows 与 Playwright 行为，需逐项诊断和分层验证
- 已确认事实：准备开始时 `main=ca36e8308` 且工作区干净，当前仅有受控发布文档改动；刷新后相对 `origin/main` 为 `0 behind / 45 ahead`；`origin` 精确为 `https://github.com/staoran/paseo-reforged.git`；upstream `v0.3.1` 已由合并提交 `948fd4a33` 纳入当前历史，根版本为 `0.3.1`；本地 upstream `v0.4.0-beta.1/.2` 均不是当前 `main` 的祖先，不能作为已采用基线
- `grilling` 结论（如使用）：`N/A；版本和发布范围可由仓库事实收敛`
- 风险与未知：用户已决定本次保留 ACP catalog 的 13 个 drift/stale pins；依赖审计报告 99 项现有漏洞（7 low、33 moderate、52 high、7 critical）；五次 Android build 均在 APK 产出前收到 runner shutdown 或 lost communication，最新 heartbeat run `31793673753` 运行约 65 分钟后失联且日志 blob 持续 `BlobNotFound`；arm64-only APK 不支持 32 位 ARM 设备或 x86/x86_64 模拟器；同基线 `0.3.1-beta.1` 的 SemVer 排序低于裸 `0.3.1`，已安装 `0.3.1` 的客户端需手动安装首个 beta

## 3. 涉及文件与计划

| 文件或系统                                         | 计划变化或动作                                         | 事实源                                 |
| -------------------------------------------------- | ------------------------------------------------------ | -------------------------------------- |
| `CHANGELOG.md`                                     | 原位刷新当前 beta 条目为用户向完整发布说明             | `docs/release.md#Changelog policy`     |
| `packages/app`                                     | 修复已批准的 App unit 与 Playwright CI 失败            | CI 工件、定向测试、既有产品合同        |
| `packages/server`                                  | 修复已批准的 Server unit/Windows CI 失败               | CI 日志、定向 Vitest                   |
| `.github/workflows/android-apk-release.yml`        | 仅编译 `arm64-v8a` 并校验 APK 的唯一 native ABI        | 三次 runner shutdown、Gradle 属性合同  |
| `scripts/ci-workflow.test.mjs`                     | 固化 arm64 构建参数与 ABI 校验门禁                     | 现有 Android workflow 静态合同测试     |
| `docs/release.md`、`docs/android.md`               | 记录 beta APK 的 arm64-only 兼容边界                   | 发布与 Android 权威文档                |
| 根与 workspace `package.json`、`package-lock.json` | 由 `release:beta:next` 同步为批准后的目标版本          | 版本脚本与 release flow                |
| `mydocs/todolist.md`、本 micro-spec                | 记录门禁、批准、发布与验收证据                         | 项目任务记录规则                       |
| GitHub                                             | 推送 `main` 与不可变 tag，核验 prerelease 和 workflows | `docs/release.md#Completion checklist` |

1. 已使用失败测试逐项修复 App、Server 与 Playwright CI，并在 release commit 精确 SHA 上取得主 CI、Docker、Desktop 与 Release Notes Sync 成功证据
2. 已在 `main` 提交 workflow-only Gradle 缓存修复且未移动 `v0.3.1-beta.1`；第三次 dispatch 验证 checkout 正确，但 runner shutdown 使 build 取消且 save 被跳过
3. 已废弃两 job 限时预热候选；按用户批准在 Build step 通过 Gradle project property 只编译 `arm64-v8a`，并在上传前要求 APK `native-code` 精确只有该 ABI
4. heartbeat run 仍在宿主失联时中断；在保留 arm64-only 和应用内存配置的前提下，为免费 runner 提供临时 2 GiB swap，用单一资源变量复验实际 Actions build
5. Android 成功后核验 APK 签名、包名、版本、唯一 ABI、release 资产与禁止发布范围，再完成任务记录

## 4. 执行前检查点

- 当前目标：完成 `v0.3.1-beta.1` 缺失的 Android APK，并保持 tag 与应用源码不变
- 当前进度：`main=origin/main=17ff99b4c`，tag 仍为 `cec97b8b9`；2 GiB swap 提交已推送，但 GitHub 在 workflow 载入阶段生成无 job 的失败 run `31814878881`，尚未进入 Android 构建
- 当前动作是否仍服务核心目标：是；新增的 job-level `${{ runner.temp }}` 是相对上一个有效 workflow 的唯一载入时表达式，将路径改为 step 运行时由 `$RUNNER_TEMP` 初始化
- 下一步：移除 job-level `runner` 上下文，在 swap step 写入 `ANDROID_BUILD_SWAP_FILE` 到 `$GITHUB_ENV`；本地复验后单独提交推送，等待新精确 SHA 的 CI/Docker 和 workflow 载入证据
- 风险与回退：2 GiB swap 会占用 runner 临时磁盘并可能降低构建速度，但不改变 APK 内容或 EAS 资源级别；若提前分配失败将明确终止而不进入构建；若仍失联，不再叠加未证实 Gradle 参数
- 验证方式：YAML 解析、定向 workflow 合同测试、format/lint/typecheck、Git refs、Actions step 结果、APK `apksigner`/`aapt` 日志和 GitHub Release API
- TDD 判定、测试 seam 与验收行为：`N/A；外部 hosted-runner shutdown 无法构造秒级确定性本地 Red，静态合同只能证明 arm64、心跳与校验门禁存在；真实验收 seam 是 Actions run 和产出的 APK native-code`
- seam 确认：`N/A；不写产品测试`
- Execution Approval / Source：`Approved / User；2026-08-14 明确采用免费 arm64-v8a GitHub runner 方案并继续当前 beta；ACP pins 继续保留`

## 5. 执行与变更记录

- `2026-08-13`：本轮已完成已批准的 timeline summary 分页上限、新 epoch 本地用户消息保留、Agent retry 启动竞态、inline code 行高和跨平台测试合同修复；定向测试、`npm run build:client`、`npm run typecheck`、`npm run lint`和 `git diff --check` 已通过
- `2026-08-13`：`npm run acp:version-drift:check` 实际报告 checked 15 / drift 13 / skipped 21；按用户要求保留当前 ACP pins，不运行 `acp:version-drift:update`
- `2026-08-13`：剩余 shard 1 失败定位为 `DraftPanel.handleCreated` 绕过 `upsertAgentReplica`，running create snapshot 未同步到 `agentTurnLiveness`；改用统一 replica 写入后，原始 authoritative hydration Playwright 用例 1/1 通过
- `2026-08-13`：剩余 shard 4 用例在 `4ff2689` 中误删三页分页动作却继续断言第三页内容；已恢复真实分页，并保留旧 daemon 原始响应允许跨页重复、客户端 projection anchor 保证唯一渲染 ownership 的兼容合同。对应 reducer 单文件 122/122 通过；完整 E2E 在 Windows 于测试前命中既有 `spawnSync npm.cmd EINVAL`，由 Linux CI 复验
- `2026-08-13`：`04df943` 精确 HEAD 的 Docker run `31751209842` 通过；主 CI run `31751209774` 的 16 个非失败 jobs 通过，Playwright shard 1/4 与 2/4 已绿，shard 3/4 与 4/4 失败，继续按已批准范围修复后再发布
- `2026-08-13`：shard 3 定向 Red/Green 确认当前 `main` 的 replica cache 仍使用 Web `AsyncStorage`/localStorage；测试在旧文档直接删 `messageId` 后，reload 的 `pagehide` flush 会用内存权威状态覆盖该改写。把 legacy 改写注册为新文档 init script，使其发生在旧页 flush 后、App restore 前；精确 Playwright 1/1 通过
- `2026-08-13`：shard 4 trace 与引入兼容测试的 `26bdb1d` 历史确认旧 `0.2.5` daemon 会跨页重复 projected entry，客户端按 projection anchor 只渲染唯一归属页；`04df943` 将原本证明该兼容场景的 `expectRepeatedEntries()` 误改成了 `expectNoRepeatedEntries()`，恢复原测试合同且不改 reducer
- `2026-08-14`：剩余 Playwright 修复提交 `a3bafd111` 已推送；精确 SHA 的 Docker run `31755113599` 成功且不发布镜像，主 CI run `31755113589` 除 Windows server 外全部通过，包括四个 Playwright shard
- `2026-08-14`：Windows server 仅 `workspace-git-service.observation.test.ts` 两个用例失败；同文件本地首次 37/37 通过，但两个精确用例循环在第 8 次复现相同的首次窄刷新未启动，确认不是产品回归或纯基础设施失败
- `2026-08-14`：根因是 observation setup、初始快照与后台 fetch 使用真实 Promise 链，而测试在阶段尚未建立时推进 fake timer，`vi.waitFor` 又按 fake time 快速耗尽；改为等待初始 listener、fetch settle 与刷新阶段的明确 signal，不修改 `WorkspaceGitService` 产品逻辑

- 实际改动：已创建并推送 `0.3.1-beta.1` 用户向 changelog、版本 manifests、release commit 与不可变 tag；已完成已批准的 App、Client、Server、跨平台测试和首次 Android workflow 缓存修复
- 偏差与用户决策：用户补充确认必须推送本地 `main` 的最新代码后再发 beta；`36859c0` CI 失败后又明确批准定向 CI 修复、提交、推送和继续公开 beta；ACP pins 按用户决定保持现状
- Change Log：
  - `2026-08-13`：用户要求从当前主分支发布测试版；开始 `release-beta` 两阶段流程
  - `2026-08-13`：确认 adopted upstream base 为 `0.3.1`；本地 upstream `v0.4.0-beta.*` 未合入当前 `main`，不改变本次三段式基线
  - `2026-08-13`：`npm run format` 通过；oxfmt 写回时间戳触发 Git for Windows `diff-files` stat 假阳性，过滤后的 blob 哈希与 `HEAD` 一致，索引重算后仅剩本任务记录变化且无产品文件 diff
  - `2026-08-13`：`npm run lint`、`npm run typecheck` 与 Release Notes Sync 定向测试 4/4 通过；中断安装导致的 React Native 补丁部分应用已通过精确重装受影响依赖恢复，未修改产品代码
  - `2026-08-13`：`npm run release:prepare` 通过，全部仓库补丁成功应用；`npm run release:check` 完整通过，六个发布 workspace 的 typecheck/build/pack dry-run 成功
  - `2026-08-13`：ACP drift 检查确认 checked 15 / drift 13 / skipped 21；依赖审计报告 99 项现有漏洞，均作为发布前发现展示，不自动修改依赖
  - `2026-08-13`：GitHub CLI 登录恢复；`origin/main=59ff692bb` 的 CI 与 Docker 通过，本地 `main=ca36e830` 领先 45 个提交且尚无该精确 HEAD 的远端 CI
  - `2026-08-13`：用户明确回复 `go ahead`，批准 changelog、目标版本、推送最新 `main` 和公开 beta 发布，并决定本次保留 ACP pins
  - `2026-08-13`：发布准备提交 `36859c0` 已推送且与 `origin/main` 一致；精确 HEAD 主 CI run `31698387882` 失败，按发布门禁暂停打 tag
  - `2026-08-13`：定向诊断确认失败同时包含过期测试合同与真实行为回归；用户明确批准已展示的 CI 修复清单并继续发测试版，任务深度升级为 Light `deep`
  - `2026-08-13`：一次误作纯预览的 `release:beta:next -- --print` 在 `release:prepare` 的 `npm install` 阶段被完整终止；未进入版本写入或推送，工作区 clean、无 beta tag、无残留进程
  - `2026-08-13`：第一轮 CI 修复提交 `4ff2689` 已推送；精确 HEAD 主 CI run `31746889748` 仅余 Playwright shard 1/4 两项失败，Docker run `31746889727` 通过
  - `2026-08-13`：剩余两项修复已完成；rewind 精确 Playwright 1/1 与全文件 4/4、authoritative hydration Playwright 1/1、`agent-directory-sync.test.ts` 7/7、`session-stream-reducers.test.ts` 122/122 通过
  - `2026-08-13`：`npm run format`、`npm run lint`、`npm run typecheck` 与 `git diff --check` 通过；所有本轮测试进程和临时端口已回收，未触碰 `6767` daemon
  - `2026-08-14`：剩余 Playwright 修复提交 `a3bafd111` 已推送；精确 SHA 的 Docker 通过，主 CI 仅 Windows server observation 测试失败
  - `2026-08-14`：workspace Git observation 的精确两个用例修复前循环在第 8 次复现，稳定化后 20/20 通过；完整文件先通过 37/37，再连续 5 次均 37/37 通过
  - `2026-08-14`：本轮目标文件格式化、lint、全 workspace typecheck 与 `git diff --check` 通过；未触碰用户并行文件或 `6767` daemon
  - `2026-08-14`：Windows server 稳定化提交 `fb0455df7` 与 release commit `cec97b8b9` 已推送；`v0.3.1-beta.1` 不可变 tag 解引用到 `cec97b8b9`
  - `2026-08-14`：Desktop Release `31764185857`、Release Notes Sync `31764185948`、Docker `31764185783` 成功；主 CI `31764185780` 的 Playwright shard 1 首次易失失败，attempt 2 成功
  - `2026-08-14`：Android tag run `31764185813` 与同 tag dispatch `31766785579` 分别约 40、34 分钟后收到宿主 SIGTERM；两次均无 Gradle 编译错误，校验与上传未执行
  - `2026-08-14`：用户批准 Android workflow-only CI 修复；采用唯一 run key 恢复 Gradle 缓存，并用 `always()` 在失败或取消后保存新的部分缓存，dispatch 仍 checkout `v0.3.1-beta.1`
  - `2026-08-14`：缓存修复提交 `92115248b` 已推送；完整 typecheck、Android workflow 定向合同测试、YAML 解析和格式检查通过；最新主 CI `31770200126` 与 Docker `31770200128` 成功
  - `2026-08-14`：Android run `31770318916` 验证 workflow 源为 `92115248b`、checkout 为 `v0.3.1-beta.1 -> cec97b8b9`，自定义 Gradle cache miss；41.2 分钟后 runner service shutdown，build cancelled 且 cache save、APK 校验和上传均跳过
  - `2026-08-14`：GitHub 状态页显示 Actions operational，仓库无 self-hosted runner；候选恢复方案改为独立 job 限时预热并在 runner shutdown 前保存 Gradle cache，尚待新的执行批准
  - `2026-08-14`：核验 Expo EAS Large 为付费资源，用户明确只接受免费方案；不改用 EAS Large
  - `2026-08-14`：用户批准在免费 GitHub runner 上只构建 `arm64-v8a` APK，并继续提交、推送、重跑和验收当前 beta；两 job 预热候选废弃
  - `2026-08-14`：Android Build step 已通过 `ORG_GRADLE_PROJECT_reactNativeArchitectures=arm64-v8a` 限制 Gradle ABI；上传前会拒绝 `native-code` 不是精确单一 `arm64-v8a` 的 APK，发布与 Android 文档已同步兼容边界
  - `2026-08-14`：workflow YAML 解析、arm64 定向合同 1/1、`npm run format`、全 workspace typecheck、lint 与 `git diff --check` 通过；完整 `ci-workflow.test.mjs` 在 Windows 命中既有反斜杠路径前缀断言，新增 Android 合同独立通过，待 Linux 主 CI 完整复验
  - `2026-08-14`：arm64 修复提交 `c45749a28` 已推送；精确 SHA 的主 CI `31784435935` 与 Docker `31784435987` 成功，tag 仍解引用到 `cec97b8b9`
  - `2026-08-14`：Android run `31787040884` 的 workflow head 为 `c45749a28`，immutable tag 校验通过；日志只出现 `arm64-v8a` CMake tasks，证明 Gradle 架构限制生效
  - `2026-08-14`：该 run 在 Build 约 20 分钟后收到 `The runner has received a shutdown signal`，Gradle 未先报错；cache save、APK 校验和上传均跳过。四次失败都发生在 `Done writing sourcemap output` 后，静默至 shutdown 分别约 78、174、130、120 秒
  - `2026-08-14`：成功基线 run `31321031662` 已使用 `--max-workers=1 -Dorg.gradle.parallel=false`，因此不重复添加并发限制；其 Gradle build 约 31 分钟，sourcemap 后约 106 秒恢复输出并最终成功
  - `2026-08-14`：Context7 核验 GitHub-hosted job 官方上限为 6 小时；runner 源码在 `RunnerShutdownToken` 触发时把 job 标为 Failed 并跳过 post steps，与四次日志一致。官方未提供本任务可用的秒级 shutdown 复现 seam
  - `2026-08-14`：诊断假设按资源压力、日志静默回收、随机 hosted-runner 驱逐排序；下一次 Build 每 30 秒输出可用内存与 `$RUNNER_TEMP` 磁盘，既提供心跳，也用真实 run 区分假设
  - `2026-08-14`：心跳修复的 workflow YAML、Git for Windows Bash `-n`、定向静态合同 1/1、完整 format、全 workspace typecheck、lint 与 `git diff --check` 通过；系统 `bash.exe` 是未配置的 WSL 启动器，不作为脚本验证结果
  - `2026-08-14`：心跳提交 `93ebc4cf4` 已推送；精确 SHA 的主 CI `31791510755` 与 Docker `31791510748` 成功
  - `2026-08-14`：Android run `31793673753` 已越过此前 20–41 分钟终止窗口，但在约 65 分钟后失联；GitHub check annotation 指向 CPU/内存饥饿、进程终止或网络阻断，job log endpoint 多次返回 `BlobNotFound`
  - `2026-08-14`：对比上游 `getpaseo/paseo` 的 Android workflow 确认其 GitHub job 只提交并等待 EAS 远程 build，不在 GitHub runner 上执行 Gradle；上游绿色不能反证本地 EAS build 资源问题
  - `2026-08-14`：Context7 引用 Gradle 官方文档确认 `org.gradle.jvmargs` 及命令行覆盖语义；为保持 `expo-gradle-jvmargs` 的应用配置不变，本轮不先降低 Gradle heap，只增加 runner 临时 swap
  - `2026-08-14`：Android job 已在 `$RUNNER_TEMP` 内预分配并启用 2 GiB swap，heartbeat 新增 `SwapFree`，job 结束后在受限路径内 best-effort 清理；应用配置、`eas.json`、tag 与 arm64 合同未变
  - `2026-08-14`：定向 Android workflow 合同 1/1、YAML 解析、Git for Windows Bash `-n`、`npm run format`、全 workspace typecheck、lint 与 `git diff --check` 通过；变更仅限 workflow、静态合同与本任务记录
  - `2026-08-14`：swap 提交 `17ff99b4c` 已推送；GitHub 立即产生无 jobs 的 workflow-file failure `31814878881`，证明失败发生在 YAML 载入/表达式校验而非 swap 命令或 Android build 运行时
  - `2026-08-14`：载入失败最小化到 job-level `${{ runner.temp }}`；改由 swap step 使用 `$RUNNER_TEMP` 初始化并通过 `$GITHUB_ENV` 传给后续步骤
  - `2026-08-14`：GitHub Actions 官方 context availability 表确认 `jobs.<job_id>.env` 的允许上下文不包含 `runner`，而 `jobs.<job_id>.steps.run/env` 包含，与远端载入失败完全一致
  - `2026-08-14`：运行时 swap 路径修复的定向合同 1/1、YAML、Bash `-n`、format、全 workspace typecheck、lint 和 `git diff --check` 通过

## 6. 验证与完成判断

| 验收项      | 命令或步骤                                                | 结果 | 证据                                                                                 |
| ----------- | --------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------ |
| 分支与远端  | `git status`、`fetch`、divergence、remote URL             | 通过 | `main=origin/main=93ebc4cf4`；tag 仍为 `cec97b8b9`；origin URL 精确匹配              |
| 目标版本    | adopted base、tag 占用、版本策略                          | 通过 | upstream base `0.3.1`；不可变 `v0.3.1-beta.1` 解引用到 `cec97b8b9`                   |
| 本地门禁    | format、lint、typecheck、ACP drift、release prepare/check | 通过 | YAML、Bash、arm64/swap 合同、format、typecheck、lint 与 diff 通过；ACP pins 保持不变 |
| changelog   | 用户向、原位、完整范围、严格标题、用户批准                | 通过 | Release Notes Sync `31764185948` 已镜像到 GitHub prerelease                          |
| GitHub 发布 | refs、prerelease、三条 workflow、release notes、资产      | 阻塞 | Desktop 与 notes 已绿；Android 五次 runner shutdown/失联，APK 尚未上传               |
| 禁止范围    | npm、Docker、store、stable、Cloudflare                    | 通过 | Docker workflow 仅构建；其余渠道均未发布                                             |

- 未验证项与原因：Android APK 尚未完成，因此签名、包名、版本和 release 资产仍待最终 run 验证
- 剩余风险：依赖树有 99 项现有漏洞；本次有意保留 13 个 ACP stale pins；首个同基线 beta 需从裸 `0.3.1` 手动安装
- Done Contract 是否由证据满足：否；Android workflow 未绿且 APK 未上传

## 7. 恢复与同步

- 状态说明：公开 beta、产品 CI 修复和首次 workflow-only Android 缓存恢复均已执行；除 Android APK 外的发布契约已满足
- 当前卡点：三次全 ABI、一次 arm64-only 与一次 arm64+heartbeat 构建均在 APK 产出前收到 runner shutdown/失联；heartbeat 已排除纯日志静默，但最新日志 blob 不可用
- 下一步唯一动作：增加 2 GiB 临时 swap 与 `SwapFree` 采样，推送并通过精确 SHA 主 CI/Docker 后再次 dispatch `v0.3.1-beta.1`
- Resume / Handoff：从本文件第 4 节继续；不得移动 tag，不改应用源码或 `packages/app/eas.json`，ACP pins 保持不变
- Project Sync Candidates：`无；当前事实已由 docs/release.md 和发布 Skill 覆盖`
- 长期文档同步：暂不需要

### 提交记录

| 提交信息（Commit Message）                                       | 提交脚注（Commit Footer） | 关联改动或阶段             | 文档同步状态 | 备注   |
| ---------------------------------------------------------------- | ------------------------- | -------------------------- | ------------ | ------ |
| `36859c086 docs(release): prepare 0.3.1-beta.1`                  | `N/A`                     | changelog 与任务记录       | 已同步       | 已推送 |
| `4ff2689f2 fix: unblock beta release CI`                         | `N/A`                     | 第一轮 CI 修复             | 已同步       | 已推送 |
| `a3bafd111 fix: unblock remaining beta release CI`               | `N/A`                     | 剩余 Playwright CI 修复    | 已同步       | 已推送 |
| `fb0455df7 test: stabilize workspace git observation timing`     | `N/A`                     | Windows server 测试稳定化  | 已同步       | 已推送 |
| `cec97b8b9 chore(release): cut 0.3.1-beta.1`                     | `N/A`                     | release version 与 tag     | 已同步       | 已推送 |
| `92115248b fix(release): preserve Android Gradle cache`          | `N/A`                     | 首次 Android 缓存修复      | 已同步       | 已推送 |
| `c45749a28 fix(release): build arm64 Android beta APK`           | `N/A`                     | arm64-only Android 修复    | 已同步       | 已推送 |
| `93ebc4cf4 fix(release): keep Android build runner active`       | `N/A`                     | 心跳与资源诊断             | 已同步       | 已推送 |
| `17ff99b4c fix(release): add Android build memory headroom`      | `N/A`                     | 免费 runner 临时 swap 修复 | 已同步       | 已推送 |
| `<待提交> fix(release): initialize Android swap path at runtime` | `N/A`                     | workflow 表达式修复        | 待同步       | 待推送 |
