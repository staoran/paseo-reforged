# 从当前主分支发布 0.3.1 测试版 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                       |
| ------------------ | -------------------------------------------------------- |
| task_id            | `0062`                                                   |
| spec layer         | `Feature Spec`                                           |
| task status        | `发布中`                                                 |
| document status    | `Active`                                                 |
| depth              | `standard`                                               |
| phase              | `Execute`                                                |
| Execution Approval | `Approved（提交、推送与公开 beta 发布）`                 |
| Approval Source    | `User / 2026-08-13 明确 go ahead`                        |
| file path          | `mydocs/micro_specs/0062_从当前主分支发布0.3.1测试版.md` |
| parent spec        | `N/A`                                                    |
| superseded by      | `N/A`                                                    |
| created / updated  | `2026-08-13`                                             |

## 1. 目标与完成契约

- 当前理解：把本地当前 `main` 比 `origin/main` 更新的最新代码先推送并通过 CI，再发布一版 Paseo Reforged beta；用户明确 `go ahead` 后才提交、推送和公开发布
- 核心目标：以已采用的 upstream `0.3.1` 为三段式基线发布下一可用 beta，并核验不可变 tag、GitHub prerelease、Desktop Release、Android APK Release 与 Release Notes Sync
- Done Contract：目标版本和 changelog 获批；本地发布门禁通过；`main` 与不可变 tag 成功推送；三条发布 workflow 全绿；release notes 与资产核验通过；npm、Docker、商店、Cloudflare 和稳定渠道均未发布

## 2. 范围与事实

- 范围内：任务记录、`CHANGELOG.md` 当前 beta 条目、format/lint/typecheck、ACP catalog drift、release prepare/check、版本 manifests、发布提交与 tag、GitHub prerelease 和三条发布 workflow 核验
- 范围外：产品代码变更、stable/formal release、npm/GHCR、App Store/Play Store、Cloudflare、移动或强推既有 tag
- 当前任务单元：准备并发布当前 `main` 的单个 beta，失败时保留不可变 tag 规则并按文档选择重试或下一 beta
- 轻量评估：`升级 standard`；代码范围不扩张，但涉及 changelog、版本提交、不可变 tag、远端主分支和公开 GitHub 发布
- 已确认事实：准备开始时 `main=ca36e8308` 且工作区干净，当前仅有受控发布文档改动；刷新后相对 `origin/main` 为 `0 behind / 45 ahead`；`origin` 精确为 `https://github.com/staoran/paseo-reforged.git`；upstream `v0.3.1` 已由合并提交 `948fd4a33` 纳入当前历史，根版本为 `0.3.1`；本地 upstream `v0.4.0-beta.1/.2` 均不是当前 `main` 的祖先，不能作为已采用基线
- `grilling` 结论（如使用）：`N/A；版本和发布范围可由仓库事实收敛`
- 风险与未知：用户已决定本次保留 ACP catalog 的 11 个 stale pins；依赖审计报告 99 项现有漏洞（7 low、33 moderate、52 high、7 critical）；当前 HEAD 尚未推送，不能预先确认该精确提交的远端 CI；同基线 `0.3.1-beta.1` 的 SemVer 排序低于裸 `0.3.1`，已安装 `0.3.1` 的客户端需手动安装首个 beta

## 3. 涉及文件与计划

| 文件或系统                                         | 计划变化或动作                                         | 事实源                                 |
| -------------------------------------------------- | ------------------------------------------------------ | -------------------------------------- |
| `CHANGELOG.md`                                     | 原位刷新当前 beta 条目为用户向完整发布说明             | `docs/release.md#Changelog policy`     |
| 根与 workspace `package.json`、`package-lock.json` | 由 `release:beta:next` 同步为批准后的目标版本          | 版本脚本与 release flow                |
| `mydocs/todolist.md`、本 micro-spec                | 记录门禁、批准、发布与验收证据                         | 项目任务记录规则                       |
| GitHub                                             | 推送 `main` 与不可变 tag，核验 prerelease 和 workflows | `docs/release.md#Completion checklist` |

1. 复核采用基线、同基线 tag 占用和下一 beta，运行本地准备门禁并记录任何文件变化
2. 从 previous-stable-to-`HEAD` 差异起草用户向 changelog，展示目标版本、排序约束、CI/凭据状态和草案，等待审阅
3. 用户明确 `go ahead` 后提交批准的 changelog 与任务记录，先推送最新 `main` 并等待该提交 CI 通过，再执行 `npm run release:beta:next`，最后核验远端 refs、prerelease、release notes、资产和三条 workflow

## 4. 执行前检查点

- 当前目标：把已批准的最新 `main` 推送并通过 CI，再发布 `0.3.1-beta.1`
- 当前进度：用户已批准 changelog、目标版本、保留 ACP stale pins，以及提交、推送和公开 beta 发布
- 当前动作是否仍服务核心目标：是；下一步提交发布说明与任务记录并推送最新 `main`
- 下一步：提交并推送最新 `main`，等待该精确提交 CI 通过后执行 `npm run release:beta:next`
- 风险与回退：任何最后一分钟文件变化都需单独审阅；tag 创建后不可移动；最新代码需先推送并通过 CI；不触碰 `6767` daemon
- 验证方式：命令退出码、clean status、版本脚本 `--print`、Git refs、GitHub Actions 与 Release API
- TDD 判定、测试 seam 与验收行为：`N/A；本任务不新增或改变产品行为，使用既有发布脚本和检查作为反馈环`
- seam 确认：`N/A；不进入 TDD 循环`
- Execution Approval / Source：`Approved / User；2026-08-13 明确回复 go ahead，并决定保留当前 ACP pins`

## 5. 执行与变更记录

- 实际改动：创建任务记录并起草 `0.3.1-beta.1` 用户向 changelog；未修改产品代码、版本 manifest、tag 或远端 refs
- 偏差与用户决策：用户补充确认必须推送本地 `main` 的最新代码后再发 beta；发布顺序增加“推送最新 `main` 并等待 CI”步骤
- Change Log：
  - `2026-08-13`：用户要求从当前主分支发布测试版；开始 `release-beta` 两阶段流程
  - `2026-08-13`：确认 adopted upstream base 为 `0.3.1`；本地 upstream `v0.4.0-beta.*` 未合入当前 `main`，不改变本次三段式基线
  - `2026-08-13`：`npm run format` 通过；oxfmt 写回时间戳触发 Git for Windows `diff-files` stat 假阳性，过滤后的 blob 哈希与 `HEAD` 一致，索引重算后仅剩本任务记录变化且无产品文件 diff
  - `2026-08-13`：`npm run lint`、`npm run typecheck` 与 Release Notes Sync 定向测试 4/4 通过；中断安装导致的 React Native 补丁部分应用已通过精确重装受影响依赖恢复，未修改产品代码
  - `2026-08-13`：`npm run release:prepare` 通过，全部仓库补丁成功应用；`npm run release:check` 完整通过，六个发布 workspace 的 typecheck/build/pack dry-run 成功
  - `2026-08-13`：ACP drift 检查确认 15 个可检查项中 11 个 stale pins；依赖审计报告 99 项现有漏洞，均作为发布前发现展示，不自动修改依赖
  - `2026-08-13`：GitHub CLI 登录恢复；`origin/main=59ff692bb` 的 CI 与 Docker 通过，本地 `main=ca36e830` 领先 45 个提交且尚无该精确 HEAD 的远端 CI
  - `2026-08-13`：用户明确回复 `go ahead`，批准 changelog、目标版本、推送最新 `main` 和公开 beta 发布，并决定本次保留 11 个 ACP stale pins

## 6. 验证与完成判断

| 验收项      | 命令或步骤                                                | 结果     | 证据                                                    |
| ----------- | --------------------------------------------------------- | -------- | ------------------------------------------------------- |
| 分支与远端  | `git status`、`fetch`、divergence、remote URL             | 准备通过 | 仅有受控发布文档改动；`origin` 正确，`main` 领先 45     |
| 目标版本    | adopted base、tag 占用、版本脚本 `--print`                | 通过     | 精确输出 `0.3.1-beta.1`                                 |
| 本地门禁    | format、lint、typecheck、ACP drift、release prepare/check | 已接受   | 静态检查与 release check 通过；ACP pins 决定保留        |
| changelog   | 用户向、原位、完整范围、严格标题、用户批准                | 已批准   | 用户明确 go ahead；Release Notes Sync 定向测试 4/4 通过 |
| GitHub 发布 | refs、prerelease、三条 workflow、release notes、资产      | 待验证   | 等待 go ahead                                           |
| 禁止范围    | npm、Docker、store、stable、Cloudflare                    | 待验证   | 发布后核对                                              |

- 未验证项与原因：精确 HEAD 的 CI 和所有公开发布结果尚未验证；即将推送最新代码
- 剩余风险：依赖树有 99 项现有漏洞；本次有意保留 11 个 ACP stale pins；首个同基线 beta 需从裸 `0.3.1` 手动安装
- Done Contract 是否由证据满足：否；当前仅完成事实核对和任务落盘

## 7. 恢复与同步

- 状态说明：公开发布已获批准，尚无本次发布 commit、版本变更、tag 或远端写入
- 当前卡点：无；进入提交、推送与 CI 验证
- 下一步唯一动作：提交并推送已批准的 changelog 与任务记录
- Resume / Handoff：从本文件第 4 节继续；不得把“继续”解释为 release go-ahead
- Project Sync Candidates：`无；当前事实已由 docs/release.md 和发布 Skill 覆盖`
- 长期文档同步：暂不需要

### 提交记录

| 提交信息（Commit Message） | 提交脚注（Commit Footer） | 关联改动或阶段         | 文档同步状态 | 备注     |
| -------------------------- | ------------------------- | ---------------------- | ------------ | -------- |
| `<待提交>`                 | `N/A`                     | changelog 与任务记录   | 待填写       | 已获批准 |
| `<待提交>`                 | `N/A`                     | release version 与 tag | 待填写       | 已获批准 |
