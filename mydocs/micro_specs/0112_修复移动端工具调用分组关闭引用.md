# 修复移动端工具调用分组关闭引用 Micro Spec

## 0. 状态与索引

| 字段               | 值                                                          |
| ------------------ | ----------------------------------------------------------- |
| task_id            | 0112                                                        |
| spec layer         | Feature Spec                                                |
| task status        | 待手工验收                                                  |
| document status    | Active                                                      |
| depth              | fast                                                        |
| phase              | Review                                                      |
| Execution Approval | Approved                                                    |
| Approval Source    | User                                                        |
| file path          | `mydocs/micro_specs/0112_修复移动端工具调用分组关闭引用.md` |
| parent spec        | N/A                                                         |
| superseded by      | N/A                                                         |
| created / updated  | 2026-09-12 18:32                                            |

## 1. 目标与完成契约

- 当前理解：移动端紧凑布局渲染 `OverviewToolCallGroupView` 时，`onClose={close}` 引用了未定义标识符，使 Hermes 抛出 `ReferenceError` 并进入重试页
- 核心目标：为移动工具调用分组 sheet 恢复与当前 `expansionId` 一致的关闭回调
- Done Contract：紧凑布局打开工具调用分组后不再触发 `ReferenceError`；关闭 sheet 将对应的展开状态设为 `false`；桌面内联交互不改变

## 2. 范围与事实

- 范围内：`packages/app/src/tool-calls/detail-level/overview/view.tsx` 的关闭回调，以及已有紧凑布局 Browser E2E 和静态检查
- 范围外：Bottom sheet 结构、工具调用投影规则、Android 打包和工作区既有未提交改动
- 当前任务单元：恢复缺失回调，并以 `expansionId` 而非 `group.run.id` 关闭正确的嵌套组
- 轻量评估：`足够小`
- 已确认事实：upstream 提交 `32d6b18db` 曾定义 `close`；当前合并结果保留 `onClose={close}`，但组件内不存在其定义；`ab267da9c` 合并了相关的两个父分支
- `grilling` 结论（如使用）：未使用
- 风险与未知：本机没有可用 Android 自动化运行时，不能直接复现 Hermes 的同一错误；浏览器的全局 `close` 会将渲染期未定义引用掩盖为 dismiss 时的 `Illegal invocation`

## 3. 涉及文件与计划

| 文件                                                         | 计划变化                                                   | 事实源                |
| ------------------------------------------------------------ | ---------------------------------------------------------- | --------------------- |
| `packages/app/src/tool-calls/detail-level/overview/view.tsx` | 增加基于 `expansionId` 的关闭回调并传给 sheet              | 当前组件、`32d6b18db` |
| `packages/app/e2e/browser/tool-call-overview-sheet.spec.ts`  | 优先复用其紧凑布局关闭断言，仅在不能覆盖正确状态时最小补充 | 现有真实 Browser E2E  |

1. 运行紧凑布局 Browser E2E，确认当前关闭路径的失败表现并最小化证据（已完成：sheet dismiss 处 `Illegal invocation`，调用栈为 `createBottomSheetVisibilityTracker...onClose`）
2. 恢复关闭回调，确保它关闭当前 `expansionId`
3. 重跑该 E2E，并执行 App typecheck、lint 与受影响文件格式检查

## 4. 执行前检查点

- 当前目标：消除移动端未定义 `close` 引用，并使 sheet 关闭正确回写展开状态
- 当前进度：已恢复 `close` 回调，类型、文件 lint 与格式检查通过；等待 Android 手工验收
- 当前动作是否仍服务核心目标：是，现有 E2E 的关闭断言直接覆盖回调的用户可见结果
- 下一步：在 Android 打开含概览工具调用的会话，并打开再关闭分组 sheet
- 风险与回退：修改只新增一个 memoized 回调；若验证发现浏览器全局 `close` 掩盖问题，记录原生运行时缺口，不扩展为打包或设备配置任务
- 验证方式：定向 Playwright 紧凑布局回归、`npm run typecheck`、`npm run lint`、受影响文件格式检查
- TDD 判定、测试 seam 与验收行为：TDD；现有真实 Browser E2E 的关闭按钮断言是可复用 seam，验收为 sheet 隐藏且分组仍可见
- seam 确认：N/A；现有用例已覆盖用户操作路径
- Execution Approval / Source：Approved / User；用户于 2026-09-12 18:20 批准实施 0112，并限定不新增测试

## 5. 执行与变更记录

- 实际改动：在 `toggle` 后新增 memoized `close` 回调，调用 `onExpandedChange(expansionId, false)`
- 偏差与用户决策：用户明确要求只做源码修复，不新增测试
- Change Log：仅修改 `packages/app/src/tool-calls/detail-level/overview/view.tsx` 的 3 行；未修改测试

## 6. 验证与完成判断

| 验收项                    | 命令或步骤                                                                                                                            | 结果     | 证据                                                                                                                  |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------- |
| 紧凑布局分组 sheet 可关闭 | `rtk proxy npx playwright test --project=browser e2e/browser/tool-call-overview-sheet.spec.ts --grep \"compact overview tool calls\"` | 未全绿   | 修复后不再出现 `Illegal invocation` 或错误边界；后续流式断言仍为 `Expected >= 11, received 0`，未到达显式关闭按钮断言 |
| 根类型检查                | `rtk proxy npm run typecheck`                                                                                                         | PASS     | 退出码 0，包含 `@getpaseo/app`                                                                                        |
| 根 lint                   | `rtk proxy npm run lint`                                                                                                              | 既有阻断 | `.tmp-ci-34665313270-playwright-3-trace/` 的生成 JS 产生 1142 个错误，另有无关的 `workspace-header-source.ts` 错误    |
| 本次文件 lint             | `rtk proxy npx oxlint packages/app/src/tool-calls/detail-level/overview/view.tsx`                                                     | PASS     | 0 warnings, 0 errors                                                                                                  |
| 格式                      | `rtk proxy npm run format:check:files -- packages/app/src/tool-calls/detail-level/overview/view.tsx`                                  | PASS     | 1 file                                                                                                                |

- 未验证项与原因：原生 Hermes 渲染和用户点击关闭按钮待 Android 手工验收；Orca runtime 未启动，无法驱动设备
- 剩余风险：Browser E2E 的流式消息断言独立失败，未覆盖到最后的显式关闭按钮断言
- Done Contract 是否由证据满足：源码与跨平台错误边界证据支持修复，移动端最终验收待完成

## 7. 恢复与同步

- 状态说明：源码修复完成，等待手工验收
- 当前卡点：Android 自动化因 Orca runtime 未启动不可用
- 下一步唯一动作：在 Android 验证含概览工具调用的会话可打开并关闭分组 sheet，且不进入重试页
- Resume / Handoff：保持当前补丁；先做 Android 手工验收，再决定是否单独诊断 Browser E2E 的流式消息断言；不得覆盖用户正在执行的 0111 或 0113 任务
- Project Sync Candidates：无；本次是一次性合并回归事实
- 长期文档同步：无

### 提交记录

| 提交信息（Commit Message）                               | 提交脚注（Commit Footer） | 关联改动或阶段   | 文档同步状态 | 备注                |
| -------------------------------------------------------- | ------------------------- | ---------------- | ------------ | ------------------- |
| `fix(app): restore mobile tool call group close handler` | N/A                       | 缺失关闭回调修复 | 未同步       | 待 Android 手工验收 |
